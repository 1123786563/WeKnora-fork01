// Docker/Lite replaces this file at container start. Keep a safe development
// default so the Vite artifact has the same runtime-config contract.
window.__RUNTIME_CONFIG__ = {
  MAX_FILE_SIZE_MB: 50,
  MAX_SKILL_BUNDLE_SIZE_MB: 256,
  DEFAULT_LOCALE: ""
};
