import { useEffect, useMemo, useState } from 'react';
import * as React from 'react';
import type { ModelConfiguration, ModelProvider, WeKnoraClient } from '@weknora/api-client';
import { Button, Card, Status } from '@weknora/ui';
import { modelCredentialInput, modelDraftFromRecord, modelPayload, modelType, newModelDraft, validateModelDraft, type ModelDraft, type ModelType } from './model-settings.ts';

type Props = { client: WeKnoraClient; role: 'viewer' | 'admin' | 'owner' | 'system-admin'; initialModels: readonly ModelConfiguration[] };
const TYPES: ModelType[] = ['chat', 'embedding', 'rerank', 'vllm', 'asr'];
const TYPE_LABELS: Record<ModelType, string> = { chat: 'Chat', embedding: 'Embedding', rerank: 'Rerank', vllm: 'VLLM', asr: 'ASR' };

function params(model: ModelConfiguration): Record<string, unknown> { return model.parameters && typeof model.parameters === 'object' && !Array.isArray(model.parameters) ? model.parameters : {}; }
function label(model: ModelConfiguration): string { const value = (model as Record<string, unknown>).display_name; return typeof value === 'string' && value.trim() ? value : model.name; }

export function ModelSettingsPanel({ client, role, initialModels }: Props) {
  const [models, setModels] = useState<readonly ModelConfiguration[]>(initialModels);
  const [filter, setFilter] = useState<'all' | ModelType>('all');
  const [draft, setDraft] = useState<ModelDraft | null>(null);
  const [providers, setProviders] = useState<ModelProvider[]>([]);
  const [loadingProviders, setLoadingProviders] = useState(false);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [notice, setNotice] = useState<string | null>(null);
  const canCreate = role === 'admin' || role === 'owner';
  const visible = useMemo(() => filter === 'all' ? models : models.filter((item) => modelType(item) === filter), [filter, models]);

  useEffect(() => { setModels(initialModels); }, [initialModels]);
  useEffect(() => { if (!draft) return; setLoadingProviders(true); void client.configuration.models.providers.list(draft.type).then(setProviders).catch(() => setProviders([])).finally(() => setLoadingProviders(false)); }, [client, draft?.type]);

  function updateDraft<K extends keyof ModelDraft>(key: K, value: ModelDraft[K]) { setDraft((current) => current ? { ...current, [key]: value } : current); }
  async function reload() { try { setModels(await client.configuration.models.list()); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Unable to load models'); } }
  async function save(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); if (!draft || busy) return;
    const invalid = validateModelDraft(draft); if (invalid.length > 0) { setError(invalid.join(', ')); return; }
    setBusy(true); setError(null); setNotice(null);
    let savedId = draft.id;
    try {
      const saved = draft.id ? await client.configuration.models.update(draft.id, modelPayload(draft)) : await client.configuration.models.create(modelPayload(draft));
      savedId = saved.id;
      const credentials = modelCredentialInput(draft);
      if (Object.keys(credentials).length > 0) { try { await client.configuration.models.credentials.put(saved.id, credentials); } catch (cause) { setDraft({ ...draft, id: saved.id }); throw cause; } }
      setDraft(null); setNotice(draft.id ? 'Model updated.' : 'Model added.'); await reload();
    } catch (cause) { if (savedId) setDraft((current) => current ? { ...current, id: savedId } : current); setError(cause instanceof Error ? cause.message : 'Unable to save model'); }
    finally { setBusy(false); }
  }
  async function remove(model: ModelConfiguration) { if (!canCreate || (model as Record<string, unknown>).is_builtin === true || busy) return; if (!window.confirm(`Delete model “${label(model)}”?`)) return; setBusy(true); setError(null); try { await client.configuration.models.remove(model.id); setNotice('Model deleted.'); await reload(); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Unable to delete model'); } finally { setBusy(false); } }

  return <section className="wk-model-settings" data-testid="model-settings"><div className="wk-settings-panel-heading"><div><h3>Models</h3><p className="wk-muted">Configure tenant model providers. Credentials are write-only and never returned in model records.</p></div><div className="wk-list-actions"><Button type="button" disabled={busy} onClick={() => void reload()}>Refresh</Button>{canCreate ? <Button type="button" onClick={() => setDraft(newModelDraft())}>Add model</Button> : null}</div></div>{error ? <Status tone="error">{error}</Status> : null}{notice ? <Status tone="success">{notice}</Status> : null}<nav className="wk-model-tabs" aria-label="Model type"><button type="button" className={filter === 'all' ? 'is-active' : ''} onClick={() => setFilter('all')}>All ({models.length})</button>{TYPES.map((type) => <button type="button" key={type} className={filter === type ? 'is-active' : ''} onClick={() => setFilter(type)}>{TYPE_LABELS[type]} ({models.filter((item) => modelType(item) === type).length})</button>)}</nav>{visible.length === 0 ? <Status>No models configured.</Status> : <div className="wk-model-grid">{visible.map((model) => { const type = modelType(model); const modelParams = params(model); const builtin = (model as Record<string, unknown>).is_builtin === true; return <Card key={model.id} className={`wk-model-card wk-model-card--${type}`}><div className="wk-model-card-header"><div><span className="wk-model-type">{TYPE_LABELS[type]}</span><h4>{label(model)}</h4></div>{builtin ? <span title="Built-in model">Built-in</span> : null}</div><p className="wk-muted">{typeof model.source === 'string' ? model.source : 'remote'} · {typeof modelParams.provider === 'string' ? modelParams.provider : 'generic'}{typeof modelParams.dimension === 'number' ? ` · dimension ${modelParams.dimension}` : ''}{typeof modelParams.context_window === 'number' ? ` · ${modelParams.context_window} context` : ''}</p><div className="wk-list-actions">{(canCreate || (role === 'system-admin' && builtin)) ? <Button type="button" onClick={() => setDraft(modelDraftFromRecord(model))}>Edit</Button> : null}{canCreate && !builtin ? <Button type="button" disabled={busy} onClick={() => void remove(model)}>Delete</Button> : null}</div></Card>; })}</div>}{draft ? <div className="wk-model-editor" role="dialog" aria-modal="true" aria-label={draft.id ? 'Edit model' : 'Add model'}><div className="wk-settings-panel-heading"><div><h3>{draft.id ? 'Edit model' : 'Add model'}</h3><p className="wk-muted">Choose a model type, source, provider, and connection details.</p></div><Button type="button" disabled={busy} onClick={() => setDraft(null)}>Close</Button></div><form className="wk-settings-editor" onSubmit={(event) => void save(event)}>{!draft.id ? <label>Model type<select value={draft.type} onChange={(event) => updateDraft('type', event.target.value as ModelType)}>{TYPES.map((type) => <option key={type} value={type}>{TYPE_LABELS[type]}</option>)}</select></label> : null}<label>Model name<input required maxLength={100} value={draft.name} onChange={(event) => updateDraft('name', event.target.value)} /></label><label>Display name<input maxLength={100} value={draft.displayName} onChange={(event) => updateDraft('displayName', event.target.value)} /></label><label>Source<select value={draft.source} onChange={(event) => updateDraft('source', event.target.value as ModelDraft['source'])}><option value="remote">Remote</option><option value="local">Local</option></select></label>{draft.source === 'remote' ? <><label>Provider<select value={draft.provider} disabled={loadingProviders} onChange={(event) => updateDraft('provider', event.target.value)}><option value="generic">Generic</option>{providers.map((provider) => <option key={provider.value} value={provider.value}>{provider.label}</option>)}</select></label>{draft.provider !== 'weknoracloud' ? <label>Base URL<input type="url" required value={draft.baseUrl} onChange={(event) => updateDraft('baseUrl', event.target.value)} /></label> : null}</> : null}{draft.type === 'embedding' ? <label>Dimension<input type="number" min={128} max={4096} value={draft.dimension} onChange={(event) => updateDraft('dimension', event.target.value === '' ? '' : Number(event.target.value))} /></label> : null}{draft.type === 'chat' || draft.type === 'vllm' ? <label>Context window<input type="number" min={1024} value={draft.contextWindow} onChange={(event) => updateDraft('contextWindow', event.target.value === '' ? '' : Number(event.target.value))} /></label> : null}{draft.type === 'chat' ? <label><input type="checkbox" checked={draft.supportsVision} onChange={(event) => updateDraft('supportsVision', event.target.checked)} /> Supports vision</label> : null}<label>Max concurrency<input type="number" min={1} value={draft.maxConcurrency} onChange={(event) => updateDraft('maxConcurrency', event.target.value === '' ? '' : Number(event.target.value))} /></label><label>API key<input type="password" autoComplete="new-password" value={draft.apiKey} placeholder={draft.id ? 'Leave blank to keep configured key' : ''} onChange={(event) => updateDraft('apiKey', event.target.value)} /></label>{draft.provider === 'weknoracloud' ? <label>App secret<input type="password" autoComplete="new-password" value={draft.appSecret} onChange={(event) => updateDraft('appSecret', event.target.value)} /></label> : null}<div className="wk-list-actions"><Button type="submit" loading={busy}>{draft.id ? 'Save changes' : 'Create model'}</Button><Button type="button" disabled={busy} onClick={() => setDraft(null)}>Cancel</Button></div></form></div> : null}</section>;
}
