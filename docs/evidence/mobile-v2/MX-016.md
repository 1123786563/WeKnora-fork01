# MX-016 证据 · Agent 目录与可用能力选择

| 项 | 值 |
|---|---|
| 任务 | MX-016 Agent 目录与可用能力选择（S02 / core；UX 对应 M06） |
| BASE | `964bf811`（MX-014 fix 后） |
| 写锁 | AgentPickerScreen.tsx / agent-options.ts / test+probe + domain 出口 owners 扩展 |

## 交付

- **domain agent-options.ts**：`toAgentOptions`（原始行投影——敏感 prompt 字段不进展示模型）；`codingAgentCapability`（编码类必须绑定**实际已授权且未撤销** target 才 supported，缺省→unavailable(driver_unavailable)——不以名字启用 shell）；`selectAgent`（不可用返回原因，禁止静默 fallback）；`defaultAgent`（首个 supported；无可用=undefined）；`filterAgents`（类型+关键词纯函数）。
- **AgentPickerScreen（M06）**：名称/摘要展示（无 Prompt 配置）；类型 chips+关键词筛选；不可用条目**可见但不可选**并显示原因（badge+读屏完整句）；选择不可用时确认区警示；选中经 onConfirm 返回原表单；令牌视觉。
- probe 覆盖：无授权/有授权/已撤销三种 coding 裁决；默认回退 general；敏感字段投影过滤断言。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-016.test.ts | 0 | 1/1（frozen unavailable-agent：selectedAgent=general、unavailableReason=driver_unavailable） |
| pnpm run test:mobile-v2 | 0 | 17/17 |
| pnpm run test:shared | 0 | 590/590 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线（新增 1 类型错误当轮修：AgentOption 显式标注） |

## 验收对照

- [x] 显示名称/能力摘要而非敏感 Prompt 配置（投影过滤+泄漏断言）
- [x] 类型与关键词筛选；选中返回原表单（onConfirm/onCancel）
- [x] 不可用能力显示原因、不隐藏入口、不默认 fallback（frozen：coding 不可用时默认选择=general 而非静默选 coding）
- [x] 编码 Agent 依赖实际已授权 target（无授权/撤销均不可用；frozen 观测 driver_unavailable）
- [~] 真实 Agent 目录 API 接线：目录数据源（服务端 Agent 列表端点）不在本任务写集——rows 端口就绪，随 MX-015（新建任务流）接线；挂载 spec 随同

## 审查记录

- 待独立 review（提交后追加）。
