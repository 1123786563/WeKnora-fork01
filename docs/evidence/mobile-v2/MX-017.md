# MX-017 证据 · 产品对话渲染与富消息

| 项 | 值 |
|---|---|
| 任务 | MX-017 产品对话渲染与富消息（S02 / core / 关闭 G02 页面半边+G07 页面半边+G03/G08 消费；UX 对应 M07） |
| BASE | `336b0ffb`（MX-014 后） |
| 写锁 | view-model.ts / ConversationScreen.tsx / ProductMessageList.tsx / MessageBlock.tsx / test+probe |

## 交付

- **view-model.ts 产品投影（重写）**：`createProductConversationViewModel`（原死代码桩替换为真实实现）——
  - 事件幂等：同一 (run_id, seq) 只应用一次（frozen：seq42 重复投递 → 1 条消息）；
  - 消息稳定 id：text.delta 以 payload.message_id 为准（缺失回退 seq）；增量文本 upsert；
  - 三态分离（run/execution/settlement 同源保留，G08 消费）；revision 保留；
  - 命令准入消费 `evaluateCommand`（D-013，G03 消费）——canCancel/canSteer 来自能力与 revision，不再硬编码；cancel/steer 无快照 revision 即拒绝；
  - pendingInteractions 来自真实 InteractionRecord 列表（kind 语义映射 approval/permission/question；状态由 decision_id/action 派生）；
  - **零 Happy import**（probe 源级观测 0 处 hooks/sync API）；
  - legacy `createConversationViewModel`/`createSendController` 原样保留（SessionView 兼容，不制造回归）。
- **ConversationScreen.tsx（重写）**：产品版——状态头（三态+读屏）；ProductMessageList；指令输入（Field+Button 单飞）；发送经注入的持久提交 SendController（request_id 语义归 MX-006 协调器，屏内不发明 ID、不 fetch）；取消按钮被 evaluateCommand 门控且文案明示「申请取消≠执行停止」。
- **ProductMessageList/MessageBlock**：倒序列表稳定 id；角色语义色+读屏完整句；分段 append（同 id upsert 不重建）。

## RED 记录

- 重写直接消除 3 个 typecheck 基线错误（createRequestID 悬空/approve·reject 不在类型——G02/G04 编译面）：**基线 14→11（D-026）**。
- frozen 探针（重复 seq42 → 1 条「你好」、happySyncReads=0）一次通过；杀伤力：投影的 `appliedSeqs` 集合若移除，第二次 apply 会产生重复消息（messagesById 同 id upsert 文本翻倍——断言 text==='你好' 抓翻倍）。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-017.test.ts | 0 | 1/1（frozen duplicate-message-event） |
| pnpm run test:mobile-v2 | 0 | 16/16 |
| go test ./internal/... | 0 | 100 包全 ok（MX-013 fix 后全量复验） |
| apps/mobile tsc --noEmit | 非0（基线） | **11=新基线**（ConversationScreen 3 错误消除，D-026） |
| 挂载套件 | 0 | 9/9 |

## 验收对照

- [x] 产品 renderer 不再依赖 Happy useSession/useIsDataReady（源级 0；legacy 兼容路径保留不破坏）
- [x] messages/pendingInteractions/三态/能力同源投影（单一 VM 状态）
- [x] 稳定 message ID + 分段 append（同 id upsert）；重复事件零重复渲染（frozen）
- [x] 输入端只有类型化 submit（注入端口）——不调用任意 RPC
- [~] SSE 实流消费编排（snapshot→events→投影→持久化 drain）：部件齐备（MX-004 parser/MX-012 storage/MX-017 投影），装配随 M07 路由接线（MX-015 创建流后）
- [~] 富消息类型（工具卡/引用/审批卡片渲染）：MessageBlock 文本主通道就绪，卡片形态随 MX-019/020
- [~] 屏挂载 spec 与路由挂载：随 M07 文件路由（MX-015 后接）

## 审查记录

- 待独立 review（提交后追加）。
