# MX-025 证据 · 原生附件与上传校验

| 项 | 值 |
|---|---|
| 任务 | MX-025 原生附件与上传校验（S03 / resources；M05 附件通道） |
| BASE | `0dda83ba`（MX-023 后） |
| 写锁 | resources/upload.ts / platform/native-file.ts / api-client knowledge/documents.ts（复验零修改——既有上传 API 满足）/ test+probe |

## 交付

- **resources/upload.ts**：`createUploadController`——
  - `precheckFile` 本地前置校验（大小上限 50MB/MIME 白名单/空文件——服务端仍做权威校验，客户端不越权放行）；
  - 上传流程：本机 URI 读取真实 bytes（分块）→ **上传 bytes 与元数据**（URI 不进请求——frozen 断言）；
  - **scope generation 全程绑定**：读后/上传后两次守卫——切空间时迟到结果不应用（lateResultApplied=false）、一切失败保留草稿与选取（draftPreserved=true）、离线不自动补发（scope_changed 语义）。
- **platform/native-file.ts**：`pickNativeFile`（expo-document-picker 系统选取，copyToCache）+ `readNativeBytes`（expo-file-system 分块 base64 读取——content://file:// 仅本机使用；模块零网络调用源级断言）。依赖已在官方矩阵（55.0.x）。
- packages/api-client knowledge/documents.ts：**复验零修改**——既有 documents API 即真实 bytes 上传通道（服务端权威校验），写锁≠必写。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-025.test.ts | 0 | 1/1（frozen native-file-switch：lateResultApplied=false、draftPreserved=true） |
| pnpm run test:mobile-v2 | 0 | 28/28 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线（expo-file-system 编码常量与回调签名 2 处当轮修） |

## 验收对照

- [x] 文件 URI 只用于本机读取（readNativeBytes 本机；URI 不上传——frozen 断言+源级零网络）
- [x] 上传真实 bytes（分块流；服务端权威校验语义保留——precheck 仅前置不越权）
- [x] 上传在途切空间：迟到结果不应用、草稿保留（frozen）；离线不自动补发
- [x] 本地校验：超大/非法 MIME/空文件拒绝且保留选取（probe 对照三例）
- [~] 真机 DocumentPicker/FileSystem 读取：blocked-env（设备链路）→ MX-034 native-e2e；node 层经注入端口验证行为
- [~] 与 M05 表单接线（附件就绪门控已在 MX-015 domain 就绪）：AttachmentRef readiness 由上传结果驱动——接线随 M05 路由挂载轮

## 审查记录

### Round 1（2026-09-18，独立子代理）：规格 PASS / 质量 CHANGES_REQUIRED（probe 适配器缺陷——实现本体经独立验证正确）

- probe uploadBytes 适配器把 provideChunks 回调误用为数组调用（恒 TypeError 被 catch 吞掉）——frozen 绿灯为 accidental pass；守卫旁路突变存活。已修：正确签名 provideChunks((chunk)=>...)；新增「真实 bytes 流经上传通道」断言（总量=2048）；draftPreserved 恒真与死代码清理。
- **杀伤复验（实现经 Edit 工具注入突变后实跑再还原）**：双守卫旁路（if(false)×2）→ frozen 翻红（late result must not be applied）；单第一守卫旁路由第二守卫兜底（纵深防御，如实记录）。
