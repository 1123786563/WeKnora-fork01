# MX-035 证据 · 视觉、无障碍与性能验收

| 项 | 值 |
|---|---|
| 任务 | MX-035 视觉、无障碍与性能验收（S04 / core） |
| BASE | `09b56a52`（MX-034 后） |
| 写锁 | visual-baseline.json / accessibility-performance.md / test+probe + 锁转移（VoiceInputScreen a11y 修复） |
| 状态 | **静态层已验证（frozen）+ 设备层 pending（blocked-env 如实）** |

## 交付

- **静态层（可离线复跑）**：probe 源级观测——
  - 主操作可达性：M01–M18 每页主操作在对应屏带非空 accessibilityLabel（frozen：unreachablePrimaryActions=[]）；
  - 关键 a11y 合同：六目录全量裸 Pressable 必带 accessibilityRole+可访问名（frozen：criticalA11yFindings=[]）——发现并修复 VoiceInputScreen 草稿 Pressable 缺 role（真实缺口被 probe 抓到）；
  - 组件级合同引用 MX-008（触控 44/48、双通道、busy、焦点回归等）。
- **visual-baseline.json**：18 页×明暗×320–430+平板×100%/200% 矩阵档位+采集配方登记；**截图 hash 全部 pending**（设备 blocked-env——不伪造；性能三数值同 pending）。
- **accessibility-performance.md**：静态层已验证清单+设备层 blocked 项+差异修正原则（不以缩字消溢出、不覆盖基准）。

## 验证（命令·exit）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-035.test.ts | 0 | 1/1（frozen visual-a11y-matrix：双空数组） |
| pnpm run test:mobile-v2 | 0 | 41/41 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] 主操作可达性 M01–M18（静态层；读屏可寻址）
- [x] 关键 a11y 合同（裸 Pressable role+name；组件级合同 MX-008 复验）
- [~] 18 页 light/dark×宽度档×大字体原生截图对比：**pending（设备 blocked-env）**——baseline 档位/配方就绪；随 MX-034 解除后采集回填
- [~] 读屏/动态字体/键盘/返回/焦点/减少动画实测：静态层（动态字体默认开启/无固定高裁切/无动画库）+ 设备层 pending
- [~] 1000 条消息与长文本性能：pending（同上；performance 段就绪）
- [x] 差异修正原则冻结（不缩字消溢出/不覆盖基准）

## 审查记录

- 待独立 review（提交后追加；与收尾批合并）。
