# MX-004 证据 · SSE 完整事件与控制帧接通

| 项 | 值 |
|---|---|
| 任务 | MX-004 SSE 完整事件与控制帧接通（S00 / core / 关闭 G01；映射 W03、W09、UX04） |
| BASE | `2bd4c682`（MX-003 后） |
| 写锁 | task-index 原集（workbench_read.go / stream-transport.ts / execution-frames.ts / test+probe）+ 锁扩展 2 文件 |

## RED 记录（有效行为 RED）

Go emit 测试（真实 writer 原语）产出 v2 字节后，**改造前**的真实 TS parser 逐字节消费在控制帧崩溃：
`pnpm exec tsx /tmp/mx004-red.mjs` → `RED-CONFIRMED: ContractError schema_version: SCHEMA_VERSION: expected 1`
（{code,message} 控制载荷没有 envelope 字段——G01 另一半面的端到端实证。）改造后同一字节流通過。中间一次 `parseStreamControl is not a function`（@weknora/domain 根出口不含 mobile 子路径）为接线修正。

## 实现

- **Go（workbench_read.go）**：`writeWorkbenchSSEV2`（envelope 进 data，先 Validate）；`writeWorkbenchControlSSE`（event: control，无业务 id）；`?version=2` 协商（默认 1 兼容，非法值 400）；flushWorkbenchEvents/writeWorkbenchStreamError 版本化（v1 行为不变）。
- **Go 测试（workbench_stream_mx004_test.go）**：真实原语经 CRLF 规范化管道产出两份字节流：单事件（心跳+control+seq42 中文 payload+截断尾帧 EOF 路径）与 300 事件（每 60 插 1 control）。
- **TS（stream-transport.ts）**：ParsedExecutionFrame 联合类型；control/error 事件走 parseStreamControl（带业务 id 即拒绝）；业务帧 id===seq 校验保留；增量 UTF-8/CRLF 跨 chunk 解析保留。
- **domain（execution-frames.ts 新）**：StreamControl/parseStreamControl/classifyExecutionFrames——控制帧不进业务列表、cursor 只来自业务 seq、同 seq 去重升序、未知 type 保留。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-004.test.ts | 0 | tests 2 / pass 2（frozen seq42+control1+cursor42；附加 300 无丢失/4 control/cursor300） |
| pnpm run test:mobile-v2 | 0 | tests 5 / pass 5（MX-001..004 回归） |
| go test ./internal/handler/session/ | 0 | ok（全量含 v1 兼容既有测试 + 2 个 emit 测试） |
| pnpm --filter @weknora/mobile test | 1 | 869 passed / 1 failed——`useNavigateToSession.test.ts`（createProductSessionNavigation 缺失，最后相关提交 3e767d66，**继承缺陷**，与 SSE 无关，归属 MX-009 导航接线）；82 文件级失败为 G10 既有 node:test/vitest 混用 |

## 验收对照

- [x] version=2 协商明确，v1 兼容测试保留（handler 既有测试全绿）
- [x] 完整 envelope 写入业务帧；control/error 不占业务 seq（分类器结构性保证+300 帧测试）
- [x] UTF-8 中文逐字节、CRLF 拆分（split-every-byte 故障注入）、心跳、多 data 行（parser 既有行为保留）、EOF 截断帧（单事件 fixture 尾帧无空行，final flush 解析）
- [x] Go 实际输出直接喂 TS parser（probe 先跑 go emit 再消费；.bin 为再生产物 gitignore，D-015）
- [x] 控制帧不推进 cursor；300>256 条无丢失
- [~] 终态 drain/cursor_expired 快照路径：writer/分类器侧就绪；**消费编排（快照恢复调用链）在 MX-012 接**（记录）

## 未执行项

- 真机流（fetch transport 实链）——transport 单元与跨语言字节已证；设备链路 MX-034。
- 409 首读与 Last-Event-ID 既有行为未改（复验通过，handler 测试覆盖）。

## 审查记录

- 待独立 review（提交后追加）。
