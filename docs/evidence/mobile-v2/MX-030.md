# MX-030 证据 · 账户偏好、外观与退出

| 项 | 值 |
|---|---|
| 任务 | MX-030 账户偏好、外观与退出（S03 / core；UX 对应 M17） |
| BASE | `620ddcb8`（MX-022 后） |
| 写锁 | ProfileScreen.tsx / preferences/store.ts / test+probe |

## 交付

- **preferences/store.ts**：scope（origin/user）隔离偏好存储——
  - 主题三态 system/light/dark（默认跟随系统；读取侧丢弃未知键——偏好永不携带 Provider secrets，结构固定）；
  - 通知偏好三开关与系统权限语义分离（注释冻结：关偏好≠关权限≠停止任务）；
  - **退出序列**：generation 前进 → teardown（关流/语音）→ revokeDevice → 清凭据+本 scope 偏好；`captureCredentialGuard`——退出前捕获的写守卫在退出后写 token 抛 `CREDENTIAL_WRITE_AFTER_LOGOUT`（迟到 token 刷新不得写回）；
  - 只清本 scope 键（其他账户/空间数据不动）。
- **ProfileScreen（M17）**：账户卡（hero）；外观三 chips（当前生效态可见——「跟随系统」自动切换说明）；通知偏好三 Switch（说明「不影响任务执行」）；切换空间/空间用量入口位；退出按钮 danger 变体+完整读屏动作说明（撤销设备/关流/清数据）；令牌视觉。

## RED 记录

frozen 探针一次通过；杀伤力：logout 若不推进 generation，`inFlightGuard.write` 会成功 → oldTokenRewritten=true 翻红；teardown/revoke 次序与「他账户数据不动」均为真实 Map 观测。类型修复 3 处（Partial 标注）当轮清零。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-030.test.ts | 0 | 1/1（frozen logout-device-generation：credential=null、activeStreams=0、oldTokenRewritten=false） |
| pnpm run test:mobile-v2 | 0 | 19/19 |
| pnpm run test:shared | 0 | 590/590 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] 主题 system/light/dark 三态（原型 light/dark 对照由双主题令牌与 useWeknoraTheme 承接）
- [x] 通知偏好与系统权限分开、语义注释冻结（关闭偏好不停止任务）
- [x] 退出撤销设备、关闭流/语音、清凭证与本 scope 缓存（frozen 全观测）；迟到 token 刷新写回被拒
- [x] 偏好不含 Provider secrets（固定结构+读取侧丢弃未知键）
- [x] 不清别的账户数据（他用户键保留断言）
- [~] 设备注册/推送真实撤销 API：revokeDevice 端口就绪，真实注册通道随 MX-021（通知注册）接线
- [~] 退出接真实 auth.logout（session 层）：ProfileTab 宿主接线随产品壳 profile 插槽（MX-013 入口模式扩展）；本任务交付全部可消费能力

## 审查记录

### Round 1（2026-09-18，独立子代理）：规格 PASS（7/8，离线条目部分）/ 质量 CHANGES_REQUIRED（2×P2）

- P2-1（已修 0119d82a）：logout 内 credentialWriteGuard 调用后 void 弃置的死代码——删除，LogoutDeps 不再声明该依赖（真实防线 captureCredentialGuard 经变异验证有效）。
- P2-2（已修 0119d82a）：revokeDevice 失败中断本地清理——try/catch/finally：远端撤销失败记录最小撤销意图键（weknora.pending-revoke:*，下次注册通道重放），本地凭据与 scope 缓存必清；probe 补离线场景三断言。
- P3（已修）：ProfileScreen 形式导入移除。
- 复核（scoped）：代码/测试/离线场景全部达标（mv2 19/19、tsc 11=基线）；**证据记录项 FAIL**——本节与 progress 行在修复提交时遗漏（跨会话边界丢失的编辑），本条即补救；probe:53 过时参数 credentialWriteGuard 一并清除。MX-030 在记录补齐后 accepted。
