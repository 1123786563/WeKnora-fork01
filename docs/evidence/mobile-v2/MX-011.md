# MX-011 证据 · 空间切换、缓存与迟到响应隔离

| 项 | 值 |
|---|---|
| 任务 | MX-011 空间切换、缓存与迟到响应隔离（S01 / core；映射 W07、W11、UX02） |
| BASE | `35ad2c0d`（MX-010 后） |
| 写锁 | query-scope.ts / SpacePickerScreen.tsx / test+probe；**product-session.ts 零修改（复验裁决）** |

## 复验裁决：product-session.ts 零修改

generation/abort/生命周期原语（createProductScope：capture/accept、switchTo 推进 generation、abort 旧信号、lifecycle 清理、logout）在基线已完备且被 probe 直接消费验证——**不制造退化**。MX-011 增量为查询键隔离层与选择页。

## 交付

- **query-scope.ts（domain 新）**：`normalizeOrigin`（协议+小写 host+去尾斜杠，同源不同写法收敛）；`productQueryKey(scope, ...parts)`——所有查询键经 [weknora, origin, user, tenant, *parts] 构造（缓存按规范化 origin/user/tenant 隔离，页面不手拼）；`createGenerationGuard`（迟到响应守卫：提交前 accept 判定，false 必须丢弃）。
- **SpacePickerScreen.tsx（M02）**：真实 memberships 数据（props 注入，来自 MX-010 bootstrap）；当前空间标记（hero 卡+badge）；选择即 scope.switchTo（由宿主接，屏内不重复实现切换语义）；空态给退出动作；令牌视觉；切换期间防重复触发。
- **probe（node 直跑，真实 createProductScope）**：A→B 切换 → A 迟到响应 guard.isCurrent()=false（不覆盖 B）；lifecycle 清理必须触发；查询键 tenant 段隔离；**服务端 Run 取消计数=0**（本地隔离≠服务端取消——退后台/断流/切空间均不取消服务端 Run）。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-011.test.ts | 0 | 1/1（frozen late-response-after-switch：B/false/0） |
| pnpm run test:mobile-v2 | 0 | 12/12 |
| pnpm run test:shared | 0 | 590/590 |
| apps/mobile tsc --noEmit | 非0（基线） | 14=基线（SpacePicker/query-scope 零新增） |

## 验收对照

- [x] 切空间推进 generation、中止旧请求（真实 scope 捕获/接受语义被 probe 消费）
- [x] 迟到结果不覆盖新空间（guard 观测 false）
- [x] 缓存按规范化 origin/user/tenant 隔离（query key 结构+normalize 收敛断言）
- [x] 切空间不取消服务端 Run（canceledServerRuns=0 probe 观测；服务端取消另有显式命令通道 MX-018）
- [~] 退后台/断流不取消服务端 Run：原语层结构保证（无取消代码路径），设备级观测随 MX-034
- [~] 「清除旧可见数据」的 UI 级清空（列表页状态重置）随消费页面（MX-013/014）以 query key 失效实现
- [~] 挂载级验证（屏渲染）：SpacePickerScreen 为 props 驱动纯展示，挂载验证随 MX-013 入口接线（D-023）

## 审查记录

- 待独立 review（提交后追加）。
