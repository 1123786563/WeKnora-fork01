# MX-031 证据 · 空间用量与只读商业视图

| 项 | 值 |
|---|---|
| 任务 | MX-031 空间用量与只读商业视图（S03 / core；UX 对应 M18） |
| BASE | `188da5fd`（MX-019 后） |
| 写锁 | UsageScreen.tsx / commercial/usage-presenter.ts / test+probe |

## 交付

- **commercial/usage-presenter.ts**：`presentUsage`（只读投影）——三桶（reserved/pending/settled）**原值透传**；pending 恒标「未最终」（最终性以服务端 settled 为准）；账单账户作用域标签（空间专属，不与其他空间混用）；as_of+单位+结算说明；`paymentRequests` 类型即 `0`（结构性无支付）；`canManageBilling` 分权（owner 才见管理说明）。
- **UsageScreen（M18）**：hero 卡（as_of 读屏+作用域）；三桶汇总卡（badge=预占/未最终/最终+释放/转入说明）；费用构成卡（平台服务费与 BYOK 模型费分列，「并非所有任务都免费」）；分权说明卡（管理在 Web 控制台、本页只读、无购买/退款入口）；加载/错误态；令牌视觉。
- probe：三桶原值/未最终/as_of/作用域/零支付断言 + 源级无 purchase/refund/钱包词条扫描。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-031.test.ts | 0 | 1/1（frozen usage-finality：120/38/1260/paymentRequests=0） |
| pnpm run test:mobile-v2 | 0 | 23/23 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] 只适配既定商业域事实（OpenMeter 模型投影端口），不建新钱包（paymentRequests 类型=0）
- [x] 显示时间与账单账户作用域，不混用其他空间余额（scopeLabel 断言）
- [x] BYOK 模型费和平台服务费分开说明（breakdown 分列+文案）
- [x] 普通 Admin 与 billing admin 分权（canManageBilling=owner；非 owner 文案导向 Web 控制台）；购买/退款不纳入本轮（无入口+源级扫描）
- [~] 真实 OpenMeter/商业 API 接线：snapshot 端口就绪（服务端用量读接口属 B/C 类——billing 域 OM-01..10 已 runtime pass（SDD 台账），移动消费端点随 MX-032 能力清单核验后接）；挂载随 profile Tab 路由（M17→M18）
- [~] 挂载 spec：随路由挂载补

## 审查记录

- 待独立 review（提交后追加）。
