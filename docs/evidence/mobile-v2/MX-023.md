# MX-023 证据 · 连接详情与授权生命周期

| 项 | 值 |
|---|---|
| 任务 | MX-023 连接详情与授权生命周期（S03 / connectors；UX 对应 M13） |
| BASE | `e3639894`（MX-020 后） |
| 写锁 | ConnectionScreen.tsx / resources/connection-controller.ts / test+probe |

## 交付

- **connection-controller.ts**：`createConnectionController`——
  - `revoke` 携带**当前授权版本**（版本冲突=VERSION_CONFLICT 拒绝）；撤销成功本地状态立即 revoked（正在显示的权限失效）；
  - `dispatchWithAuth` 撤销/旧版本双拒绝（迟到派发零外写）；最新版本授权通过——但**本控制器不实现任何 Provider 直写**（providerWriteCount 结构性 0；真实外写开关只由发布门禁控制，不以 T18 passed 自动开启）；
  - `rawSecretFields` 结构性空（客户端模型无凭据字段）。
- **ConnectionScreen（M13）**：个人/空间连接显式区分（ownership 标签+读屏）；「服务商凭据由服务端保管」明示；授权经产品 API+系统浏览器（复用 MX-020 ConnectionConsent）；撤销=danger+确认 Sheet（明示版本号与「正在显示的权限立即失效」）；版本冲突错误导向重查；加载/错误态。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-023.test.ts | 0 | 1/1（frozen connection-revoke-auth-version：providerWriteCount=0、rawSecretFields=[]） |
| pnpm run test:mobile-v2 | 0 | 27/27 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] 个人连接和空间连接明确区分（ownership 语义标签）
- [x] 不暴露 Provider 凭据（rawSecretFields 结构性空+屏文案「本设备不保存不显示密钥」）
- [x] 授权入口使用产品 API；系统浏览器回跳后查询（onAuthorizeInBrowser 端口+refresh）
- [x] 撤销带当前版本并使正在显示的权限失效（frozen：v3 撤销→v4；旧版本派发双拒绝）
- [x] 真实外写开关仅由对应门禁控制（控制器零直写；不因 T18 passed 自动开启——结构性保证）
- [~] 真实连接 API（产品连接读/撤销端点）：ports 注入就绪；connectors profile 服务端接线随 MX-032 能力清单核验（B/C 类判定）
- [~] M13 路由挂载与挂载 spec：随资源 Tab 详情路由（M12/M13 轮）

## 审查记录

- 待独立 review（提交后追加；与 020/021/024/031 合并重派——上轮代理 TLS 失败）。
