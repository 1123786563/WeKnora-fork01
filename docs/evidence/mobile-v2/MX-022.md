# MX-022 证据 · 资源与知识消费页面

| 项 | 值 |
|---|---|
| 任务 | MX-022 资源与知识消费页面（S03 / resources；UX 对应 M11/M12 前身） |
| BASE | `424cfe97`（MX-016 后；本提交与 MX-014 证据补齐/MX-017 P2 修复同批入库） |
| 写锁 | ResourcesScreen.tsx / KnowledgeScreen.tsx / resource-presentation.ts / test+probe + domain 出口 owners 扩展 |

## 交付

- **domain resource-presentation.ts**：`toKnowledgeResource`（未知扫描状态按 pending 展示，不臆造 indexed）；`revokedKnowledgeProjection`（403 撤权→敏感字段零可见，不从缓存回填）；`knowledgeRefForPrompt`（提问仅携带引用 ref——不复制文档跨空间；撤权返回 null）；`createScopedFavorites`（收藏按 scope 键隔离）；`scanStatusPresentation`（文字+tone 双通道）。
- **ResourcesScreen（M11）**：知识/连接/成果三段 tab——不复制全后台菜单；加载/错误/空态；段落内容宿主注入。
- **KnowledgeScreen**：知识列表（扫描状态 badge+读屏完整句）；撤权态=noPermission StateView（重试不缓存回填）；「用此知识提问」按钮被领域规则门控（ref null 即禁用+原因朗读）；收藏切换（标注「仅保存在当前空间」）。
- probe：撤权前缓存投影完好 → 403 后撤权投影零敏感字段、ref 构造 null；允许时 ref 只含 knowledgeId。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-022.test.ts | 0 | 1/1（frozen knowledge-access-revoked：visibleSensitiveFields=[]、canAskWithKnowledge=false） |
| pnpm run test:mobile-v2 | 0 | 18/18 |
| pnpm run test:shared | 0 | 590/590 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线（domain 出口遗漏级联 10 错当轮修） |

## 验收对照

- [x] 资源分知识/连接/成果段（非全后台菜单）
- [x] 撤权后敏感字段不可见（撤权投影，无缓存回填）；重试为显式动作
- [x] 「用此知识提问」仅引用不复制（ref 结构只有 knowledgeId）
- [x] 收藏归属 scope（createScopedFavorites 按 scopeKey 隔离）
- [x] 扫描/索引状态文字+颜色双通道
- [~] 文档预览「每次重新授权」与 M12 知识详情页：预览/详情流随 M12 文件路由（本轮任务写集不含 [kbId] 路由文件）——领域规则（撤权投影/ref 门控）已就绪供其消费
- [~] 真实知识 API 接线（服务端 knowledge-bases 列表 A 类已有）：rows 端口就绪，随资源 Tab 路由挂载接线（MX-013 入口模式）

## 审查记录

- 待独立 review（提交后追加）。
