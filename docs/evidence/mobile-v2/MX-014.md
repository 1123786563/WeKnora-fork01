# MX-014 证据 · 会话列表、搜索与分页

| 项 | 值 |
|---|---|
| 任务 | MX-014 会话列表、搜索与分页（S02 / core；UX 对应 M04） |
| BASE | `cd10655d`（MX-013 后） |
| 写锁 | SessionListScreen.tsx / session-list.ts / test+probe + domain 出口 owners 扩展 |

## 交付

- **domain session-list.ts**：`createSessionListController`——
  - 稳定分页游标 (updated_at, runId) 二元组（**相同 updated_at 跨页由 id 决胜**，frozen fixture 专测）；
  - 筛选/搜索进查询身份（queryEpoch+scope generation 双守卫）：filter 变更立即换查询；旧查询迟到响应被丢弃（不写入新状态）；切空间由 generation 前进自动失效（消费 MX-011 capture/accept）；
  - 搜索去抖只控制读请求（250ms 默认，测试可注 0；输入即时入 state）；
  - 条目按 id 去重 + duplicateIds() 观测口（重放/重复页零重复）；
  - 排序稳定：updated_at 同值时按 runId 倒序，翻页边界确定。
- **SessionListScreen（M04）**：筛选 chips（tablist 语义+selected 态）；搜索 Field 接 controller.setSearch（去抖读）；FlatList 稳定 keyExtractor=runId；onEndReached 分页；加载/空/无匹配三态（含新建任务入口位）；令牌视觉零散落常量。
- 控制器与屏解耦（注入 ports）：网络边界由宿主替换——**服务端列表端点**（B 类 GET /workbench/executions 全量分页）不在本任务写集，接线随需要它的页面任务（MX-015/017）与服务端任务落地；当前控制器/屏的验证以注入端口驱动（见下）。

## RED 记录

frozen 探针一次通过前无中间 RED（domain 先行、probe 随写随跑）；probe 的丢弃断言具有真实杀伤力：若旧筛选响应被误应用，`state.items 含 running 条目` 即抛（人为破坏守卫可复现失败——审查可验证）。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-014.test.ts | 0 | 1/1（frozen pagination-filter-scope：duplicateIds=[]、appliedFilter=waiting_user） |
| pnpm run test:mobile-v2 | 0 | 15/15 |
| pnpm run test:shared | 0 | 590/590 |
| apps/mobile tsc --noEmit | 非0（基线） | 14=基线（Screen 零新增） |

## 验收对照

- [x] 稳定分页（updated_at+id；相同时间戳跨页无重复/无漏）
- [x] 筛选条件纳入查询身份；迟到旧筛选响应丢弃（frozen 观测 appliedFilter=waiting_user）
- [x] 搜索去抖仅读请求；滚动与加载状态分离（loading 不阻塞已渲染项）
- [x] 虚拟列表 stable id（keyExtractor=runId；duplicateIds=[]）
- [~] 服务端列表端点接线（B 类 /workbench/executions 分页）：无服务端写集——controller 端口就绪，随 MX-015/017 与对应服务端任务接线（空搜索不反复查询全库由去抖+查询身份保证，端点侧限制由服务端任务出证）
- [~] 挂载级验证（屏渲染）：屏为 props 注入纯视图，挂载 spec 随入口任务（MX-017 会话 Tab 接线）补

## 审查记录

### Round 1（2026-09-18，独立子代理）：核心逻辑 PASS（epoch 维度断言经验证有真实杀伤力）/ 质量 CHANGES_REQUIRED（2×P2+5×P3）

- P2-1（已修）：generation 守卫在 frozen 场景无断言——probe 补「代际前进后旧空间在途响应不得落地」观测（staleVisible=false，移除守卫即翻红）。
- P2-2（已修）：已落地 items/nextCursor 无失效路径——controller 新增 `invalidate()`（清状态+作废在途）与接线合同注释（空间切换必须 invalidate 或重建实例）；probe 补失效观测。
- P3（已修）：subscribe/dispose 取代 50ms 轮询（卸载清去抖）；`gap:8` 令牌化（nativeTokens.spacing）；dedup 拷贝排序；翻页 footer 加载指示。
- 「切空间自动失效」原表述过强已更正（在途丢弃≠已落地清除）。修复后 mv2 复跑见 progress。
- 复核（scoped）：核心逻辑 PASS/APPROVED；**证据缺口（P2）**——frozen 场景原未真正翻页（waiting_user 首页无 nextCursor 即 exhausted）。R2 补齐：waiting_user 两页全同 updated_at（runId 决胜游标）+ 第二页含重放条目，断言 3 个唯一条目（跨页无重复/无漏自此为真实观测）。
