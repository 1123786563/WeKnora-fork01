# MX-036 证据 · 发布门禁、回退与交接

| 项 | 值 |
|---|---|
| 任务 | MX-036 发布门禁、回退与交接（S04 / core） |
| BASE | `a799d30c`（MX-035 后） |
| 写锁 | release-report.md / acceptance-index.json / mobile-release.md / test+probe |
| 状态 | 发布材料就绪；**未发布**（push/发布/生产均单独授权，未执行） |

## 交付

- **acceptance-index.json**：36 任务逐项终态+证据路径+交付 SHA（accepted 证据文件存在性由 probe 强校验）；发布裁决：core=releasable-with-conditions（条件：MX-034 设备证据、MX-035 设备矩阵、收尾批复核）、remote=blocked-env（MX-027 not-activated+manifest 强制关闭）、personal_node/full_happy=deploy-disabled、**globalAllPassed=false**。
- **release-report.md**：基线/最终 SHA、变更范围、回退方案（能力开关回退：先关新准入保留旧运行清理结算；无破坏性迁移）、交接（复现命令/责任人/风险）、单独授权项清单。
- **mobile-release.md**：发布 runbook——前置核验、构建（SDK55 锁定组合）、灰度顺序（服务端先发→客户端→core 灰度）、回退步骤、禁止事项。
- **probe**：真实读取验收索引+manifest+accepted 证据存在性+**全量回归真实执行**（独立子进程，排除自身）——frozen：core=releasable、remote=blocked-env、globalAllPassed=false。

## 验证（命令·exit）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-036.test.ts | 0 | 1/1（frozen；内嵌全量回归 42 用例全绿） |
| pnpm run test:mobile-v2 | 0 | 42/42 |
| pnpm run test:shared | 0 | 590/590 |
| go test ./internal/... | 0* | 99 包 ok；**1 项既有 flaky**（TestCreateTargetIfTrustedConcurrentCredentialRotation——上游对齐轮 2d6d18bd 引入，本轮 diff 为零；~1/6 概率并发窗口，**非本轮改动**，登记不动他人锁内文件） |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] 读取本轮全部前置并校验（acceptance-index 逐任务 hash+证据存在性强校验）
- [x] 未完成能力明确禁用（manifest+CapabilityService；支付/公开节点/E2EE 单独授权未申请）
- [x] 回退方案：先关新准入保留旧运行清理/结算（runbook 冻结）；兼容窗口说明（SDK55 锁定+首版商店包）
- [x] 逐路径提交（单分支串行 60+ commits）；push/PR/发布未执行（单独授权）
- [x] 交接：复现命令/风险/责任人（release-report §4）

## 审查记录

### Round 1（2026-09-18，收尾批独立审查）：其余 6 项全 PASS/APPROVED；本任务规格 FAIL（1 项）/ 质量 CHANGES_REQUIRED

- **P1（已修）**：probe「全量回归真实执行」为空门——NODE_TEST_CONTEXT 置空字符串（键仍存在）导致子进程 node:test 判定递归上下文**跳过全部 35 个文件**（exit 0 假绿）。修复：`delete env.NODE_TEST_CONTEXT`（键移除）+ 通过/失败计数双保险断言（pass≥40 且 fail=0；静默跳过时 pass=0 即红）。**杀伤验证**：恢复空门突变 → frozen 翻红（exit=1，错误消息含 skipping running files 证据）。
- 收尾批其余结论：MX-025/029 修复复核 PASS（各含变异验证）；MX-032/033 终审 APPROVED；MX-034 blocked-env 如实（探测真实读取环境——审查者 touch 标记+env 实证）；MX-035 APPROVED（变异验证：移除 accessibilityRole 被精确捕获）。
- minor 更正：前次「runbook 41→42 已修」声明失实（811720bf 未触及该文件——终核抓获）；终核轮真实修正 runbook 42/42 与 native-e2e.md 两处路径引用（含第 5 行叙述残留）。
- 修复后：mv2 42/42；mx-036 frozen 真实执行回归（~16s）通过。
- 终核（scoped）：**PASS**——空门修复经独立杀伤实证（变异翻红+skipping 证据捕获+计数门兜底）；两处 minor 已在本轮真实补修（含前次虚报声明的更正）。
