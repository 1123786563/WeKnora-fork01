# MX-018 证据 · 执行详情与保守取消展示

| 项 | 值 |
|---|---|
| 任务 | MX-018 执行详情与保守取消展示（S02 / core / 关闭 G08 展示面；UX 对应 M08） |
| BASE | `082489e6`（MX-015 后） |
| 写锁 | ExecutionScreen.tsx / execution-presentation.ts / test+probe + domain 出口 owners 扩展 |

## 交付

- **domain execution-presentation.ts**：
  - `ExecutionObservation`（run/execution/settlement 三原值+revision+observedAt）；
  - `presentCancelAcknowledged`：取消 ACK → **stop_pending（停止待确认）**——不改写 run 原值、refundRequested 恒 false（批准记录≠执行停止≠退款）；
  - `presentExecution`：空执行状态显示 **unknown_lookup**（不默认 failed/succeeded）；终态中文标签；结算 pending 标注「未最终」；
  - `shouldRequestCancel`：no_revision / terminal / already_pending 三拒因——无快照 revision 不发送 command。
- **ExecutionScreen（M08）**：状态卡（run badge+标签+观察时间读屏）；状态详情卡（三态+revision 分行展示+「取消为申请语义」说明）；终态可查看成果（结算以服务端为准）；申请取消=danger 按钮+**确认 Sheet**（明示修订号与「不是停止也不是退款」）→ACK 后 stop_pending StateView；重复申请/无 revision/终态禁用+原因朗读；全部令牌视觉。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-018.test.ts | 0 | 1/1（frozen cancel-not-stop：executionStatus=stop_pending、label=停止待确认、refundRequested=false） |
| pnpm run test:mobile-v2 | 0 | 21/21 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] run/execution/settlement 三原值+观察时间保留（观察模型+详情卡分行）
- [x] unknown 专用查询值（unknown_lookup，不显示为失败/完成）
- [x] 无 revision 不发送 command（shouldRequestCancel no_revision——与 MX-017 commands 修订号拒绝同语义）
- [x] 取消前确认（Sheet 二次确认+修订号+语义说明）；ACK 后 stop_pending 等待真实确认（不改写 run 原值）
- [x] 终态结果可查看、结算仍可 pending（「结算完成以服务端为准」）；禁止显示自动退款（refundRequested 结构性 false + 全展示断言）
- [x] 申请取消≠停止已确认（already_pending 拒因+文案「批准记录不代表执行已停止」）
- [~] 真实 command API 接线（onCancelRequest 端口）与事件流驱动的 stop_confirmed 转换：随 M07/M08 路由挂载（MX-017 SSE 装配轮）；停止确认以事件流为准的展示位就绪
- [~] 挂载 spec（Sheet 确认流交互）：Sheet 组件本身已有挂载验证（MX-008），屏级挂载随路由补

## 审查记录

- 待独立 review（提交后追加）。
