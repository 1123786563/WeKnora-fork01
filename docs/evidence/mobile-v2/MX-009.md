# MX-009 证据 · 四 Tab 与产品路由容器

| 项 | 值 |
|---|---|
| 任务 | MX-009 四 Tab 与产品路由容器（S01 / core / 关闭 G07 容器面；映射 W10、W11、UX01） |
| BASE | `08a5c9cb`（MX-008 后；本任务与其修复轮一并提交） |
| 写锁 | routes.ts / ProductShell.tsx / test+probe + spec（锁扩展）；两个 layout 文件**本轮零修改**（裁决见下） |

## 裁决：layout 文件零修改（写锁≠必写，同 MX-006 admission.go 先例）

app/_layout.tsx 与 (app)/_layout.tsx 当前承载 Happy 壳与既有重定向（无 host→server、无凭据→login）。产品入口切换点（登录/SSO 完成后进产品壳 vs Happy 壳）是 MX-010 的身份流决策——提前改 layout 会制造无鉴权壳或破坏 legacy 兼容。MX-009 交付容器+路由模型+挂载证明；入口接线随 MX-010 落地（其写锁含 auth 域），G07 的「渲染不依赖 Happy」由容器挂载证明（见下）。

## 交付

- **routes.ts**：PRODUCT_TABS 四固定入口（工作台/会话/资源/我的）；PRODUCT_ROUTES M01–M18 唯一 screen id+path+tab 归属（模块加载自检唯一性：重复 id/path 即抛）；PRODUCT_ENTRY_GUARDS 声明深链守卫链（ProductAuthProvider→ScopeProvider，实现 MX-010/011）；routeFor() 为导航唯一入口（页面不手写路径）。
- **ProductShell.tsx**：四 Tab 壳——固定四项 tablist（accessibilityRole=tab + selected 态）；内容 per-tab 插槽（后续任务注入真实 screen）；未接线 Tab 显示诚实「建设中」空态（StateView，不假装完成）；**零 Happy import**（模块图无 session/sync/auth）；主题全走 tokens（tab 高度 size.tab-height、选中 brand 色）。
- **spec**（product-shell.mobilev2.spec.tsx）：挂载观测四 Tab 标签 + 挂载与切换全程 fetch 计数（拦截 globalThis.fetch）——happyAuthRequests=0 为行为观测非静态声明。

## RED 记录

首跑 spec：tabLabels 命中 8 项（复合组件+宿主元素双层）——findAll 过滤宿主层（type===string）后真实通过。该失败为测试观察缺陷（组件行为正确），如实记录。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm run test:mobile-v2 | 0 | 10/10（frozen product-route-no-happy：visibleTabs 四项、happyAuthRequests=0） |
| pnpm --filter @weknora/mobile exec vitest run -c vitest.mobile-v2.config.ts | 0 | 4/4（MX-008 2 + 返回键场景 + MX-009） |
| apps/mobile tsc --noEmit | 非0（基线） | 14=基线（MX-009 新增 2 个类型错误当轮修复） |

## 验收对照

- [x] 四 Tab 固定且唯一（模型自检+挂载断言）；M01–M18 唯一 screen id 与导航路径（routes.ts）
- [x] 无需 Happy daemon/sync 渲染产品壳（零 Happy import + 挂载零网络请求）
- [~] 详情返回栈按 Tab 分组（模型 tab 归属）；「返回不回到另一空间旧页」的 generation 清栈在 MX-011
- [~] 深链不绕过 Provider：守卫链已声明（PRODUCT_ENTRY_GUARDS）；深链接线在 MX-021（通知深链）
- [~] expo-router 文件路由接线（/product/* 组与页面文件）：随 MX-010 起逐任务落（每任务写集含各自 screen 文件）；本轮无文件路由改动（见裁决）

## 未执行项

- 真机渲染/深链实链（native-e2e）→ MX-034；G07 页面半边（SessionView 解耦）→ MX-017。

## 审查记录

- 待独立 review（提交后追加）。
