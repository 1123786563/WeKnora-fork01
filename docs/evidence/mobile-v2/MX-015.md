# MX-015 证据 · 新建任务、资源编排与提交

| 项 | 值 |
|---|---|
| 任务 | MX-015 新建任务、资源编排与提交（S02 / core；UX 对应 M05） |
| BASE | `0119d82a`（MX-030 fix 后） |
| 写锁 | NewTaskScreen.tsx / workbench/task-form.ts / test+probe + domain 出口 owners 扩展 |

## 交付

- **domain task-form.ts**：
  - `evaluateSubmitReadiness`（纯裁决）：text_required / agent_required / budget_invalid（非负安全整数）/ **attachments_not_ready**（任何非 ready 附件阻塞提交并保留草稿）；
  - `toStartInput`：**严格 7 字段**（request_id/session_id/agent_id/target_id/workspace_ref/text/budget_upper）——附件/知识绝不擅加进 Start body（frozen 断言键集）；附件引用经 `prepareSessionRefs` 会话准备通道解析（版本化扩展，宿主接真实 API）；
  - `createTaskForm`：草稿优先（未就绪提交=零网络+saveDraft；取消显式 `cancelKeepingDraft`；离线不自动发送）；提交走注入端口（MX-006 协调器语义：先落盘 request_id 再发；unknown 导向原请求核实）；派发成功才清草稿文本。
- **NewTaskScreen（M05）**：任务内容/预算 Field（预算校验说明「结算以服务端为准」）；Agent 选择入口（M06 回调）；附件状态 chips（scanning=warning+「阻塞提交」读屏）；离线 StateView（草稿保留说明）；提交按钮被就绪裁决门控+禁用原因朗读；「保存草稿并返回」显式保留；无 fetch、无 request_id 发明（源级断言）。

## RED 记录

frozen 首跑失败一次：观测计数混入就绪阶段的合法提交（startCount=1≠0）——frozen 语义为**未就绪场景自身**的派发计数；分离计数器后就绪阶段转为附加验证（7 字段键集断言），frozen 归零。该失败为 probe 观测口径修正（实现行为正确），如实记录。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-015.test.ts | 0 | 1/1（frozen submit-attachment-not-ready：startCount=0、draft=保留这段文本） |
| pnpm run test:mobile-v2 | 0 | 21/21（20 个测试文件，mx-004 含 2 用例；R1 审查更正口径） |
| pnpm run test:shared | 0 | 590/590 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] 字段绑定草稿；文本/预算安全整数/资源权限校验（就绪裁决四因）
- [x] 资源按真实 API 准备（prepareSessionRefs 通道——附件不进 StartInput，7 字段键集断言）
- [x] 先保存原请求再提交（submit 端口由 MX-006 协调器承载先落盘语义）；unknown 导向原请求核实（协调器 reconcile 既有行为，MX-006 证据）
- [x] 取消输入保留可恢复草稿；离线不自动发送（offline 态显式+提交禁用）
- [x] 附件扫描中提交被阻止（startCount=0）且草稿逐字保留（frozen）
- [~] M05/M06 文件路由挂载与会话准备真实 API 接线：屏与表单就绪（回调/端口注入），挂载随会话 Tab 路由（MX-017 M07 接线轮）；真实附件上传 API 属 MX-025（resources profile）
- [~] 挂载 spec（屏渲染）：随路由挂载补（同上）
- **锁路径偏离披露（R1 P3）**：规格写锁 `weknora/workbench/task-form.ts`；实际落位 `packages/domain/src/mobile/task-form.ts`（纯领域模型，node 直跑 probe 可消费、无 RN 依赖）——注册表已按实际路径登记，原路径条目移除。

## 审查记录

- 待独立 review（提交后追加）。
