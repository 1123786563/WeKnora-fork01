# MX-026 证据 · 执行目标选择与能力解释

| 项 | 值 |
|---|---|
| 任务 | MX-026 执行目标选择与能力解释（S03 / remote 入口；UX 对应 M15） |
| BASE | `339bea50`（MX-028 后） |
| 写锁 | TargetPickerScreen.tsx / domain target-options.ts / test+probe + domain 出口 owners 扩展 |

## 交付

- **domain target-options.ts**：`selectExecutionTarget`——
  - 远程目标 **create/observe 双满足**才可选：create-only（不可观察，frozen）拒绝并给 `observe_unavailable:原因`；不可见拒绝 not_visible；不静默选择不可用远程（无可用→platform 回退）；
  - 偏好尊重（admitted 集合内）；`platformTarget()` 恒可选；
  - `targetCapabilityExplanation`：能力解释文案含原因（读屏与视觉共用——不只靠禁用态）。
- **TargetPickerScreen（M15）**：目标卡（名称+能力解释+可选/不可选/已选择 badge+完整读屏句）；不可选卡无 onPress（可见但不可触发）；确认/返回；只有平台可用时的空态说明；令牌视觉。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/mx-026.test.ts | 0 | 1/1（frozen target-not-observable：selectedTarget=platform、admittedRemoteCount=0） |
| pnpm run test:mobile-v2 | 0 | 30/30 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] create/observe 能力分开裁决（frozen：create=true observe=false → 拒绝+原因）
- [x] 普通可见性≠选择权限（visibility 与 capability 双门）
- [x] 无可用远程回退 platform（不静默选择）
- [x] 能力不可用显示原因（解释函数+读屏完整句）
- [~] 真实 execution-targets API 接线（A 类 GET /execution-targets 已存在——MX-001 矩阵）：remoteTargets 端口就绪，服务端 capability 字段映射随 M15 路由挂载轮接线；remote profile（MX-027 条件边）未激活
- [~] 挂载 spec：随路由挂载补

## 审查记录

- 待独立 review（提交后追加；随 MX-025/028/029 批）。
