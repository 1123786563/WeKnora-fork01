# MX-032 证据 · 服务端能力清单与部署开关

| 项 | 值 |
|---|---|
| 任务 | MX-032 服务端能力清单与部署开关（S04 / core） |
| BASE | `9496ec29`（MX-029 后） |
| 写锁 | capabilities.go / profile-manifest.json / profile-gates.md / test+probe + 锁扩展（Go 测试） |

## 交付

- **capabilities.go**：`CapabilityService`——
  - `EvaluateCapability` 四交集裁决（部署∩健康∩权限∩**验收证据**）→ 三态（supported / unavailable+原因 / forbidden）；**证据要求表**：remote 必须有 cancel-evidence（frozen：缺失→missing_cancel_evidence）；
  - `RevokeCapability`（core 不可撤销；撤销位独立——刷新不清除，重新准入走发布门禁）；`IsCapabilityEnabled`（服务端拒绝已撤销能力新准入）；`RefreshCapability`（部署/健康变化后调用——语义：刷新≠恢复）。
- **profile-manifest.json**：九 profile 部署开关+证据要求+说明——core 开启；oidc/resources/connectors/remote/dictation/voice 开启但证据未取得；personal_node/full_happy **deploy 直接关闭**（隐藏入口不置灰）。
- **profile-gates.md**：能力门禁单一事实源——各 profile 当前裁决+依据+待授权清单；D-016 P2-2（read-models 枚举收紧）与 D-025 的移交在此收口：枚举收紧随首个真实 read-model API 消费接线（MX-013 overview 已接——run_status 枚举在 TS parse 侧已强校验 [MX-003 契约]；inbox read-model 未发布——保持收紧项登记，不虚报完成）。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| go test ./internal/application/service/workbench/ -run TestMX032 | 0 | PASS（frozen+四交集+撤销+core 保护） |
| pnpm exec tsx --test tests/mobile-v2/mx-032.test.ts | 0 | 1/1（frozen：remoteCapability=unavailable、reason=missing_cancel_evidence） |
| pnpm run test:mobile-v2 | 0 | 32/32 |

## 验收对照

- [x] capability 取部署/服务健康/用户权限/验收证据交集（四输入裁决）
- [x] 九 profile 分开（core/oidc/resources/connectors/remote/personal_node/dictation/voice/full_happy）
- [x] 能力撤销后刷新并强制服务端拒绝新准入（RevokeCapability+IsCapabilityEnabled；Refresh 不清除撤销）
- [x] 配置只隐藏未发布能力（unavailable=入口不渲染——客户端三态消费已备；不置灰按钮当完成）
- [~] 客户端消费接线（CapabilityService → 移动 capability 查询 API）：服务端裁决就绪；HTTP 端点接线随首个 gated 能力页面发布（MX-034/036 出证后）——当前 manifest 为部署侧事实源
- [~] conditional_dependencies（resources/connectors/remote/dictation/voice 五组）：resources 组（022✓+024✓+025✓ 待审后）、connectors（023✓）、dictation（028 待审）、voice（029 待审）实现面已交——**证据面**仍 unavailable（见 profile-gates）；remote 组 MX-027 未实施（条件边未激活）

## 审查记录

- 待独立 review（提交后追加；与 MX-033 批合并）。
