# MX-034 证据 · 双平台产品链 E2E

| 项 | 值 |
|---|---|
| 任务 | MX-034 双平台产品链 E2E（S04 / core；conditional resources 组实现面已交） |
| BASE | `ddfe1831`（025/029 probe 修复后） |
| 写锁 | maestro/{core,scope}.yaml / native-e2e.md / test+probe（测试落位 e2e/ 子目录——显式命令运行，见下） |
| 状态 | **blocked-env（如实）**——测试以抛错表达（非 skip 假通过）；`pnpm run test:mobile-v2` 全量门禁不含本测试（glob 不入 e2e/），显式命令 `pnpm exec tsx --test tests/mobile-v2/e2e/mx-034.test.ts` exit=1 + blocked-env 消息（已实测验证） |

## 交付

- **maestro/core.yaml + scope.yaml**：核心产品链（M01→M02→M03→M05→M07→M08→M09→M17→退出）与空间切换/杀进程恢复链的 E2E 编排（前提注释齐备：dev client 构建/模拟器/后端栈/受控账户）。
- **probe（mx-034）**：三要素真实探测——dev client 构建标记（`.e2e-{platform}-build.ok`，本机构建生成）、iOS booted 模拟器（xcrun simctl）/Android 设备（adb devices）、后端环境（E2E_WEKNORA_ORIGIN + E2E_PRODUCT_ACCOUNT）；齐备则运行 Maestro 并解析 JSON（flows 全 ok + run 去重）；**任一缺失抛 blocked-env**。实测：当前 exit=1 + blocked-env（未伪造通过）。
- **native-e2e.md**：运行配方（逐步解除阻塞：构建→模拟器→Maestro→frozen 链）+ 记录要求（build/OS/后端 SHA/数据库来源/冷启动/前后台/杀进程/权限拒绝逐项）。
- 已验证等价层（如实分层，**不替代 native-e2e**）：挂载 9/9、故障注入 7/7、跨语言字节 frozen——全部在 native-e2e.md 登记。

## 验证（命令·exit）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/e2e/mx-034.test.ts | **1** | blocked-env（三要素缺失：devBuild/adb-设备/账户环境）——正确的红，不假绿 |
| pnpm run test:mobile-v2 | 0 | 40/40（E2E 门禁独立，不混入单元层） |

## 验收对照

- [x] E2E 编排与真实执行探测交付（blocked-env 语义正确——缺失即抛错）
- [x] 浏览器原型截图不替代 Native E2E（native-e2e.md 明示；视觉对比归 MX-035）
- [~] 真实产品账户+平台 Agent 走完核心链：**blocked-env**（受控账户未授权+dev client 未构建+adb 不在 PATH）——配方就绪，解除后执行并回填
- [~] 冷启动通知/前后台/杀进程/权限拒绝分别记录：套件含场景位；执行随上项
- [~] build/OS/后端 SHA/数据库来源记录：native-e2e.md 模板就绪，执行时回填

## 审查记录

- 待独立 review（提交后追加；与 MX-032/033/035/036 收尾批合并）。
