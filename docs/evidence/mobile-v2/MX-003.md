# MX-003 证据 · 跨语言契约与能力规则冻结

| 项 | 值 |
|---|---|
| 任务 | MX-003 跨语言契约与能力规则冻结（S00 / core / 关闭 G03、G08；映射 W01、W06） |
| BASE | `52c87b1c`（MX-002 后） |
| 写锁 | task-index 原集 + 锁扩展 6 文件（见 file-ownership.json `mx-003-lock-extension`） |

## RED 记录（真实）

1. **TS 行为 RED**：快照冻结字段进入消费方后 `pnpm run test:shared` 出现 **3 个真实断言失败**（api-client executions「uses encoded paths…」「unwraps and validates…」+ contracts「validate execution DTO capabilities…」——fixture 缺 incomplete/confirmed_watermark，解析器如实拒绝）。这是契约收紧暴露的真实行为缺口，非构造失败。
2. **Go 接缝 RED**：`go test ./internal/workbench/ -run TestMX003` 编译失败 `InteractionDecision has no method Validate`（接缝缺失，记录为无效 RED）；实现后转绿。
3. TS 侧首跑（实现前）为模块缺失（interactions.ts 不存在），同记为接缝阶段。

## 冻结内容

- **interactions.ts（新）**：InteractionKind/Action、kind×action 矩阵（镜像 Go ValidateInteractionAction）、parseInteraction/parseInteractionDecision（decision_id/args_hash 必填镜像 handler；kind/action 互换拒绝）。
- **execution.ts（扩展）**：ExecutionSnapshot 增 `incomplete`/`confirmedWatermark`（修复 Go→TS 字段漂移：Go ExecutionSnapshot 原有 Incomplete/ConfirmedWatermark 而 TS 不解析）；新增 `CommandAction`/`CommandDecision`/`evaluateCommand` 冻结命令准入（D-013）。
- **read-models.ts（新，B 类提案）**：WorkbenchOverview/ExecutionListPage/InboxPage 严格解析器；文件头与服务端接通前不发布语义明确。
- **interaction.go**：新增 `InteractionDecision.Validate()`（id/decision_id/args_hash 非空 + 动作矩阵 + revision 安全范围），handler 校验可复用。
- **contracts.go**：**无需修改**——既有 Validate 已含 time.Parse 日历校验、payload 对象检查、seq 上界（复验而非重写；MX-001 审计对正则的担心不成立，已复验）。
- **共享 fixture**：tests/mobile-v2/fixtures/mx-003-crosslang.json 被 TS probe（probes/mx-003.ts）与 Go 测试（crosslang_mx003_test.go）**同一份**消费。
- **规则冻结**：未知事件 type 保留（两端测试断言 future.event 存活）；未知控制状态（run_status/capability state）拒绝；seq 溢出/缺 run_id/数组 payload/非法日历两端一致拒绝；budget×approve 互换拒绝。

## 验证（命令·退出码·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm run test:mobile-v2 | 0 | tests 3 / pass 3（mx-003 1 + 回归 2） |
| pnpm run test:shared | 0 | tests 579 / pass 579（含更新后的 2 个消费方 fixture） |
| go test ./internal/workbench/ | 0 | ok（含 3 个新跨语言测试 + 既有 contracts/interaction 测试） |
| go test ./internal/handler/session/ | 0 | ok（未破坏 handler 层） |
| go vet ./internal/workbench/ | 0 | clean |

## A/B/C 与兼容

- A 类路由 body 未变（InteractionDecision 结构原样；handler 兼容——handler 已有校验与 Validate 一致）。
- B 类 read-models 仅契约形状冻结，无路由/DI/SDK 发布（对应 MX-013/014/021）。
- Start body 7 字段不变；未增加任何未注册字段。

## 审查记录

- 待独立 review（提交后追加）。
