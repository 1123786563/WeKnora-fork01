# MX-007 证据 · 语义设计令牌与原生主题

| 项 | 值 |
|---|---|
| 任务 | MX-007 语义设计令牌与原生主题（S01 / core） |
| BASE | `c9505e26`（MX-005 R2 后） |
| 写锁 | task-index 原集（design-tokens mobile 三件、theme.ts、test+probe）+ 锁转移 2（package.json×2，D-019） |

## RED 记录

- 首跑（拷贝后、probe 修正前）两次真实失败：①flatten 未解包 W3C `{$type,$value}` 节点（56 键对56 键被误报差异）；②CSS 校验假设 `--color-` 前缀而 tokens.css 实为 `--<key>` 按 `[data-theme]` 分块。均为 probe 对真实数据的观察缺陷（python 预检已证令牌源零漂移），修正后 GREEN。过程中一次 Edit 被 Mimosa 误判「命令注入」（动态 RegExp 构造），改纯字符串查找后通过——D-011 模式复用。

## 交付

- `packages/design-tokens/src/mobile/{tokens.json,tokens.css,native-tokens.ts}`：设计包逐字拷贝（native-tokens.ts 仅加两行溯源注释；diff 验证）。出口 `@weknora/design-tokens/mobile`（子路径注册）。
- `apps/mobile/sources/weknora/ui/theme.ts`：weknoraThemes（light/dark 解析）、resolveThemeMode（系统外观默认+受控覆盖入口，MX-030 接偏好）、useWeknoraTheme、touchSizes（44/48 合同）。数值主题无 CSS px/box-shadow 字符串；elevation 留组件侧平台适配。
- apps/mobile 增依赖 `@weknora/design-tokens: workspace:*`（锁转移登记）。

## 验收对照（probe 观测，全部真实数据）

- [x] 浅/深全部必需 key 同构：56/56，missingThemeKeys=[]
- [x] web/native 颜色零漂移：tokens.json($value) vs native-tokens vs tokens.css 分块值三方一致，webNativeColorDiff=[]
- [x] 关键 ACTIVE 文本/状态对比率 ≥4.5（10 对 × 2 主题全过：ink/bg/surface/surface-alt、muted/bg、on-brand/brand、hero-ink/hero、warning/danger/info 各自 soft 底、brand/bg）；disabled 对 light=3.31/dark=5.22 按 WCAG 1.4.3 豁免（probe stderr 报告，D-019）
- [x] reduced motion（motion.reduced=0）与动态字体（fontSize 数值随系统缩放）在令牌层具备；组件级验证归 MX-008
- [~] 生成无漂移：三文件自设计包逐字拷贝 + probe 三方比对常驻（后续任何人改令牌即被抓住）

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm run test:mobile-v2 | 0 | 8/8（mx-007 1 + 回归 7） |
| pnpm run test:shared | 0 | 589/589 |
| apps/mobile tsc --noEmit | 退出码非零（既有基线） | 错误数 **14=基线持平**（本轮新增 6 个已全部修复，见 D-021；含 flash-list 对齐连带 1 个定点 @ts-expect-error） |
| vitest stream-transport.test.ts | 0 | 6/6（联合帧形状回归） |

## 审查记录

- 待独立 review（提交后追加；与 MX-006 修复确认合并派发）。
