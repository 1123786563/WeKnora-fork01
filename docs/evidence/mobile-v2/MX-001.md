# MX-001 证据 · 当前代码、接口与台账对账

| 项 | 值 |
|---|---|
| 任务 | MX-001 当前代码、接口与台账对账（S00 / core / 关闭 G11、G12） |
| BASE SHA | `6a70c35a19c58511b8372b5a68b5147ab577ad14`（main） |
| 工作区 | `.worktrees/expo-mobile-v2`（codex/expo-mobile-v2） |
| HEAD SHA | 见本文件末尾「提交记录」 |
| 写锁 | docs/evidence/mobile-v2/{current-state-matrix.md,file-ownership.json}；tests/mobile-v2/{mx-001.test.ts,probes/mx-001.ts}（另建 progress.md/preflight.md/decisions.md 为台账必需，属协调者职责） |

## 行为 RED（真实断言失败）

命令：`tsx --test tests/mobile-v2/mx-001.test.ts`（worktree 根，node_modules 未装时借主 checkout tsx 二进制，probe 按 `__dirname` 解析 worktree 根）
结果：exit=1，tests 1 / pass 0 / fail 1。失败点：`assert.deepEqual` —— `unownedGaps` 为全部 182 个 task-index 声明文件（file-ownership.json 不存在）。依赖与 probe 模块均已就位，属有效行为 RED。

## GREEN 与迭代中发现的真实缺口

1. 首版注册表写入后仍 RED：probe 观察到 `internal/router/routes_workbench.go` 声明者 MX-027 未被注册表覆盖（MX-001 预登记初版漏掉该声明者）——**这是 probe 从真实数据发现的缺口，不是构造的失败**。
2. 修正 owners=[MX-013,MX-021,MX-027,MX-032] 后 GREEN：exit=0，tests 1 / pass 1 / fail 0，无 skip。

## 交付物

- `docs/evidence/mobile-v2/current-state-matrix.md`：G01-G12 逐项（当前事实/代码位置/已有测试/接线/修复/负责人/缺失证据）+ A/B/C 接口分类 + DI 与迁移基线。
- `docs/evidence/mobile-v2/file-ownership.json`：189 文件锁（182 来自 task-index + 4 热点预登记 + 4 协调者台账；workbench_read.go 双源重叠计 1）；kind 以基线 `6a70c35a` ls-tree 为准：26 modify / 163 create；8 个多任务/协调者文件显式 `shared_serialized:true`；迁移序列头 PG 000136/SQLite 000058 待用。
- `docs/evidence/mobile-v2/preflight.md`：基线、环境、已有台账盘点。
- `docs/evidence/mobile-v2/decisions.md`：D-001~D-008。

## 三路审计要点（详见 current-state-matrix.md）

- 确认互通性缺陷：G01（Go payload-only vs TS 全 envelope）、G02（approve/reject 悬空）、G04（createRequestID 悬空导入 + request_id 拼接）。
- 新发现（超出 G 清单）：Go `GormCancelPort` 允许 `recovering` 而 `validRunStatus` 不含（枚举不一致，记 MX-003 对齐）；admission publish 为 no-op、budget 固定 NoopTaskBudget（现状基线）。
- 台账事实：W 索引从未更新 vs SDD 台账部分完成；main 已含集成代码。MX 轮不重置历史。

## 验收对照

- [x] 每个 G 缺口有代码位置、MX 负责人、缺失证据（current-state-matrix.md）
- [x] Create/Modify 与实际文件状态一致（file-ownership.json kind 按基线 `6a70c35a` ls-tree 裁定：26 modify / 163 create；probe 内置基线不变量守护，冲突即失败）
- [x] 未把原 pending/passed 复制为本轮事实（W/T 台账原样；MX 全部任务仍 pending，progress.md 单独跟踪）
- [x] 接口 A/B/C 标注完成；B 类在服务端接通前不发布（写入 decisions D-004 语义）

## 环境与命令记录

- 审计：三路只读 Explore 子代理（Go 后端 / 移动 TS / 台账测试门禁），结论均有文件:行号。
- 测试命令与退出码：RED exit=1（/tmp/mx001-red.log）→ GREEN exit=0（/tmp/mx001-green.log），tests 1 / pass 1。
- 环境：go1.26.3、node v26.7.0、pnpm 10.28.2；Docker WeKnora 栈运行中；Xcode 27 + iOS 26.5 模拟器可用；adb 不在 PATH。

## 审查记录

### Round 1（2026-09-18，独立子代理，规格 FAIL / 质量 CHANGES_REQUIRED）

P1-1 注册表把本任务两个测试文件误标 modify（自参照磁盘快照错误）→ 已改为按基线 ls-tree 裁定（create）并在 probe 加 kind 不变量守护。
P1-2 MX-001.md 计数失实（23/162）→ 已重算为 26 modify / 163 create。
P2-3 preflight「主 checkout clean」过期 → 已更正为如实记录（首次记录时 clean，复核时存在用户 miniprogram 未提交改动，保留不动）。
P2-4 四个协调者台账文件未入注册表 → 已补入（MX-001.md/preflight.md/decisions.md/progress.md，协调者追加式单写）。
P2-5 审查节悬空前向引用 → 已改为按轮次如实记录（本节）。
P2-6 probe 增加 kindConflicts 偏离冻结断言的替代实现：以抛错守护，Observation 形状保持规格冻结。
Round 1 修复后复核（同审查代理 scoped）：6/6 已修复，测试实跑 exit 0、1/1 通过，总结论 **PASS**。MX-001 按验收条款 accepted。

## 未覆盖项与剩余风险

- 视觉/原生/GIF 对比未做（MX-008+ 各自覆盖）；本任务无原生环境需求。
- W 轮 SDD 台账中 W08/W10 修复循环的最终归属未逐行核验（对 MX 轮无阻塞；如 MX-010 触及相同文件再复核）。
- progress.md 为协调台账首建，随任务推进更新。

## 提交记录

- 交付 commit：`04176329`（BASE 6a70c35a；`git diff --cached --check` 通过，退出码 0）
- Round 1 复核：独立审查代理 scoped 复核 6/6 已修复，总结论 PASS（2026-09-18，报告于会话证据 /tmp 快照 + 本节记录）
- SHA 回填与本行：证据修订随后续协调提交入库
