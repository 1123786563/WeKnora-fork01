# MX-002 证据 · Expo 与 React 原生依赖基线

| 项 | 值 |
|---|---|
| 任务 | MX-002 Expo 与 React 原生依赖基线（S00 / core / 关闭 G05；映射 W36） |
| BASE | `64b8da68`（MX-001 台账收尾后） |
| 写锁 | apps/mobile/package.json、package.json、pnpm-lock.yaml、docs/evidence/mobile-v2/native-baseline.md、tests/mobile-v2/{mx-002.test.ts,probes/mx-002.ts} |
| 环境门槛 | iOS/Android 开发构建（见「未执行项」） |

## 行为 RED

命令：`pnpm run test:mobile-v2`（即 task-index 的 `pnpm exec tsx --test tests/mobile-v2/mx-002.test.ts` 等价根脚本）
RED：exit 1；actual `{ rendererMatchesReact: false, nativeBuildPlatforms: ['android','ios'] }`。false 由真实矩阵偏差驱动（react-native 0.83.1≠0.83.10、react 19.3.0≠19.2.0、keyboard-controller/skia/flash-list 等 13+ 项，见 native-baseline.md 对齐前列）。依赖与 probe 均就位，属有效行为 RED。

## 实现与 GREEN

- `npx expo install <pkg>@<官方期望>`×17 → package.json 对齐官方 SDK55 矩阵；`pnpm install --prefer-offline` 同步锁文件与 node_modules。
- apps/mobile packageManager → pnpm@10.28.2（与根一致）。
- 根 package.json：新增 `test:mobile-v2`（tests/mobile-v2 门禁入口，D-006 落地）；`test:shared` 补入 `packages/domain/src/mobile/*.test.ts`（G10 部分修复，2 个既有测试文件纳入门禁）。
- GREEN：`pnpm run test:mobile-v2` exit 0，tests 2 / pass 2 / fail 0（mx-001 回归同绿）。
- 迭代中发现：flash-list 官方期望为**精确 2.0.2** 而非 ~2.0.2（首装 ~2.0.2 解析到 2.0.3 仍 RED）——probe 从真实数据逼出，二次精确对齐后转绿。

## 验收对照

- [x] 锁文件解析记录（node_modules 实测版本表见 native-baseline.md，非 manifest 声明）
- [x] doctor 未解决项逐条列明（4 项，处置见 D-009）
- [x] 双平台 prebuild 通过（ios/android 均 exit 0，生成目录 gitignored，无删除行为）
- [x] 未通过关闭新架构/移除功能掩盖不兼容（newArchDefault 保持，无功能移除）
- [~] 双平台**完整编译+启动**：未执行，见下

## 回归与共享检查

- `pnpm run test:shared`：exit 0，579/579（含新纳入的 domain/mobile 2 个测试）。
- `pnpm --filter @weknora/mobile typecheck`：exit 2 —— 3 个错误为 MX-001 已登记继承缺陷（G04/G02），对齐前后一致，归属与恢复时点见 D-010。非本任务回归。

## 未执行项 / 阻塞

- xcodebuild/gradle 完整编译与模拟器启动：未执行（工具链在位；安排集成检查点+MX-034 同一 lockfile 出证）。据此 MX-002 状态记 **accepted-with-followups**：core 契约面（版本矩阵+prebuild+回归）通过，原生完整构建证据延后补录，不以本任务声称真机可用。

## 审查记录

- 待独立 review（提交后追加）。
