# MX-013 证据 · 工作台聚合读模型与首页

| 项 | 值 |
|---|---|
| 任务 | MX-013 工作台聚合读模型与首页（S02 / core / 关闭 G12 服务面；UX 对应 M03） |
| BASE | `6d94a204`（MX-012 fix 后；本提交同时携带 MX-012 观察项闭环批次） |
| 写锁 | task-index 原集（overview.go/workbench_overview.go/overview.ts/HomeScreen/test+probe）+ D-004 预登记热点（routes/DI）+ 锁转移/扩展 7 条（见注册表 mx-013 组） |

## 交付（关闭 G12 服务面：B 类 overview 全链接线）

- **Go 服务**（service/workbench/overview.go）：`OverviewService`——tenant+owner 谓词、单查询聚合（进行中≤20 按 updated_at 倒序、待处理交互≤20、计数、as_of=UTC RFC3339）；unread_notifications/recent_artifacts 在 MX-021/024 接入前如实为 0/[]。
- **Go handler + 路由 + DI**：`GET /api/v1/workbench/overview`（Viewer+API-key 同读边界；身份双通道提取与既有 handler 一致）；container 双 Provider + router params + RegisterWorkbenchOverviewRoutes（B 类路径按 04-api-contracts §2 采用 /workbench/overview，D-025）。
- **SDK**：`createOverviewApi`（api-client/mobile/overview.ts + 出口）；严格 parseWorkbenchOverview（MX-003 冻结形状；title 可选化锁转移记录——服务端现无该字段，B 类未发布期收紧发布前对齐）。
- **HomeScreen（M03）**：一次聚合加载（单 loader 调用，**零逐会话补读**）；加载/错误/离线/空态齐备（StateView）；as_of 可见+读屏；待审批与进行中卡片真实入口位（onOpenApprovals/onOpenRun 回调，路由目标随 MX-014/017 接）；查询键经 productQueryKey 按 scope 隔离。
- **入口接线（D-023 落地）**：`app/(app)/product.tsx`（产品入口：未认证→login、无 host→server，深链不绕 Provider）；根布局认证+已引导用户访问索引 → Redirect 产品壳。legacy Happy 路由不动。

## RED 记录

- Go frozen 测试先行：首跑编译失败（service/handler 尚未实现的符号）→ 实现（读模型别名行/双通道身份）后 GREEN（属主隔离：u1 只见 owned-run，other-run 不可见）。
- 挂载 spec 两轮真实失败：①label 断言文案不匹配（「待审批 1」vs 实际「查看 1 项待审批」）；②未传 onOpen* 处理器时 Card 是纯 View 无 accessibilityLabel——传真实处理器后通过。均如实记录。
- 注册表守护再立功：read-models.ts kind 误标 modify 被 mx-001 基线不变量当场拦截（修复后 14/14）。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| go test ./internal/handler/session/ ./internal/application/service/workbench/ | 0 | 全 ok（含 frozen TestMX013OverviewOwnerScope：visibleRunIds=[owned-run]、perSessionHTTPRequests=0） |
| go build ./internal/... | 0 | 全量构建（路由/DI 接线） |
| pnpm run test:mobile-v2 | 0 | 14/14（frozen overview-owner-scope：Go+挂载双观测链） |
| 挂载套件（vitest -c mobile-v2） | 0 | 9/9（home 3：单次聚合/错误重试/壳内挂载；其余回归） |
| pnpm run test:shared | 0 | 590/590 |
| apps/mobile tsc --noEmit | 非0（基线） | 14=基线（新增 7 个类型错误当轮全修：出口/参数/typed-routes/@types 缺声明） |

## 验收对照

- [x] 服务端授权后聚合计算（tenant+owner 谓词；上限 20 防大租户）
- [x] 客户端零逐会话请求（frozen perSessionHTTPRequests=0 双层观测：Go 结构性 + 挂载 aggregateCalls=1）
- [x] as_of 服务端时间戳（UTC RFC3339）并在 UI 可见
- [x] UI 首页层次+分页留位+错误/空/离线态+上次同步（M03）
- [x] 产品入口接线（D-023）：认证→产品壳；不绕 Provider；legacy 不破坏
- [~] 进行中/待审批卡片的导航目标（M07/M09 路由）随 MX-014/017 落地（回调已留）
- [~] PostgreSQL 聚合查询证据：blocked-env（TRPC_TEST_POSTGRES_DSN 未设置；SQLite 真实执行）——发布门禁 MX-036 前补

## 审查记录

- 待独立 review（提交后追加）。
