# MX-005 证据 · 类型化交互与乐观并发命令

| 项 | 值 |
|---|---|
| 任务 | MX-005 类型化交互与乐观并发命令（S00 / core / 关闭 G02、G03 服务面；映射 W05、UX05） |
| BASE | `98002709`（MX-004 后） |
| 写锁 | task-index 原集（workbench_commands.go / service interaction.go / api-client interactions.ts+test+probe）+ 锁扩展 5 文件（含 MX-003 P2 锁转移，D-016） |
| 环境门槛 | 隔离 PostgreSQL + SQLite —— SQLite 真实执行；**PostgreSQL blocked-env**（TRPC_TEST_POSTGRES_DSN 未设置，容器在位但凭据未授权入证据） |

## RED 与复验

- **已有正确行为复验**：GormInteractionStore.Decide 的 CAS/幂等/冲突（含 SQLite busy 重试与 8 次退避）在基线已有测试 `TestGormInteractionStoreScopesOwnerAndCASesDecision`（两 goroutine 并发决定 → 1 成功 1 冲突）。本任务**不制造退化**，仅追加结构化观测行供跨语言 probe 消费。
- **真实 RED**：handler 接线 `input.Validate()` 首版遗漏「wire 的 id 来自 URL 参数」，3 个既有 HTTP 测试立即失败（400 而非预期）——真实断言失败暴露 wire/规则差异；修复为绑定 URL id 后再 Validate（D-017）。SDK 测试首版 mock 自身缺陷（多包一层函数）亦被真实断言捕获。
- probe GREEN：`pnpm exec tsx --test tests/mobile-v2/mx-005.test.ts` exit 0，1/1（accepted=1 conflict=1 来自真实 Go 测试输出解析）。

## 实现

- **SDK（packages/api-client/src/mobile/interactions.ts 新）**：createInteractionsApi——list（GET :run_id/interactions，pending 项 decision_id 空串合法）、decide（POST interactions/:id/decisions；**请求前**本地 parseInteractionDecision 冻结 body+矩阵校验，非法组合零网络请求）；错误语义注释冻结（409 不自动重发旧决定；403 不刷新 token；unknown 不伪装 failed）。
- **handler（workbench_commands.go）**：手工校验替换为 URL-id 绑定 + 共享 Validate（D-017），行为与原等价且覆盖更全（kind×action 矩阵/revision 范围进入服务端 400 路径）。
- **Go 服务**：CAS/幂等/撤权回滚既有行为复验通过（含 RollbackDecision、gate 注入失败补偿）；budget/recovery kind 现无 pending 落库来源（仅 tool_approval CreatePending）——不虚构预算审批链，留 MX-020。
- **MX-003 审查 P2 承接（D-016）**：P2-1 快照 ConfirmedWatermark 上界、P2-3 注释精确化已修；P2-2 归 MX-013/021。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| go test ./internal/handler/session/ | 0 | ok（含 3 个决定 HTTP 测试修复后全绿 + MX-004 emit） |
| go test ./internal/application/service/workbench/ | 0 | ok（CAS 并发、gate 竞态、投影、回滚全套） |
| go test ./internal/workbench/ | 0 | ok（P2-1 上界校验入套） |
| pnpm run test:shared | 0 | tests 582 / pass 582（新增 3 个 interactions SDK 测试） |
| pnpm run test:mobile-v2 | 0 | tests 6 / pass 6（MX-001..005） |

## 验收对照

- [x] 显式适配真实 DTO（无 any/optional-call 掩盖；SDK 出口类型即 contracts InteractionRecord）
- [x] 绑定 actor/tenant/摘要/revision：服务层从持久记录取身份（commandContext 注入 tenant/principal/actor；store 查询按 tenant_id+owner_id 过滤——复验）
- [x] 同意/拒绝 CAS；相同决定幂等；旧版本 409（ErrConflict → handler 映射，既有+复验）
- [x] 两设备并发只接受一次（probe 1/1 实测）
- [x] 工具审批不叠加第二次商业准入（gate 唯一注入点 + 注入失败 RollbackDecision 补偿复验）；预算审批单独角色检查——**无预算 pending 来源，未启用**（MX-020）
- [~] 撤权/到期/修改目标后旧批准不可执行：args_hash 不匹配即 ErrInteractionActionMismatch（复验）；**到期时间字段现网 wire 无 expires_at**——B 类扩展留 MX-019/020 版本化
- [x] 403 不刷新 token / unknown 不伪装 failed：SDK 注释冻结 + 无任何自动重发逻辑（结构性）
- [~] PostgreSQL 并发证据：blocked-env（DSN 未设置），SQLite 真实并发已证；PG 恢复时重跑 service 测试即可

## 未执行项

- PG 行级锁并发（blocked-env）；页面消费（approve/reject 按钮接通）在 MX-017/019（VM 重写）。
- 移动端 ConversationScreen 的悬空 approve/reject 调用仍存在（MX-017 重写时消除，G02 页面半边由其关闭）。

## 审查记录

### Round 1（2026-09-18，独立子代理）：规格 FAIL（P1-1）/ 质量 CHANGES_REQUIRED

- **P1-1（已修复）**：真实服务端 pending 交互 wire 为 `action:""`（审查者端到端喂入实测），parseInteraction 强制非空导致 list() 必失败。修复：InteractionRecord.action 类型放宽为 `InteractionAction | ''`，pending（decision_id 空）允许空 action；已决定项仍须矩阵动作；decide 路径拒绝空 action。新增 packages/contracts/test/mobile-interactions.test.ts 以审查者提供的真实 wire 字面量为永久回归。
- **P2-3（已修复）**：SDK 测试 pending fixture 改为真实 wire 形状（action 空串），并断言解析结果。
- **P2-2（已修复）**：probe 接线 fixture/fault 输入（不支持的组合显式拒绝）。
- **P2-1（如实记录，不虚构）**：「绑定权限版本」在当前冻结契约（A 类 wire 与 Go 持久层）中**不存在该字段**——actor/tenant/args_hash/revision 已绑定；permission_version 需服务端版本化扩展引入后再绑定（MX-019/020 提案路径），本任务不擅加字段。

### Round 1 修复后回归（如实更正）

- pnpm run test:shared → exit 0，589/589（含新增契约回归 3 项）。
- pnpm run test:mobile-v2：修复代码后曾实测 exit 0（7/7）；但随后注册表 kind 误改（create→modify）未重跑——复核代理实跑 exit 1（6/7，mx-001 基线守护正确拦截该回归）。Round 2 已恢复 kind=create 并复跑，见下。
- 修复复核（scoped）：1-3 项确认已修复并实测；第 4 项发现注册表回归（本轮 R2 修复）；第 5 项指出回归声明失实（本节即更正）。

### Round 2（注册表回归修复）

- file-ownership.json：packages/contracts/src/mobile/interactions.ts kind 恢复 create（相对基线恒为 create；owners/shared_serialized 保留）。
- 复跑 `pnpm run test:mobile-v2`：结果见 progress.md（以实跑为准）。
