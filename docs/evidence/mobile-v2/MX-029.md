# MX-029 证据 · 实时语音与独立中断语义

| 项 | 值 |
|---|---|
| 任务 | MX-029 实时语音与独立中断语义（S03 / voice；conditional remote 未激活） |
| BASE | `b9e00fda`（MX-026 后） |
| 写锁 | voice/realtime.ts / voice/VoiceSessionPanel.tsx / service workbench voice_session.go / test+probe + 锁扩展（Go 测试） |

## 交付

- **Go voice_session.go**：`VoiceSessionService`——Authorize（**短期媒体令牌**，hash 存储+TTL；服务端不存明文长期密钥）；ValidateToken（过期→ErrVoiceSessionExpired 重授权）；InterruptOutput（仅清输出位——**会话与任务保持**，frozen Go 断言）；EndSession（令牌/流失效）；ReportUsage（**迟到用量入账**——结算解耦沿既有商业入口）。
- **voice/realtime.ts**：`createVoiceSessionController`——三停语义互斥：interrupt_output（只停播报，runCancelCount 结构性 0）/ end_session（会话终止≠取消任务）/ cancel_run（独立命令通道，仅此计数+1）；handleLateUsage（结束后的用量仍转发结算）；canApproveFromVoice 恒 false（**语音不携带审批权**）。
- **VoiceSessionPanel**：三按钮显式分离（文案明示各自影响范围）；短期令牌过期提示重授权；「语音通道不携带审批权」提示；读屏完整句。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| go test ./internal/application/service/workbench/ -run TestMX029 | 0 | PASS（生命周期：令牌校验/播报中断会话保持/结束拒令/迟到用量/过期） |
| pnpm exec tsx --test tests/mobile-v2/mx-029.test.ts | 0 | 1/1（frozen voice-interrupt-not-cancel：mediaOutputStopped=true、runCancelCount=0） |
| pnpm run test:mobile-v2 | 0 | 31/31 |
| apps/mobile tsc --noEmit | 非0（基线） | 11=基线 |

## 验收对照

- [x] 产品 API 授权与限额（短期令牌+TTL；不下发长期服务密钥——客户端/服务端均不持）
- [x] 媒体会话与 Run 绑定（voice session mapping 持久）；账务沿原商业入口（ReportUsage 入既有账务表，不建新钱包）
- [x] 暂停播报/结束语音/取消任务三种状态转移互不误触发（frozen：interrupt 时 runCancelCount=0；三停各自影响范围 Go+TS 双侧断言）
- [x] 语音结束后迟到用量仍能结算（Go 迟到入账 + TS handleLateUsage）
- [x] 不可语音绕过危险写审批（canApproveFromVoice 恒 false+面板提示）
- [~] 真实媒体引擎/推送环境：**blocked-env**（无授权语音供应商）——能力未开放（发布门禁控制）；状态机/面板/服务端就绪
- [~] 后台与来电打断处理（AVAudioSession interruption）：随真实引擎接线（同上 blocked-env 边界）
- [~] MX-027（remote 条件边）未激活：本任务在非 remote 前提下交付（voice 不依赖 remote）

## 审查记录

### Round 1（2026-09-18，独立子代理）：规格 PASS / 质量 CHANGES_REQUIRED（TS 侧副作用断言缺失——Go 侧突变被杀、实现正确）

- probe 只断言控制器自报计数与返回值字面量——interrupt 偷调 cancelRun 的突变存活。已修：ports 级 runCancels/sessionEnds 零调用断言（interrupt 与 end_session 后）；返回值断言保留。
- 证据「三停各自影响范围 Go+TS 双侧断言」表述过强的部分已由 ports 级断言补实。
