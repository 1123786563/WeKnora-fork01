# MX-033 证据 · 恢复、并发与安全故障注入

| 项 | 值 |
|---|---|
| 任务 | MX-033 恢复、并发与安全故障注入（S04 / core） |
| BASE | `1e015177`（MX-032 后） |
| 写锁 | recovery.test.ts / security.test.ts / fault-matrix.md / test+probe |

## 交付

- **recovery.test.ts（3 例，全真实实现）**：ACK 丢失+进程重启（零重发→lookup 同 run bound）；事件乱序（缺口拒绝→顺序落库→迟到补齐）；重复事件幂等；cursor_expired 原子重建（replaceRun）。
- **security.test.ts（4 例）**：并发批准（decideCalls=1+第二确认 already_decided 幂等）；撤权/换空间迟到批准（零发送 refresh）；scope generation 迟到响应丢弃；连接撤销后旧版本派发（零 Provider 外写+无凭据字段）。
- **fault-matrix.md**：故障矩阵单一事实源（恢复 6 项+安全 4 项+frozen 场景+墓碑/迟到用量语义+blocked 项如实：remote/voice/PG）。
- **frozen probe**：Go admission 恢复套件 + 两故障注入套件（独立 spawn 执行——node:test 递归规避）；frozen 观测 providerWriteCount=1/settlementCount=1（服务端副作用恰好一次+重启对账后结算恰好一次）。

## RED 记录

frozen 链两次真实失败：①node:test 嵌套 run() 递归跳过（子套件不执行）——改独立 spawn；②`pnpm exec tsx` 包装仍继承 runner 上下文——清除 NODE_OPTIONS+NODE_TEST_CONTEXT 后通过。这是真实执行环境问题（非断言口径），如实记录。

## 验证（命令·exit·发现数）

| 命令 | exit | 结果 |
|---|---|---|
| pnpm exec tsx --test tests/mobile-v2/recovery.test.ts | 0 | 3/3 |
| pnpm exec tsx --test tests/mobile-v2/security.test.ts | 0 | 4/4 |
| pnpm exec tsx --test tests/mobile-v2/mx-033.test.ts | 0 | 1/1（frozen crash-recovery-budget：1/1；内含 Go admission 套件 ok） |
| pnpm run test:mobile-v2 | 0 | **40/40**（新增套件被 glob 收录） |

## 验收对照

- [x] 注入 ACK 丢失、事件乱序、游标过期、进程重启（DB 写失败引用 MX-012 frozen 既有证据）
- [x] 并发批准/撤权/换空间/迟到回调写入前边界（4 例全绿）
- [x] 墓碑保留清理与迟到用量（引用 MX-014/021/029 已钉语义；矩阵登记）
- [x] remote/voice 环境缺失保留 blocked（fault-matrix 如实；不以 mock 记通过）
- [~] 真机杀进程/冷启动实机注入：blocked-env（设备）→ MX-034；当前以进程边界（新协调器/store 实例）等价注入

## 审查记录

- 待独立 review（提交后追加；与 MX-032/034 批合并）。
