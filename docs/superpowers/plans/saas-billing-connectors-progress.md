# SaaS 商业能力与 Connector 实施进度

更新：2026-09-11。总计划：[实施计划](2026-09-11-saas-billing-connectors-implementation.md)。状态：计划已整理，功能实现未开始。

状态值：pending / implementing / review / done / blocked。done必须包含实现提交、定向验证和对应门槛证据；没有真实环境时保留blocked并说明影响，不能用skip充当成功。

| 任务 | 状态 | 实现提交 | 验证结果 | 下一步 |
| --- | --- | --- | --- | --- |
| V01 | review | 待提交 | `python3 -m unittest scripts.saas.contract_inventory_test -v`：3/3 通过；外部只读探测未运行（blocked-env） | 在固定官方 schema 文件上运行 hash 校验与四个 GET 探测 |
| V02 | review | `待提交` | focused regression suite and diff checks pass; external OM-02～OM-10 remains blocked-env | 提交后复核 |
| V03 | blocked | f84b98b | `python3 -m unittest scripts.saas.gate_test -v`：4/4 通过；`git diff --check` 通过；CLI 对 selected-model.json 返回 blocked；OM-01～OM-10 blocked-env（无 OpenMeter 服务／商户凭据，未伪造 pass） | 准备隔离环境后重跑两套官方模型实验；在 OM-01～OM-09 全部 runtime pass 前 gate 拒绝 P01～P03 |
| F01 | done | 5a1111e8e77b3338cbc364656c475c6c90cacf7a | `go test ./internal/commercial -run 'Test(MonthBoundary|ParseCredits|CreditsString)' -count=1`：通过；`go test ./internal/commercial -count=1`：通过；`git diff --check`：通过 | 提交后复核 |
| F02 | done | bb7e8c6b64c64ba34fda6d8bb7176b577a52e1cb | `go test ./internal/commercial ./internal/application/repository/commercial -run 'Test(BillingAccess|Account)' -count=1`：4/4 通过；`go vet`：通过；`go build`：通过；`git diff --check`：通过；SQLite 集成覆盖同租户幂等、租户/Customer 双唯一冲突与并发竞争；独立审查 PASS（task-F02-review.md，B1–B7 全过）；PostgreSQL 并发证据仍 blocked-env | 已 PASS，生成并派发 F03；COM-01/AC-01/02 的真实 PostgreSQL 证据待环境可用后补跑 |
| F03 | done | 2f4462ace578d5db341f7ce5284ba24bdefa63dc | RED：`go test ./internal/commercial -run 'Test(Prorate|Quote|Catalog)' -count=1` 因 undefined: Prorate/QuoteAmount/Segment 等失败；GREEN：同命令 12/12 通过；`go vet ./internal/commercial ./internal/application/repository/commercial`：通过；`go build` 同两包：通过；`git diff --check`：通过；仓库层（published 不可变、ConsumeQuote 单事务）仅经 vet/build 编译验证，SQLite 行为测试未包含于本任务文件清单故未运行，不标 pass；独立审查 PASS（task-F03-review.md，C1–C9 全过） | 已 PASS，生成并派发 F04 |
| F04 | done | af899cefb0bfb0f7d013e8ee057f40e7e7a07717 | RED：`go test ./internal/commercial ./internal/application/service/commercial -run 'Test(Monthly|Lifecycle)' -count=1` 因 undefined: MonthlyGrantKey/Subscription/BaseTier 等失败（service 包当时不存在，commercial 包 RED 为有效 RED）；GREEN：同命令 12/12 通过（5 域用例 + 7 SQLite 集成：同周期 8 worker 并发恰一 job、外部已发未确认 pending+external_ref 重放不换 key、年付逐月 Tick、提前续费不提前发未来月、过期保留 top-up 行并记录 base 档投影与超限原因、外部发放成功保存批次 ID、失败保 key）；`go vet` 三包通过；`go build` 三包通过；`git diff --check` 通过；`gofmt -l` 五个新 Go 文件收尾时修复 2 个 service 文件格式后为空；真实 PostgreSQL 迁移与外部发放方端到端证据 blocked-env 未运行，不标 pass；独立审查 PASS（task-F04-review.md，D1–D8 全过） | 已 PASS，生成并派发 F05 |
| F05 | done | 195cf6ec0fbffae49322c38a428704658a44c3b3 | GREEN（实现者中断后由 finisher 验证，RED 阶段未经 finisher 复核）：`go test ./internal/commercial ./internal/router -run "Test(Quota|Commercial)" -count=1`：11/11 通过（5 配额纯函数：超限拒绝增长/清理放行/零上限非无限/负计数拒绝/溢出防护；6 SQLite httptest：同用户 A/B 空间 usage/summary 不串、无 grant Admin POST 403 且 owner/billing-grant 放行、25 并发新增 CAS 不超限且计数=落库行数、充值 credits 不动资源配额、/health 关库后仍 200 不触计费、full-access key 403 且显式 commercial capability 放行）；brief 原命令 `-run "Test(Quota|CommercialScope)"` 6/6 通过；`go vet ./internal/commercial ./internal/router ./internal/container ./internal/handler ./internal/types`：通过；`go build` 同五包：通过；`gofmt -l` 8 个新增/改动文件修复 2 处对齐后为空；`git diff --check`：通过；`go test ./internal/router ./internal/handler ./internal/types ./internal/commercial ./internal/container -count=1` 全通过；finisher 收尾修复（均为最小修复）：handler commercialUserID 改用 types.UserIDFromContext（原 c.Get 读 gin Keys 取不到请求上下文 user，grant 判定恒 false）、types 新增 APIKeyCapabilityCommercial 常量+NormalizeAPIKeyCapability 分支（原 "commercial" 未注册被归一化剥离，HasCapability 恒 false，无法发放）、handler 常量改为别名 types.APIKeyCapabilityCommercial、gofmt 对齐 commercial.go/router.go；PostgreSQL 方言与外部计费端到端证据 blocked-env 未运行，不标 pass；独立审查 PASS（task-F05-review.md，E1–E7 全过） | 已 PASS；F 基础段完成，生成并派发 C01 |
| C01 | done | 19e64ef98f6640610cb48b51a60bf4fd437a4c8b | RED：`go test ./internal/commercial ./internal/application/repository/commercial -run "Test(Payment|Order|Outbox)" -count=1` 因 undefined: Order/PaymentFact/ValidatePayment 构建失败（有效 RED）；GREEN：同命令 12/12 通过（5 域用例：金额/租户空间/币种/非 succeeded/错单号拒绝+合法放行；7 SQLite 事务用例：确认后 fact 落库+订单 paid 非 fulfilled+恰一 fulfill 事件、付款确认与 Outbox 写入间注入失败全量回滚（订单回 pending v1、fact 未落、0 事件）、响应丢失重放同 ConfirmPayment 幂等（仍 1 fulfill、0 审计、版本只加一次）、并发两渠道都成功恰一 fulfill+1 条 over_payment 审计、报价恰一订单消费（quote_id UNIQUE）、paid/fulfilled 状态区分+MarkFulfilled 幂等、未注册商户/篡改金额拒绝且无副作用）；`go vet` 两包通过；`go build` 两包通过；两包全量 `go test -count=1` 通过；`gofmt -l` 五个新文件为空；`git diff --check` 通过；PostgreSQL 方言 000113 迁移未在真实 PG 执行（blocked-env），SQLite 侧以等价逻辑约束验证；独立审查 PASS（task-C01-review.md，F1–F7 全过） | 已 PASS，生成并派发 C02 |
| C02 | done | e269415e7e1f2dcd9cbdce71b9162fdaea6e4e8d | RED：`go test ./internal/payment -run TestWechat -count=1` 因 undefined: WechatProvider/VerifyWechatSignature/newWechatProvider 等构建失败（有效 RED）；GREEN：同命令 9/9 通过（brief 原始 body 篡改拒绝、错 key 拒绝、未配置 Serial 拒绝、篡改 ts/nonce 拒绝、非法 base64 签名拒绝、过期时间戳拒绝、合法签名+AES-256-GCM 解密+mchid/appid 校验放行且 fact 不携带本地身份、错商户拒绝、重复通知防重放拒绝）；`go vet ./internal/payment ./internal/handler ./internal/router`：通过；`go build` 同三包：通过；`gofmt -l` 三包目录为空；`git diff --check`：通过；`go test ./internal/router -count=1`：通过（既有路由测试不受影响）；错金额/错商户订单路径由 handler 调用 C01 ConfirmPayment→ValidatePayment 事务拒绝（C01 套件已覆盖该路径，本次未重复 SQLite 集成测试）；WX-01～05 真实渠道验收全部 blocked-env（无已授权测试商户/平台证书/APIv3 密钥，未以生成 RSA 签名冒充渠道验收，未标 pass）；真实请求签名（商户私钥引用）与回调路由生产接线（容器未改，路由默认 fail-closed 503）待 C03/C05 配置接入；独立审查 PASS（task-C02-review.md，G1–G9 全过） | 已 PASS，生成并派发 C03 |
| C03 | done | 187570b96658a3733d51a7786e067d27c36c2301 | GREEN（实现者中断后由 finisher 完成回调接线并验证，RED 阶段未经 finisher 复核）：首跑 `go test ./internal/payment -run TestAlipay -count=1` 构建失败（alipay_test.go:217 declared and not used: p），最小修复（alipayGatewayFixture 首返回值改空白标识符）后通过 14/14（金额精确分与非生产格式拒绝、篡改 body 拒绝、错 key/错 app_id/错卖家拒绝、四状态映射且 fact 不携带本地身份、重复通知 Verify 层幂等、同步 return 永不确认、查单补偿、超时返回 unknown 并键原 out_trade_no、退款使用 Alipay 自有标识不混用微信字段、退款查询状态映射、缺 key 引用拒绝）；`go test ./internal/payment -count=1`：通过（Alipay 14 + WeChat 9）；`go vet ./internal/payment ./internal/handler`：通过；`go build` 同两包：通过；`go test ./internal/handler -count=1`：通过（既有套件不受影响）；`gofmt -l` 三个新增/改动文件为空；`git diff --check`：通过；handler 最小增量新增 Alipay 分支（WeChat 路径未动）：raw body 读一次→AlipayProvider.Verify→按 out_trade_no 解析本地 attempt 重建可信租户/订单→ConfirmPayment 成功后才回纯文本 `success`（Alipay 规范），验证失败/未注册/不匹配/内部错误回非 success 非２xx 触发渠道重试且不落库，重复通知经 ConfirmPayment 幂等后重复 ack；ALI-01～05 真实渠道验收 blocked-env（无授权支付宝商户应用/官方支付宝公钥/沙箱环境；离线自生成 RSA 仅验证签名逻辑，不冒充渠道验收），已按官方公开契约固定方法与版本：gateway https://openapi.alipay.com/gateway.do、协议 version=1.0、sign_type RSA2=RSA-SHA256 PKCS#1 v1.5（RSA 备选）、alipay.trade.precreate/query/close/refund 与 alipay.trade.fastpay.refund.query、异步通知表单值单次解码验签不重编码，字段级终验与沙箱行为待官方可读正文+商户环境；生产 providers map 接线留给 C05；独立审查 PASS（task-C03-review.md，H1–H8 全过） | 已 PASS，生成并派发 C04 |
| C04 | done | 1e7de9ac9c58c44ec4fb9509bdad1cd0376d0881 | GREEN（实现者提交域/连接器/worker 与域级测试后由 finisher 补 worker DB 测试、容器接线并跑门禁）：定向命令 `go test ./internal/commercial ./internal/infrastructure/openmeter ./internal/application/service/commercial -run "Test(Fulfillment|ApplyBenefit)" -count=1` 通过 12/12 —— 域层 5/5（key 按订单行分离且抗分隔符注入、BenefitRequest 校验、网关结果分类 unknown 不落成功、充值 effective_at 首次成功固定复用）；openmeter 连接器 4/4 为 httptest 桩证据（与真实渠道验收分开计）：official_v3 字段映射 grant+lookup、保存后断连经 FindBenefit 恢复、业务拒绝永不记成功、未配置网关保持 blocked-env；worker 3/3（真实 SQLite orders/outbox/fulfillment_records + 桩网关仅作外部边界）：保存后断连首过 attention 不记成功→FindBenefit 对账重跑 fulfilled 恰一次、effective_at 不漂移、outbox 无重复、重跑幂等；8 goroutine 并发 Recover 恰一次 Apply/一条 applied 记录/一条 sent 事件；业务拒绝后订单保持 paid、后续 pass 完成履约。门禁：`go vet`/`go build` 四包（含 container）通过；`gofmt -l` 全部新增/改动文件为空；`git diff --check` 通过；`go test ./internal/application/service/commercial -count=1` 既有套件 ok 不受影响；container.go 新增最小接线：`ommeter.NewGatewayFromEnv`（dig.As domain.CommercialGateway）+ `commercialsvc.NewFulfillmentService` Provide + `startCommercialFulfillment` Invoke（StartBackground 后台恢复，Stop 注册 ResourceCleaner）。OM-04/09 真实 OpenMeter（V03 official_v3 实端点/密钥）重跑仍 blocked-env：无真实环境，仅 httptest 契约桩证据且已单独标注；独立审查 PASS（task-C04-review.md，I1–I9 全过） | 已 PASS，生成并派发 C05 |
| C05 | done | d22aad3ca5232a31b6d87bef08e36383c5bcd3bd | GREEN（实现者中断后由 finisher 完成验证；RED 证据未保留——finisher 接手时实现已在工作区，首跑定向命令捕获 2 个真实失败后最小修复）：定向命令 `go test ./internal/commercial ./internal/application/service/commercial -run "Test(Refund|RevokeBenefit)" -count=1` 通过 11/11 —— 域层 6/6（unknown/pending/succeeded/revocation_pending 不解锁、failed_confirmed 释放、渠道状态机成功只进 revocation_pending、撤权确认才 completed、RefundRequestState 投影、默认资格检查 not-ready）；服务层 5/5 均真实 SQLite 事务（无 P03 时 Approve 返回 ErrRefundNotReady、requested→reviewing 且评审人/依据/原因持久记录、零锁零出款事件、重放不逃逸；60%+60% 部分退款与 50% 用量真 DB 并发竞争经同一 admitLotLocksTx 准入事务协调：锁定不超 100 Credit、至多一个部分退款胜出、败者留 reviewing 带原因；渠道成功撤权失败恢复仅重试撤权、渠道退款恰 1 次不重复出款、unknown 停 reviewing 按原退款键重查不换键、failed_confirmed 才释放锁）。finisher 三处最小修复：ApproveRefund 的 not-ready 评审记录移出事务（原随拒绝错误回滚丢失、停留 requested）、stubGateway 撤权计数改为含失败尝试（failed attempt + retry ≥2）、测试种子时间由硬编码 2028 改为相对真实时钟（period_effective 判定）。门禁：`go vet`/`go build` 六包（commercial、service/commercial、repository/commercial、handler、router、infrastructure/openmeter）通过；`gofmt -l` 十个新增/改动 go 文件为空；`git diff --check` 通过；`go test ./internal/commercial ./internal/application/service/commercial -count=1` 既有套件 ok；`go test ./internal/router -count=1` ok；并发竞争测试加 `-race` 通过。P03 诚实性：生产接线 handler/commercial.go `NewRefundService(db,nil,nil,nil)` 资格检查 nil→DefaultRefundEligibility 拒绝，批准绝不进入 paid-out 状态（测试已钉住）。真实渠道出款/撤权 blocked-env：handler 接线 provider/gateway 为 nil，ProcessPayouts 显式返回 refund_channel_unconfigured/refund_gateway_unconfigured，不伪造渠道结果（无 WX/Ali 商户凭据与真实 OpenMeter 服务，未标 pass）；独立审查 PASS（task-C05-review.md，J1–J8 全过） | 已 PASS；支付段 C01–C05 完成，生成并派发 U01 |
| U01 | done | 5449a1c40dd6f79438837fbdf895f58145f3f811 | RED：`go test ./internal/commercial ./internal/application/repository/commercial -run "Test(BYOK|Usage)" -count=1` 因 undefined: BillableModel/ValidateFunding/UsageFact/UsageStore 构建失败（有效 RED）；GREEN：同命令 14/14 通过 —— 域层 6/6（BYOK 不收模型维度、platform 计费、unknown 不当付费、伪造 funding 经 ValidateFunding 在派发前拒绝、BYOK 仅免 model 维度而 sandbox/parsing 保留为独立计费服务事实、定点费率 big.Rat 精确求和后单次末尾舍入 1/3+1/3→1 micro、未定价维度返回 ErrUnknownDimensionRate）；仓库层 8/8 真实 SQLite 事务（重复 final 事件幂等：1 fact/1 settlement 事件；流式 partials 存观测 final 恰一 delta 且 charge_micro=300 与 partial 之和无关；父聚合 display_only 落库但仅子调用产生 1 事件无双重计费；真实重试新 attempt 分开记录：2 fact/2 事件/2 当前指针；同 revision 不同内容 ErrUsageRevisionConflict 且原行原事件不动；迟到修正新 revision 使当前指针移至 2、每个 final revision 各一事件供 U03 按当前版本净额；status=unknown 落观测不发零结算；无费率解析器时 final 拒绝 ErrUsageRatesUnavailable 不静默免费）。事实+settlement Outbox 同一事务写入（C01 模式），usage 唯一键 (tenant_id,call_id,attempt_id,revision)+当前版本指针表 (tenant_id,call_id,attempt_id)。门禁：`go vet`/`go build` 两包通过；`gofmt -l` 修复 1 处对齐后为空；`git diff --check` 通过。blocked-env 诚实性：PostgreSQL 方言 000115 迁移未在真实 PG 执行（无环境），SQLite 侧以等价逻辑约束经 GORM AutoMigrate 验证；价格版本费率真实存储表不在本任务文件清单（RateResolver 注入，U03 结算消费 ChargeForCall 单次舍入点）；额外披露：为满足行为列 DB 反例新建了仓库层测试 internal/application/repository/commercial/usage_test.go（brief 文件清单之外，随本任务提交）；独立审查 PASS（task-U01-review.md，K1–K9 全过） | 已 PASS，生成并派发 U02 |
| U02 | done | a89920d（初版）+ 修复 0cfa73ec891bc1449c68c4a7ea32d2a79d4689ea（fix: roll back budget reservation on guarded miss） | RED：`go test ./internal/commercial ./internal/application/repository/commercial -run "Test(Available|Budget)" -race -count=1` 因 undefined: Available 构建失败（有效 RED）；GREEN：同命令 10/10 通过（-race）——域层 4/4（brief 原用例 100-20-30-10→40 与负数保护、逐项负数拒绝 invalid_projection、不足→0 非 error、恰好耗尽→0）；仓库层 6/6 真实 SQLite 事务（20 并发 Reserve 竞争 100 余额：恰一成功且成功总额=账户 held=任务 held=批次 held=lot 分配行总和=60；父子不复制预算：AttachChildRun 生成零限额 root_run_id 映射行、child 预占计入父任务预算一次、40+70>100 拒 ErrTaskBudgetExhausted、恰好 60 放行至 100；ApplyExternalBalance 外部余额只推进 verified/watermark 永不覆盖本地 unreflected（20 不变、500/w2 生效、版本递增、stale watermark 拒绝且 Available=480 仍计入未确认花费）；同 key 幂等重放不重复计数、同 key 不同内容 ErrReservationKeyConflict 零副作用；账户不足拒绝零副作用；LockRefunds 与 Reserve 20 并发竞争同一投影恰一胜出 held+refund_locked=60）；Reserve 单事务版本 CAS（账户与任务计数守卫、期限、批次最早到期分配），无进程锁。门禁：`go vet -tags=commercial_integration`（PG 并发回归文件编译证明）、`go vet`/`go build` 两包、`gofmt -l` 空、`git diff --check` 通过、两包全量 `-race` 通过。blocked-env 诚实性：budget_pg_test.go 三场景（模板 20 并发恰一断言+两独立任务竞争各断言 per-task/account held+退款锁 vs 预占竞争断言最终 refund_locked+held）仅编译未运行（无 SAAS_TEST_PG_DSN，不标 pass）；PG 迁移 000116 未在真实 PG 执行，SQLite 侧以等价逻辑约束经 GORM AutoMigrate 验证。修复轮（独立审查 task-U02-review.md L4 FAIL，三缺陷）：(1)(2) 守卫未命中/预占 INSERT 唯一键竞争不再 return nil 提交部分状态——事务闭包改回 errBudgetCASRetry 使 GORM 整体回滚（账户/任务增量、reservation 行、更早 lot hold 全部撤销）；(3) errBudgetCASRetry 哨兵由 tryReserve/tryLockRefunds/tryApplyExternalBalance 以 errors.Is 消费并驱动重读重试循环，绝不跨 API 面（tryApplyExternalBalance 原为空提交式 retry，改为同一哨兵纪律，行为等价、纪律统一）；确定性失败（余额不足/期限/watermark）仍返回真实错误。回归测试 5 个（先对旧代码运行证伪：guard-miss 旧代码将预占不足额提交后经幂等重放返回成功、insert-race 旧代码逐次提交部分状态耗尽账户后返回 insufficient_budget、哨兵旧代码以 budget_cas_retry 泄漏——三测试 RED；shortfall 与并发同 key 重放为契约钉）：TestBudgetReserveLotShortfallRollsBackAllWrites（lot 余量 50<账户可用 200 请求 60 → ErrBudgetLotsInsufficient 且账户/任务 held=0 version=1、reservation/lot held/分配行全零；过期 lot 同路径）、TestBudgetReserveLotGuardMissRollsBackAllWrites（RAISE(IGNORE) 触发器确定性模拟并发 lot 守卫未命中：UPDATE 0 行无错 → 重试至耗尽 ErrBudgetContention、零副作用、哨兵不外泄）、TestBudgetReserveInsertRaceRollsBackAllWrites（RAISE(ABORT) 触发器模拟唯一键竞争 INSERT 失败 → ErrBudgetContention、零副作用）、TestBudgetRetrySentinelNeverCrossesAPI（RAISE(IGNORE) 使账户守卫 UPDATE 0 行而新鲜读仍可见足额可用——即输掉版本竞争场景：Reserve 与 LockRefunds 只见真实域错误或成功，永不见哨兵）、TestBudgetReserveConcurrentSameKeyReplayIdempotent（20 并发同 key 全部幂等成功、held 恰计一次）。新计数：同门禁命令 15/15 通过（域层 4/4 + 仓库层 11/11，旧 10/10 + 新 5）；全量两包 -race 通过；go vet 两包、go vet -tags=commercial_integration、gofmt -l 空、git diff --check 通过；复审 PASS（task-U02-rereview.md，R1–R6 全过；首轮 L4 三缺陷经 0cfa73e 修复后验证） | 已 PASS，生成并派发 U03 |
| U03 | done | 3ef37426b75da3f3172ee895f049baa35a170c2c | GREEN（实现者与 finisher-1 均中断后由 finisher-2 验证；RED 证据未保留——接手时实现已在工作区，定向命令首跑即通过）：定向命令 `go test ./internal/commercial ./internal/application/service/commercial -run "TestSettlement" -race -count=1` 通过 10/10 —— 域层 5/5（KeepProtection 对 dispatched/unknown/sent/accepted 保留保护、仅 confirmed 释放；状态表；settlement revision 键复用；Settlement Validate；回执必须携带事件/交易关联证据而非余额推断）；服务层 5/5 为真实内存 SQLite 事务+桩网关仅作外部边界（dispatched 未确认保留保护无短暂重复准入、HTTP 接收后崩溃重放幂等无永久双扣、确认丢失 watermark 恰一推进、外部水位倒退不回撤、退款与确认并发协调）。门禁：`go vet`/`go build` 四包（commercial、service/commercial、repository/commercial、infrastructure/openmeter）通过；`gofmt -l` 七个新增/改动文件为空；`git diff --check` 通过；回归 `go test ./internal/commercial ./internal/application/service/commercial ./internal/application/repository/commercial -race -count=1` 三包 ok（58+20+29 个测试函数）既有套件全绿。blocked-env 诚实性：真实渠道 settlement-ack 确认策略由 V03 gate 选定，V03 仍 blocked-env（无 OpenMeter 服务/商户凭据），真实渠道确认水位未运行不标 pass，本任务证据限于域/SQLite 事务与桩边界；独立审查 PASS（task-U03-review.md，M1–M9 全过） | 已 PASS，生成并派发 U04 |
| U04 | done | a137c794a7936fa89edb877d4a2c0687643a35e6 | RED：`go test ./internal/commercial ./internal/application/service/commercial -run "Test(ExpiredLease|BudgetRecovery|BudgetExtend)" -race -count=1` 因 undefined: MayReleaseWithoutQuery 构建失败（有效 RED）；GREEN：同命令 8/8 通过（-race）——域层 2/2（brief 原用例 dispatched/settling 不释放、仅 held 直接释放；状态表 held=true，dispatched/settling/settled/released/confirmed/unknown/空串全部 fail-closed 不释放）；服务层 6/6 真实内存 SQLite 事务（未启动 held 过期/取消经 Reconcile 直接释放：账户/任务/批次 held 全归零、分配行清空、重复 Reconcile 幂等；任务取消但远端已成功：SettleReservationHold 转换+dispatched 结算记录在发生区间内 → ErrReconcileNeedsConfirmation 保护量 unreflected=300 不清零、dispatched 未结算预占 held 保留、区间外迟到报告不改变判定；重复 Extend 同 key 幂等仅一次加额 ext_1×3+ext_2→2000 且恰 2 行扩展记录；空间降级后新 Extend 以 ErrBudgetUnauthorized+显式原因暂停且限额不动、同一降级下既有 Reconcile 照常释放；租约恢复 worker_1→worker_2 fence 递增：旧 owner/fence 与新 owner+旧 fence 均 ErrLeaseFenceStale、新组合放行、dispatched 后拒绝再接管；过期释放 vs 退款锁并发竞争：两操作均成功、held=0/refund_locked=600/可用恰 400（释放恰一次不双重释放）、二次释放 ErrReservationNotHeld）。门禁：`go vet`/`go build` 三包通过；`gofmt -l` 五个文件为空；`git diff --check` 通过；`go test ./internal/commercial ./internal/application/service/commercial ./internal/application/repository/commercial -race -count=1` 三包全绿既有套件不受影响。blocked-env 诚实性：PostgreSQL 方言并发（SAAS_TEST_PG_DSN）与真实渠道查询确认证据仍 blocked-env 未运行不标 pass；额外披露：为覆盖行为列反例新建 internal/application/service/commercial/budget_test.go（brief 文件清单之外，随本任务提交）；仓库层新增均为加性：ReservationRow 增加 owner 列（AutoMigrate 加列）、新增 ReleaseReservation/MarkReservationDispatched/ExtendTaskLimit/TakeoverLease/VerifyLeaseCommit 与 TaskBudgetExtensionRow 表，既有 Reserve/LockRefunds/结算语义与 0cfa73e 回滚/哨兵纪律未动（errBudgetCASRetry 同纪律复用）；独立审查 PASS（task-U04-review.md，N1–N8 全过） | 已 PASS，生成并派发 U05 |
| U05 | done | 0a380921a199630d2ee1ce345582cc90e3974fde | RED：`go test ./internal/commercial ... -run "Test(UnknownFunding|Commercial|Usage)" -count=1` 因 undefined: ServiceModel/ServiceDimension/ExecutionLimits/ExecutionGate/TrustedUsageFact/CheckAbnormal 构建失败（有效 RED；ValidateFunding 部分为 U01 已有语义，按 brief 视为回归钉）；GREEN：同命令三包 ok（commercial 8 用例：brief Step1 原文用例 + 每服务独立维度门禁（model/parsing/embedding/rerank/sandbox/connector 各映射自身维度、未知服务拒绝）、BYOK 仅豁免 model 维度（sandbox 等仍计费）、输出/时长上界实际拒绝（ErrOverLimit）、Finish 只收服务端可信 funding+final（client funding→ErrUntrustedUsage、display_only 父汇总→ErrNotBillable 不双计）、异常成本 CheckAbnormal→ErrAbnormalCost、Begin 拒绝→零预留停派发）。门禁：`go vet`/`go build` 五包通过；`gofmt -l` 七文件为空；`git diff --check` 通过；`go test ./internal/agent -count=1` 全绿；`go test ./internal/models/chat -count=1` 除 TestBuildChatCompletionRequest_GPT5MaxCompletionTokens（9 子用例 SSRF/DNS blocked-env：example.openai.azure.com 解析到受限网段，git stash 干净树上同样失败，与本变更无关，未标 pass）外全绿；`./internal/application/service/commercial -count=1` 全绿。runtime_binding 检查未完成（诚实记录）：Craft/OpenCode 子 Runtime 适配入口未交付，无同名假模块；commercial_adapter.go 仅绑定当前 AgentEngine 入口（Begin 在 callLLMWithRetry 前、Finish 在 usage 记录后），nil gate 保持既有非商业行为；真实渠道 Begin/Finish 证据 blocked-env 未运行不标 pass；费率解析器为 unavailableRates 占位（ErrUsageRatesUnavailable 拒绝零计价），定价管理落地后 WithRates 注入；独立审查 PASS（task-U05-review.md，P1–P9 全过） | 已 PASS；预算计量段 U01–U05 完成，生成并派发 A01 |
| A01 | done | a798e777ffb18e86c466ac96753564eb3111be1d | RED：`go test ./internal/appconnector -run "Test(PersonalConnection|Installation)" -count=1` 因 undefined: Connection/CanUseConnection 构建失败（有效 RED）；GREEN：同命令 6/6 通过（brief Step1 原文用例 + 空间连接授权矩阵（授予成员放行/无显式 grant 拒绝含 owner/跨租户 grant 拒绝）、停用态连接拒绝（revoked/pending_reauthorization/空）、跨租户拒绝（personal+space）、未知 kind 拒绝（shared/org/空）、安装权限与商业购买权限分离（owner/admin 可安装、member/contributor/viewer 仅申请，CanInstallInstallation 不调用 CanManageBilling））；仓库层 `go test ./internal/application/repository/appconnector -count=1` 7/7 通过（真实 SQLite：同 app 双空间独立（停用 7 不动 8）、单安装多连接三连接各自授权、个人连接 owner-only、空间连接无 grant 拒绝有 grant 放行、scope 扩大升级仅可落 reauthorization_required（试 active 返 ErrReauthorizationRequired 且行不动，落库后连接新调用拒绝）、停用安装后 personal(owner)/space(grant) 新调用全拒、CAS 陈旧 expected 拒绝且版本不漂移（含 absent 行 expected≠0 冲突））；门禁：`go vet`/`go build` 两包通过；`gofmt -l` 修复 2 文件后为空；`git diff --check` 通过；ApplyInstallation 固定签名 CAS（expected 0=创建、行读回校验目标版本与状态）；credentials 不入 config_json（无 credentials 列，仅 credential_ref 引用）。blocked-env：PostgreSQL 方言 000117 迁移未在真实 PG 执行（无环境），SQLite 侧等价逻辑约束经 GORM AutoMigrate 验证；独立审查 PASS（task-A01-review.md，Q1–Q7 全过） | 已 PASS，生成并派发 A02 |
| A02 | done | 18b5b45（初版）+ 1b5f1498f35f3446ac8e009e6b5a2b3116a443d8（迁移补齐） | GREEN（实现者中断后由 finisher 验证，RED 证据未保留——接手时实现已在工作区，定向命令首跑捕获真实失败后最小修复）：定向命令 `go test ./internal/appconnector ./internal/application/repository -run "Test(OAuth|MCPOAuth)" -count=1` 通过 17/17 —— appconnector 5/5（brief Step1 原文用例：跨空间拒绝+state 重放拒绝；补充：恰好过期即无效（严格 now.Before）、错 actor 拒绝、空 state 拒绝、合法放行防漂移）；repository 12/12，其中本任务 9 个真 SQLite 事务用例（callback 快乐路径单事务绑定且 token 落库为 enc:v1 密文、Connection 仅存 mcp_oauth_token: 引用；state 重放恰绑一连接；过期不绑；错租户/错 actor 零副作用；发起人被移除返回 ErrOAuthBindingActorNotMember 且 state 未消费；安装停用整事务回滚 Used 不落；成员离开仅 personal 连接 revoked+auth_version+1，space 连接与他租户连接不动；并发同 state 赎回恰一胜者；8 并发 refresh 租约恰一胜者）+ 3 个既有 MCPOAuth 仓库测试不回归；`go test ./internal/application/service/appconnector -count=1` 6/6（解析快乐路径、revoked 永不解析且守卫刷新前置拒绝、auth_version 不匹配拒绝、owner 失效成员拒绝、8 并发守卫刷新恰一租约胜者、脱敏视图与哨兵错误序列化均不含 secret 及 credential 引用）；`go test ./internal/application/service -run "TestMemberLeave" -count=1` 2/2（RemoveMember 恰触发一次个人连接撤销钩子且钩子失败仅记日志不阻断移除、既有四参构造不受影响）。门禁：`go vet`/`go build` 五包（appconnector、application/repository、application/service、application/service/appconnector、handler）通过；`gofmt -l` 九个新增/改动 Go 文件为空；`git diff --check` 通过；`go test ./internal/application/repository -count=1` 全绿；`go test ./internal/application/service -count=1` 仅 TestSkillPythonVerifier 失败（同失败在未改动的 main 检出 d370254 复现——venv 外 Python 依赖探测环境差异，与本变更无关，未标 pass）。finisher 最小修复 4 处：(1) credentials_test.go 与 mcp_oauth_binding_test.go fixture 加 sqlDB.SetMaxOpenConns(1)（对齐 commercial/budget_test.go 既有惯例；shared-cache 多连接并发多语句事务触发 SQLITE_LOCKED，repo 并发赎回用例隔离重跑 3/4 轮 0 胜者，修复后两套件分别 5×/3× 全绿稳定）；(2) handler app_connections.go 补缺失 context import；(3) NewTenantMemberService 补 opts ...TenantMemberOption 变参（With 选项已定义但构造器未接收，五参测试调用编译失败）；(4) gofmt -w 对齐 oauth.go/mcp_oauth.go 结构体字段并去 EOF 空行。blocked-env 诚实性：真实提供方 OAuth code 交换/PKCE 未运行（无提供方凭据/环境；生产 exchanger 未接线保持 nil 即 503 fail-closed，未伪造渠道结果）；PostgreSQL 方言 000118 迁移未在真实 PG 执行（无环境），SQLite 侧等价逻辑经 GORM AutoMigrate 验证；额外披露：实现者所建仓库/服务层测试文件 internal/application/repository/mcp_oauth_binding_test.go 与 internal/application/service/tenant_member_connections_test.go 随本任务一并提交（brief 文件清单之外，边界反例所在）；migrations/versioned/000118_mcp_oauth_binding_states.{up,down}.sql 仍在工作区未提交（不在本任务指定暂存清单，待协调者处置）；迁移补齐 —— 000118 PG（初版未跟踪）+ 新增 SQLite 000038 配对，逻辑约束一致，down 仅逆序 DROP；PostgreSQL 实际执行仍 blocked-env。；独立审查 PASS（task-A02-review.md，S1–S8 全过） | 已 PASS，生成并派发 A03 |
| A03 | done | 02cc527657d0418a2356db33f4eaf1e0f5aa1eb9 + 测试提交 2a92436 | RED：brief Step2 命令因 undefined: NeedsExplicitApproval/Action/ActionDigest 构建失败（有效 RED）；GREEN：同命令 `go test ./internal/appconnector ./internal/agent/approval -run "Test(GeneralWrite|Action|Approval)" -count=1` 两包 ok（appconnector 15/15 含既有套件；approval 无匹配用例，`go test ./internal/agent/approval -count=1` 全绿不回归）；服务/仓库层 `go test ./internal/application/service/appconnector ./internal/application/repository/appconnector -count=1` ok —— 新增 7 个服务用例钉住行为边界（快照+digest 生命周期：错 digest/旧 digest 拒绝且派发字节=已批准规范化快照、审批后权限撤销 A02 复查阻断零预留零派发、8 并发执行恰一胜者无超耗（-race 通过）、pending 跨 DB 重开保留、费用批准≠写批准（预算通过但无写审批/审批过期/预算拒绝三路全阻断）、send 预授权独立 scope（自 scope 直达 authorized、send 预授权不覆盖 write 动作、write 预授权不覆盖 send）、unknown 停泊仅经提供方查询解决永不重回队列）；门禁：`go vet`/`go build` 四包通过、`gofmt -l` 空、`git diff --check` 通过。差异披露：(1) 迁移编号修正——原计划 000118/000038 已被 A02（mcp_oauth_binding_states）占用，按协调者预先授权改用下一空闲编号 PostgreSQL 000119_app_actions / SQLite 000039_app_actions，逻辑约束不变；(2) 为覆盖行为列服务层反例新建 internal/application/service/appconnector/action_test.go（brief 提交清单之外，未随本次暂存，待协调者决定纳入）；gate.go 仅加性新增 PromptPersistedAction 显示/唤醒适配（内存 pending 非权威，持久 store 权威）；真实 PostgreSQL 000119 迁移与真实提供方派发证据 blocked-env 未运行不标 pass；独立审查 PASS（task-A03-review.md，T1–T7 全过） | 已 PASS，生成并派发 A04 |
| A04 | done | 21bb6cd535e8ee336117d4741d4764bfaf7b8863 | RED：brief Step2 命令 `go test ./internal/appconnector ./internal/agent/approval -run "Test(HTTP|MCP|Action)" -count=1` 因 undefined: PublicAddress/HTTPPolicy/NewHTTPPolicy/IPResolverFunc/ActionResult/QueryUnsupported 构建失败（有效 RED）；GREEN：同命令两包 ok —— 14 个新用例全过：TestHTTPRejectsInternalDestinations（brief Step1 原文）、TestHTTPAddressClassification（RFC1918/IPv6 ULA fc00::/fd12::、link-local、multicast 224.0.0.1/ff02::1、unspecified 0.0.0.0/::、nil IP 拒绝；8.8.8.8/2606:4700::1111 公网放行）、TestHTTPHappyPathWithinAuthorizedNetwork、TestHTTPUnlistedDestinationsRejected（公网 IP ≠ 已授权：evil.example 解析到公网 93.184.216.34 仍 ErrUnlistedDestination；未列 method/scheme/越界 path 全拒）、TestHTTPRedirectToInternalBlocked（302→10.0.0.9 被拦截）、TestHTTPDNSRebindingMixedRecordsRejected（公网+私有混合 DNS 答案 ANY 无效即拒；::1 答案同拒）、TestHTTPAuthHeaderNotForwardedCrossHost（跨 host 跳转 Authorization/Cookie 先剥离再拒绝）、TestMCPToolDefinitionVersionPin（schema 改动/risk 改动/缺工具/多出未钉工具均 ErrMCPDefinitionChanged→pending_review）、TestMCPReadOnlyHintAdvisoryOnly（远端 readOnly 伪造不降风险不过审、hint 翻转不触钉）、TestMCPStdioIsolationAndRemoteCommandRefusal（remote_description 派生命令与非白名单二进制拒、无隔离目录/继承宿主环境拒）、TestActionExecuteWrapsIntentBudgetInner（顺序恰好 intent→begin→inner→finish→release 且预算请求绑定 tenant/target）、TestActionIntentDenialBlocksEverything（零副作用停泊）、TestActionBudgetDenialNeverDispatches（U05 拒绝时 inner 不跑、release 以 queued 回报、ErrInsufficientBudgetGate）、TestActionQueryUnsupportedReturnsUnknown（不支持查询返回 State=unknown 且 err=nil，绝不伪造失败触发重发）；既有 2 个 TestActionDigest 用例不回归；`go test ./internal/appconnector ./internal/agent/approval -count=1` 全绿；门禁：`go vet`/`go build` 两包通过、`gofmt -l` 修复 2 文件后为空、`git diff --check` 通过。实现要点：PublicAddress 按 brief 原文；HTTPPolicy 校验管理员审核 scheme/host/port/method+目标资源路径前缀；Transport.DialContext 先解析 DNS 对全部地址逐个校验再连已验证 IP（TLS ServerName 保持审核 host）；CheckRedirect 每 hop 重验且跨 host 剥离 Authorization/Cookie/Proxy-Authorization；AuthorizedNetworks 为管理员显式网络授权（结构上不可由 Action.Args/模型参数触达），测试以 127.0.0.0/8 + IPResolverFunc 假解析器作已记录 test hook（未削弱 PublicAddress）；GatedAdapter 将真实调用包在 A03 ClaimDispatch intent 与 U05 ExecutionGate 之间（intent→Begin→real→Finish→release）；MCP 工具 schema/risk 版本钉（NormalizeArgs 规范化后 sha256，advisory readOnly 不入钉）；stdio 仅限管理员白名单二进制+受控隔离目录+洁净环境，remote_description 派生命令一律拒；tool_policy.go 最小加性新增 ToolPin/PinToolDigest/VerifyToolPin/ErrToolDefinitionChanged（版本钉 schema/risk 检查集成点，未动既有 EnabledTools 行为）。blocked-env：未对真实外部提供方发起任何网络调用（httptest 回环 + 假 resolver 证据 only）；真实 MCP 会话传输与 stdio 进程实际启动未接线（MCPAdapter.Call/AdvertisedTools 为注入点），未标 pass；独立审查 PASS（task-A04-review.md，U1–U7 全过） | 已 PASS，生成并派发 A05 |
| A05 | done | 0888fac7298b6439a5a91335252dedf214fd3518 | RED：brief Step2 命令 `go test ./internal/appconnector -run TestFeishu -count=1` 因 undefined: CanRetryProviderWrite/FeishuSendAdapter 构建失败（有效 RED）；GREEN：同命令 9/9 通过 —— brief Step1 原文用例 + guaranteed key 且窗口内放行、FS-01 契约钉定（host open.feishu.cn、/open-apis/im/v1/messages、tenant_access_token、receive_id_type=chat_id、审核消息类型集合、im:message:send_as_message、官方同 uuid 1 小时至多一条消息→窗口 1h、uuid≤50 字符）、错目标（策略只审了另一 host/path）被 ValidateRequest 拒且零请求触网、审批后撤销（Recheck）停泊 awaiting_approval 零派发、成功路径保存真实 om_ 消息 ID 且保留原始提供方载荷、快照逐字段相等（receive_id/msg_type/content 与已批准快照逐字节一致、receive_id_type=chat_id、Bearer tenant token；两次执行同一确定性 uuid 绝不生成新 UUID；快照多字段/未审 msg_type 拒绝）、响应丢失（httptest 保存后 hijack 断连）→unknown+ErrFeishuOutcomeUnknown→Query 以会话历史逐字段匹配发现真实消息 ID 且零重复发送、窗口过期 CanRetry 端到端 false 仅 Query 解决无重发、窗口内同 key 重试被提供方去重恰一消息（同 uuid 两attempt 恰一 saved）、KeyGuaranteed=false 恒不重试；门禁：`go vet`/`go build` ./internal/appconnector 通过、`gofmt -l` 空、`git diff --check` 通过、`go test ./internal/appconnector ./internal/agent/approval -count=1` 全量套件不回归；FS-01 契约经官方 create-message 文档核验（uuid 幂等窗口与 1 小时上限、receive_id_type、msg_type、请求路径），oapi-sdk-go v3.9.7 可复用但未用作传输（SDK client 会绕过 A04 出站策略的 dial/redirect 校验），适配器以 A04 策略 client 发送已验证方法契约；FS-04 真实发送 blocked-env：无用户明确指定并授权的测试会话与凭据，未伪造真实发送，不标 pass；独立审查 PASS（task-A05-review.md，V1–V8 全过） | 已 PASS，生成并派发 A06 |
| A06 | done | 1cfc95770528cd681485a90e6a7ccf122f27bb23 | RED：brief Step2 命令 `go test ./internal/appconnector -run TestNotion -count=1` 因 undefined: NextNotionStep/CanRouteNotionStep/NotionPageProgress/NotionCreateAdapter 构建失败（有效 RED，协调者捕获，实现文件当时不存在）；GREEN：同命令 12/12 通过 —— brief Step1 原文用例、步骤路由（空 pageID→create、unknown 永不经 NextNotionStep 仅 Query/人工核对、confirmed 状态才可路由）、NO-01 契约钉定（api.notion.com、/v1/pages、Notion-Version 2022-06-28、parent page_id 类型、insert_content 能力、追加批上限 100）、父页面越界零触网拒绝、审批后撤销 Recheck 停泊零派发、缺 insert 能力零触网拒绝、成功保存真实 real_page_1 且保留创建原始载荷（append 响应不覆盖）、快照逐字段钉定（parent/title 不得改写、Notion-Version 与 Bearer token 逐字节、追加块=批准块规范化字节）、创建后本地崩溃新进程从持久 page_id 恢复恰一页面零重复、部分追加成功 BlocksDone=2 持久化后仅补发剩余块（creates=1 appended=3）、未知创建保持 unknown 且 title 搜索结果绝不作为存在证明（ExternalID 空、无重建）、Query 仅以持久 id 可靠读取逐块核对（块不完整保持 unknown 不伪造成功）；门禁：`go vet`/`go build` ./internal/appconnector 通过、`gofmt -l` 空、`git diff --check` 通过、`go test ./internal/appconnector ./internal/agent/approval -count=1` 全量不回归。组装披露（两个子代理先后 wall-clock 超时）：实现者交付完整测试文件（并修复其语法错误）、finisher 交付 notion_create.go，协调者按 C05/F05 先例做 3 处最小收尾修复：(1) Execute 输出保留创建响应而非被 append 响应覆盖（A05 保留原始载荷同例）；(2) 测试桩 title 解析结构补一层嵌套以匹配实现与桩自身 search decoy 均使用的官方嵌套形状 properties.title.title[]（实现未改）；(3) 部分追加用例改用新测试桩触发器 appendFailOnNth:2（原 appendFailNext:1 语义上必然失败第一批 2 块，与该用例"batch of 2 成功 + batch of 1 失败"的断言内在矛盾且无任何实现可同时满足本用例与崩溃恢复用例；所有断言未动）。NO-01～NO-03 以 httptest 桩证据覆盖；NO-04 真实 Notion 写入 blocked-env：无用户指定测试父页面与集成凭据，未伪造真实写入，不标 pass；独立审查 PASS（task-A06-review.md，W1–W10 全过；审查者对三处收尾修复做了实证复推：恢复 appendFailNext:1 复现 BlocksDone:0 矛盾、扁平 title 解析实证读不出官方嵌套体；非阻塞备注 8 条：实现/台账拆为两提交、恢复执行 Output 为 nil、Query 逐字节块比对对真实 Notion 偏保守保持 unknown（安全方向）、5xx 追加恢复同页可能重复块但页面级不重复硬保证成立、SaveProgress 失败分支未测、大小限制交由提供方、createDrop 连 GET 一起劫持、空 scope 用例少一次 wire 复断言） | 已 PASS，生成并派发 A07 |
| A07 | done | 0e30173103abd4f24e2c07b7c65c3ffc2c409537 | RED：实现者 wall-clock 超时致原始 RED 记录丢失（披露，同 U03/C05/F05 先例）；协调者复构 RED——移除 sync.go 后 brief Step2 命令因 undefined: CanAdvanceCheckpoint/StoredSyncBinding/ResolveSyncBinding/BindingState/ErrSyncBindingNotFound 构建失败（有效 RED），恢复后构建通过；GREEN：brief Step2/Step4 定向命令 `go test ./internal/appconnector ./internal/application/service ./internal/datasource -run "Test(Sync|DataSource)" -count=1` 三包 ok——appconnector 10 用例（brief Step1 原文 checkpoint 门、活跃空间连接解析、旧凭据 requires_reauthorization、不可用状态 requires_reauthorization、缺关联行 not_found、五种暂停原因 budget/plan/permission/quota/provider_limit 去Distinct且合法、套餐到期停新同步但已持久化结算允许、SyncPausedError 携带原因、源失败 404/权限拒绝/不可用三分类、源报告删除默认保留本地副本）；service/appconnector 6 用例（计划到期在调度前暂停、旧凭据暂停、撤销空间连接暂停、活跃连接放行派发、未绑定旧数据源仍受计划门、checkpoint fence 拒绝过期 worker）；门禁：`go vet`/`go build` 五包（appconnector、application/service、datasource、application/repository、service/appconnector）通过、`gofmt -l` 空、`git diff --check` 通过、六包全量回归仅既有无关 TestSkillPythonVerifier 失败（A02 台账已记录为先在）；三个既有文件修改为加性（scheduler 增 SyncGateFunc 钩子在派发前解析空间连接与预算、service +171 行调度前门、repo +72 行绑定存取）；迁移编号按协调者修正 PG 000120/SQLite 000040（原计划 000119/000039 已被 A03 占用，task-A07-brief.md deviation note）；真实飞书/Notion 同步端点与真实 PostgreSQL 迁移执行 blocked-env：同步行为以 SQLite/in-memory 与桩证据验证，未伪造 runtime pass；独立审查 PASS（task-A07-review.md，X1–X10 全过；披露偏差：ResolveSyncBinding 采用 (ctx,store,tenant,ds,state) 依赖注入形参而非 brief 三参草图，判定非阻塞；审查者补跑 brief 定向命令未覆盖的 6 个 service 用例 6/6 通过；非阻塞备注 8 条：进程内 fence 非分布式、调用点 contentCommitted=true 硬编码、MaySettlePersistedSync 参数未用、台账命令-用例归属、游标键盖戳、装配钩子待后续接线、被删 1 行为原地展开等） | 已 PASS；A 段（A01–A07）完成，生成并派发 W01 |
| W01 | pending | 未实施 | 未运行 | 等待 C01, C05, F05 |
| W02 | pending | 未实施 | 未运行 | 等待 W01 |
| W03 | pending | 未实施 | 未运行 | 等待 W01, C05 |
| W04 | pending | 未实施 | 未运行 | 等待 A01, A02, A07 |
| W05 | pending | 未实施 | 未运行 | 等待 A03, U04, W04 |
| O01 | pending | 未实施 | 未运行 | 等待 C04, C05, U03, A03 |
| O02 | pending | 未实施 | 未运行 | 等待 F02, F04, U01, A07, O01 |
| O03 | pending | 未实施 | 未运行 | 等待 W02, W03, W04, W05, O01, O02, C02, C03, A05, A06, U05 |

本次只完成计划与文档自检，未执行实际实现测试、外部付款或Connector写入。
