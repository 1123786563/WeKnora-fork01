# 移动工作台实施计划自检

日期：2026-09-12。范围：[总计划](2026-09-12-mobile-ai-saas-workbench.md)、六个分册、任务索引和W台账。此为计划编写者的静态自检，非独立代码审查或运行验收。

## 1. 规格覆盖

架构17章均在总计划§8映射到任务或明确继承边界。新增工作分成37个W任务；原Happy管理/高级交互继续依赖原H任务及其细分单元，不能因新端口存在就标完整保留。私密E2EE、动态应用发布、公共节点生产发行和商店支付改造仍是明确独立规格范围。

核心平台、OIDC、托管远程、个人节点、多模态、语音、完整Happy交互分别定义发布门槛。语音和个人节点未完成不阻塞核心平台；任何启用能力都必须补齐相应安全、数据库、费用和双平台证据。

## 2. 修正的具体问题

| 检查发现 | 修正 |
| --- | --- |
| 数据库强制engine_type=trpc，不能仅新增driver字段 | W02增加组合CHECK与SQLite受控父表重建、子表保留和拒绝破坏性down |
| 现有Admit也要求会话trpc | W02按driver区分检查，不修改旧会话引擎来绕过 |
| logout可能退回初始origin | W07使用当前scope的origin并失效刷新 |
| OIDC路由与刷新测试路径易误认 | 核对routes_auth_tenant.go及packages/api-client/src/auth.test.ts |
| 设备/通知需要应用环境隔离 | W13 store绑定environment，W14唯一键包含environment |
| 产物/通知测试需要真实父记录 | 先创建Run与设备，再测冲突和去重 |
| 语音未覆盖用户编辑与取消区分 | W29读取最新草稿；W31区分interrupt/endVoice/cancelTask |
| 后续任务修改尚未建立的文件 | 增加W11→W16、W03→W18依赖 |
| git add路径包含括号 | 分册提交清单逐路径单引号引用 |
| 核心发布可能被扩展验收误阻塞 | W33–W37记录profile条件依赖，不把未验收能力改为通过 |

## 3. 执行过的计划检查

- 七份计划均包含Goal、Architecture、Tech Stack、Spec、Global Constraints。
- 37个任务均有Files、Interfaces、失败测试代码、RED命令、实现接缝、真实接线、GREEN/额外验收和范围提交步骤。
- 任务索引检查依赖存在性与无环性，包含条件依赖；Modify路径必须已存在或由依赖任务Create。
- 检查本地Markdown链接、代码围栏、未完成占位标记与文档尾部空白。
- 使用已安装TypeScript解析器解析43个TS/JS代码块，无语法错误。没有执行其中的测试或验证新增模块已存在。
- 使用gofmt解析27个Go代码块，无语法错误。没有编译生产模块或执行Go测试。
- 来源和外部运行证明留在W17/W27等执行任务；没有把可变上游main当作已验收版本。

## 4. 证据限度

代码块中的接口片段是实施契约，关键函数示例不能替代完整接线与额外验收矩阵。语法解析不代表跨模块类型检查、数据库迁移或原生运行已经通过。

所有W任务保持pending。尚未安装依赖、执行数据库迁移、启动Paseo/模型/推送/语音服务、运行原生构建或发起外部业务写入。执行时重新核对源码SHA、未提交文档、迁移序号和并行文件所有权。

运行可用性只能由执行阶段的RED/GREEN、集成、原生和真实服务证据确认。
