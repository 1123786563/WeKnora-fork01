# tRPC 原生 Agent 迁移证据台账

| Phase / task | Dependencies | Owner | Commit | Implementation | Spec review | Quality review | Verification layer | Command | Log path | Environment | Gaps / next owner |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P0-1 固定功能基线与证据台账 | 已确认规格、当前源码、活跃客户端 manifests | P0 Task 1 implementer | `da16fcf5`, `a64d2640`, `df82068b` | passed（文档覆盖审查；round 2 fix committed） | round 1 re-review finding addressed; re-review pending | round 1 re-review finding addressed; re-review pending | static inventory / TSV schema | `awk -F '\t' ... features.tsv`; `git diff --check` | `.superpowers/sdd/2026-09-19-trpc-native-agent-p0/task-1-report.md` | local isolated worktree; no service/provider/device started | P2 SDK probe、P3/P7 恢复、P1 数据权威、P4 能力接入、P5 客户端、P6/P7 archive/cutover |
| P0-2 原生 Runner / Session 探针 | P0-1 feature IDs、固定 SDK v1.10.0 | P0 Task 2 implementer | `8b1ba101`, `d1d18575` | complete（确定性工具往返与 Session 键空间通过；required race probe 失败，原生产品执行保持 blocked） | complete（review clean） | complete（review clean） | deterministic SDK unit/race; fixed-source contract | `GOWORK=off go test ./internal/agent/nativeprobe -count=1 -v -timeout 45s`; `GOWORK=off go test -race ./internal/agent/nativeprobe -count=1 -v` | `docs/superpowers/plans/trpc-native/sdk-probes.md`; `.superpowers/sdd/2026-09-19-trpc-native-agent-p0/task-2-report.md` | SDK module cache; no real provider required | v1.10.0 `Session.Clone`/`UpdateUserSession` race blocks all native Runner product execution. P3 must add provider proof; key-space test does not prove authorization/database isolation. |
| P0-3 恢复证据与运行语义 | P0-1、P0-2 runner evidence | P0 Task 3 implementer | `d63879ab` | complete（基线记录；无生产代码改动） | pending | pending | deterministic SQLite SDK probe; runtime race policy; isolated SQLite process/fault harness | `GOWORK=off go test ./internal/agent/trpc -run 'TestCheckpointProbe|TestSQLiteSaverPendingWrites' -count=1 -v`; `GOWORK=off go test -race ./internal/agent/runtime -count=1 -v`; `GOWORK=off go test -json ./internal/agent/recoverytest -count=1 > /tmp/weknora-native-recovery-p0.json` | `docs/superpowers/plans/trpc-native/evidence/task-3-recovery-2026-09-19.md`; `/tmp/weknora-native-recovery-p0.json` (temporary) | PostgreSQL matrix/contention and standalone real-provider case are `blocked-env`: `TRPC_RECOVERY_PG_DSN` / `TRPC_RECOVERY_GRAPH_PROVIDER` unset | six recovery gaps in `recovery-gaps.md`; Session Service is the new execution-history authority while run/tool-decision/checkpoint retain their recovery roles; unknown non-idempotent calls stay `waiting_user`; v1.10.0 native Runner product execution remains blocked by P0-2 race gate; P1/P2/P3/P7 own remediation and acceptance |
| P0-4 SDK 能力与扩展决策 | P0-1 plus P0-2/P0-3 results | P0 Task 4 implementer | 本任务文档提交（见 git log） | complete（90 行能力矩阵、v1 接口；round 1 决策详情与准入语义修订；待复审） | pending | pending | fixed v1.10.0 source; prior Task2/3 evidence; extracted Go contract compilation only | TSV schema/feature/provider/source-path coverage check; `GOWORK=off go test /tmp/trpc-native-contract-v1.go`; `git diff --check` | `.superpowers/sdd/2026-09-19-trpc-native-agent-p0/task-4-report.md`; `sdk-capabilities.tsv`; `interfaces.md` | isolated local worktree; no product/provider/database/client execution in Task4 | 4 verified rows are bounded prior probes; 79 source-only, 2 blocked-env, 5 incompatible; no approved target SDK/Session config; native Runner remains no-go until clean race probe and separate persistence/recovery gates; P0-5 records blocked implementation dependencies |
| P0-5 P0 gate and P1/P2 plans | P0-1 through P0-4 | P0 Task 5 implementer | `ea10b352` | complete（gate documentation；no product implementation） | pending | pending | prior deterministic/SQLite evidence, fixed v1.10.0 source and blocker decision | matrix/status cross-check; `python3` documentation consistency check; `git diff --check` | `p0-decision.md`; `.superpowers/sdd/2026-09-19-trpc-native-agent-p0/task-5-report.md` | PostgreSQL and real Provider remain `blocked-env`; no target SDK/Session/Memory persistent configuration selected | **NO-GO** for v1.10.0 native Runner product execution: SDK Session race is reproducible. P1/P2 executable plans intentionally not created because their fixed SDK/storage prerequisites are absent; future selection must pass the unchanged race probe, then SQL/backend and recovery gates. |

## 证据规则

* `passed` 仅用于工作包完成条件；本任务的完成条件是文档覆盖审查。
* `unverified` 表示尚无对应行为证据，不因源码、Mock、SQLite、构建或单元测试而改为运行通过。
* `blocked-env` 仅在后续运行实际缺少凭据、服务、数据库或设备时使用；当前没有把未执行的项目伪装成环境阻塞。
* 每个 TSV 行都有目标责任阶段；新注册入口或消费者必须先补行，再改变迁移结论。

## P0-4 gate clarification

P0-2 表格原 pending 行是当时未回填的计划状态，不覆盖已提交的 Task 2 证据：`sdk-probes.md` 和 Task 2 report 记录确定性探针通过、race 失败、产品执行门关闭。本任务未改其审查状态。P0-3 的 SQLite/策略行为证据及 PostgreSQL/真实 Provider skips 按已提交 evidence 文件引用，未重新执行或提升等级。P0-4 文档完成也不等于 P0 gate 通过；最终版本、Session/Memory 持久化配置、append failure barrier、普通/嵌套 Agent 恢复组合仍需后续决策与验收。

P0-4 review round 1：P1 补齐授权 pending 决策详情/读取/OAuth/resolve 与事件引用；P2 独立覆盖 ENG-006 的 enabled、admission_enabled、drain、读取消清理和排空契约。新增 2 个 source-only 矩阵行，静态与 Go 契约编译重新通过；未改变任何产品执行阻塞，等待独立复审。

## P0-5 decision

P0-5 records the explicit product-execution **NO-GO** in [p0-decision.md](p0-decision.md). The deterministic tool and SQLite recovery evidence remain bounded; the fixed v1.10.0 Session race, append-failure barrier gap, unselected Session/Memory persistence combination, blocked PostgreSQL matrix and blocked real-Provider case prohibit native Runner product wiring. The named P1/P2 detailed plan files are not created: doing so would require fabricated SDK/storage calls before a reviewed target version and backend exist. This is a downstream planning decision, not a reduction of the confirmed specification or a release conclusion.
