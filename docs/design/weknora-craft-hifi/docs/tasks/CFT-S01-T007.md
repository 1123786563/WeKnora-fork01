# CFT-S01-T007 · 稳定消息与工具卡投影

**目标：**把既有消息日志映射assistant-ui，稳定ID与工具卡关联，主/子运行状态不混淆。

| 调度项 | 内容 |
|---|---|
| 阶段 | S01 |
| 执行级别 | 实现/集成；已有能力优先补证据 |
| Track | aui |
| Repository | 1123786563/WeKnora-fork01 |
| 建议角色 | 前端 |
| Module Key | aui |
| File Lock Key | craft-message-projection |
| 依赖 | CFT-S00-T002、CFT-S00-T006 |
| 可并行 | 依赖完成且无重叠文件锁即可；同一文件变更串行合并 |
| 旧任务映射 | CR-02 |
| 状态 | 计划，未实施到业务仓库 |

## 输入与交付接口

**消费：**T002事件合同、T006Provider、既有持久化消息。

**产出：**稳定消息转换与工具/产物卡。所有命名与DTO以T001/T002冻结映射为准，不平行创建第二套同职责模块。

## 文件落点

- 修改 packages/views/src/craft/presentation.ts
- 新增建议 packages/views/src/craft/thread.tsx
- 新增建议 packages/views/src/craft/presentation-replay.test.ts

以上“新增建议”文件不是已存在声明；开工先查找同职责实现。已具备能力时补测试/接线/证据。共享文件修改先取得File Lock；不顺带重构其他领域。

## 验收输入与断言

```json
{
  "fixture_id": "CFT-S01-T007-acceptance",
  "scope": "two tenants, two users, one writable session, immutable v1/v2",
  "assertions": [
    "同seq重放两次消息不增加",
    "增量文字不重复拼接",
    "子任务完成后主消息仍可running"
  ],
  "expected_evidence": "消息数量、ID、正文和主状态与fixture一致"
}
```

## 执行步骤

- [ ] **确认输入与落点。**读取 `01-详细设计.md`、`04-API与状态契约.md` 和依赖任务产物；定位以上文件，记录已实现部分与本轮差异，不默认重建。
- [ ] **先写反例。**在 `packages/views/src/craft/presentation-replay.test.ts` 建立本任务断言：同seq重放两次消息不增加；增量文字不重复拼接；子任务完成后主消息仍可running。使用真实领域函数或服务边界，仅对网络/存储依赖作必要可控fixture。文档/脚本任务使用清单验证而非伪造编译失败。
- [ ] **确认失败原因。**执行下方目标命令；失败必须来自缺失行为，而不是文件不存在、导入错误或测试根本没被收集。已有行为已通过时记录证据并只补缺口。
- [ ] **实现最小差异。**在锁定文件内完成“稳定消息转换与工具/产物卡”，遵守消费接口，不改变无关租户、RAG或非Craft页面。
- [ ] **验证并提交。**运行目标命令及受影响包的typecheck/build；涉及外部引擎的任务同时跑固定镜像集成测试。记录失败/成功输出、fixture、commit与必要截图后请求review；review通过才标verified。

## 目标验证命令

```bash
pnpm exec tsx --test packages/views/src/craft/presentation-replay.test.ts
```

这是实施后执行的命令，不是本包已经运行的结果。新测试文件与新增脚本必须先由该任务/T001建立。Playwright命令依赖T001核对并提供仓库兼容的runner配置与浏览器；不得把命令空跑、跳过或not_run记为passed。

## 完成证据

消息数量、ID、正文和主状态与fixture一致。证据建议存 `docs/craft/evidence/CFT-S01-T007/`：commit、镜像/依赖版本、命令输出、fixture哈希、trace/截图与review结论。

## 不允许与回退

用随机ID生成历史；子finish结束主任务。失败时保留旧能力与类型开关；回退本任务PR，不覆盖其他任务的共享文件。生产数据迁移/对象生命周期改变必须单独提供可逆方案，不依靠git revert恢复数据。
