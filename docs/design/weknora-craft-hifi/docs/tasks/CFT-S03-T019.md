# CFT-S03-T019 · 文件收集、检查与不可变发布

**目标：**将staging文件经策略、真实检查和hash计算后发布，保证历史版本不变且无半成品发布。

| 调度项 | 内容 |
|---|---|
| 阶段 | S03 |
| 执行级别 | 实现/集成；已有能力优先补证据 |
| Track | artifacts |
| Repository | 1123786563/WeKnora-fork01 |
| 建议角色 | 后端 |
| Module Key | artifacts |
| File Lock Key | craft-manifest-version |
| 依赖 | CFT-S02-T016 |
| 可并行 | 依赖完成且无重叠文件锁即可；同一文件变更串行合并 |
| 旧任务映射 | CR-06 |
| 状态 | 计划，未实施到业务仓库 |

## 输入与交付接口

**消费：**T016workspace/fence、既有VersionStore。

**产出：**不可变manifest、检查记录、发布指针。所有命名与DTO以T001/T002冻结映射为准，不平行创建第二套同职责模块。

## 文件落点

- 新增建议 internal/application/service/craft_artifact_publish.go
- 新增建议 internal/application/service/craft_artifact_publish_test.go
- 修改 internal/craft/contracts.go（必要manifest扩展）

以上“新增建议”文件不是已存在声明；开工先查找同职责实现。已具备能力时补测试/接线/证据。共享文件修改先取得File Lock；不顺带重构其他领域。

## 验收输入与断言

```json
{
  "fixture_id": "CFT-S03-T019-acceptance",
  "scope": "two tenants, two users, one writable session, immutable v1/v2",
  "assertions": [
    "路径逃逸/软链接/secret文件拒绝",
    "检查失败不发新版本",
    "对象上传后事务失败留下可清理staging而非公开版本",
    "v2发布后v1 hash不变"
  ],
  "expected_evidence": "真实文件hash和事务失败证据齐全"
}
```

## 执行步骤

- [ ] **确认输入与落点。**读取 `01-详细设计.md`、`04-API与状态契约.md` 和依赖任务产物；定位以上文件，记录已实现部分与本轮差异，不默认重建。
- [ ] **先写反例。**在 `internal/application/service/craft_artifact_publish_test.go` 建立本任务断言：路径逃逸/软链接/secret文件拒绝；检查失败不发新版本；对象上传后事务失败留下可清理staging而非公开版本；v2发布后v1 hash不变。使用真实领域函数或服务边界，仅对网络/存储依赖作必要可控fixture。文档/脚本任务使用清单验证而非伪造编译失败。
- [ ] **确认失败原因。**执行下方目标命令；失败必须来自缺失行为，而不是文件不存在、导入错误或测试根本没被收集。已有行为已通过时记录证据并只补缺口。
- [ ] **实现最小差异。**在锁定文件内完成“不可变manifest、检查记录、发布指针”，遵守消费接口，不改变无关租户、RAG或非Craft页面。
- [ ] **验证并提交。**运行目标命令及受影响包的typecheck/build；涉及外部引擎的任务同时跑固定镜像集成测试。记录失败/成功输出、fixture、commit与必要截图后请求review；review通过才标verified。

## 目标验证命令

```bash
go test ./internal/application/service -run TestCraftArtifactPublish -count=1
```

这是实施后执行的命令，不是本包已经运行的结果。新测试文件与新增脚本必须先由该任务/T001建立。Playwright命令依赖T001核对并提供仓库兼容的runner配置与浏览器；不得把命令空跑、跳过或not_run记为passed。

## 完成证据

真实文件hash和事务失败证据齐全。证据建议存 `docs/craft/evidence/CFT-S03-T019/`：commit、镜像/依赖版本、命令输出、fixture哈希、trace/截图与review结论。

## 不允许与回退

仅复制目录或后缀验证。失败时保留旧能力与类型开关；回退本任务PR，不覆盖其他任务的共享文件。生产数据迁移/对象生命周期改变必须单独提供可逆方案，不依靠git revert恢复数据。
