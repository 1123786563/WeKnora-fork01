# CFT-S01-T009 · 首页、作品库与模板接线

**目标：**按P01–P03实现目标输入、授权作品列表、过滤/分页、模板填充与创建恢复。

| 调度项 | 内容 |
|---|---|
| 阶段 | S01 |
| 执行级别 | 实现/集成；已有能力优先补证据 |
| Track | entry-pages |
| Repository | 1123786563/WeKnora-fork01 |
| 建议角色 | 前端 |
| Module Key | entry-pages |
| File Lock Key | craft-entry-views |
| 依赖 | CFT-S00-T004、CFT-S00-T005、CFT-S01-T008 |
| 可并行 | 依赖完成且无重叠文件锁即可；同一文件变更串行合并 |
| 旧任务映射 | CR-04 |
| 状态 | 计划，未实施到业务仓库 |

## 输入与交付接口

**消费：**T004壳、T005能力、T008命令桥。

**产出：**可创建并进入真实会话的三个页面。所有命名与DTO以T001/T002冻结映射为准，不平行创建第二套同职责模块。

## 文件落点

- 新增建议 packages/views/src/craft/home.tsx
- 新增建议 packages/views/src/craft/library.tsx
- 新增建议 packages/views/src/craft/templates.tsx
- 新增建议 packages/views/src/craft/entry-pages.test.tsx

以上“新增建议”文件不是已存在声明；开工先查找同职责实现。已具备能力时补测试/接线/证据。共享文件修改先取得File Lock；不顺带重构其他领域。

## 验收输入与断言

```json
{
  "fixture_id": "CFT-S01-T009-acceptance",
  "scope": "two tenants, two users, one writable session, immutable v1/v2",
  "assertions": [
    "空目标不能提交",
    "模板只填表不授权/执行",
    "列表404/空/无匹配/分页失败分开",
    "创建成功后上传失败不重复创建Session"
  ],
  "expected_evidence": "主路径与四种空错态可复现"
}
```

## 执行步骤

- [ ] **确认输入与落点。**读取 `01-详细设计.md`、`04-API与状态契约.md` 和依赖任务产物；定位以上文件，记录已实现部分与本轮差异，不默认重建。
- [ ] **先写反例。**在 `packages/views/src/craft/entry-pages.test.tsx` 建立本任务断言：空目标不能提交；模板只填表不授权/执行；列表404/空/无匹配/分页失败分开；创建成功后上传失败不重复创建Session。使用真实领域函数或服务边界，仅对网络/存储依赖作必要可控fixture。文档/脚本任务使用清单验证而非伪造编译失败。
- [ ] **确认失败原因。**执行下方目标命令；失败必须来自缺失行为，而不是文件不存在、导入错误或测试根本没被收集。已有行为已通过时记录证据并只补缺口。
- [ ] **实现最小差异。**在锁定文件内完成“可创建并进入真实会话的三个页面”，遵守消费接口，不改变无关租户、RAG或非Craft页面。
- [ ] **验证并提交。**运行目标命令及受影响包的typecheck/build；涉及外部引擎的任务同时跑固定镜像集成测试。记录失败/成功输出、fixture、commit与必要截图后请求review；review通过才标verified。

## 目标验证命令

```bash
pnpm exec tsx --test packages/views/src/craft/entry-pages.test.tsx
```

这是实施后执行的命令，不是本包已经运行的结果。新测试文件与新增脚本必须先由该任务/T001建立。Playwright命令依赖T001核对并提供仓库兼容的runner配置与浏览器；不得把命令空跑、跳过或not_run记为passed。

## 完成证据

主路径与四种空错态可复现。证据建议存 `docs/craft/evidence/CFT-S01-T009/`：commit、镜像/依赖版本、命令输出、fixture哈希、trace/截图与review结论。

## 不允许与回退

保留原型硬编码6件与模拟资料。失败时保留旧能力与类型开关；回退本任务PR，不覆盖其他任务的共享文件。生产数据迁移/对象生命周期改变必须单独提供可逆方案，不依靠git revert恢复数据。
