# CFT-S02-T017 · 授权资料解析与输入边界

**目标：**资料引用由既有资源ACL解析并最小化送入执行环境，索引状态和上传状态分开。

| 调度项 | 内容 |
|---|---|
| 阶段 | S02 |
| 执行级别 | 实现/集成；已有能力优先补证据 |
| Track | sources |
| Repository | 1123786563/WeKnora-fork01 |
| 建议角色 | 后端 |
| Module Key | sources |
| File Lock Key | craft-source-guard |
| 依赖 | CFT-S00-T002 |
| 可并行 | 依赖完成且无重叠文件锁即可；同一文件变更串行合并 |
| 旧任务映射 | CR-06 |
| 状态 | 计划，未实施到业务仓库 |

## 输入与交付接口

**消费：**T002输入合同、原Session/知识资源授权。

**产出：**授权输入快照与citation映射。所有命名与DTO以T001/T002冻结映射为准，不平行创建第二套同职责模块。

## 文件落点

- 修改 internal/application/service/craft_session.go
- 新增建议 internal/application/service/craft_source_guard.go
- 新增建议 internal/application/service/craft_source_guard_test.go

以上“新增建议”文件不是已存在声明；开工先查找同职责实现。已具备能力时补测试/接线/证据。共享文件修改先取得File Lock；不顺带重构其他领域。

## 验收输入与断言

```json
{
  "fixture_id": "CFT-S02-T017-acceptance",
  "scope": "two tenants, two users, one writable session, immutable v1/v2",
  "assertions": [
    "跨租户/用户未授权资料拒绝",
    "共享知识按资源ACL而非仅同租户",
    "撤权后下一次读取失败",
    "文档指令不能扩大网络与工具权限"
  ],
  "expected_evidence": "ACL正反例及索引未就绪行为明确"
}
```

## 执行步骤

- [ ] **确认输入与落点。**读取 `01-详细设计.md`、`04-API与状态契约.md` 和依赖任务产物；定位以上文件，记录已实现部分与本轮差异，不默认重建。
- [ ] **先写反例。**在 `internal/application/service/craft_source_guard_test.go` 建立本任务断言：跨租户/用户未授权资料拒绝；共享知识按资源ACL而非仅同租户；撤权后下一次读取失败；文档指令不能扩大网络与工具权限。使用真实领域函数或服务边界，仅对网络/存储依赖作必要可控fixture。文档/脚本任务使用清单验证而非伪造编译失败。
- [ ] **确认失败原因。**执行下方目标命令；失败必须来自缺失行为，而不是文件不存在、导入错误或测试根本没被收集。已有行为已通过时记录证据并只补缺口。
- [ ] **实现最小差异。**在锁定文件内完成“授权输入快照与citation映射”，遵守消费接口，不改变无关租户、RAG或非Craft页面。
- [ ] **验证并提交。**运行目标命令及受影响包的typecheck/build；涉及外部引擎的任务同时跑固定镜像集成测试。记录失败/成功输出、fixture、commit与必要截图后请求review；review通过才标verified。

## 目标验证命令

```bash
go test ./internal/application/service -run TestCraftSourceGuard -count=1
```

这是实施后执行的命令，不是本包已经运行的结果。新测试文件与新增脚本必须先由该任务/T001建立。Playwright命令依赖T001核对并提供仓库兼容的runner配置与浏览器；不得把命令空跑、跳过或not_run记为passed。

## 完成证据

ACL正反例及索引未就绪行为明确。证据建议存 `docs/craft/evidence/CFT-S02-T017/`：commit、镜像/依赖版本、命令输出、fixture哈希、trace/截图与review结论。

## 不允许与回退

把请求中的tenant_id作为身份；索引未完成当可检索。失败时保留旧能力与类型开关；回退本任务PR，不覆盖其他任务的共享文件。生产数据迁移/对象生命周期改变必须单独提供可逆方案，不依靠git revert恢复数据。
