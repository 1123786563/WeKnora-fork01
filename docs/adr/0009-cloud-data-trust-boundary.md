# 云端执行采用服务端可处理的分层加密边界

WeKnora 在 Task Grant 与空间策略允许的范围内解密并处理任务内容，因为云端 Agent 执行、知识检索和 Connector 调用无法与服务端永远不可见的端到端加密同时成立。传输、数据库、对象存储、备份和移动缓存均加密，凭据进入专用 Secret 管理并以短时最小权限提供给受控执行；客户管理密钥可以作为部署能力，但执行期间服务端仍需访问获准内容。私有任务的管理员内容访问另走有理由、有时限且完整留痕的合规流程，而不是普通 Admin 权限。
