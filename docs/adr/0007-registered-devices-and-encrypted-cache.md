# 移动端采用可撤销设备身份与加密离线缓存

移动 AI Office 保留 WeKnora 现有用户登录与 Tenant 授权作为身份权威，同时登记可独立撤销的移动设备，用于推送、设备策略和本地密钥封装。获准离线内容按用户与 Tenant 分区并加密，空间可限制缓存范围和保留时间；登出、切换空间、设备撤销或权限失效后删除对应缓存或使其密钥不可用。我们不采用 Happy 的公钥账户模型，也不把操作系统 App Sandbox 单独视为足够的企业数据保护。
