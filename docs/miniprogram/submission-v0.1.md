# Taro 小程序 v0.1 源码提交说明

本次按用户授权将已交付 v0.1 源码发布到 `feat/taro-miniprogram-v0-1`，不是继续完成全部后端任务或发布生产版本。

代码基线：`38fe970593dbd17cbce8216042a31cf159473d3d`。主分支不直接修改；未经验证的版本不得自动合并。

## 提交范围

- 新增 `apps/miniprogram`：配置、20 个页面入口、组件、6 组业务页面、平台适配、核心逻辑、125 项设计令牌、图标与测试。
- 新增共享回归测试 `packages/api-client/src/chat/mini-regression.test.ts`。
- 对原 `client.ts`、`knowledge/documents.ts`、`chat/attachments.ts`、`chat/stream.ts` 应用定点补丁；增加 `pnpm-workspace.yaml` 的小程序工作区。原文件 Git blob SHA 与基线逐一核验，未用不完整片段覆盖文件。
- 10 个 PNG 采用无损压缩，解码后的 RGBA 像素与原代码包一致。其余源码沿用 v0.1，不重新设计业务。
- 保留已有 `docs/WeKnora-Miniapp-Implementation-Kit`、`docs/WeKnora-Taro-Design`，不重复上传大型 HTML 和截图，也不覆盖既有设计文件。

## 本次已执行的验证

| 检查 | 结果与范围 |
| --- | --- |
| 小程序核心与原生传输适配测试 | Node 隔离测试 25/25 通过，不含微信真实运行时 |
| SSE parser 隔离回归 | 3/3 通过；分片、CRLF 边界及不完整帧 |
| 原交付补丁脚本安全函数 | Python 测试 7/7 通过；不等于脚本已在真实克隆上安装/构建成功 |
| 设计令牌 | 125 项导出一致，SCSS 引用有效 |
| 既有文件基线 | 5 个原文件哈希匹配，按唯一锚点应用 9 处修改 |
| 图标压缩 | 10 个文件解码后逐像素一致 |

## 尚未验证与阻断项

当前工作环境无法解析外部 npm/GitHub 域名，没有完整依赖安装。本次通过获授权的 GitHub 连接器发布代码，不宣称本地完整克隆、全仓测试或构建通过。

尚未完成：完整 TypeScript 类型检查、Taro 微信构建、锁文件解析更新、共享包与 Web/Desktop/Embed/Expo 回归、Go 集成测试、真实登录/上传/任务/支付联调、微信开发者工具以及 iOS/Android 真机验收。HTML 原型测试与独立函数测试都不能替代上述验证。

本次不修改 Go 后端。微信登录桥接、任务集合与待办读模型、审批详情、产物定位、获准支付渠道和通知属于后续开发内容。批准/购买等入口按现有代码关闭；未装配的接口显示不可用，不展示伪造数据。所有原任务不得因这次源码提交而批量标记为完成。

## 合并前要求

1. 在受信任环境安装依赖并审查根锁文件变更，验证候选 Taro/React/TypeScript 组合。
2. 跑小程序测试、令牌检查、类型检查和微信构建；补齐共享包及受影响消费者回归。
3. 真机验证登录态/空间隔离、弱网和后台恢复、上传解析分离、重复提交与审批冲突。
4. 对照既有设计与未实现清单评审；保持支付和未完成能力关闭，再决定是否合并。
