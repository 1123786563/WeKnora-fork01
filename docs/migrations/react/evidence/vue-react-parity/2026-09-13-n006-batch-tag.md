# 2026-09-13 N006 文档列表批量/标签切片（batch/tag prompt parity）

矩阵行：N006（R010）"documents skeleton usable; batch/tag prompt remains gap" → 本切片关闭其中的批量选择入口、批量操作提示（prompt→对话框）、标签编辑入口、按标签筛选。范围锁定在实施代理独占可写区 `apps/web/src/documents/**`，未触碰 knowledge-bases/App.tsx/faq/organizations/GlobalCommandPalette 等他人区域。

## Vue 权威基准（frontend/src/views/knowledge/KnowledgeBase.vue，下称 KB.vue）

### 批量选择与批量操作
| 语义点 | Vue 行号 | 内容 |
| --- | --- | --- |
| 选择状态 | KB.vue L440, L1989-2018 | `selectedIds` Set；`toggleSelectRow` 支持 shift 范围选；`toggleSelectAll`（列表头全选 L2690）；`clearSelection` |
| 批量模式开关 | KB.vue L2025-2052 | `batchMode` toggle；取消=清空+退出；卡片/列表 "…" 菜单 `batch-manage` 进入（L2241/L2257）；框选 marquee 自动开启（L2053-2068） |
| 批量栏 | DocumentBatchBar.vue 全文；KB.vue L2710-2717 | `已选 {count} 项`(selectedCount) + 取消选择(clearSelection) + 重建(popconfirm confirmBatchReparseDocument) + 批量打标签(batchTag) + 移动文件夹 + 批量删除(popconfirm confirmBatchDeleteDocument) |
| 批量删除 | KB.vue L2093-2162 | batchDeleteKnowledge → 提交/轮询/成功 batchDeleteSuccess |
| 批量重建 | KB.vue L499-532 | 跳过解析中文档（L502-513 skipped 提示 batchReparseSkippedInFlight；全部在飞提示 rebuildInProgress） |
| 批量打标签 | KB.vue L2164-2191；BatchTagDialog.vue 全文 | 打开 BatchTagDialog（标题 batchTagDialogHeading、副标题 batchTagSubtitle{count}、已选/可选分区、搜索 tagEditSearch、chips、空态 tagEmptyResult、新建标签 tagNewPlaceholder/Enter/tagCreateAction"查询词"、页脚 tagSelectedCount+取消/确认）；确认后 `updateKnowledgeTagBatch({updates})`（L2178），成功 batchTagSuccess、清空选择、刷新列表+标签 |
| 预选标签 | KB.vue L445-460 | `batchTagPreSelectedIds`＝所选文档标签 id 的**交集**；对话框打开时回填（BatchTagDialog.vue L126-135） |
| 筛选变化清空选择 | KB.vue L2260-2266 | watch(selectedTagIds/关键词/类型/状态/来源/时间/kbId) → clearSelection |

### 标签编辑（单个文档）
| 语义点 | Vue 行号 | 内容 |
| --- | --- | --- |
| 入口 | KB.vue L2668/L2692（卡片/列表 `@tag-edit`）；DocumentListView.vue L327-335（行内标签 chips 可点击） | 打开 TagEditDialog（标题 tagEditDialogHeading、分区 tagEditSelected/AvailableSection、空态 tagEditNoSelected） |
| 提交 | KB.vue L733-746, L1005-1012 | `updateKnowledgeTagBatch({updates:{[id]:tagIds}})` → tagUpdateSuccess → 重置页码+刷新列表+刷新标签 |
| 行内标签展示 | DocumentListView.vue L333-343 | 文档行显示标签 chips（有则显示，可点击编辑） |

### 按标签筛选
| 语义点 | Vue 行号 | 内容 |
| --- | --- | --- |
| 状态 | KB.vue L534-556 | `selectedTagIds`（多选）、`tagFilterCleared` 占位、`tagSearchQuery`、TAG_PAGE_SIZE=50 + load more |
| 请求参数 | KB.vue L668-683（L671） | 服务端过滤：`tag_ids` 逗号连接，空则省略 |
| 触发器文案 | KB.vue L708-719 | 空+未清空→allTags；空+已清空→tagFilterPlaceholder；单选→标签名；多选→tagFilterMulti{count}；title=可见名顿号连接（L721-729） |
| 面板 | KB.vue L2465-2556 | 标题+总数、搜索框(tagSearchPlaceholder)、chips（名称+knowledge_count）、load more（tenant.loadMore）、空态 tagEmptyResult、页脚 管理标签（canEdit，→ KbTagManageDrawer） |
| 交互 | KB.vue L948-964 | 点击 chip 切换；清空 → tagFilterCleared=true + 空数组 |

## React 现状（改前）与差异表

| # | Vue | React 改前（KnowledgeDocumentsPage.tsx） | 差异判定 | 本切片处置 |
| --- | --- | --- | --- | --- |
| 1 | 多选标签筛选（chips 面板+搜索+计数+load more+占位/多选文案） | 单选 `<select>`（原 L1487 tagId、L2511-2520），仅标签名 | **GAP** | 关闭：触发按钮 `.doc-tag-filter-trigger` + `TagFilterPanel`（chips/计数/is-active/搜索/空态/清空/50 条分页 load more） |
| 2 | `tag_ids` 逗号连接传参（L671） | `tag_id` 单值；且列表 effect 依赖只带 `filtering` 布尔——同非空标签间切换不重新拉列表（隐性缺陷） | **GAP+缺陷** | 关闭：`joinTagIds()`；`selectedTagIds` 进入 effect 依赖 |
| 3 | 批量打标签对话框（BatchTagDialog） | `window.prompt` 让用户手输逗号分隔的**裸标签 ID**（原 L2258-2282） | **GAP（本切片主目标"prompt parity"）** | 关闭：`TagPickerDialog mode="batch"`，语义照 Vue（预选=交集、搜索、新建标签、页脚计数、确认→PUT /knowledge/tags→成功提示→清空选择→刷新） |
| 4 | 单文档标签编辑入口（TagEditDialog + 行内 chips） | 无入口，文档不显示标签 | **GAP** | 关闭：行内 `标签` 按钮（tagLabel）+ `TagPickerDialog mode="single"`（tagEdit* 文案）+ 行内只读 chips |
| 5 | 取消选择（批量栏） | 无 | **GAP** | 关闭：`取消选择`（clearSelection）按钮 |
| 6 | 全选（列表头 checkbox） | 无 | **GAP** | 关闭：全选（selectAll）checkbox（本页全选） |
| 7 | 批量删除/重建确认提示 | 已有确认 Dialog + skip 逻辑（此前提已经过 audit 修复） | 基本对齐 | 保留；补 batchReparseSkippedInFlight/rebuildInProgress/batchReparseSuccess 键为共享 i18n 消费 |
| 8 | 筛选变化清空选择（L2260-2266） | 无 | **GAP** | 关闭：effect 清空 selected |
| 9 | 批量模式开关/框选/shift 范围选 | 无（checkbox 常显） | 部分差异 | **遗留**（见下） |
| 10 | 管理标签抽屉（tagManageLink→KbTagManageDrawer） | 无 | **GAP** | **遗留**（独立抽屉组件，超出本切片） |

## 实现（apps/web/src/documents/**，TDD 先红后绿）

新文件：
- `tags.ts` — 纯函数：`documentTags`（防御式读后端内嵌 tags）、`commonTagIds`（交集，KB.vue L445-460）、`filterTagOptions`（排除已选+搜索，BatchTagDialog L145-152）、`joinTagIds`（L671）、`tagFilterLabel`（L708-719）、`tagFilterTitle`（L721-729）、`TAG_PANEL_PAGE_SIZE`+`paginateTagOptions`（50/页）、`tagUpdatesFor`（L2169-2178）
- `tags-locale.ts` — `:tagSurfaceT`：先共享 i18n（formatMessage），再 5 语逐字节回退表。**fallback 仅 3 键**：`common.confirm`、`common.clear`、`tenant.loadMore`（Vue locale 已有、packages/i18n 尚缺——标注 **zh-only-gap/迁移待办**，值取自 frontend/src/i18n/locales/*.ts L4673/L4701/L3318 各语对应行）。其余全部直接消费共享键（见下）
- `TagPickerDialog.tsx` — `TagPickerDialog`（batch/single 双模式，挂载即回填 preSelectedIds，等价 Vue watch(visible)）+ `TagFilterPanel`
- `tags.test.ts`(10) + `tags-ui.test.tsx`(9)

修改文件：
- `KnowledgeDocumentsPage.tsx` — 状态多选化（selectedTagIds/tagFilterCleared/tagFilterOpen/tagDialog）、筛选栏触发器+面板、批量栏（全选/取消选择/批量打标签）、行内标签 chips+标签入口、submitTagDialog/createKnowledgeTag（client.request POST /knowledge-bases/:id/tags，同页既有 graph-exact 模式）、成功提示 actionNotice、筛选变化清空选择
- `documents.css` — 触发器/面板/chips/对话框/批量栏样式（沿用 Vue 类名 tag-filter-*、batch-tag-*）

i18n 消费（packages/i18n 共享键，formatMessage）：knowledgeBase.batchTag / batchTagDialogHeading / batchTagSubtitle / batchTagSelectedSection / batchTagAvailableSection / batchTagNoSelected / batchTagSuccess / batchTagFailed / tagEditDialogHeading / tagEditSearch / tagEditSelectedSection / tagEditAvailableSection / tagEditNoSelected / tagFilterTitle / tagFilterPlaceholder / tagFilterMulti / tagSearchPlaceholder / tagNewPlaceholder / tagClearAction / tagCreateAction / tagSelectedCount / tagEmptyResult / tagUpdateSuccess / tagLabel / noTags / allTags / selectAll / selectedCount / clearSelection / batchDelete / confirmBatchDeleteDocument / confirmBatchReparseDocument / rebuildInProgress / batchReparseSkippedInFlight / batchReparseSuccess；common.cancel。**zh-only 局部表（3 键，5 语回退）**：common.confirm、common.clear、tenant.loadMore——证据标注为 packages/i18n 迁移待办，键入库后可删表。

## TDD 记录

| 阶段 | 命令（cd apps/web） | 结果 |
| --- | --- | --- |
| 基线 | `npx tsx --test 'src/documents/*.test.ts' 'src/documents/*.test.tsx'` | **79/79 绿**（账本基线一致；注：`tsx --test src/documents/` 目录形式在 tsx 下不展开目录，账本 79/79 对应 glob 形式） |
| 红 1 | `npx tsx --test src/documents/tags.test.ts` | fail（ERR_MODULE_NOT_FOUND: tags.ts） |
| 绿 1 | 同上（实现 tags.ts 后） | 9/10 → 修正测试自身错误断言（'法' 并非 '合同' 子串，改 '同'）→ **10/10** |
| 红 2 | `npx tsx --test src/documents/tags-ui.test.tsx` | fail（ERR_MODULE_NOT_FOUND: TagPickerDialog.tsx） |
| 绿 2 | 实现 TagPickerDialog/tags-locale/页面接线后 | 2 处测试断言写错键文案（tagEditSearch=搜索标签... 而非 tagSearchPlaceholder；漏传 createTag prop），修测试 → **9/9** |
| 全量 | glob 形式全跑 | **98/98 绿**（79 基线 + 19 新增，无回归） |
| 类型 | `pnpm run typecheck:web`（仓库根） | documents 范围 0 错误；剩余 4 个错误全部位于 packages/views/src/integrations/**（另一代理 imWizard 切片在飞、git status 可证非本代理改动），与本切片无关 |

页面级 SSR 断言（tags-ui.test.tsx）：批量打标签 按钮存在且 `Set tag IDs for selected documents` prompt 文案消失、取消选择/全选存在、doc-tag-filter-trigger/按标签筛选/全部标签 存在、既有筛选 chrome（全部类型/状态/来源、doc-filter-bar）不回归（page-chrome.test.tsx 原断言继续绿）。

## 遗留（登记后续）

1. **标签管理抽屉**（KbTagManageDrawer：标签列表/重命名/删除 + 面板页脚 管理标签 链接）未移植——独立组件，建议单开切片。
2. **批量形态增强**：Vue 的 batchMode 显式开关、框选 marquee、shift 范围选、卡片 "…" 菜单 批量管理 入口、底部悬浮批量栏形态——React 维持常显 checkbox+行内批量栏骨架，行为等价性以本切片为界。
3. **标签服务端分页**：Vue 面板走 listKnowledgeTags 50/页服务端 load more；React 目前一次性拉全量后客户端分页（tags() API 已支持 params，接服务端分页属增量）。
4. **common.confirm / common.clear / tenant.loadMore 迁移 packages/i18n**（本切片以 5 语回退表过渡）。
5. 行内标签 chips 为只读展示（点击走 标签 按钮），Vue 的 chips 溢出折叠（useTagChipsOverflow）未做。
