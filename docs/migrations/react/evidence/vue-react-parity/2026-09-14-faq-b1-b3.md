# FAQ B1 · 编辑抽屉 + B3 · 条目卡

- 日期：2026-09-14
- Vue 基线：`frontend/src/views/knowledge/components/FAQEntryManager.vue`
- React：`apps/web/src/faq/FAQPage.tsx`、`apps/web/src/faq/faq.css`、`apps/web/src/faq/FAQPage.test.tsx`
- 执行方式：TDD 先红后绿（新增 15 用例先确认 13 红，再实现转绿）。

## B1 编辑抽屉（Vue :440-577 + :1542-1552 + :1673-1783）

| 差异点 | Vue | React 之前 | React 现在 |
| --- | --- | --- | --- |
| 形态 | t-drawer 520px 右侧 (:442) | 520px 抽屉壳已有 | 保留 520px 抽屉，header/body/footer 三段（:4510-4570 CSS 语义） |
| 字段 | 标准问*/相似问/反例/答案*/标签，逐字段 desc (:448-562) | 5 个 textarea/textarea 类字段，无 desc | 同 Vue 5 字段，desc 全部走共享目录键 |
| desc 键 | standardQuestionDesc 等 5 键 | 无 | `knowledgeEditor.faq.{standard,similar,negative}QuestionsDesc`/`answersDesc`/`tagDesc`（packages/i18n generated/knowledgeSurfaces.ts:494-503） |
| 列表编辑 | input+加号添加、条目行可删、去重、上限 10/10/5、答案 x/5 计数 (:467-549, :1714-1753) | 单 textarea 换行拆分 | pushListItem/removeListItem 纯函数复刻 trim/dedupe/cap；答案计数 `n/5` 只计已提交项 (:539) |
| 校验 | editorRules：标准问必填（messages.nameRequired）→ 答案≥1（faq.answerRequired）(:1542-1552) | 依赖 HTML required + 后端报错（英文） | editorFormError 同序前置校验，错误以共享键文案显示在抽屉内（faq-editor-error） |
| 保存 | 成功提示 messages.createSuccess/updateSuccess (:1771,1774) | 英文字面量 | faqSaveResultKey → 共享键 |
| 其它 | 无启用/推荐 checkbox；标准问 maxlength 200 (:457) | 有 enabled/recommended checkbox；无长度上限 | 移除 checkbox（对齐 Vue）；maxlength=200；Enter 添加相似问/反例、Ctrl/Cmd+Enter 添加答案 (:470,:498,:531) |

## B3 条目卡（Vue :238-427 结构 + :3664-4090 样式 + :1592-1594 默认态）

| 差异点 | Vue | React 之前 | React 现在 |
| --- | --- | --- | --- |
| 布局 | faq-card 卡片列表 | wk-faq-item 列表行 | faq-card-list + faq-card（header/body/footer 三段） |
| header | 标准问（title 悬浮、两行截断）+ more 按钮 (:259-286) | 行内 strong | faq-question（title + line-clamp 2）+ card-more-btn |
| more 菜单 | popup：编辑/删除两项（:271-282） | 无菜单，行内 编辑/删除 按钮 | card-more-popup（hidden 切换、单开互斥、stopPropagation），编辑/删除走 common.* 键 |
| 三段折叠 | 相似问/反例仅非空渲染，答案常驻；label+计数+chevron；默认全部折叠 (:291-357, :1592-1594) | 无分段 | faq-section similar/negative/answers，section-count (n)，collapsed 默认 true，折叠体 hidden 留存 DOM，aria-expanded 同步 |
| 多选 | 整卡点击选择（handleCardSelect :1654） | 每行 checkbox + 表头全选 | 整卡点击 + 卡内 checkbox（stopPropagation 防双触发）；复用既有 selected Set 与批量条（启用/禁用/推荐/打标/删除不变）；Vue 无表头全选，已移除 |
| footer | 标签 chip + 状态开关（无标签兜底 untagged）(:361-408) | 行内合并展示 | faq-card-footer：faq-tag-chip（tag_id→seq 名称→无标签）+ faq-status-switch（title 已启用/已禁用，viewer 不渲染） |
| 选择态 | selected 类高亮 (:3696-3702) | 无 | .faq-card.selected / .is-selectable hover |

## TDD 轨迹

- 红：新增 15 用例后首跑 `npx tsx --test src/faq/FAQPage.test.tsx` → 35 中 13 失败（抽屉结构/desc/校验/纯函数/卡片结构均未实现），既有 20 用例全绿。
- 绿：实现后 41/41（FAQPage.test.tsx 35 + faq-import-poll 3 + import-export 3，含既有 import-poll/tags/import-excel 用例未回退）。
- `pnpm run typecheck:web`：faq 文件 0 错误（全仓仅余 src/integrations/embedWizardRender.test.tsx 2 个错误，属 embed 切片在途文件，非本切片范围）。

## 备注 / 偏差

- 折叠体采用 `hidden` 属性而非 v-if 卸载：视觉与 Vue 一致，且保证 SSR 场景答案文本可测（Vue Transition 卸载差异记录在案）。
- Vue 卡片无 checkbox（整卡点击即选）；按任务要求两者并存，行为一致。
- 并发情况：本切片实施期间 apps/web/src/faq/FAQPage.tsx 存在并发写入者（方向一致），且该文件曾被第三方以 f277aa35 提前入库（任务约定为不 commit）；未回滚他人提交，本切片剩余改动保留在工作树待主协调者复核集成。
- 截图：本轮未采集（无运行中的前端实例）；SSR 断言 + 类名对齐 Vue CSS 段落为替代证据。

## 遗留

- B2 标签管理：已由并发侧 ed93dd21 落地，本切片未触碰 FAQTagManageDialog。
- B4-B7：账本仅有“FAQ B1-B7 精确交接”占位（vue-react-parity-progress.md:1273），尚未定义具体条目，状态=待精确交接。
- 建议：单条目卡上的标签下拉改派（Vue :362-375 支持卡内改标签）未实现，当前靠批量打标覆盖，可并入 B4-B7 交接。
