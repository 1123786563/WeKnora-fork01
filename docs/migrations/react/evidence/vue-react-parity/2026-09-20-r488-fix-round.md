# 2026-09-20 R488 修复轮：组织成员区 D-B5 + FAQ 域三件（K2 新差异清零）

- 主仓 main；代理 L1+L2（文件域互斥，均带事实勘误）。证据：`.omc/state/r488-main/report-{L1,L2}.md`；门禁六绿。

## L1 组织成员管理区（organizations 54/54，基线 49+5 零回退）

**勘误**：K2 称「Vue members 区无添加入口」不实——源码 L408-446 有 admin 专属 icon 按钮+popup。按源码对齐：
- 常驻「添加成员」表单移除，改 members-list-actions icon 按钮+弹窗（选空间搜索≥2 字符/候选高亮/分配角色/取消+添加 disabled 语义/成功 refetch）。
- 成员列表表格化：表头 成员/角色/加入时间/操作（操作列仅 admin）；加入时间 Vue formatDate（本地 YYYY-MM-DD）；成员格 空间名主标+用户名副标；搜索框移入列表头行。
- owner 行：「创建者」+「我」静态 tag（user_id===currentUserId）；角色列 owner/非 admin 静态 tag（admin 绿/editor amber/viewer 灰）；owner 操作列空。
- D-B4 三低残差：编辑态 nameTip/descriptionTip；邀请码/链接复制+刷新全 icon 化（刷新 loading 旋转）；有效期 closed 自定义 select（无 option 泄漏）+成员上限纯 number input（0-10000 clamp）。

## L2 FAQ 域三件（scoped 222/222，web 全量 2136/2136）

**勘误**：A3「React=居中弹窗」系误判——本就是 520px 右抽屉（justify-end+faq-editor-drawer）。真实残差全修：
- **A3 残差**：进出动画 4 组 keyframes（0.28s cubic-bezier 对齐 tdesign + reduced-motion 关闭）+useDrawerExit（280ms 退出后卸载）双抽屉接线；FAQTagSelect（combobox+portal listbox）替换原生 select，关闭态不再泄漏「请选择标签」。
- **A6**：根因=Vue t-drawer 默认 footer 渲染 [确认,取消]——React 检索抽屉重构滚动 body+pinned footer（确认执行/取消关闭，i18n 复用）。
- **summary_model_id 矛盾**：两个自由文本 input→ModelOptionSelect（LLM 前/Embedding 后+星号+desc+前往全局设置入口）；创建自动预填默认模型（避开闭包时序）；save() 拦截「请选择 Summary 模型」+跳 models 分区。

## 遗留（L2 报告第四节）

FAQ 批量标签栏原生 select（无数据不可测，后端 500 未解）；ModelOptionSelect 无 placeholder（无模型租户角落）；纯 Wiki clearable 与编辑态 Embedding 锁定未接；后端 FAQ entry 500 阻断 A7。

## R489 队列

后端依赖项：FAQ entry 500（code 1007）排查、模板/预设/占位符三端点、quick-answer 模板 vendor；前端角落：批量标签栏 select（需后端 500 解锁）、Embedding 锁定。**R482→R488 前端侧已知差异全部清零（除环境阻断项）。**
