# N007 upload-confirm file list parity

## Vue baseline

`UploadConfirmDialog.vue` renders each file row with an 8px gap, 24px icon slot, 12px/1.35 medium filename, 11px/1.3 metadata line, ellipsis overflow, and a 22px icon-only remove control at 45% opacity that becomes visible on row hover or focus.

## React change

`UploadFilesPanel` now uses the same row anatomy and Tailwind values for staged URLs and files. Relative directories and byte sizes remain in the metadata line; React per-file upload status remains as an additional right-side state indicator. Removal remains disabled during upload and keeps the existing callbacks and aria label.

## Verification

- Focused upload-confirm tests: 37/37 passed.
- `pnpm typecheck:web`: passed.
- `git diff --check`: passed.

## Evidence boundary

This slice is Vue source comparison plus React unit/type evidence. Authenticated same-condition browser screenshots, real upload backend behavior, and Wails/native evidence remain open.
