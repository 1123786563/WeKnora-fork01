# 2026-09-20 R486 修复轮：编辑器残项 + websearch 凭证/logo + Wiki 弹窗化（R482 清单收尾批）

- 主仓 main；三代理 J1-J3（文件域互斥）+ 协调者。证据：`.omc/state/r486-main/report-{J1,J2,J3,verify}.md`；截图 `screenshots/r486-20260920/`。
- 合并态：typecheck 0、**六门禁全绿**（含 integrity——对方 lane 已提交其在途文件）。

## J1 智能体编辑器残项（五项全 DONE，agents 151/151、apps/web 全套 2112/2112）

- **D4 提示词三件**：可用变量条+点击光标插入（promptPlaceholdersFor 静态移植 placeholder.go；系统提示词按模式 4/5 变量集+上下文模板均有）；恢复默认（预设绑定优先/custom 回落 progressive_rag_agent）；使用模板（4 内置模板弹层，元数据移植 yaml，选择只写正文=Vue 语义）。收窄：quick-answer 模式模板控件不渲染（正文未 vendor+端点缺），测试锁定+记录。
- **嵌入规则编辑器**：新 `AgentParserRules.tsx`（逐文件族行+引擎下拉+xlsx 首行 checkbox+无引擎去配置链接；纯函数复用 knowledge-settings/parserSettings 未改其文件；数据走 settings.parserEngines()，空注册表降级提示）。
- **P3-2 文件类型下拉**：`FileTypeMultiSelect` 单触发复选面板（空选「全部类型」），集合语义不变；旧 chip 行与 2 个旧测试改写。
- **类型切换冲突 toast**：四纯函数移植 Vue 3190-3288 + **补齐 Vue watch 3639-3650 的 KB 列表清空（此前缺失）**；警告 4000ms role=status。微偏离（兼容切换即清，Vue toast 独立自灭）已记录。
- **D9 租户阈值默认**：`tenantRetrievalDefaultsFromConfig` 逐字段对齐 Vue `||`/`!== undefined` 语义（阈值显式 0 覆盖、top_k 0 保留默认）；仅 create 分支应用。
- SKIP：预设运行时拉取（api-client 缺三端点，静态 vendor 沿用，租户自定义模板不生效→R487）。

## J2 websearch 凭证 + 品牌 logo（settings 317/317、ui 24/24 零回退）

- **凭证逐字段管理**：CredentialResource 等价移植——已配置 ✓+更换/移除（两步确认红色二次点击才 DELETE）、未配置+配置、Enter 提交；PUT/DELETE /credentials 独立提交，删除主 save 的 piggyback PUT 分支（update body 恒无 api_key）；移除成功行内闪「凭据已移除」2.4s。i18n 零新增（复用 dataSource.credential.*）。
- **品牌 logo**：Sheet 加可选 headerIcon（不传 DOM 逐字节不变）；三引擎弹窗 color-img/mono-mask/monogram 三档；PROVIDER_BRAND.websearch 9 色值补齐+卡片 mono 染色缺口修正。Node 测试环境只走到 monogram（import.meta.glob 限制，构建生效，与卡片同现状）。

## J3 Wiki 弹窗化 + org 取证（wiki 67/67）

- **P3-1 弹窗化**：新建改共享 Dialog（「新建 Wiki 页面」，520px；打开重置表单不清选中页=Vue openCreatePageDialog；确认提交 {slug,title,page_type,content} 后关闭+选中新页；取消/×/Esc/遮罩仅关闭；slug 随标题同步可手动接管）。红 8→绿 8。
- **org 编辑态取证（12 项清单）**：核心=Vue 三分组导航（基础/成员与协作/共享资源）+邀请能力内嵌 6 控件 vs React 扁平 6 项+孤立「邀请链接」（仅 2 件、缺 5 组）；另有成员文案/组标题/badge/emoji picker/申请升级位置/shares 描述/保存位置差异。修复锚点 summary.ts+OrganizationsPage.test 记录。

## 浏览器复验

（见 report-verify；九项清单：提示词三件/嵌入规则/下拉/冲突 toast/租户阈值/凭证卡+logo/三徽标/Wiki 弹窗/防卡死）

## R487 队列

org 编辑态 12 项修复（J3 清单）→ 预设/模板/占位符三端点后端+运行时拉取 → quick-answer 模板 vendor → fixture（FAQ KB/工具会话/org/system-admin）→ R482 未覆盖面（org 行菜单/设置弹窗等）复扫。
