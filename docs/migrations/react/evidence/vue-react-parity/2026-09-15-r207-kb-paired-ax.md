# R207 Knowledge-base paired AX boundary (2026-09-15)

Fresh same-browser Chrome tabs at `/platform/knowledge-bases` were compared:

- React and Vue expose the same platform navigation, knowledge-base scope rail, empty/initialization notice, `我创建的` section, `Parity KB Demo` row and settings affordance.
- React additionally exposes semantic names for controls (checkboxes and buttons), while Vue exposes the same visual controls primarily as containers/images.
- The authenticated fixture contains one demo knowledge base and no initialized model configuration on both routes.

This confirms the current React knowledge-base list structure is aligned at the accessibility/interaction-anchor level. Computed-style, responsive widths, protected create/edit/share/upload mutations and native rendering remain separate gates.
