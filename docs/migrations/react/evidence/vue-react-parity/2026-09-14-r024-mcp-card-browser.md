# R024/N016 · MCP 卡片与新增抽屉浏览器复核

- 日期：2026-09-14
- React：`http://localhost:5181/platform/settings?section=mcp`
- 条件：已登录管理员、zh-CN、默认浅色主题、浏览器视口 1440×900；后端 `http://localhost:8080`。

## Web 浏览器证据

1. 管理员无服务状态仍显示网格内“添加服务”虚线入口；viewer 分支不显示新增入口。
2. 点击“添加服务”后，新增抽屉可见：`基本信息 → 连接配置 → 认证配置 → 高级配置`，步骤条为“连接配置 → 工具与用途说明”。
3. 连接配置中实际可见 `SSE` 与 `HTTP Streamable` 两项，没有 stdio；自定义请求头入口和“取消 / 保存并下一步”页脚顺序可见。
4. 浏览器截图已在本轮 CUA 输出中采集（1440×900）；当前证据未写入可复用文件路径，故不以截图单独宣称验收。

## 边界

- 本次只验证 Web DOM/可见布局入口；未提交 MCP 服务、未输入凭证、未触发真实工具同步，因此不构成真实 MCP 后端链路证据。
- 未执行 Wails 桌面原生运行验证；Shared 419/419、Desktop 2/2、Embed 7/7 是共享包回归，不替代桌面 UI 验收。
