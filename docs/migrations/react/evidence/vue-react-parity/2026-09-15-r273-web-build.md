# R273 Web 生产构建（2026-09-15）

文档文件夹卡片和知识库卡片调整后，`pnpm build:web` 成功完成 TypeScript 与 Vite 构建。仅保留既有大 chunk advisory（`index`、`cynefin`、`mermaid` 等超过 500 kB），没有构建错误。

原始日志：`/tmp/weknora-react-web-build-r273.log`
