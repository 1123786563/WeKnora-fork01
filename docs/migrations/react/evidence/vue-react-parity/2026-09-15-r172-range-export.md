# R172 range package export

- Scope: `packages/ui/package.json`.
- Change: exposed `@weknora/ui/range` alongside the existing shared control subpaths, keeping the package boundary consistent for future consumers.
- Validation: shared suite passed 469/469; Web typecheck and diff check passed.
