# R242 知识库文件夹路径面包屑（2026-09-15）

React 文件夹树显示时补齐 Vue 的 `doc-folder-path`：根目录入口、层级祖先路径、当前文件夹状态，以及搜索时的“包含子树”提示。根目录和祖先项可直接返回对应文件夹，现有文件夹筛选请求逻辑不变。

验证：

- 文档页面 chrome 与文件夹路径测试：10/10
- `pnpm run typecheck:web`：通过
- `git diff --check`：通过

真实文件夹浏览器交互、非空文档、上传/预览变更、响应式和桌面/native 证据仍待补齐。
