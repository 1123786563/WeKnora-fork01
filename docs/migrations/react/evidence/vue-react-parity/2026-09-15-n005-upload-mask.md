# N005 upload mask visual parity

## Vue baseline

`frontend/src/components/upload-mask.vue` renders the upload illustration, a 24px/26px brand-colored title, and two 12px centered supported-format lines. The mask itself is a centered flex column without the generic bordered card chrome.

## React change

`apps/web/src/documents/KnowledgeDocumentsPage.tsx` now uses the Vue `upload-mask.svg` asset, the same illustration dimensions and title/format typography, with all visible copy supplied by the shared translator. The existing percentage text and progress bar remain available for the React upload progress state and accessibility semantics.

## Verification

- Focused upload-confirm tests: 37/37 passed.
- `pnpm typecheck:web`: passed.
- `git diff --check`: passed.

## Evidence boundary

This is source/style and unit/type evidence. Authenticated Vue/React screenshot comparison, real upload backend behavior, and Wails/native evidence remain open.
