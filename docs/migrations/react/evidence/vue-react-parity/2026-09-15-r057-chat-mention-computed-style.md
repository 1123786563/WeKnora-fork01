# R057 Chat mention computed style runtime

- Runtime: authenticated React Web `/platform/creatChat`, with the mention listbox open.
- Computed style snapshot:
  - trigger: 28px × 28px, 6px radius, `rgb(238, 238, 238)` hover background, no border;
  - listbox: 280px width, 1px solid `rgb(231, 231, 231)`, 8px radius, `0 8px 24px rgba(0,0,0,.12)` shadow.
- The snapshot is a React baseline for the corresponding Vue mention selector comparison.
