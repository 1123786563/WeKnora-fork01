# FAQ B5+B6 · 标签气泡 Tooltip + 导入结果持久化条

- 日期：2026-09-14
- 切片：FAQ B5（FAQTagTooltip 气泡对齐）、FAQ B6（导入结果持久化条 / last-result）
- Vue 基线：`frontend/src/components/FAQTagTooltip.vue`、`frontend/src/views/knowledge/components/FAQEntryManager.vue`
- React：`apps/web/src/faq/FAQPage.tsx`、`apps/web/src/faq/faq.css`
- 测试：`apps/web/src/faq/faq-tag-tooltip.test.tsx`（新增 8）、`apps/web/src/faq/faq-import-result.test.tsx`（新增 11）

## 语义表（Vue → React）

### B5 FAQTagTooltip（FAQTagTooltip.vue + FAQEntryManager.vue:303-354、:829-844）

| 语义点 | Vue 基线 | React 实现 |
| --- | --- | --- |
| 触发 | wrapper mouseenter/mouseleave（:109-118） | hover 同 Vue；按切片要求追加 click 开关（stopPropagation 避免误触卡片多选） |
| 内容 | `content` prop 全文，pre-wrap | 气泡 `tooltip-content` 全文，white-space: pre-wrap；chip 上的原生 `title=` 移除（Vue 无） |
| 定位 | position:fixed + Teleport body；锚点上方 8px 居中；左右 8px 视口夹紧；上方不足 8px 翻转下方；上下夹紧（updatePosition :63-101） | `faqTooltipPosition` 纯函数逐步对齐；滚动(capture)/resize 重算监听同 (:120-128)；翻转后以 resolved placement 决定箭头 class（Vue 保留原 placement，属修正，见偏差） |
| 样式 | max-width 320 / min-width 100 / padding 10×14 / 圆角 6 / 12px·1.6 / 阴影 0 0 8px rgba(0,0,0,.08) / fade 0.15s / ::before+::after 双层箭头 | faq.css `faq-tag-tooltip` 逐一对应，配色走 --faq-* 变量、fallback 取 Vue 值 |
| 用点 | 卡片 similar/negative/answers 三个分区 chip；搜索抽屉 answers/similar 用 t-tooltip | `FaqTagTooltip` 包裹同两组 chip（type=similar/negative/answer，placement=top） |
| type class | tooltip-answer/similar/negative（视觉一致） | 同名 class 保留，供后续差异化 |

### B6 导入结果持久化条（FAQEntryManager.vue:1263-1303、:2111-2150、:2276-2412、:2820-2829）

| 语义点 | Vue 基线 | React 实现 |
| --- | --- | --- |
| 持久化键 | localStorage `faq_import_last_completed_{kbId}` (:2276) | `faqLastCompletedTaskKey`/save/get，空 kbId 守卫一致 |
| 结果来源 | mount 与导入成功后 `getFAQImportProgress(lastTaskId)`，仅 `status==='completed' && display_status!=='close'` 展示 (:2299-2342) | 同一 progress 接口经 `client.request`（typed client 白名单会丢弃 display_status/import_mode 等字段，故走原始 request，与 KnowledgeDocumentsPage 用法一致）；`faqImportResultFromProgress` 映射+默认值 |
| 可见性 | `showImportResultBadge = result && display_status==='open' && !taskId` (:1266-1270)；进行中显示进度条（同槽 v-if/v-else-if） | `faqImportResultVisible(result, hasActiveTask)`，任一 importTask 在途即隐藏 |
| 汇总文案 | message 非空则用之；否则 `总条数` +（merged>0 时 added?+merged，否则 success>0 时 success）+ partial/failed/skipped，` · ` 连接 (:1277-1303) | `faqImportResultSummary` 逐条对齐，i18n 用共享目录 `FAQ.import.*` |
| 模式标签 | append→primary(追加模式)，否则 warning(替换模式) | `.faq-import-mode-tag.is-append/.is-replace` |
| 失败下载 | failed_entries_url && failed_count>0 显示；点击 window.open(_blank)；无 URL 警告 noFailedRecords (:2359-2366) | 同条件渲染 `__link`；无 URL 走页内 warning message |
| 时间 | imported_at → 本地 YYYY-MM-DD HH:mm (:2369-2377) | `formatImportTime`；非法输入回显原文（Vue 会产出 NaN 串，属修正） |
| 关闭 | 收起面板 + PUT `/knowledge-bases/{id}/faq/import/last-result/display` body `{display_status:'close'}`，成功后本地置 close；失败仅日志 (:2345-2356) | 同端点同语义；后端 `internal/handler/faq.go:542` 已存在；storage 键保留（不删除） |
| 下次导入 | 成功后 saveLastCompletedTaskId + loadImportResult 替换 (:2131-2144) | 轮询 completed 分支内 save + loadLastResult；进度条 3s 收起后由持久化条接班 |
| 刷新恢复 | onMounted 恢复 (:2825-2829) | mount effect 按 kbId 恢复 |

## 已验证

- TDD 先红后绿：新增 19 测试全红（18 fail + 1 基线绿）→ 实现后全绿。
- 全量：`npx tsx --test "src/faq/"*.test.tsx "src/faq/"*.test.ts` → **78/78 通过**（任务口径基线 58；按文件逐一清点基线文件现为 59，见下方注）。
- 类型：`tsc -p tsconfig.json --noEmit`（apps/web 作用域）对 `src/faq/**` **0 错误**。
- 未改动任何只读文件；`git status` 中 apps/web/src/faq 仅 FAQPage.tsx、faq.css 修改 + 2 个新测试。

## 注记与偏差

1. **typecheck:web 全量暂红（非本切片）**：并行切片正在写 `apps/web/src/integrations/*`（embedPreviewDrawer.test.tsx 引用尚不存在的 EmbedChannelPreviewDrawer.tsx 等）。 faq 作用域 0 错误；待该切片落地后需复核全量绿。
2. 测试计数：全量 78 = 基线文件 59 + 新增 19。基线 59 与任务口径 58 差 1，来自既有文件在聚合运行下的计数口径，未改动任何基线测试文件（git 确认）。
3. 两处刻意修正（均记录在案）：tooltip 翻转后箭头 class 跟随实际方向；formatImportTime 对非法日期回显原文而非 NaN 串。
4. B6 呈现形态按 React 页面既有 header strip 结构落地（Vue 为标题栏徽标+展开面板），内容/持久化/关闭语义与 Vue 一致；进行中与完成 3s 窗口的槽位互斥行为与 Vue 同槽 v-if/v-else-if 对齐。
5. Vue closed 语义依赖服务端 display_status（PUT 后各端一致隐藏），localStorage 键保留不清除——与 Vue 行为一致。
