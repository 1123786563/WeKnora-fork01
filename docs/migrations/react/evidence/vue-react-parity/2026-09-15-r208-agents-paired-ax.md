# R208 Agents paired AX boundary (2026-09-15)

Fresh same-browser Chrome tabs at `/platform/agents` expose matching platform navigation, scope controls (`全部/收藏/最近/本空间`), heading/description, create actions, `内置 4` group and all four built-in agent cards. React and Vue now expose equivalent card titles, descriptions and management/favorite affordances; React uses semantic button/checkbox names where Vue exposes image/container wrappers.

The captured fixture contains four built-in agents and no user-created rows. Protected create/edit/share mutations, expanded rail, responsive and native evidence remain separate gates.
