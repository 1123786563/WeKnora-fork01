# 2026-09-20 R490 修复轮：实质残留清零（R489 五项全除）

- 主仓 main；代理 A+B+复验。证据：`.omc/state/r490-main/report-{A,B,verify}.md`；截图 `screenshots/r490-20260920/`（15 张）。
- 门禁：首跑三红=并行 lane 在途提交 847dbb5f（agent-editor-resources 未完成态）；其 fix-forward afb098c9 后**六门禁全绿**。

## A：抽屉三分区+标签入口（4 实质残留全除）

- #10 模型配置：补 LLM/Embedding 两行 desc（对齐 KBModelConfig.vue:36-40；wiki-only 分支 wikiOptionalDesc）。
- #13 分块设置：共享 chunkingSection.tsx 补 9 行 per-field desc（键名与 KBChunkingSettings.vue 逐一同名）+4 组 slider marks 档位。
- #18 存储引擎：形态锁定 Vue 单 select 绑定+endpoint/bucket hint+管理入口（无实例清单）。
- #7 标签管理：TagFilterPanel footer 入口（canEdit）→ TagManageDialog 完整移植（搜索/新建/重命名/删除/计数+openTagManageDrawer/onTagManageChanged 语义）。
- 数字：红 4→绿；knowledge-settings **131/131**（126+5）、documents **270/270**；顺修 App.tsx 遗留 TS2339。

## B：D12 补全+轻残留批（六项全闭环）

- **D12 @ 弹层（实质清零）**：已落地的修复只含智能体兼容性过滤；本轮补真正清空列表的**初始化过滤** `isKbModelReady`（无 summary_model_id/缺 embedding_model_id 不入列）+`mergeSharedKbsForMention`（共享 KB 合并，Input-field.vue:1262-1284）接入 ChatRoutePage；真挂载集成测试红（泄漏 3KB+2 文件）→绿（3/3 断言）；**活体：React 弹层 0 选项=Vue「无结果」**。
- N1 `{'{{'}`：已由并行 lane 修（5 locale 净 {{）+锁定测试，独立核验。
- N2 加入知识库：BookmarkAnswerDialog 已落地（handleAddToKnowledge 语义逐项等价）；轻残留：发布入库/标签/预览 tab 未移植。
- 「我」badge：role-prop 早退根因修+锁定，organizations 55/55。
- **models 服务商预填=误报**：live 探针证明 Vue select 本就预填「自定义 (OpenAI兼容接口)」（R489 采集缺陷：TDesign 选中值不进 innerText）——双端一致，未改。
- 三引擎按钮：footer=测试连接/取消→保存，15/15 锁定。
- 数字：chat **281/281**、agents 152/152、organizations 55/55、settings 321/321；typecheck 0（修自引入 2 处收窄）。

## 复验：五面 5/5 PASS（双端码位级比对）

模型配置 desc 逐字节一致+锁定提示同款；分块 5 组 desc+档位 `100 1000 2000 4000`/`0 250 500` 一致；存储单 select 绑定态+管理入口（option 文本=既有噪音）；标签面板/管理弹窗打开+取消（新建钮 icon vs 文字=已登记 minimal-form 噪音）；@ 弹层双端空态。

## 结论

**R489 的 5 项实质残留全部清除并活体验证；轻残留仅剩已登记噪音级（icon vs 文字钮、composer 文案约定）+N2 深层子功能（发布入库等）。前端侧逐页对齐完成。**后端依赖项（FAQ 500、三端点）已由并行 lane 开始落地（847dbb5f 运行时目录能力）。
