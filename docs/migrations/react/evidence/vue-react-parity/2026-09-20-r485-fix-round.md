# 2026-09-20 R485 修复轮：族 B 智能体编辑器 + 族 C 引擎结构化表单 + 抽屉形态批

- 主仓 main；三代理 H1-H3（首轮因 API 中断全灭，重启后全部交付）+ 协调者合并验证。
- 证据：`.omc/state/r485-main/report-{H1,H2,H3,verify}.md`；截图 `screenshots/r485-20260920/`。
- 收编：6 个 R483 孤儿测试文件（清扫漏提交，被测代码已在库）提交 5c920d40。

## H1 族 B：智能体编辑器（R482 D1/D2/D3/D10/D5/D6）

- **D1 三分区**：问题推荐（双 tab 开场/回答后全字段）、附件上传（图片/VLM/OCR/存储 6 引擎接 legacy.status 禁用态/语音/ASR/超时；AgentConfigForm +9 multimodal 字段）、MCP 服务（范围单选/清单禁用项「(已禁用)」/授权超时门控）；navGroups 对齐 Vue 顺序。
- **D2 类型预设体系**：静态移植 agent_type_presets.yaml（5 预设+4 系统提示词模板）；类型下拉默认 rag-qa；切换含名称/描述保护与 supported_file_types 同步。
- **D3+D10 预填**：新建即应用 rag-qa（名称「我的RAG 问答」/描述/You are WeKnora 提示词/4 RAG 工具/iterations 30）。
- **D5+D6**：新建预填对话+ReRank 模型；ReRank 必填按 KB 范围 ragEnabled 判定（红星/无清空项）。
- 数字：agents **74/74**（新增 20 红先）。
- 剩余（报告）：D4 提示词变量/恢复默认/模板、KBParserSettings 嵌入规则编辑器（现标题+规则数提示）、预设改运行时拉取、类型切换 KB 冲突 toast、D9 租户检索阈值默认。

## H2 族 C：三引擎结构化表单（B3-D1）

- 新 `resource-forms.ts` 纯函数域（类型元数据/payload 组装/测试 gate）；`ResourceSettingsPanel` 三弹窗重构：
  - 向量库：引擎类型+名称+按 connection_fields schema 动态字段（boolean→Switch+TLS 警告/sensitive→password/enum/number）+高级索引折叠（replica≤10/shard≤64）；编辑=immutable 提示+只 name 提交+测试禁用（对齐 Vue）。
  - 存储：类型/MinIO 部署模式 pill/Endpoint/Region/凭证/Bucket/路径前缀/provider switches/临时桶；编辑连接字段 disabled（后端 MergeSecrets 保 secret）。
  - 搜索：类型/名称空回退 display_name/备注/实例地址/API 密钥/引擎 ID/config_fields/HTTP 代理/设为默认（首 provider 自动）；api_key 仅 create 入 parameters，编辑新 key 走 PUT /credentials（Vue paramsOut 语义）。
  - 测试连接三引擎接真实 API（create 实时+edit 按各 Vue 分派）+loading+必填 gate。
- 数字：红 16+9 → 绿 16/16+11/11；settings 全目录 **306/306**；typecheck exit 0。
- 债（报告）：websearch 编辑凭证单 password 输入（未复刻 Vue CredentialResource 逐字段）；per-engine 品牌 logo 未移植（Sheet 无 headerIcon slot）。**测试基建发现**：react-dom/client 必须在 jsdom globals 后动态 import，否则 react-dom 静默禁用事件委托。

## H3 抽屉形态批 + pin（G1 剩余 4 项全 DONE）

- 向量存储只读绑定卡（VectorStoreBadge 移植：系统默认(postgres)/user/不可用三态）；存储引擎 instanceDesc+管理入口（/settings?section=storage）；共享管理新组件 KBShareSettingsSection（表格+行内权限 select+取消确认条+添加弹层）；高级设置 autoTag 三行（1-10 clamp）+表格元数据 textarea+ragEnabled gate。
- 活动面板 pin：next_cursor:0 → 「没有更早的记录了」出现（防回归锁）。
- 数字：红 12→绿 12；knowledge-settings **126/0**（基线 114+12）、knowledge-bases 122/0、graph 18/0、i18n 82/0。
- 死循环教训遵守：share 加载 effect 仅依赖 [client, knowledgeBaseId]。

## 合并态验证

- H2 报的 KnowledgeDocumentDetailPage 38 失败 = **v22 createPortal 伪红**（v26 复跑 44/44 绿，第三次确认该模式）。
- `pnpm gates`：test:shared/typecheck×2/test:web/build 全 PASS；check:integrity 我方 2 个被清扫新文件（agent-type-presets/resource-forms）重暂存后清零，剩余 1 P0=SP14 lane 在途 swagger.ts（非本车道职责）。
- **浏览器复验七项全 PASS**（60 截图，report-verify.md）：三分区结构/顺序一致、预填五要素逐项一致、类型 5 选项一致、三引擎结构化+测试连接（存储 COS 动态字段 10 项逐项一致）、抽屉四形态（绑定卡/管理入口/共享表格+权限下拉/autoTag 三行+placeholder 逐字）、活动 end hint 双端到底即现、防卡死（抽屉切 6 分区 0-15ms、编辑器切 5 分区无冻结）。
- 附加观察：①编辑器 nav 多「个性化」「子智能体」=React 后加豁免；②附件解析策略默认态 Vue 逐文件 12 行 vs React 单行摘要（=H1 嵌入规则编辑器债）；③引导规避需 8 个 localStorage key（contextual-guide-* 系列也会拦截点击）。

## R486 队列

H1 剩余批（D4 三件/嵌入规则编辑器/预设运行时/冲突 toast/D9）→ H2 债（websearch 凭证逐字段/logo）→ P3 两项形式裁决（Wiki 弹窗化/文件类型下拉化）→ org 编辑态分组取证 → fixture（FAQ KB/工具会话/org/system-admin）。
