# R169 Web regression after shared range

- Scope: full Web suite after adding the shared `Range` primitive and UI alias fixes.
- Validation: `pnpm test:web` passed 895/895; React warnings were limited to existing test `act(...)` notices.
- Boundary: browser visual comparison and protected backend acceptance remain open.
