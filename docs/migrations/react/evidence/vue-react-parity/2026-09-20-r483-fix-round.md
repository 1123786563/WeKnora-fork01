# 2026-09-20 R483 修复轮：R482 六批差异中的五批落地（D13/D16/D0/快修批/行菜单）

- 主仓 main；四修复代理并行（文件域互斥）+ 协调者补修行菜单下载门（复验 FAIL 的双根因）。
- 证据：`.omc/state/r483-main/report-{F1,F2,F3,F4,verify}.md`；截图 `screenshots/r483-20260920/`（40+ 张）。
- 外部清扫两次（并行 lane 重置暂存区，30 文件重新精确暂存，Go/migration 改动零卷入）。

## 修复明细（全部 TDD 红先行）

### F1 聊天域（D13/D16）
- **D13 模型芯片禁用=React 潜伏 bug 被并行数据触发**：`display_name ?? name`（9-15 引入）遇 display_name 空串不回退 → 模型列表被过滤空 → 芯片 disabled。修：`model-chip.ts` 新增 `resolveChatModelOptions()`（`display_name || name || id`，Vue 语义）。实测芯片恢复 enabled+下拉。
- **D16 头部菜单**：`packages/views/src/chat/page.tsx` 新增 `headerUtilityItems` prop；`header-menu-actions.ts` 移植 4 项（复制会话 ID/对话链接/复制为 Markdown/新窗口打开）+2 分隔线，Vue 顺序文案 byte-exact，剪贴板降级/Markdown 导出等价。实测 8 项+toast 链路通。

### F2 快修批（B3-D2/B1-1/B2-D18）
- i18n `{'@'}` 转义泄漏：4 文件 36 处→`@`（contextualGuide 块=正确转义保留+守卫测试）；删除 TenantMembersPanel 临时 replace 补丁。
- 筛选 manual 键：`DocumentsPageChrome.tsx` `upload.onlineEdit`→`knowledgeBase.typeManual`（5 locale 已有，零新增键）。
- 检索阈值回退：`ConfigSettingsPanel.tsx` vector/keyword 补 `||0.15/||0.3`（逐字段对齐 Vue RetrievalSettings.vue:148-155；抽屉同源双面生效）。红复现 `0.00/0.00`→绿 `0.15/0.30`。

### F3 集成域（D0/D8）
- **API Key 能力矩阵**：`apiKeys.ts` 移植 Vue 18 能力/4 分组/默认集/KB 范围集 + payload 构建；弹窗改 名称+访问类型 radio+矩阵（组内全选/清空+说明文字）+KB 范围多选。**揪出真缺陷**：`IntegrationsRoutePage` 此前硬编码 `full_access:true`（任何 key 全权限）→改透传 payload。i18n 零新增（18×2 键已在）。
- D8：chrome/claw api 步骤补「打开 API 信息」入口（切 API 集成分区）。

### F4 文档行菜单（B1-3）+ 协调者补修
- 菜单 4→7 项（下载/查看Trace/移动到.../trace 块）+api-client moveTargets/move/moveProgress 三方法；trace 块（总耗时+更新+提示）；新键 knowledgeBase.downloadDocument 五语言。
- 复验 FAIL（缺下载）→**双根因**（协调者 TDD 修复）：① React 下载权限链误读 Vue 不读的 `kb.permission` 字段——改按 Vue effectiveKBPermission 链（共享授权 `findSharedKBGrant`→`my_permission`→空）+调用点拉 `listShared`；② `hasContributorRole` 缺 `owner`（Vue hasRole 等级 viewer<contributor<admin<owner）——补 owner。终验 7/7 项。

## 验证链

- 各代理 scoped 全绿（chat 233/233+130/130、i18n、integrations 65+68、documents 97/97…）；F4 终验 apps/web 1941/1941+api-client 257/257+i18n 81/81、typecheck×2。
- 协调者补修后完整 `pnpm gates` **六门禁全绿**（首跑 upload-confirm mock 缺新方法 4 红→补 mock 复跑全绿）。
- 浏览器复验：七项 6 PASS + 行菜单 FAIL→双根因修复→最终迷你复验（见报告）。

## 剩余（R484 队列）

族 A（KB 设置抽屉 13-tab 系统性）、族 B（智能体编辑器 3 分区+预填体系）、族 C（三引擎结构化表单）、B2 其余（D2/4/5/6/7/8/9/11/12/14/15/17/18 部分未做）、B1-5 Graph 空态策略、B1-6 Wiki 新建弹窗字段、B3 低危 D3/4/5/6/7。
