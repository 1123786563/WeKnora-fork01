# R230 知识库窄屏布局边界（2026-09-15）

在 React `:5181` 的 390px 响应式检查中，平台根布局计算宽度为 600px，产生横向滚动；知识库卡片仍完整位于内容列内，没有独立卡片溢出。对照 Vue `frontend/src/views/platform/index.vue`，其 `.main` 同样声明 `min-width: 600px`，因此该行为是现有 Vue 基准的桌面最小宽度约束，不构成 React 偏差。

本轮不修改布局，避免破坏 Vue 的固定最小工作区；移动端专用体验仍由 Mobile 客户端验收。浏览器临时视口已恢复默认。
