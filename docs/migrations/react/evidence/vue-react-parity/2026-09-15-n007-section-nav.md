# N007 upload configuration section navigation

## Vue baseline

`UploadConfirmDialog.vue` renders a 216px settings sidebar. Its navigation is a vertical full-width menu with 38px rows, 9px 10px padding, 2px row spacing, 13px medium labels, 12px status text, borderless transparent rows, and a secondary-container active background. Issue labels use the error color and status text uses muted/warning/error tones.

## React change

`UploadSectionNav` now renders the same vertical row geometry and nested label/status stack. The outer sizing wrapper is non-semantic; the component's single `nav` remains the only navigation landmark. Active, issue, status, and truncation behavior remain wired to the existing section state.

## Verification

- Focused upload-confirm tests: 37/37 passed.
- `pnpm typecheck:web`: passed.
- `git diff --check`: passed.

## Evidence boundary

This slice is Vue source comparison plus React unit/type evidence. Authenticated post-change browser screenshots and computed-style capture remain open.
