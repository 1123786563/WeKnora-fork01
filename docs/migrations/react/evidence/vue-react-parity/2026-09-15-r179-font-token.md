# R179 shared font token

- Scope: `packages/ui/src/theme.css`, shared Tailwind/shadcn controls, and the platform agents page.
- Vue baseline: `frontend/src/assets/theme/theme.css` defines `--app-font-family`; the browser computed stack is the system/PingFang stack. `frontend/src/settings/GeneralPreferencesPanel.tsx` exposes the user-selected stack through `--wk-font-sans` in the React migration.
- Change: `--font-sans` now consumes `var(--wk-font-sans, <Vue system stack>)`, so shared controls and shell text follow the Vue default and runtime font preference.
- Browser evidence (Chrome, 1355x720, zh-CN, same seeded account/data): React agent title computed font changed from `Inter, ui-sans-serif, system-ui, sans-serif` to `-apple-system, "system-ui", "Segoe UI", Roboto, "Helvetica Neue", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif`, matching Vue.
- Validation: shared suite `469/469`, full Web suite `895/895`, Web typecheck, and `git diff --check` pass.
- Boundary: protected backend mutations, responsive multi-viewport comparison, desktop-native rendering, and mobile-native rendering remain separate acceptance gates.
