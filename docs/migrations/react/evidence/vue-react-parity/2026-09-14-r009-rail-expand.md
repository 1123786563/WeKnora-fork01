# R009 遗留交付:kb-list 图标轨拖拽展开切片(collapsed-strip ↔ expanded-panel)

- 日期:2026-09-14
- 分支:`codex/react-multiclient`(worktree `.worktrees/react-multiclient`,未 commit,待协调者复核集成)
- Vue authority:`frontend/src/components/ListSpaceSidebar.vue`(由 `frontend/src/views/knowledge/KnowledgeBaseList.vue:3-5` 以默认 `collapsedKey='sidebar-collapsed-list'` 挂载)
- React 落点:`apps/web/src/App.tsx`(KnowledgeBasesPage 的 rail 部分)、`apps/web/src/knowledge-list.css`、`apps/web/src/knowledge-bases/kb-list-anatomy.test.tsx`

## 1. Vue 交互语义精读(行号)

| 语义点 | Vue 行号 | 结论 |
| --- | --- | --- |
| 双态 DOM | L2-3, L5, L77 | `v-if` 折叠 `div.icon-strip` / `v-else` 展开 `nav.expanded-panel`,互斥渲染 |
| 触发方式 | L146-149, L207-216 | 右缘 `.resize-handle` 绝对定位(right:-6px,宽 12px)`@mousedown.prevent` 启动拖拽;document 级 mousemove/mouseup;无点击切换、无键盘切换 |
| 拖拽中宽度 | L3(`:style` 仅 dragging 时), L218-222 | inline width 实时跟随指针,clamp `[56, 208+20]`;`.dragging` 类禁用过渡(L311-313) |
| 吸附阈值 | L162, L230 | `SNAP_THRESHOLD=120`:释放时 width ≥ 120 → 展开(208px),否则收起(56px) |
| 宽度常量 | L160-161, L297, L306-308 | 折叠 56px / 展开 208px;非拖拽态宽度由 CSS 类承载(`.expanded`) |
| 过渡 | L304 | `width .25s cubic-bezier(.4,0,.2,1)` |
| body 状态 | L214-215, L227-228 | 拖拽中 `cursor: col-resize` + `user-select: none`,结束恢复 |
| 持久化 | L198-200, L232 | `localStorage[collapsedKey + '-expanded'] = 'true'/'false'`;初始读取,默认折叠;KB 列表用默认 key → `sidebar-collapsed-list-expanded` |
| 展开面板 DOM | L77-144 | `.sidebar-item` = `.item-left`(图标+全量文本 label)+ `.item-count` 计数徽标;L103 收藏/最近与 workspace 之间 `.sidebar-divider` |
| 计数可见性 | L83, L93, L101, L109 | all/mine:count 定义即显示(含 0);favorites/recents:仅 >0 显示 |
| 分组标题 | L113-125 | `.sidebar-section .section-title`(「共享给我」)+ per-org 行,**仅当 countByOrg 有条目**;kb-list 场景 React rail 无 org 条目 → 不渲染(见遗留) |
| 折叠态 tooltip | L7-34, L237-239 | `tooltipText(label, count)` → `"label (count)"`,placement right |
| 选中同步 | L249-258, L276-278 | `v-model` 双向;选中项在两种形态下都高亮 |

## 2. React 实现(App.tsx)

- 模块级:`railCountVisible`(App.tsx:104-107)— 展开/收起计数徽标可见性,映射 Vue L83/93/101/109。
- 组件内(App.tsx:271-330):常量 `KB_RAIL_COLLAPSED_WIDTH=56 / KB_RAIL_EXPANDED_WIDTH=208 / KB_RAIL_SNAP_THRESHOLD=120 / MAX_DRAG=228`;`railExpanded`(localStorage `sidebar-collapsed-list-expanded` 初始化,与 Vue 同键同值域)、`railDragging`、`railDragWidth`;`onRailDragStart`(App.tsx:287)与 document 级 move/up effect(App.tsx:297-330,含 body cursor/userSelect 恢复与卸载清理)。
- JSX(App.tsx:589-650):`aside.kb-list-rail` 按 `railExpanded/railDragging` 组合类名;拖拽中 inline `style.width`(与 Vue L3 一致,非拖拽态宽度交给 CSS);折叠渲染 `div.kb-list-rail-strip`(原有 `.kb-list-rail-item` + `title="label (count)"` tooltip **不变**),展开渲染 `nav.kb-list-rail-panel`(`.kb-list-rail-panel-item` = left 图标+全量 label + `.kb-list-rail-panel-count`);index 3 前插 `.kb-list-rail-divider`;右缘 `.kb-list-rail-handle`(role=separator)onMouseDown 启动拖拽。
- 语义保持:`?scope` 深链走原 `setSpace`(写 URL + aria-pressed),折叠条 tooltip 语义不变;展开面板点击同样复用 `setSpace`。

## 3. 交互语义对照表

| 维度 | Vue | React | 一致 |
| --- | --- | --- | --- |
| 形态切换 | icon-strip ↔ expanded-panel(v-if/v-else) | kb-list-rail-strip ↔ kb-list-rail-panel(条件渲染) | ✅ |
| 触发 | 右缘 handle mousedown 拖拽 | 同(mousedown → document move/up) | ✅ |
| 宽度 clamp | [56, 228] | 同 | ✅ |
| 吸附 | ≥120 展开,否则收起 | 同(120 = SNAP_THRESHOLD) | ✅ |
| 展开宽度 | 208px(CSS 类) | 208px(`.kb-list-rail-expanded`) | ✅ |
| 过渡 | .25s cubic-bezier(.4,0,.2,1),拖拽禁用 | 同 | ✅ |
| 持久化 | `sidebar-collapsed-list-expanded` | 同键同值('true'/'false') | ✅(跨框架共享同一存储槽) |
| 展开面板内容 | 全量 label + 计数徽标 + divider(+org 分组) | 全量 label + 计数徽标 + divider(org 分组见遗留) | ✅(范围内) |
| 折叠 tooltip | label (count) | 不变(title 属性) | ✅ |
| scope 深链 | v-model | setSpace → ?scope 不变 | ✅ |

## 4. TDD 记录

- 红(`apps/web && npx tsx --test src/knowledge-bases/kb-list-anatomy.test.tsx`):基线 7 通过,新增 3 用例全失败(`.kb-list-rail-handle` 不存在、无展开态、无持久化)。
- 绿:同一命令 **10 通过 / 0 失败**(7 基线 + 3 新增):
  1. 拖拽 +90 → 146px ≥ 120 吸附展开;面板全量 label(全部/收藏/最近/本空间)、计数徽标 ['3','3'](收藏/最近为 0 隐藏)、divider ×1、active=本空间;拖回 -110 → 98 < 120 收起;两个方向均写 localStorage;拖拽中 dragging 类 + inline width(56→146→98)+ body cursor col-resize → ''。
  2. localStorage 预置 'true' → 直接以展开态挂载(v-if/v-else 互斥)。
  3. 展开面板点击收藏 → URL 变为 `?scope=favorites` + aria-pressed 高亮(深链语义不回归)。
- 全量:`pnpm run test:web` → **681 通过 / 0 失败**(基线 670+,含本切片 +3;其余新增来自并行 agent 的其他切片)。
- 类型:`pnpm run typecheck:web` → 0 错误。

## 5. 修改文件(本 agent 独占范围)

| 文件 | 变更 |
| --- | --- |
| `apps/web/src/App.tsx` | 4 个 hunk,全部在 kb-list rail:react import(+Fragment/+MouseEvent type)、`railCountVisible`(:104)、rail 拖拽状态/常量/handler(:271-330)、双态 rail JSX(:589-650) |
| `apps/web/src/knowledge-list.css` | rail 容器改为定位+宽度+过渡容器;新增 `.kb-list-rail-strip/.kb-list-rail-expanded/.kb-list-rail-dragging/.kb-list-rail-handle(-line)/.kb-list-rail-panel(-item/-left/-label/-count)/.kb-list-rail-divider`,色值沿用文件既有字面量(#07c05f 品牌绿、#eef4ef/#f0f3f8 面、#e3e7ee 描边) |
| `apps/web/src/knowledge-bases/kb-list-anatomy.test.tsx` | 追加 3 用例 + 注释块(未动既有 7 用例) |

未触碰:apps/web/src/settings/**、styles.css、docs/migrations/react/vue-react-parity-matrix.md 及其他 agent 的在途修改(FAQ/share-dialog 等)。

## 6. 遗留(移交协调者)

1. **org/共享分组缺失**:Vue 展开面板在 `countByOrg` 有条目时渲染「共享给我」分组标题 + per-org 行(L113-125);React kb-list rail 模型是 4 个固定 entry,页面无 organizations 数据源。若要补齐需给 railItems 注入 org entries(identity.organizations + share 计数),超出本切片授权,建议单开切片。
2. **无键盘切换**:Vue 的 resize handle 为 mousedown-only div(无键盘路径);React 保持一致并加了 role=separator。若 parity matrix 后续要求键盘可达,需要新需求(如 handle 上 ←/→ 步进)。
3. **矩阵行更新**:`vue-react-parity-matrix.md` 归协调者所有,请据本文档将 rail 行标记为已对齐(含上述两条遗留)。
