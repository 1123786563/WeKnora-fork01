# 2026-09-20 R484 修复轮：族 A 抽屉 + Wiki/Graph + 聊天/设置低危批（四代理并行）

- 主仓 main；四修复代理（G1-G4，文件域互斥）+ 协调者合并验证。证据：`.omc/state/r484-main/report-{G1,G2,G3,G4}.md`（gitignore 内仅存盘）。
- 并行 lane 干扰应对：工作树遗留 `internal/application/repository/session.go` UU 未合并态（对方 stash 冲突遗留）→ 本轮提交采用**显式 pathspec**（只提交我方文件清单，绕开未合并路径）；外部暂存区清扫延续（各代理 add 后核对 porcelain）。

## G1 族 A：KB 设置抽屉（R482 B1 差异4 主体）

- 底部按钮：「取消」+「保存并关闭」替换「保存配置」；取消丢弃全部草稿并经 onClose 关闭宿主。
- 14 分区（13 tab+faq）标题/描述全部重指向 Vue i18n 键；移除 React 自创 eyebrow 行。
- 解析引擎：`parserSettings.tsx` 完整移植 Vue KBParserSettings——逐文件类型清单（12 已知家族+动态行+扩展名标签+每行引擎 select+「(默认)」后缀+anydoc 默认+xlsx 首行列标题复选框+无引擎「前往配置→」+buildCompleteRules 全量落盘）。
- 数据源 tab：embedded 隐藏独立页头，抽屉内呈 Vue 空态虚线「添加数据源」。
- 活动记录：「没有更早的记录了」+ 刷新钮图标化；顺带修 loadMoreShort 错键。
- 豁免执行：R455/R456 概览卡（Vue 无）从抽屉移除。
- 数字：6 红→6 绿；knowledge-settings 114/114、data-sources+knowledge-bases 180、documents 263、i18n 81；仅新增 graphSettings.title/description 两键（5 locale）。
- 剩余（展示形态批，记录在报告）：向量存储只读绑定卡、存储引擎 instanceDesc+管理入口、共享管理 KBShareSettings 形态、高级设置自动关联标签/表格元数据两组行。

## G2：Graph 空态 + Wiki 新建弹窗（B1 差异5/6）

- Graph：`graphReady`（success 且节点>0，对齐 Vue renderGraph 空节点提前 return）门控工具栏+图例；新增 unsupportedFileTypes + ParserHint 警告 banner（复用既有组件与键，零新增 i18n），「前往配置」打开就地设置 parser 分区（对齐 uiStore.openKBSettings）。
- Wiki 新建弹窗：字段集=标题+slug（自动同步+帮助文案）+页面类型四选项+正文可选；「一句话摘要」移至仅编辑模式；按钮「取消/确认」；payload 改 {slug,title,page_type,content}。
- 数字：B1-5 3红+1防回归→4/4；B1-6 6红→6/6；scoped 69/69。

## G3：聊天域低危六项（B2 D7/8/11/14/15/17）

- D7 温度文案：复核已对齐（R482 快照后被其他轮修复），加五语言锁定测试防回退。
- D8 支持的文件类型：7 选项+五语言 fallback+知识库分区 chip 行（hasKnowledgeBase 门控，空选「全部类型」），写回 config.supported_file_types。
- D11 管理沙箱链接：技能分区沙箱下拉后加链接 → settings?section=sandbox（Vue 同行「管理技能」伴生链接未在任务范围，记录）。
- D15 网页搜索按钮：**完整链路**——web-search.ts（Vue agentWebSearch 逐行移植）+composer 地球按钮（SVG 复刻+active/未配置态+五语言）+providers 加载/toggle/toast/payload 注入 web_search_enabled（双 mode）。两处浅化记录：未配置提示纯 toast 无跳转、开关 localStorage 持久化非租户 KV。
- D17 ⌘K：补「新手引导」命令（openNewUserGuide 事件）；快捷键按 Vue 复位（豁免项「专家模板」移 ⌘7 末尾）。
- D14 就绪判定：**用 R482 现场截图推翻报告结论**——双端均有警示，真实差异=图标形状；⚠ 字符改 error-circle SVG；readiness 补 isSharedAgent 分支（Vue 判定源完整移植）。
- 数字：六项先红后绿（D7 锁定即绿）；chat 241、agents+platform 334、views+i18n 212。

## G4：设置/组织域低危五项（B3 D3/4/5/6/7）

- D3：EnvVarSettingsPanel 自渲染 Vue 分区头（h2+help-circle 弹层+四段文案逐字）；壳层排除 envvars 防双标题。
- D4：models 弹窗——触发器只显 label（描述留下拉内）、thinking 说明恒显全文、按钮序 测试连接→取消→保存。
- D5：三重证据（源码+双端活体 DOM+分词分析）判**误报**，加锁定断言防回归。
- D6：取证更正弹窗为 OrganizationSettingsModal 且 Vue **有**「基础」分组——补齐基础组标题+提交钮「创建」（common.create 五语言移植）+描述 0/500 计数器。
- D7：role-denied 壳层不渲染分区标题（对齐 Vue Settings.vue L85-94）。
- 数字：scoped settings×4+orgs 97/97、i18n keys 10/10、邻接回归 47/47；残留备注：org 编辑态「成员与协作/共享资源」分组与 React 多出的「邀请链接」项待后续轮取证。

## 合并态验证

- typecheck:web 0 错误（各在途 TS 声明全消解）。
- 全量 test:web：pnpm 默认 node v22 下 39 失败=createPortal 工具链伪红（KnowledgeDocumentDetailPage 套件整簇）；v26 复跑该套件 **44/44 绿**证实非真失败。
- **真实产品缺陷（合并态发现并修复）**：graph 页 ⚙ 打开 KB 设置抽屉死循环（Maximum update depth exceeded）——KnowledgeSettingsPage L831 effect 依赖 `[currentKnowledgeBase]`，而 KB 未加载完成时 `fallbackKnowledgeBase` 每渲染新对象 → setParserEngineRules 每轮重跑。慢网络下真实浏览器同样会卡死。修复：effect 只对已加载的稳定 `knowledgeBase` 生效（graph 测试原第 9 用例挂起 38 分钟即此因，复现脚本+CPU profiler 定位）。graph 测试拆出 r484 文件（23+5），settings 套件无回归：**142/142 绿**。
- `pnpm gates` 修复后**六门禁全绿**（test:web 含拆分后全部套件）。

## 并行 lane 事故记录（第 12 次）

G1-G4 全部暂存内容被 SP13 lane 的三个提交（e22b1a65/42aab87d/e4e270f9）卷走入库；抽查核实内容完整（parserSettings.tsx/saveAndClose/EnvVar 弹层/web-search.ts 均在 HEAD）——归属记录，剩余未卷走的死循环修复+测试拆分+台账由本轮单独提交。本轮外部暂存区清扫累计 4 次，均以 porcelain 计数核对+重暂存化解；对方遗留的 session.go UU 冲突已由其自行解决。

## 浏览器复验（独立代理，58 截图）

九项：7 PASS + 2 子项 DIFF + 1 阻断（当场修复）。报告 `.omc/state/r484-main/report-verify.md`。
- PASS：KB 设置抽屉（双钮/13 分区逐字/解析引擎逐文件清单 26 引擎+15 默认标记+7 个「前往配置→」/数据源空态）、Graph 空态（无工具栏+警告 banner 逐字）、Wiki 新建（字段/取消确认/Slug 自动同步双端一致）、⌘K（含新手引导，Vue 6=React 6+1 豁免）、envvars ⓘ 弹层四段逐字、组织创建弹窗（基础组/创建钮/0-500）、智能体编辑器（文件类型 7 选项+管理沙箱链接）、**防卡死回归通过**（切分区往返 0ms）。
- **P0（已修）**：React /platform/creatChat 整页崩溃——vite alias 表缺 `@weknora/views/chat/web-search` 一行（G3 新导入）。补 alias 后 vite 自动重启，页面 200。
- **P2（已修）**：活动记录「加载更多」永不消失——`nextCursor !== undefined` 把 0 当有效游标；改 `result.next_cursor || undefined` 对齐 Vue `!!next_cursor`，「没有更早的记录了」可出现。回归 12/12 绿（补 pin 测试记为小额债）。
- P3 备查（功能等价待产品裁决）：React Wiki 新建=内联表单（Vue 弹窗）；文件类型=chip 行（Vue t-select 下拉）。

## R485 队列

族 B 智能体编辑器（3 分区+预填体系+D2/4/5/6 残项）→ 族 C 引擎结构化表单 → G1 剩余展示形态批（向量存储卡/存储引擎/共享管理/高级设置两组行）→ P3 两项裁决 → org 编辑态分组取证 → fixture（FAQ KB/工具会话/组织/system-admin）。
