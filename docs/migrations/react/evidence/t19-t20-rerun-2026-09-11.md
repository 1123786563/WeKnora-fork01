# T19/T20 发布链路复跑证据（2026-09-11）

## T19 Desktop renderer

- `pnpm typecheck:desktop`：退出码 0。
- `pnpm test:desktop`：2/2，通过。
- 首次 `pnpm build:desktop-renderer` 暴露 Vite alias 缺口：桌面配置只映射了 domain 根模块，`@weknora/domain/wiki/diff` 被拼接为 `query-key.ts/wiki/diff`。
- 修复 `apps/desktop/vite.config.ts`，补齐 Web renderer 使用的 knowledge/wiki/chat/sandbox 子路径 alias。
- 修复后 `pnpm build:desktop-renderer`：121 modules transformed，退出码 0；产物为 React Web renderer bundle。

这只证明桌面 renderer 的源代码构建，不证明 Windows/Linux Wails 原生包、安装升级或真实 Lite GUI 生命周期。

## T20 Mobile export

- `pnpm typecheck:mobile`：退出码 0。
- `pnpm test:mobile`：6/6，通过。
- `pnpm --filter @weknora/mobile exec expo export --platform ios`：退出码 0，生成 iOS Hermes bundle。
- `pnpm --filter @weknora/mobile exec expo export --platform android`：退出码 0，生成 Android Hermes bundle。
- `pnpm --filter @weknora/mobile exec expo config --type public --json`：退出码 0。

导出不是原生安装/真机验收；设备登录、SecureStore 刷新/退出、AppState 网络恢复、文件选择器与真实后端链路仍未接受。
