# WeKnora 领域词汇

该文件主要通过 Matt 的 `domain-modeling` / `grill-with-docs` 持续维护。
它是本仓库中 Agent 使用的**统一领域术语事实源**。

## 参考资料

- 已批准 Spec：`docs/specs/`
- 架构决策：`docs/adr/`

本词汇表记录当前已确认的产品概念。商业能力的设计决策见 [Lago 计费平台迁移设计](docs/specs/2026-09-20-lago-billing-migration-design.md)。

open-connector 部署与租户隔离决策见 [ADR-0001](docs/adr/0001-open-connector-shared-runtime.md)，技术建议见 [集成设计稿](docs/superpowers/specs/2026-09-12-open-connector-integration-design.md)。

## 语义知识

**语义知识服务**：负责从知识资源构建语义图谱，并提供 GraphRAG 检索和基于证据的推理能力的服务。
_避免_：将其等同于文件解析器或主 Agent 执行引擎。

**GraphRAG 检索**：结合知识图谱中的实体关系与来源文本，为问题寻找相关证据的检索能力。
_避免_：把多跳检索本身称为已经证明的推理结论。

**知识推理**：基于获准使用的知识证据，通过明确规则或模型推断得到结论的能力；结论需区分规则推导与模型推断。
_避免_：把模型生成的解释等同于规则证明或原文事实。

**证据引用（Evidence Citation）**：把 Agent 结论追溯到知识版本、外部资源版本或操作回执的可打开记录，包含来源和获取时间，并标明结论属于原文事实、规则推导还是模型推断。
_避免_：仅显示资源名称、在用户请求后临时生成的出处、把模型推断标成来源事实。

## 空间与商业归属

**空间（Tenant）**：成员协作、知识与应用资源隔离的产品边界，也是独立购买套餐、独立付款和承担费用的单位。
_避免_：将空间等同于跨空间共享组织或 Agent 的执行文件工作区。

**个人求职空间**：专供一位求职者管理求职资料与申请记录的单成员空间，沿用 WeKnora 的空间隔离与治理语义。
_避免_：学校或企业的多人空间、独立于 WeKnora 空间模型的第二套个人账号容器。

**活动空间（Active Tenant）**：移动端在当前活动部署实例中唯一生效的空间作用域，决定首页、任务、搜索、请求、订阅和缓存可访问的内容；其他空间只能在切换器中暴露不含任务内容的待处理计数。
_避免_：跨空间混合任务列表、跨空间搜索、仅靠客户端筛选实现隔离。

**空间计费账户（Billing Account）**：一个空间独有的商业身份，承载该空间的购买和付款归属。其他空间不共享该账户的套餐、余额或账单。
_避免_：个人用户钱包、企业跨空间共享钱包。

**客户账单（Billing Invoice）**：向空间列明计费周期、用量、Credits 抵扣、应付金额和付款状态的商业记录；它不是中国法定税务发票。
_避免_：税务发票、支付成功凭证、商业订单。

**税务发票（Tax Invoice）**：由合规开票主体和系统依据已确认交易开具的中国法定税务凭证；其开具、红冲和交付独立于客户账单。
_避免_：客户账单、支付回执、平台自动生成的用量明细。

**收费主体（Billing Entity）**：向空间销售套餐或 Credits、出具客户账单并承担收款责任的法定主体。
_避免_：空间、空间计费账户、支付渠道、账单管理员。

**账单管理员**：由空间 Owner 显式授权、负责本空间购买、续费、充值、套餐变更、账单查看和退款申请的成员。
_避免_：自动具有商业权限的空间 Admin、平台退款审批人。

**渠道支付单（Channel Payment Order）**：为一张确定的客户账单发起外部支付所需的协议记录，承载渠道、商户订单号、支付尝试和外部交易状态；它不决定商品、价格或应交付权益。
_避免_：商业订单、客户账单、已经生效的套餐权益。

**报价（Quote）**：付款前向空间展示的确定商品版本、价格、币种、权益和有效期；创建渠道支付单前，它必须与实际客户账单逐项一致，过期或不一致时必须重新生成。
_避免_：可在付款后调整的估价、客户端自行计算的金额、客户账单本身。

**付款事实（Payment Fact）**：经过渠道验签与商户、账单、金额和币种核对后确认的不可变收款记录；重复、部分、多收或异常付款仍须保存，但不会自动扩大应交付权益。
_避免_：浏览器返回页、支付二维码创建成功、未经核对的回调通知。

**履约（Fulfillment）**：把已确认购买对应的订阅、功能权益或 Credits 交付给购买空间的过程。
_避免_：将付款成功直接等同于权益已经到账。

**共享组织（Shared Organization）**：多个空间参与知识共享的协作关系。加入共享组织不改变各空间的独立付费归属。
_避免_：Organization、Lago Organization、统一付费企业。

## Agent 与移动使用

**部署实例（Deployment）**：一个独立运行并持有自身身份、空间、策略与数据的 WeKnora 后端实例，可以是官方云或企业自托管。移动端可登记多个实例，但同一时刻只有一个活动实例。
_避免_：空间（Tenant）、跨实例统一账号、共享 Token 或共享本地缓存作用域。

**移动 AI Office**：以当前 WeKnora 后端为业务权威、参考 Paseo 与 Happy 客户端经验，面向团队与企业成员统一承载知识工作、通用 Agent、专业 Agent 与开发工作的移动办公产品；首版成员工作入口是 iOS 与 Android 原生应用，治理和管理继续由现有 Web 承担。首版即覆盖这些一级工作类型，而不是仅交付 Developer 单一切片。个人使用可由单成员空间承载，但不是首版独立优化的产品模型。
_避免_：统一 Agent 移动客户端、个人 Agent 遥控器、仅面向知识库或编码 Agent 的移动附属页面。

**云端任务执行**：移动 AI Office 首版的所有 Agent 和工具任务均由 WeKnora 后端在平台管理的隔离云环境中执行；文件、Git、终端及外部操作均属于该任务执行，并按空间（Tenant）归属、授权与隔离。
_避免_：企业私有执行节点、用户设备执行、由移动端直接中继执行。

**任务（Task）**：移动 AI Office 中组织工作的首要用户工作单元；每个从新建入口提交的初始目标都创建一个任务，承载过程、同一目标下的追问、需要成员处理的事项与结果。知识、Agent、应用连接和开发环境都是任务可以使用的资源；快速问答也是可以立即完成并自动收纳的任务。
_避免_：功能入口、Agent 会话列表、仅指异步执行、每条消息、每次运行或重试。

**任务生命周期（Task Lifecycle）**：任务相对于用户目标的长期状态，分为进行中、已完成、已取消和已归档；已完成任务收到同目标追问后可重新进入进行中。
_避免_：最新一次运行的技术状态、Agent 自定义进度阶段。

**关注状态（Attention State）**：表示任务当前是否需要成员介入，以及需要创建者还是其他成员处理；它与任务生命周期和运行状态分别存在。
_避免_：把等待审批隐藏在“运行中”、根据最后一条消息猜测是否需要处理。

**行动通知（Actionable Notification）**：在任务需要成员处理、运行失败或结果未知、任务完成，或发生重要预算事件时发送的系统提示；通知只提示客户端重新同步，不承载权威任务状态。
_避免_：每次进度变化都推送、依赖推送内容恢复任务状态、由 Agent 任意决定通知频率。

**任务时间线（Task Timeline）**：按权威顺序记录任务目标、成员输入、Agent 结论、工具活动、审批、运行状态、产物和外部操作结果的可恢复事实流；移动端默认展示摘要，允许按需展开原始证据和日志。
_避免_：纯聊天消息列表、未经持久化的实时流、默认铺满页面的原始终端输出。

**离线草稿（Offline Draft）**：移动端断网时在当前用户与空间作用域内保存、等待用户联网后确认提交的输入或批注；离线草稿不会创建运行、完成审批或自动重放外部操作。
_避免_：离线执行队列、联网后静默提交、跨用户或跨空间复用的缓存内容。

**注册设备（Registered Device）**：在一个部署实例中与用户账号关联、具有独立设备身份和撤销状态的移动设备，用于推送寻址、本地密钥封装和企业设备策略；同一物理设备在不同部署实例中具有彼此隔离的注册，设备身份不取代用户身份，也不自动获得空间权限。
_避免_：用户账号、Task 执行节点、Happy 式 Machine。

**受控离线缓存（Governed Offline Cache）**：按用户与空间隔离并加密保存的获准移动内容；空间策略可以限制内容类型和保留时间，登出、空间切换、设备撤销或权限失效会删除缓存或使其密钥不可用。
_避免_：仅依赖 App Sandbox 的明文缓存、用户可绕过空间策略的完整本地副本。

**主理 Agent（Lead Agent）**：对一个任务的过程与结果承担统一责任的 Agent；系统可以推荐，用户也可以显式选择。主理 Agent 可在任务授权范围内使用知识和工具，或把工作委派给专业 Agent，但任务对用户仍保持一个责任主体。
_避免_：要求用户手工编排的多 Agent 房间、完全不可解释的自动路由器。

**Agent 版本（Agent Version）**：由 Web 管理端测试并发布的不可变 Agent 配置版本，包含其行为、能力声明和默认策略；任务固定创建时选择的版本，显式迁移后才使用新版本并重新检查任务授权。
_避免_：运行时自动漂移到最新配置、成员在移动任务中直接修改的 Agent 定义。

## Agent Marketplace

**Agent 定义（Agent Definition）**：在一个空间内由获授权成员维护的可编辑 Agent 设计，描述其身份、行为与能力需求；它本身不是 Task 固定使用的不可变版本，也不是 Marketplace 的分发单位。
_避免_：Agent 版本、Agent Release、运行中的 Agent。

**Agent Release**：从确定的 Agent 版本生成、经过脱敏并具有独立版本与内容摘要的不可变分发包；它可以包含可移植的行为与能力声明，但不携带知识库绑定、模型或连接凭据、Sandbox、Memory、预算及其他空间本地绑定。
_避免_：共享运行中的 Agent、会随发布者编辑自动变化的模板、完整导出源空间配置。

**Marketplace Listing**：让成员发现一个确定 Agent Release 的目录条目，记录展示信息、来源、发布者、可见范围和治理状态；Listing 可以改指向更新的 Release，但已安装或已被 Task 使用的版本不会随之变化。
_避免_：Agent Release 本身、Installed Agent、运行时能力授权。

**Agent Catalog**：向当前成员呈现可发现 Agent Listing 的统一目录，聚合 WeKnora 维护的 Built-in、经平台治理的 Public Marketplace 与仅当前空间可见的 Tenant Catalog，并明确标示每项来源。
_避免_：只指公共商店、把三个来源实现成互不兼容的 Agent 模型、任意链接分享。

**Agent 引入（Agent Adoption）**：一个空间接受某个 Marketplace Listing 并管理其来源与升级关系的记录，保存空间已接受的 Release，并可派生多个固定到具体 Release 的本地 Agent 变体；它属于空间而不是执行操作的成员，也不等同于应用安装。
_避免_：成员个人 Agent 副本、直接运行 Marketplace Release、每个 Release 建立互不关联的安装、应用安装（Installation）。

**本地能力映射（Local Capability Mapping）**：空间为已引入 Agent 的可移植能力需求选择本地模型、知识、Skill、连接、Sandbox 与审批策略的治理配置；映射不会回写或改变来源 Release。
_避免_：Release 自带凭据、自动选择空间内任意资源、Task Grant。

**Agent 变体（Agent Variant）**：从一个 Agent Adoption 派生、固定到具体 Agent Release 的本地 Agent Definition，拥有独立的能力映射、成员可见性、测试和 Agent Version 发布节奏；同一 Adoption 可以有多个变体。
_避免_：成员个人副本、Agent Fork、共享同一运行权限的部门标签。

**可用 Agent（Available Agent）**：已在当前空间完成本地能力映射、兼容性检查和测试，并发布了本地 Agent Version，因而可以被获授权成员用于创建 Task 的 Agent。
_避免_：刚完成 Agent Adoption 的草稿、仅在 Agent Catalog 中可见的 Listing、未经空间批准的公共 Agent。

**Agent Fork**：从一个 Agent Release 派生、保留完整来源与许可关系，但已修改其可移植核心并作为独立 Agent Definition 维护的 Agent；Fork 不再自动跟随来源 Listing 的升级建议，只有来源许可允许时才能作为新的 Release 重新提交审核。
_避免_：只修改本地能力映射、对来源 Release 的原地编辑、隐藏来源或许可的复制品。

**Agent 升级建议（Agent Upgrade Proposal）**：当 Listing 提供新 Release 时，为现有 Agent Adoption 展示行为、能力需求、安全与许可差异的候选升级；接受建议只创建新的本地草稿，完成重新映射、测试和发布前不影响当前可用版本或既有 Task。
_避免_：自动升级、覆盖当前 Agent Version、要求删除旧 Adoption 后重装。

**Agent Manifest**：随 Agent Release 固定的不可变声明，描述其身份与摘要、发布者与来源、许可、适用范围、能力需求、数据类别、潜在外部副作用、兼容性、变更、弃用与安全状态；它为审核和本地映射提供事实，但不授予运行权限。
_避免_：仅有名称和简介的商品卡、由系统从提示词猜出的完整声明、Task Grant。

**Release Submission**：Agent Author 把确定 Agent Version 及其 Manifest 提交给某个目录治理范围审核的候选发布；Submission 在审核通过前不是 Release，也不会进入成员可发现的 Listing。
_避免_：作者直接公开、Agent Definition 草稿、已经批准的 Agent Release。

**Agent Author**：维护 Agent Definition 并对 Release Submission 内容负责的成员或发布团队；作者身份不自动包含目录审核、公共发布或空间引入权限。
_避免_：Marketplace Reviewer、Tenant Admin、Task Owner。

**Marketplace Reviewer**：在指定治理范围内审核 Release Submission、发布或下架 Listing 的责任主体；Tenant Catalog 由空间授权的治理者负责，Public Marketplace 由平台治理者负责。
_避免_：Agent Author、仅能使用 Agent 的成员、模型自动审核结果。

**Verified Publisher**：通过平台身份与责任验证、可以向 Public Marketplace 提交 Release 的发布主体；验证表示发布者身份可信，不表示其每个 Agent 已获批准或没有风险。
_避免_：普通空间成员、Marketplace Reviewer、Agent 安全认证。

**Listing 下架（Unlisted）**：Marketplace Listing 不再参与搜索或新的 Agent Adoption 的目录状态；它不撤销对应 Release，也不影响已经完成的 Adoption 和本地 Agent Version。
_避免_：Agent Release 弃用、安全撤回、删除已引入 Agent。

**Release 弃用（Deprecated）**：Agent Release 仍可运行但不再推荐用于新引入，并指向替代 Release 或升级建议的生命周期状态。
_避免_：Listing 下架、安全撤回、自动升级。

**安全撤回（Security Revocation）**：治理主体因已知安全风险禁止一个 Agent Release 被新引入或启动新 Task/Run 的强制状态；处置保留已有 Task、产物、审计与来源记录，并明确原因、影响范围和替代版本。
_避免_：普通下架、静默删除历史、把受影响 Task 自动切换到新版本。

**Agent 许可（Agent License）**：随 Agent Release 声明其采用、使用、Fork 和再分发条件的许可关系；首版免费分发不等于没有许可，也不等于运行模型、Sandbox 或 Connector 免费。
_避免_：Task Budget、Credits、Manifest 中随意填写的 Agent 售价。

**Agent 变体退役（Agent Variant Retirement）**：空间停止用一个 Agent 变体创建新 Task 的生命周期状态，同时保留其 Agent Version、既有 Task、产物、审计和来源关系。
_避免_：删除 Agent、删除既有 Task、安全撤回。

**Agent 引入终止（Agent Adoption End）**：空间停止从一个 Listing 创建新变体或接收升级建议，并归档该 Adoption 的状态；已有变体须先退役，历史来源与许可关系继续保留。
_避免_：删除 Marketplace Listing、把本地 Agent 变成无来源副本、删除历史。

**Agent Evaluation**：对确定 Agent Release 在确定测试集、环境类别和时间下产生的结构化质量或安全证据；Evaluation 可以帮助审核和选择，但不授予权限，也不保证本地能力映射后的实际效果。
_避免_：消费者星级、营销推荐、对所有空间效果的保证。

**Marketplace Metrics**：平台从多个采用方聚合并去标识化的 Agent Adoption、升级和错误类别指标，用于目录治理与发布者反馈；它不包含 Tenant 或成员身份、Task 内容、本地映射、知识、连接或工具参数。
_避免_：Release 自带遥测、Publisher 查看采用方内容、未经采用方确认的诊断数据。

**Dependency Lock**：随 Agent Release 固定的不可变依赖解析结果，记录可再分发 Skill 与子 Agent 等依赖的类型、身份、兼容约束、实际版本和内容摘要；本地模型、知识、连接、Sandbox 与凭据不属于该依赖图。
_避免_：仅记录可漂移的名称、运行时自动获取最新版本、把本地能力打包进 Release。

**Tenant Binding Preset**：Tenant Catalog 为 Agent Release 提供的可选本地能力映射建议，只引用同一空间内的资源且不包含凭据；采用时必须重新校验权限，管理员可以接受、替换或忽略，Preset 不会进入 Public Marketplace。
_避免_：Agent Release 的一部分、自动授权、按资源名称猜测绑定、包含连接凭据的配置包。

**依赖安全阻断（Dependency Security Block）**：Agent Release 锁定的依赖被安全撤回后形成的传递状态，禁止该 Release 被新引入、用于发布新变体或启动新 Task/Run，直到新的 Agent Release 锁定安全依赖；它不删除原 Release、Task 或审计记录。
_避免_：运行时自动替换依赖、仅显示警告后继续新执行、删除受影响历史。

**Marketplace 保管（Marketplace Custody）**：Publisher 注销、失去验证或撤回 Listing 后，平台为已有 Agent Adoption 按许可与保留规则保存继续使用所必需的最小不可变 Release、Manifest、许可、来源和审核记录；保管不转移 Publisher 身份，也不开放新的 Adoption。
_避免_：平台接管 Listing 所有权、继续公开失效 Publisher、删除采用方所依赖的全部包。

**委派授权（Delegated Grant）**：Lead Agent 为一个明确子任务从 Task Grant 中划出的最小权限子集；专业 Agent 的配置只能声明可能需要的能力，不能借委派获得 Task 未授权的权限。
_避免_：继承完整 Task Grant、继承专业 Agent 配置中的全部权限、子 Agent 自行扩大授权。

**私有任务**：默认仅创建者可访问的任务；使用共享知识、空间 Agent 或空间连接不会自动扩大任务可见范围，协作访问必须显式授予。
_避免_：空间成员默认可见、继承资源可见性后自动公开、向空间外部人员共享完整任务。

**任务所有者（Task Owner）**：创建并对任务承担管理责任的成员，控制任务共享、取消与归档，并负责涉及其个人连接、代码交付或其他外部副作用的授权。
_避免_：空间 Owner、Lead Agent、任意获得查看权的成员。

**任务协作者（Task Collaborator）**：经任务所有者显式授权，可以评论、追加指令和请求新运行的成员；协作权不会转移任务所有权，也不会授予使用任务所有者个人连接或批准其外部副作用的权限。
_避免_：共同所有者、只读查看者。

**任务查看者（Task Viewer）**：经任务所有者显式授权，只能查看任务过程、引用、结果和允许下载的产物的成员。
_避免_：任务协作者、空间内默认可见成员。

**合规访问（Compliance Access）**：管理员为审计、安全响应或法律义务，在说明理由、限定期限并完整留痕后访问私有任务内容的独立流程；管理员默认只能查看任务元数据、成本、安全事件和外部操作回执。
_避免_：空间 Admin 的普通查看权、把管理员加入任务共享列表、无理由或无期限访问私有内容。

**运行（Run）**：任务内部的一次云端执行，具有排队、运行、等待、成功、失败和结果未知等执行状态；运行状态不直接决定任务生命周期。
_避免_：任务生命周期、Agent 自定义进度阶段、用短暂客户端连接或内存状态定义运行的存续。

**运行干预（Run Intervention）**：成员在运行期间明确选择调整当前执行、排队到下一次运行，或停止后再开始新运行的控制动作；系统必须显示指令实际作用于哪一次运行。
_避免_：语义不明的普通消息、默认把所有新输入注入当前运行、停止结果未知时立即启动冲突运行。

**工作区（Workspace）**：任务独有的持久云端文件工作区；同一任务的多次运行复用，不同任务默认不共享可写目录，需显式导入才能复用文件。同一任务至多一个运行可以写入工作区、产物草稿或外部目标，只读子执行可以并行。闲置计算可休眠，文件不因休眠删除；存储配额超限时限制新增写入，仍允许查看、导出和清理。
_避免_：将工作区等同于空间（Tenant）或任意用户可写的共享目录。

**发布副本**：用户从任务选择固定版本发布的独立副本，按目标权限可见；原任务仍保持私有，后续修改须重新发布，撤销任务共享不撤销已发布副本。
_避免_：将发布理解为对原任务的持续同步或自动覆盖。

**终端观察**：活跃运行期间仅可查看终端输出；交互式 PTY 或人工编辑须先停止运行并确认停止，结果未知时保持阻断。
_避免_：将观察输出视为可并发写入工作区的终端权限。

**代码平台连接**：由 WeKnora 管理、用于访问 GitHub 或 GitLab 的个人连接或空间连接。个人连接只能由其所有者使用；空间连接按仓库、成员角色和操作策略授权。每次远端写入记录发起成员、批准成员与实际远端身份。
_避免_：把个人令牌升级成共享凭据、由 Agent 任意选择连接、将凭据写入聊天产物或向共享查看者暴露。

**通用 Agent**：接受开放任务、按授权使用多种工具和资源的 Agent；使用知识库不是其必要前提。
_避免_：将通用 Agent 等同于某个模型或远程电脑。

**专业 Agent**：面向特定领域任务，具有领域工具、规则或专属交互的 Agent；与通用 Agent 共用基本会话和执行语义。
_避免_：将每种专业 Agent 都视为独立客户端或独立执行引擎。

**求职专业 Agent**：面向求职者的岗位判断、申请材料准备、申请过程跟进与面试准备提供协助的专业 Agent；求职者决定是否对外投递或发送内容。
_避免_：独立于 WeKnora 任务体系的第二套求职执行引擎、以自动海投为目标的投递机器人。

**求职档案**：求职者提供并确认的教育、经历、项目、技能、求职意向和证明材料，作为评估岗位与撰写申请材料的事实依据。
_避免_：针对单个岗位生成的简历、模型自行补全但未经核实的经历。

**求职申请**：一位求职者针对一个具体岗位的准备、投递与后续进展记录，关联自己的任务、该岗位使用的材料版本和用户确认的状态。
_避免_：一次 Agent 运行、单份简历、尚未决定申请的普通岗位发现结果。

**申请材料版本**：求职者已确认、准备对一个岗位使用的一份确定内容及其导出文件；修改内容会产生新版本，求职申请保留实际投递所用的版本。
_避免_：可在投递后静默覆盖的简历、不同正文却标成同一版本的 PDF 与 DOCX。

**持续找岗规则**：求职者明确开启、可查看、暂停或修改的岗位发现条件，用于定期提出新的岗位机会。
_避免_：一次性搜索指令、未经用户开启的后台扫描、自动投递指令。

**岗位机会**：从确定来源取得、可供求职者评估和选择的一个招聘岗位，保留来源、获取时间与原始要求；尚未投递的岗位机会不等于求职申请。
_避免_：仅凭职位名称推断的虚构岗位、已完成的求职申请。

**岗位快照**：在确定时间保存的岗位要求及来源记录，供一次评估或申请追溯当时的依据；来源页面后续变化不会改写已使用的快照。
_避免_：当前网页内容、可覆盖的职位缓存、模型生成的职位摘要。

**岗位资格判断**：以岗位明确要求和已确认的求职档案为依据，对毕业年份、学历、地域或实习时长等硬条件作出的符合、不符合或待确认判断。
_避免_：把资料缺失当作符合、把模型猜测当作岗位明示要求。

**岗位匹配评估**：在资格判断之外，依据已确认的求职档案与岗位快照说明技能、项目、地点和求职意向的契合点与差距，供求职者决定是否准备申请。
_避免_：录用概率、对未来面试结果的保证、没有证据说明的单一分数。

**申请进展事件**：求职者确认发生的一次投递、测评、面试、录用意向、拒绝或放弃等事实，按时间累积形成申请历史，并可用于确定当前阶段。
_避免_：覆盖上一状态的单一标签、未经确认的邮件分类结果、Agent 的推测。

**开发 Agent（Developer）**：完成代码修改、测试与交付的专业 Agent；用户通过任务表达需求、监督执行并审批对代码平台的写入。
_避免_：将其等同于某个编码模型、Claude Code 或 Codex 产品。

**代码交付审批**：用户对指定仓库、分支、提交与代码平台写操作作出的授权；待交付内容变化后需要重新审批。
_避免_：将一次批准解释为后续所有推送或 PR 修改的永久授权。

**代码交付（Code Delivery）**：把获批的确定提交推送到任务分支并创建或更新草稿 PR/MR 的外部操作；首版不直接写入受保护分支，也不自动合并。
_避免_：自动合并、把补丁生成等同于已经远端交付、在推送与创建 PR/MR 部分成功后盲目重试全部步骤。

**知识资源**：Agent 可按权限使用的知识库、文档及相关检索内容，同时可供用户直接问答。
_避免_：将知识库视为创建所有 Agent 会话的必要条件。

## 套餐与消耗

**用量事实（Usage Fact）**：对一次实际收费活动的不可变原始观察，记录稳定身份、发生时间、计量维度、资金来源和结果状态；它不是已经完成的价格换算或 Credits 扣减。
_避免_：最终费用、聚合账单行、缺失用量时虚构的零消耗。

**结算批次（Settlement Batch）**：一组具有确定任务、订阅、计量范围和时间边界的用量事实；系统以该边界关联权威聚合计价结果并统一结算对应预占。
_避免_：没有边界的余额总差额、把聚合结果伪装成逐事件计价回执。

**补偿用量（Compensating Usage）**：在保留原始用量事实的前提下，对尚可修正的计量结果追加的关联调整；已形成最终客户账单后的财务差额改由 Credit Note 或追加账单处理。
_避免_：覆盖或删除原始用量、静默改写已出账历史、没有原事件关联的负数。

**套餐（Plan）**：空间购买的一组功能权益、资源上限和周期内包含的消耗额度。
_避免_：将套餐等同于 Credits 余额。

**套餐版本（Plan Version）**：一次发布后不可变的套餐定义，固定其基础价格、计价规则、功能权益、资源上限和周期额度；调整任何商业内容都产生新版本，既有订阅不会被目录修改追溯改变。
_避免_：可原地改写的已发布套餐、仅有展示名称的版本标签。

**基础档（Base Plan）**：空间没有有效付费覆盖时持续生效的免费套餐版本，保留已有数据和未到期充值额度，并按其功能与资源上限限制新增操作。
_避免_：没有订阅、删除已有数据、清空充值余额。

**Credits**：空间预付费消耗的统一计价单位，可用于约定收费范围内的模型、解析、沙箱和 Connector 使用。Credits 可以来自套餐内额度或额外充值。
_避免_：原始 Token 数、人民币余额、可在空间间共享的钱包。

**套餐内额度**：套餐按月发放、仅当期有效且不结转的 Credits。年付套餐同样逐月发放。
_避免_：充值额度、年付时一次性发放的全年余额。

**充值额度**：空间额外付费购买的 Credits，每批自到账生效起有效 12 个月，在有效期内跨月保留。
_避免_：永久余额、随套餐月度重置清零的额度。

**任务预算（Task Budget）**：空间授权一个任务及其委派执行合计可消耗的 Credits 上限；达到上限后运行持久暂停，只有任务所有者或获授权的账单管理员可以增加上限。
_避免_：整个空间的余额、每个子任务各自获得一份完整额度、任务协作者可自行提高的预算。

**任务保留策略（Task Retention Policy）**：空间对任务、时间线、产物和操作回执的归档、保留、法律保留与永久删除规则；内部删除会清理服务端内容与设备缓存，但不会隐式删除已经发布到外部系统的内容。
_避免_：任务所有者可绕过空间规则立即硬删除、内部删除自动触发外部副作用、所有内容无期限保存。

**额度预占**：为已准入的收费执行暂时占用可消费额度，防止同一额度同时用于其他执行或退款。
_避免_：最终消费、延长额度有效期。

**结算待核对（Reconciliation Pending）**：收费活动已经发生或可能已经发生，但权威计价结果尚不能与该活动可靠关联的状态；对应预占继续保留，直到结果被确认、修正或进入人工处置。
_避免_：把事件接收成功当成已经结算、根据余额总差额猜测单笔费用、超时后自动释放预占。

**退款锁定**：退款批准后暂时禁止消费对应待退权益的状态，直到退款与权益撤回的结果得到处理。
_避免_：空间全部余额冻结、渠道已经退款成功。

**资源配额（Resource Quota）**：套餐允许空间占有或同时使用的资源上限，例如成员数、存储容量和并发数。
_避免_：将资源上限与可消耗的 Credits 余额混为一谈。

**超限状态**：空间已有资源超过当前套餐上限，但已有资源仍被保留的状态。该状态限制增加超限资源的操作，仍允许授权范围内的查看、导出和清理。
_避免_：将配额超限等同于空间禁用或自动删除资源。

**平台模型**：由平台提供调用凭据或模型服务，并向使用空间收取模型消耗 Credits 的模型能力。
_避免_：仅凭提供商名称或“默认模型”标记判断付费来源。

**自带模型（BYOK）**：空间自行配置凭据或服务的模型能力，其模型费用由空间自行承担，WeKnora 不重复收取该调用的模型 Credits。使用 BYOK 仍可能产生平台沙箱、解析和 Connector 等服务费用。
_避免_：将 BYOK 等同于整个任务免费或免除资源配额。

## 应用与外部连接

**插件（Plugin）**：由开发者提供、可由空间管理员安装的版本化扩展包，为 Agent 增加外部服务操作或独立能力；插件本身不代表成员已授权外部账号。
_避免_：将插件等同于某个 MCP 服务配置、应用安装或个人连接。

**插件版本（Plugin Release）**：开发者发布的一份确定的插件能力与权限声明，关联在该版本存续期间保持可用的执行端点；空间接受新版本前继续使用已接受版本。
_避免_：把同一地址上的任意服务更新视为已获空间接受的版本。

**插件清单（Plugin Manifest）**：开发者提供给空间管理员审阅和安装的插件版本说明，列明能力、授权需求和执行端点；清单不是成员凭据或执行授权。
_避免_：将可访问的清单地址视为自动获准安装或调用。

**应用（App）**：目录中可供空间安装的一组集成能力及其版本定义。
_避免_：将应用定义等同于某个外部账号或空间安装记录。

**应用安装（Installation）**：某个空间对一个 App 版本的启用关系，包含该空间的配置和能力授权。
_避免_：全平台安装一次即供所有空间使用。

**连接（Connection）**：在特定空间中访问一个外部账号或服务的授权关系；同一应用安装可以拥有多个连接。
_避免_：将安装应用等同于已获得外部账号授权。

**个人连接**：归属于特定空间和成员、仅供该成员及代表其执行的授权任务使用的连接。
_避免_：默认向空间其他成员共享个人账号。

**空间连接**：由管理员管理，并按显式授权供本空间成员和 Agent 使用的连接。
_避免_：将使用连接的权限等同于读取其原始凭据的权限。

**同步（Sync）**：把选定外部资源持续导入当前空间的集成能力，具有资源范围、同步进度和更新规则。
_避免_：将源端只读访问等同于本空间数据不会发生变化。

**操作（Action）**：通过连接执行的一次外部查询或业务变更，具有明确目标和输入内容。
_避免_：将模型描述的意图等同于已获授权的具体操作。

**操作计划（Action Plan）**：提交审批的一组确定外部操作，包含每项操作的连接、目标、内容摘要、顺序与版本；成员可整体批准或排除单项，执行结果仍逐项持久记录。任何目标、内容、连接或操作集合变化都会使既有批准失效。
_避免_：批准整个运行中的未知未来操作、一次批准后的永久自动执行。

**预授权（Preauthorization）**：管理员事先批准在限定连接、操作、资源范围、期限和执行额度内开展操作的授权。
_避免_：无限期、无限范围或可覆盖权限撤销的通用批准。

**任务授权（Task Grant）**：在空间策略与成员权限范围内，为一个任务授予的知识、Agent、连接、工具和预算使用范围；运行时超出该范围或命中必须审批的外部副作用时，需要对确定操作另行审批。
_避免_：Agent 自行声明即获得权限、一次批准覆盖任务后续所有操作、脱离空间策略的完全访问模式。

**可发现知识范围**：成员有权访问且空间策略允许 Lead Agent 为当前任务自动检索的知识范围；Agent 必须保留并展示实际使用的来源。个人连接、敏感空间连接和外部写操作不因知识可发现而自动获得授权。
_避免_：成员有权访问的全部资源、Task Grant、无需说明来源的全空间搜索。

**任务产物（Task Artifact）**：任务生成并由 WeKnora 保存的不可变版本化结果，例如文档、表格、演示文稿、PDF、图片、代码或测试报告；成员可在移动端预览、比较、批注、下载、分享，或要求 Agent 基于指定版本生成新版本，但不能原地覆盖已存在或已审批的版本。
_避免_：外部办公系统中的可变文档、聊天消息、未经保存的流式输出、可被静默覆盖的文件。

**Craft 网页作品**：Craft 任务围绕同一用户目标持续创作的一个主要交互网页；它由任务工作区中的源码形成，并以任务产物的不可变版本交付。工作区草稿和运行中的预览不等于已交付版本。
_避免_：把一次 Run、单个源码文件或沙箱预览地址称为一个独立作品。

**语音交互记录（Voice Interaction Record）**：实时语音交互中经成员确认的文字输入和 Agent 文字答复，作为任务时间线的一部分保存；原始音频默认在实时处理后删除，只有空间明确启用并提示参与者时才限期留存。
_避免_：默认永久录音、用语音直接完成高风险审批、不保留任何可审计文字语义。

**外部发布（External Publication）**：通过获准的连接，把确定版本的任务产物创建或更新到外部办公系统的操作，并保存获批版本、外部目标、目标版本与操作结果作为回执。发布后外部文档是后续协作的权威版本；再次更新前必须读取外部当前版本并形成新的候选变更。
_避免_：内置 Office 编辑器、未经审批的自动同步、WeKnora 与外部文档双向实时同步、把外部写入成功等同于任务产物本身。
