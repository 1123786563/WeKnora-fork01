# T07 执行记录：成员发现 API 与跨空间隔离

- 日期：2026-09-24（R9 范围缺口修复轮）
- 分支/worktree：`codex/issue-106-self-hosted-plugins` @ `.worktrees/issue106`
- 提交：见 git log「范围缺口 R9 修复：成员发现与跨空间隔离」[T07]
- 状态：完成（服务/仓储/handler/路由落地；测试 GREEN）

## 做了什么

按计划 Task 7（docs/plans/2026-09-23-issue-106-03-install-discover.md）：

1. **repo**：ListInstallationsByTenant/GetInstallation 参数绑定
   `WHERE tenant_id = ?`；未命中 (nil,nil)。
2. **service**：ListInstallations 摘要映射（不回传快照原文/端点——成员看
   WHAT+状态，不看已核验目录细节）；GetInstallation 详情含审批 Enabled 合并
   （策略读取 best-effort，故障降级 Enabled=nil）。
3. **handler/路由**：GET /plugins/installations 与 GET /plugins/installations/:id
   （Viewer+；静态段 preview 先注册防 gin 通配冲突，routes_agent.go 先例）。

## 测试证据（全部真实运行）

- `go test ./internal/modules/plugins/ -run 'TestListInstallations|TestGetInstallationRejects|TestManualMCP' -count=1` ok：
  - TestListInstallationsTenantScoped：两租户各恰见自己的、字段含 state/drift_state/
    tool_count/requires_personal_auth、空租户空列表。
  - TestGetInstallationRejectsForeignTenant：跨租户 ErrInstallationNotFound
    （不泄露存在性）；本租户详情写工具 Enabled=false。
  - TestManualMCPServiceUnaffectedByInstallations：手工服务（plugin_installation_id
    NULL）在插件安装后存活、字段原样、无 approval 行的工具仍启用（缺省语义保持）。
- `go test ./internal/handler/ -run TestPluginDiscoveryHandlers -count=1` ok
  （列表 200 摘要字段、详情 200、跨租户 404）。
- 门控随 T06 同轮全量复跑（见 T06.md 测试证据节）。

## 实现偏差

- 手工服务测试 URL 不能用假域名（CreateMCPService 的 SSRF 校验做真实 DNS），
  改指受控 httptest host（白名单 127.0.0.1）。

## 续作记录（2026-09-25，恢复语义核实轮）

- 恢复检查：`git log -F --grep="[T07]"` 命中 270dec1da（模块级验收测试 + 本记录
  首轮）；实现面（repo `ListInstallationsByTenant`/`GetInstallation` 参数绑定、service
  `ListInstallations`/`GetInstallation`、dto/handler、路由 `GET /plugins/installations(/:id)`
  Viewer+）已在位且已提交（随 T06 链）。
- 核实缺口：Brief 所有权文件 `internal/handler/plugin_discover_test.go` 不存在——
  此前 handler 层发现测试落在 T06 所有的 `plugin_install_test.go`（名
  `TestPluginDiscoveryHandlers`），任务书命令 `-run TestListInstallationsHandler`
  实测零匹配空转（`testing: warning: no tests to run`，exit 0 但非意图检查）。属
  部分完成，本轮续作补齐。
- 补齐（TDD，RED=测试缺失已实录）：新建 `plugin_discover_test.go`（复用同包
  `stubPluginInstallService`/`newPluginInstallRouter` 助手，零改动 T06 文件）：
  - `TestListInstallationsHandler`：摘要字段 envelope/租户从上下文透传、空列表
    `data:[]` 非 null、仓储故障 500、列表 wire 不含 schema 原文。
  - `TestGetInstallationHandler`：200 详情含工具目录元数据且无 schema 回显；
    跨空间/未知 ID 404 响应体完全一致（存在性不泄露的 wire 钉子）。
- 测试证据（全部真实运行，worktree 根）：
  - `go test ./internal/modules/plugins/ -run 'TestListInstallations|TestGetInstallationRejects|TestManualMCP' -v` → 3 PASS，exit 0。
  - `go test ./internal/handler/ -run TestListInstallationsHandler -v` →
    TestListInstallationsHandler + TestGetInstallationHandler 共 6 子测试全 PASS，exit 0。
  - `go build ./...` → exit 0（仅既有 ld 重复 '-lc++' 告警，environment.md 基线同款）。
  - 三条命令 `&&` 串联整体复跑 exit 0。

Ruling（模型路由）：任务书要求按 gpt-5.6-sol/terra/luna 分级派发，但本 host 仅配置
GLM-5.3 系列模型，动态工作流亦不支持按子代理选模型。实际全部子代理运行在会话模型
GLM-5.3（最高推理档）上。此为记录的替代方案，不声称使用了不可用模型。

## OCR 一轮分流记录（2026-09-25，报告 docs/plans/issue-106-ocr-T07.md）

任务评审通过后任务级增量 OCR 5 项 medium 分流：

### 已修复（a 类，所有权内真实缺陷）

**T07-OCR1-F5**（medium，plugin_install_service.go:478-482）——按 systematic-debugging：

- 根因（代码证据）：ConfirmInstallation Step 5 以 `ServiceID: ""` 建安装行
  （:192），Step 6 物化服务带 `PluginInstallationID` 回引（:220）后经
  UpdateInstallationServiceID 持久化绑定（:239）——两步之间中断（进程崩溃，或
  绑定失败且补偿级联也失败保留安装行）即留下空 service_id 锚行 + 孤儿服务行；
  UninstallInstallation `if inst.ServiceID != ""` 跳过级联并删锚行 → 孤儿滞留
  （Enabled=true、运行时按非插件服务不过滤目录、占 (tenant_id, name) 唯一槽
  阻断重装）。
- RED：TestUninstallInstallationHealsEmptyServiceIDAnchor（install_discover_test.go，
  复用同包 installTestStack，模拟中断窗口后卸载）——修复前 FAIL 于
  `require.Empty(t, s.mcpRepo.services)`（孤儿滞留，锚行已删）。
- 修复：UninstallInstallation 在 service_id 为空时经 `serviceIDByInstallation`
  （既有 `mcpServiceRepo.List(ctx, tenantID)` + `PluginInstallationID` 回引过滤）
  反查孤儿并入既有级联（HardDeleteServiceCascade + closeServiceClient）；反查
  故障 fail-closed 返回 ErrInstallationPersistFailed 保留锚行供重试自愈。
- GREEN：新测试 PASS 且断言孤儿服务/策略行/缓存连接全清、唯一槽释放可重装；
  邻近套件（TestConfirmInstallation*、TestSetInstallationState*、
  TestUninstallInstallationReleasesUniqueSlot 等）无回归。
- **实现偏差**：OCR 建议在 MCPServiceRepository/PluginRepository 增
  GetByPluginInstallationID 查询——未采纳：PluginRepository 加方法会破坏 T06
  所有的 install_service_test.go 内 fakePluginInstallRepo（全量实现无内嵌，
  编译必断）且该测试文件与 interfaces/mcp_service.go 均不在本任务所有权；
  改用既有 List(ctx, tenantID) 在服务层过滤，仅走空 service_id 异常路径，
  租户内服务数有界。若后续任务愿收口为仓储级查询，可平滑替换。

### low 级顺带修复（所有权内、同一函数族）

- **:277-280 行消失误报**：reclassifyPreviewMiss 对 `preview == nil`（行被
  DeleteExpiredPreviews 惰性清理，只删已过期行）误报 ErrPreviewAlreadyConsumed，
  与自身 R12 F17 契约矛盾 → 改为 `preview == nil || ConsumedAt == nil` 均判
  ErrPreviewExpired；回归测试 TestConfirmInstallationVanishedPreviewReadsExpired
  （onTenantPlugin 钩子模拟惰性清理删行）。
- **:272-276 重读故障误判 400**：GetPreview 重读故障返回
  ErrPreviewAlreadyConsumed（确定性 400 判决）违反 R12 F01 → 改
  ErrInstallationPersistFailed（5xx 哨兵）。注：该分支的故障注入测试需给
  installPreviewRepo.GetPreview 加错误钩子（T06 所在测试文件，无此钩子），
  不越权改动，修复依据与相邻 MarkPreviewConsumed 非 NotFound 分支同一
  既有模式（:267-268），未单独注入测试——如实记录。

### 转交（b 类，不在所有权内）

- **OCR-HANDOFF: T07-OCR1-F1** apps/web/src/settings/PluginsSettingsPanel.tsx
  前端面板镜像解析器重复——T07 Brief 仅含 Go 后端六文件 + 两测试文件，
  PluginsSettingsPanel.tsx 属 T03（管理端面板）与 T08（前端收敛）所有权；
  报告自身亦注明与 T03-OCR1-F1 同源、T03 侧已豁免转出。
- **OCR-HANDOFF: T07-OCR1-F7** mcp_tool.go:630-635 守卫失败分支零日志——
  internal/modules/agentruntime/agent/tools/mcp_tool.go 属 T09 所有权
  （RegisterMCPTools 快照守卫注入即 T09 交付），与 T06-OCR1-F3 同源转交项。
- **OCR-HANDOFF: T07-OCR1-F8** mcp_tool.go:585-587 PG jsonb 数字字面量重渲染
  致假阳性漂移——运行时摘要比对链路（mcp_tool.go/mcp_metadata 缓存回读）属
  T09 运行时目录过滤所有权；摘要协议数字逐字语义（types/plugin.go、
  manifest_test.go）亦非本任务文件。
- **OCR-HANDOFF: T07-OCR1-F9** CreateMCPService 接受客户端
  PluginInstallationID 可伪造物化行——internal/application/service/
  mcp_service.go 与 handler/mcp_service.go 属 MCP 服务域（T06 转交修复落点），
  报告自身注明「已在 T05-OCR2-F1 转交记录中载明但至今未闭合」，为跨任务
  既有转交项，不属 T07 Brief 文件面。

### low 级不属所有权的备注（OCR 台账已录，此处仅登记归属）

PluginsSettingsPanel viewer 死分支（T03/T08）、SettingsPage 十层三元（前端
T08 域）、jira-todo-mcp main.go:221 端口名单写法与 oauth.go:547 持锁
randomToken（T04/T05 示例服务域）、mcp_service.go 哨兵无 API 消费者与
ClearMCPCredential 守卫缺口（T06/MCP 域）。

### 本轮测试证据（全部真实运行，worktree 根）

- `go test ./internal/modules/plugins/ -run 'TestUninstallInstallationHealsEmptyServiceIDAnchor|TestConfirmInstallationVanishedPreviewReadsExpired' -v` → 双 PASS。
- `go test ./internal/modules/plugins/ -count=1` → ok（全包，含 T06 全部套件无回归）。
- 三连命令 `go test ./internal/modules/plugins/ -run 'TestListInstallations|TestGetInstallationRejects|TestManualMCP' -v && go test ./internal/handler/ -run TestListInstallationsHandler -v && go build ./...` → exit 0（build 仅既有 ld '-lc++' 告警）。
- `go test ./internal/handler/ -count=1` → ok。

## OCR 二轮分流记录（2026-09-25，报告同文件追加节）

### 已修复（a 类，所有权内真实缺陷）

**T07-OCR2-F3**（medium，plugin_install_service.go:714-720）——按 systematic-debugging：

- 根因（代码证据）：GetMyConnectionStatus 在 confirm 中断窗口（安装行
  service_id 空、物化服务与成员令牌已在）下，以空 inst.ServiceID 调
  GetTokenForPrincipal（SQL `service_id = ''` 必无命中）→ 三态误判
  unauthorized，且 :709 `if inst.ServiceID != ""` 使
  AuthorizeURLPath/RevokePath 皆空——无法引导重授权的死端视图；
  UninstallInstallation 已有 serviceIDByInstallation 反查（OCR 一轮 F5）
  而此读路径未复用。
- RED：TestGetMyConnectionStatusHealsEmptyServiceIDAnchor（install_discover_test.go，
  复用同包 connection_status_test 的 fake 与 installTestStack 家族的
  fakeInstallMCPServiceRepo）——修复前 FAIL 于
  `require.True(t, status.Authorized, "token found via the orphan back-reference...")`。
- 修复：GetMyConnectionStatus 在 inst==nil 检查后对空 ServiceID 复用
  serviceIDByInstallation 反查（fail-closed 于反查故障，返回连接域
  ErrConnectionQueryFailed）；serviceIDByInstallation 增 nil mcpServiceRepo
  防御（按「无孤儿」处理，兼容 T11 fake 层 mcpServiceRepo=nil 装配，绝不
  panic）。
- GREEN：新测试断言 authorized/ServiceID=svc-orphan/两端点路径照常，且
  「反查不到孤儿 + nil mcpServiceRepo」降级路径不 panic 返回既有形态；
  T11 连接套件（TestGetMyConnectionStatusStates/PerPrincipal/TenantScoped）
  与全包无回归。
- low 顺带修复（:680-684）：GetMyConnectionStatus 读路径故障由写语义哨兵
  ErrInstallationPersistFailed 改连接域读语义哨兵 ErrConnectionQueryFailed
  ——mapPluginConnectionError（handler/plugin.go:437-440）两哨兵同落 5xx，
  HTTP 行为中性，仅日志归类准确；无既有测试锚定旧哨兵（全仓检索核实）。

### 转交（b 类，不在所有权内）

- **OCR-HANDOFF: T07-OCR2-F1** mcp_tool.go:682-686 nil 守卫下插件物化服务
  静默落 legacy 未过滤路径（失败开放）——mcp_tool.go 属 T09 所有权
  （RegisterMCPTools 快照守卫即 T09 交付），本轮 git status 亦显示该文件
  被并行 T09 会话修改中。
- **OCR-HANDOFF: T07-OCR2-F2** mcp_tool.go:575-577 服务级 instructions 未经
  基线核验直达模型提示面——同上 T09 所有权；且「核验阶段采集 instructions
  基线」的修复面横跨 T01（快照协议）/T17（漂移比对），非 T07 文件面。

### 本轮测试证据（全部真实运行，worktree 根）

- `go test ./internal/modules/plugins/ -run TestGetMyConnectionStatusHealsEmptyServiceIDAnchor -v` → 修复前 FAIL（死端症状），修复后 PASS（含自愈日志行）。
- `go test ./internal/modules/plugins/ -run 'TestGetMyConnectionStatus|TestListInstallations|TestGetInstallationRejects|TestManualMCP|TestUninstallInstallationHeals|TestConfirmInstallation|TestSetInstallationState|TestUninstallInstallationReleases' -count=1` → ok。
- `go test ./internal/modules/plugins/ -count=1` → ok（全包）。
- 三连命令 + `go test ./internal/handler/ -count=1` → exit 0 / ok（build 仅既有 ld '-lc++' 告警）。

## OCR 三轮分流记录（2026-09-25，报告同文件追加节）

### 已修复（a 类，所有权内真实缺陷）

**T07-OCR3-F2**（medium，maintainability，plugin_install_service.go:539-544）：

- 缺陷：二轮 F3 修复中给 serviceIDByInstallation 加的
  `if s.mcpServiceRepo == nil { return "", nil }` 让生产语义向测试夹具形态
  妥协——与本文件 oauthRepo == nil 的 fail-loudly 惯例相悖；一旦装配回归，
  UninstallInstallation 会静默跳过孤儿反查、删锚行后物化服务成永久孤儿
  （唯一索引阻断重装），连接视图退回二轮 F3 要消除的死端。
- RED：扩展 TestGetMyConnectionStatusHealsEmptyServiceIDAnchor——nil 装配 +
  空 ServiceID 子场景断言 `ErrorIs(ErrConnectionQueryFailed)`，修复前 FAIL
  （静默返回 nil error 的降级视图）。
- 修复：nil 防御改为响亮失败（返回显式错误，调用方已按各自域哨兵
  fail-closed：卸载→ErrInstallationPersistFailed 保锚行、连接视图→
  ErrConnectionQueryFailed）；测试夹具侧改注入 stub（我的测试内
  `&fakeInstallMCPServiceRepo{}` 承载「无孤儿」语义子场景）。
- 未采纳的替代：NewPluginService 构造器断言非 nil——构造器在
  plugin_service.go（不在 T07 Brief 所有权）；T11 的
  connection_status_test.go（nil 装配夹具）不动，其用例均非空 ServiceID
  不触达该路径（全包测试核实无回归）。
- GREEN：新子场景 PASS（含响亮 ERROR 日志）；全包 + 三连命令 + handler 包
  全部通过。

### 本轮测试证据（全部真实运行，worktree 根）

- `go test ./internal/modules/plugins/ -run TestGetMyConnectionStatusHealsEmptyServiceIDAnchor -v` → 修复前 FAIL（install_discover_test.go:180 Expected error ... got nil），修复后 PASS（日志含 ERROR "plugin service store is not wired"）。
- `go test ./internal/modules/plugins/ -count=1` → ok（全包）。
- 三连命令 + `go test ./internal/handler/ -count=1` → exit 0 / ok（build 仅既有 ld '-lc++' 告警）。
