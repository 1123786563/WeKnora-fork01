# T07 执行记录：成员发现 API 与跨空间隔离

- 日期：2026-09-24（R9 范围缺口修复轮）
- 分支/worktree：`codex/issue-106-self-hosted-plugins` @ `.worktrees/issue106`
- 提交：见 git log「范围缺口 R9 修复：成员发现与跨空间隔离」[T07]
- 状态：完成（服务/仓储/handler/路由落地；测试 GREEN）

## 做了什么

按计划 Task 7（docs/plans/2026-09-23-issue-106-03-install-discover.md）：

1. **repo**：ListInstallationsByTenant/GetInstallation 参数绑定
   `WHERE tenant_id = ?`；未命中 (nil,nil)。
2. **service**：ListInstallations 摘要映射（不回传快照原文/端点——成员看
   WHAT+状态，不看已核验目录细节）；GetInstallation 详情含审批 Enabled 合并
   （策略读取 best-effort，故障降级 Enabled=nil）。
3. **handler/路由**：GET /plugins/installations 与 GET /plugins/installations/:id
   （Viewer+；静态段 preview 先注册防 gin 通配冲突，routes_agent.go 先例）。

## 测试证据（全部真实运行）

- `go test ./internal/modules/plugins/ -run 'TestListInstallations|TestGetInstallationRejects|TestManualMCP' -count=1` ok：
  - TestListInstallationsTenantScoped：两租户各恰见自己的、字段含 state/drift_state/
    tool_count/requires_personal_auth、空租户空列表。
  - TestGetInstallationRejectsForeignTenant：跨租户 ErrInstallationNotFound
    （不泄露存在性）；本租户详情写工具 Enabled=false。
  - TestManualMCPServiceUnaffectedByInstallations：手工服务（plugin_installation_id
    NULL）在插件安装后存活、字段原样、无 approval 行的工具仍启用（缺省语义保持）。
- `go test ./internal/handler/ -run TestPluginDiscoveryHandlers -count=1` ok
  （列表 200 摘要字段、详情 200、跨租户 404）。
- 门控随 T06 同轮全量复跑（见 T06.md 测试证据节）。

## 实现偏差

- 手工服务测试 URL 不能用假域名（CreateMCPService 的 SSRF 校验做真实 DNS），
  改指受控 httptest host（白名单 127.0.0.1）。

## 续作记录（2026-09-25，恢复语义核实轮）

- 恢复检查：`git log -F --grep="[T07]"` 命中 270dec1da（模块级验收测试 + 本记录
  首轮）；实现面（repo `ListInstallationsByTenant`/`GetInstallation` 参数绑定、service
  `ListInstallations`/`GetInstallation`、dto/handler、路由 `GET /plugins/installations(/:id)`
  Viewer+）已在位且已提交（随 T06 链）。
- 核实缺口：Brief 所有权文件 `internal/handler/plugin_discover_test.go` 不存在——
  此前 handler 层发现测试落在 T06 所有的 `plugin_install_test.go`（名
  `TestPluginDiscoveryHandlers`），任务书命令 `-run TestListInstallationsHandler`
  实测零匹配空转（`testing: warning: no tests to run`，exit 0 但非意图检查）。属
  部分完成，本轮续作补齐。
- 补齐（TDD，RED=测试缺失已实录）：新建 `plugin_discover_test.go`（复用同包
  `stubPluginInstallService`/`newPluginInstallRouter` 助手，零改动 T06 文件）：
  - `TestListInstallationsHandler`：摘要字段 envelope/租户从上下文透传、空列表
    `data:[]` 非 null、仓储故障 500、列表 wire 不含 schema 原文。
  - `TestGetInstallationHandler`：200 详情含工具目录元数据且无 schema 回显；
    跨空间/未知 ID 404 响应体完全一致（存在性不泄露的 wire 钉子）。
- 测试证据（全部真实运行，worktree 根）：
  - `go test ./internal/modules/plugins/ -run 'TestListInstallations|TestGetInstallationRejects|TestManualMCP' -v` → 3 PASS，exit 0。
  - `go test ./internal/handler/ -run TestListInstallationsHandler -v` →
    TestListInstallationsHandler + TestGetInstallationHandler 共 6 子测试全 PASS，exit 0。
  - `go build ./...` → exit 0（仅既有 ld 重复 '-lc++' 告警，environment.md 基线同款）。
  - 三条命令 `&&` 串联整体复跑 exit 0。

Ruling（模型路由）：任务书要求按 gpt-5.6-sol/terra/luna 分级派发，但本 host 仅配置
GLM-5.3 系列模型，动态工作流亦不支持按子代理选模型。实际全部子代理运行在会话模型
GLM-5.3（最高推理档）上。此为记录的替代方案，不声称使用了不可用模型。
