# T20 执行记录——Spec 十条边界核销验收套件、文档与矩阵复核

日期：2026-09-25。执行者：实现-T20（SDD 单任务模式，无子代理派发）。
Brief：`.superpowers/sdd/2026-09-23-issue-106/briefs/T20.md`；切片计划：
`docs/plans/2026-09-23-issue-106-12-acceptance.md`。

## 做了什么

1. **恢复语义核实**：`git log --grep="[T20]"` 无既有提交、`tasks/T20.md`
   不存在 → 全新实施（无部分完成续作）。
2. **`internal/modules/plugins/plugin_acceptance_test.go`**（新建，
   `//go:build integration`，`package plugins_test`，按总索引测试包名约定）
   ——`TestPlugin106Acceptance` 十个子测试，每条边界复用同包既有前置任务
   基建（`openPluginDB`/`newPluginStack`/`newUpgradePGStack`/
   `newDriftPGStack`/`newApprovalPGStack`/`newJiraE2EStack`/
   `agentIT*` 会话辅助/`plugintest`+`fakejira` 受控远端），只断言对外
   可观察结果，`t.Logf("PLUGIN106-EVIDENCE <json>")` 逐场景一行、
   cleanup 汇总并核对 B1-B10 各恰一行：
   - **B1**：非管理员走真实 HTTP 面（`middleware.RequireRole(Admin,
     EnableRBAC=true)` + `handler.PreviewManifest`，生产路由
     routes_plugins.go:28 的等价装配）→ 403；管理员预览字段齐备
     （版本/端点/工具/读写分类/授权要求/scope）；**口径注记落地为断言**：
     预览结果 JSON 不含 `input_schema`（digest 核验、界面不渲染原文，
     T03 Step 7 / US9 展示粒度取舍）；受限地址 `ErrorIs
     ErrManifestURLRejected`；`ManifestHandler_mutate` 加 ghost 声明 →
     "missing from live endpoint" 拒绝。
   - **B2**：固定版本 1.0.0 + 成员调 v1 → 差异（added/removed）→
     改 `manifest_url` 为不可达（参数绑定）→ AcceptUpgrade 失败且
     version/endpoint/权威快照零改动 → 恢复后重预览接受 2.0.0 →
     成员新会话只见 v2。
   - **B3**：SetTools 漂移（t1 schema 变+t2 新增）→ 成员发现被阻 →
     CheckDrift detected+明细 → ResolveDrift 重定基后目录恢复。
   - **B4**：ListInstallations 发现；A/B 真实 OAuth 各只命中本人数据；
     A 撤销只影响 A；跨空间 not found。
   - **B5**：安装即写工具默认关（reason）；arm 后审批卡带工具名+参数
     原文 → reject 与 timeout（2s gate 自决）两场景 `WriteCalls()==0`。
   - **B6**：候选不可达 PreviewUpgrade 失败且版本保 v1；停用后
     ListEnabled 空+旧 ref 不可达（describe 提前到停用前组装）；撤销
     → unauthorized+重授权路径；过期 → expired+调用引导。
   - **B7**：Jira 纵向（fakejira 两成员）本人本周+`/browse/<KEY>` 链接、
     B 隔离、模型注入 token/jql/url/user_id 被 schema 拒绝且
     `jira.Calls()` 零增量。
   - **B8**：无账号工具 discover→describe→call 成功（Calls 恰 +1）、
     停用后隐藏。
   - **B9**：同租户手工服务（`plugin_installation_id` NULL）与插件安装
     同会话共存；手工读写工具缺行=启用（真实 gate）；**000190.down 复述**
     以 `accMigrationDownKeepsManual` 自建正确顺序（schema→最小父表→
     000189.up→000190.up→手工行→down→幸存断言）。
   - **B10**：预览/升级（改 `manifest_url` 为私网）两处受限地址拒绝；
     漂移核验（改 `endpoint_url` 为私网）fail-closed 且 drift 状态零
     持久化；跨空间 GetInstallation not found + ListInstallations 空。
3. **`website-docs/03-features/08-mcp.md`**：追加「自行托管插件安装」节
   （管理员清单安装流程/版本升级与漂移/写工具默认关闭与审批/成员个人
   授权/**自托管信任边界**（照 Spec 80 行语义：平台核验可观察的目录/
   schema 摘要/权限快照，不能保证开发者未在相同端点改变内部代码——
   不承诺锁死远端代码）/**两条明示口径**（① schema 为平台 digest 核验、
   界面不渲染原文；② 本版无插件卸载——停用即治理终点，成员连接与审计
   保留）/示例服务指引 `examples/plugins/jira-todo-mcp`，环境变量与
   README 实际内容核对过）。
4. **`docs/plans/2026-09-23-issue-106-trace-matrix.md`**：A 节 B1-B10
   证据列回填实际子测试名与 evidence 键；**逐一核对全部引用的测试函数
   名与仓库实际存在一一对应**，修正 4 处计划期预期名与实际不符
   （`TestFetchAndVerifyHappyPath`→happy path 由 `TestPreviewPersistsFingerprintAndExpiry`
   +handler ReturnsPreview 覆盖；`TestAcceptUpgrade*`(fingerprint/keeps-old)
   →`TestAcceptUpgradeIdempotentAndFingerprintGuard`+
   `TestAcceptUpgradeFailureKeepsOldVersion`；`TestEmptyWeekIsEmptyIsEmptySuccess`
   →`TestEmptyWeekIsEmptySuccess`；`TestManualServiceNotGovernedByDrift`
   →`TestDriftManualServiceNotGoverned`）；B9 行注明迁移 harness 存量
   缺陷与 `accMigrationDownKeepsManual` 复述关系。

## TDD 轨迹

- **RED**：先落骨架（journal + `t.Fatal("T20 骨架：套件未写全")`）→
  `PLUGIN_TEST_DATABASE_URL=... go test ./internal/modules/plugins/
  -tags integration -run TestPlugin106Acceptance -v -count=1` →
  **FAIL（0.00s，骨架 Fatal）**——无 DB Fatal、有 DB FAIL，符合计划
  Step 2 期望。
- **GREEN 迭代**（首轮 3 个失败，均测试侧问题，逐一根因修复）：
  1. B2 `a failed accept keeps the member directory on v1` 失败：远端
     已切 v2 目录后"成员目录仍 v1"不是失败保旧的观察点（那是 B3 漂移
     阻断语义）→ 断言改为安装行零改动（version/endpoint/权威快照）。
  2. B6 `agentITDescribe` 意外 error：停用后才组装会话再 describe——
     ListEnabled 已空；describe 移到停用前（T19 disable-then-plugin
     同款顺序）。
  3. B9 复用 `pluginMigrationsOpenDB` 在 apply 000190.up 即失败
     `relation "mcp_services" does not exist`——**存量 harness 顺序
     缺陷**（见 concerns 1），改为自建正确顺序复述。
- **GREEN**：修复后全过（首轮 4.11s→4.46s；最终干净复跑 3.52s，
  `--- PASS: TestPlugin106Acceptance`，十行 evidence + summary 行
  `10 boundaries verified (B1..B10)`）。

## 测试证据（全部真实运行）

| 命令 | 结果 |
| --- | --- |
| RED：`PLUGIN_TEST_DATABASE_URL='postgres://…@127.0.0.1:62804/plugins_t20?sslmode=disable' go test ./internal/modules/plugins/ -tags integration -run TestPlugin106Acceptance -v -count=1`（一次性 docker postgres:17-alpine，容器名 plugins-t20-pg，密码经环境变量传入未落源码，测毕销毁） | **FAIL**（骨架 Fatal，符合 Step 2 期望） |
| GREEN 同命令（三处修复后） | **PASS**：十子测试全过（B5 2.37s 内含 reject 0.19s+timeout 2.18s），十行 `PLUGIN106-EVIDENCE` JSON + summary 10 boundaries |
| GREEN 最终干净复跑同命令 | **PASS**（3.52s，ok 5.192s） |
| `go test ./internal/... > /tmp/t20-gotest-full.log` | **exit 0**，121 包 ok、0 FAIL |
| `go build ./...` | **exit 0**（仅既有 ld duplicate '-lc++' 告警） |
| `pnpm gates`（node v26.4.0 PATH 前置） | test:shared ✔ / typecheck:shared ✔ / typecheck:web ✔ / check:integrity ✔ / build:web ✔；**test:web 2333/2334**，唯一失败 `src/agents/agent-editor.test.tsx`（他人文件，并发资源竞争 flaky——T19 执行记录同款归因）；单独跑 `node --import tsx --test src/agents/agent-editor.test.tsx` → **49/49 pass, 0 fail**（16.3s） |
| 矩阵函数名核对：对矩阵引用的全部 50 个测试函数名 `grep -rl "func <Name>" --include="*_test.go" internal/ examples/` | 全部存在（all verified） |

## 提交

- `daeb5643d` test(plugins): #106 十条边界应用边界汇总验收（套件+文档+
  矩阵，3 files changed, 906 insertions）[T20]

## Ruling（按任务书要求原样记录）

- **Ruling（模型路由）**：任务书要求按 gpt-5.6-sol/terra/luna 分级派发，
  但本 host 仅配置 GLM-5.3 系列模型，动态工作流亦不支持按子代理选模型。
  实际全部子代理运行在会话模型 GLM-5.3（最高推理档）上。此为记录的
  替代方案，不声称使用了不可用模型。
- Ruling B2 失败保旧观察点：远端已切 v2 后的成员目录会被漂移阻断（B3
  语义），失败保旧的正确断言面是安装行零改动（version/endpoint/快照）。
  若错，B2 与 B3 的语义边界会重叠。
- Ruling B9 迁移复述自建：存量 harness 缺陷在他人所有权文件
  （migration_pg_integration_test.go 属 T06），按"缺陷回所属切片修复
  不在本切片绕过"与"不改他人文件"双约束，B9 在本切片所有权内以正确
  顺序复述同一迁移契约；既有缺陷记 concern 转交主流程。若错，代价是
  复述与原测试存在覆盖面差异（复述只含手工行幸存+列删除断言，原测试
  还断言 up 形态与派生行清理——那部分由原测试承载，其修复属 T06）。

## Concerns（转交）

1. **存量缺陷（他人文件）**：`TestPluginInstallationsMigrationUpAndDown`
   的 harness 顺序颠倒——`pluginMigrationsOpenDB`（migration_pg_
   integration_test.go:111-119）先 apply 000190.up（内含 `ALTER TABLE
   mcp_services`，000190.up:32）后由测试体建最小父表，任何真 PG 运行
   必失败 `relation "mcp_services" does not exist`。T11 执行记录已发现
   并转交，至今未修（本任务真 PG 复现确认）。属 T06 所有权，建议主流程
   安排修复（把最小父表 DDL 前移到 apply 之前）。全量回归不带
   integration tag 不受影响。
2. **agent-editor.test.tsx 并发 flaky**（他人文件）：`pnpm gates` 高负载
   下超时中断（"test failed" 无断言详情），单独跑 49/49 过；T19 已记录
   同款。建议主流程统一门控时低负载重跑或排查该文件的超时预算。
3. **Mimosa**：本任务两次写入尝试被 Mimosa SQL 拼接检测拦截
   （`"CREATE SCHEMA " + schema` 形式），最终改为固定字面量 schema 名
   （`plugins_acc_t20` 常量，DDL 语句零拼接、数据语句参数绑定）绕开；
   commit 时 scanner_enobufs 未完成完整扫描（hook 兼容策略放行）。
   既有 `openPluginDB`/`pluginMigrationsOpenDB`（T11/T06 文件）仍是
   UnixNano 拼接形态，未被拦（历史文件）——记录差异，不改动他人文件。
4. **并行会话工作区隔离**：worktree 内存在他人未提交改动
   （manifest.go/package.json/run-gates.mjs/frontend.yml 等）与大量
   untracked ocr 文档，本任务未触碰；全量 `go test ./internal/...`
   在含这些改动的树上 exit 0。
