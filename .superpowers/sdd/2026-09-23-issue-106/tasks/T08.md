# T08 执行记录：前端——安装确认/插件管理面板 + 成员发现页

- 任务：Issue #110 计划 Task 8（`docs/plans/2026-09-23-issue-106-03-install-discover.md`）
- 执行日期：2026-09-25（本会话）；恢复检查 `git log --grep="\[T08\]"` 为 0 条——首次实施，非恢复。
- 工作目录：`.worktrees/issue106`（分支 `codex/issue-106-self-hosted-plugins`）。

## 做了什么

### 1. api-client 扩展（单一来源收敛，转交发现统一解法）

`packages/api-client/src/plugins.ts`：
- 新增 `PluginInstallation`/`PluginInstallationSummary`/`PluginInstallationTool` 视图类型与严格解析器
  `parsePluginInstallation`（详情 envelope：state∈{active,disabled}、drift_state∈{none,detected}、
  transport 枚举、`enabled *bool omitempty` 缺失键保持 `null` 不伪造成 false、Go nil scopes 折叠 `[]`）、
  `parsePluginInstallations`（列表 envelope，data 为数组；tool_count 非负整数校验）。
- `PluginsApi` 扩展四方法：`confirmInstallation(previewId)`（POST `/api/v1/plugins/installations` body
  `{preview_id}`）、`listInstallations()`（GET 同路径）、`getInstallation(id)`（GET `:id` encodeURIComponent）、
  `setInstallationState(id,'active'|'disabled')`（POST `:id/disable|enable`）。空 id/previewId 在发请求前拒绝。
- 响应契约直接对齐 T06/T07 真实产出（`internal/handler/dto/plugin.go` PluginInstallationResponse/.Summary、
  `internal/handler/plugin.go` confirm/state/get/list 的 `{success,data}` 包装）。

### 2. 管理面板完整化（删镜像）

`apps/web/src/settings/PluginsSettingsPanel.tsx`：
- **删除面板层镜像**：`PluginPreviewView`/`parsePluginPreviewEnvelope`/`pluginPreviewRequest`/PREVIEW_PATH/
  record/required/text/flag/scopes 全部辅助函数（~90 行），改为深路径导入
  `../../../../packages/api-client/src/plugins.ts` 复用 `createPluginsApi`/`parsePluginPreview`（生产深路径
  导入先例 SandboxSettingsPanel.tsx:34；T03-OCR1-F1 parity 锁测试随之删除——单一来源后无漂移面）。
- 预览流程改走 `pluginsApi.previewInstallation(url)`；错误分类不变（ApiError 原文透传、其余统一中文 +
  console.warn）。
- 预览卡新增「确认安装」按钮（admin）：消费 `preview.previewId` → confirmInstallation → 撤卡 +
  刷新已安装列表。
- 新增「已安装插件」列表（挂载即 GET）：名称/plugin_id/版本徽标/状态徽标（启用中/已停用）/漂移徽标
  （无漂移/检测到漂移）/需个人授权/工具数；admin 行内停用/启用按钮（POST disable/enable 后刷新）；
  viewer 只读（无提交表单、无确认安装、无停用/启用按钮）。
- **卸载范围明示**：全文无「卸载」「删除插件」入口与文案（面板测试断言）；列表区描述明示
  「插件治理以停用为终点：停用后 Agent 不再调用该插件，成员连接与审计记录保留」（rulings.md R4 口径）。

### 3. 成员发现页（新建）

`apps/web/src/integrations/PluginsPanel.tsx`：
- props `{ client, initialInstallations? }`：SSR 直出 initialInstallations，挂载后经
  `createPluginsApi(client.request).listInstallations()` 刷新（GET Viewer+）。
- **只读发现面**：零按钮（测试断言 `<button` 计数 0）、不停用/不启用/不确认安装，全文不含治理动词——
  状态徽标因此用「可用/不可用」而非管理侧「启用中/已停用」；无卸载/删除文案。
- 空态「暂无已安装插件」；错误态 ApiError 原文/统一中文（console.warn 留痕）。

### 4. registry 注册

`packages/views/src/integrations/registry.ts`：`IntegrationKey` 追加 `'plugins'`；`INTEGRATION_SECTIONS`
追加 `{ key: 'plugins', viewId: 'PluginDiscoverPanel', apiDomain: null, minRole: 'viewer', external: false,
operations: ['manage'] }`（不设 capability，随 integrations 页默认开放）——与 Brief 逐字段一致。

### 5. 机械连带（Brief 未列但为计划改动的必然结果）

- `packages/views/src/integrations/registry.test.ts`：穷举 keys deepEqual 断言追加 `'plugins'`，并新增
  plugins section 字段断言测试。
- `packages/views/src/integrations/view.ts:32/41`：`Record<IntegrationKey, string>` 的 HEADING_KEYS/
  DESCRIPTION_KEYS 补 `'plugins'` 条目——IntegrationKey 联合扩员后 typecheck:shared 报 TS2741 的唯二
  错误；新 key（`integrations.plugins.title/subtitle`）当前无消费方（page.test.tsx 五 locale 断言只遍历
  六个旧 key；plugins tab 的页面接线属后续任务）。

## TDD 证据（RED → GREEN）

RED（先写测试后实现，逐文件确认失败原因）：
- `node --import tsx --test packages/api-client/src/plugins.test.ts` → FAIL：
  `SyntaxError: The requested module './plugins.ts' does not provide an export named 'parsePluginInstallation'`。
- `node --import tsx --test src/settings/PluginsSettingsPanel.test.tsx`（apps/web）→ FAIL：
  确认安装/列表功能缺失（viewer 无列表发现 AssertionError）。
- `node --import tsx --test src/integrations/PluginsPanel.test.tsx` → FAIL：组件文件不存在。
- `node --import tsx --test packages/views/src/integrations/registry.test.ts` → FAIL：
  `actual: undefined, expected: 'PluginDiscoverPanel'`。

GREEN（聚焦复跑，全部真实运行于 node v26.4.0，PATH 前置见 environment.md §3）：
- `packages/api-client/src/plugins.test.ts`：**17/17 pass**（预览 8 + 安装 envelope/四方法 9）。
- `apps/web/src/settings/PluginsSettingsPanel.test.tsx`：**12/12 pass**（含确认安装消费 preview_id、
  停用调用 disable 端点且徽标翻转、viewer 只读、无卸载/删除文案 4 个新交互测试）。
- `apps/web/src/integrations/PluginsPanel.test.tsx`：**5/5 pass**（SSR 直出、无管理操作、无卸载文案、
  空态、挂载拉取恰好一次 GET）。
- `packages/views/src/integrations/registry.test.ts`：**4/4 pass**。
- `pnpm run typecheck:shared`：exit 0（view.ts 连带补齐后）。

## pnpm gates 结果（如实）

`pnpm gates` 后台全程运行（本机 load average 持续 19-23，多个外部会话并发跑测试——含另一 worktree
issue30-sweep-t40 的测试进程与 tsc）：

| 阶段 | 结果 |
| --- | --- |
| test:shared | **FAIL（环境性）**：999 tests，997 pass，唯一失败 `packages/domain/src/mobile/compatibility.test.ts:322`「performance harness: authoritative state restored within the frozen window (5s)」——性能断言 best pass 5757.96ms > 5000ms 窗口。该文件不在 T08 diff 内、无未提交改动、与 T08 改动零 import 关联；失败发生于机器高负载（gates 自身多进程 + 外部会话）。单独复跑（仍处高负载）同样以 5.76s 超窗失败。 |
| typecheck:shared | **PASS**（120.6s） |
| test:web | 进行中 >3.5h 未完成：已完成部分 143+ tests 全 ✔、零失败；被 `apps/web/src/agents/agent-editor.test.tsx`（1594 行，tsx 无缓存转译 tdesign-react 大库）单文件拖住——该进程 111min CPU 仍活跃推进，与 T08 改动零 import 交集（已核实其 import 面仅 AgentEditorModal/agent-editor-resources/agent-type-presets）。 |
| typecheck:web / check:integrity / build:web | gates 串行未到达（test:web 未结束）。 |

T08 所有权内四个测试文件均已聚焦跑全绿（见上）；gates 的环境性失败项不涉及 T08 文件。建议主流程在
低负载时段统一复跑 `pnpm gates` 收全量门控。

## Ruling（按任务书要求记录）

**Ruling（模型路由，原文转记）**：任务书要求按 gpt-5.6-sol/terra/luna 分级派发，但本 host 仅配置
GLM-5.3 系列模型，动态工作流亦不支持按子代理选模型。实际全部子代理运行在会话模型 GLM-5.3（最高推理档）
上。此为记录的替代方案，不声称使用了不可用模型。

**Ruling（registry 机械连带）**：Brief 文件清单未列 `registry.test.ts` 与 `view.ts`，但 registry.ts 的
`IntegrationKey` 扩员使前者的穷举断言必然失败、后者的两处 `Record<IntegrationKey, string>` 必然报
TS2741（typecheck:shared 实测唯二错误）——两者均为计划强制改动的编译/测试级机械连带，按最小改动更新
（断言数组加 `'plugins'`、两 Record 补语义 key），不改变任何行为逻辑。判断错误的代价：若最终不接受
registry 扩员，revert registry.ts 即一并还原这两处。

## 转交发现处理（上游 OCR，全部为本任务所有权文件）

- **T06-OCR3-F1 / T03-OCR1-F1 / T07-OCR1-F1 / T09-OCR1-F3**（四条同源：面板镜像解析器 vs
  `packages/api-client/src/plugins.ts` 的双份实现漂移面）：统一解法——**删除面板侧全部镜像实现**，
  面板与成员页均深路径导入 api-client 单一来源（`createPluginsApi`/`parsePluginPreview`），T03 的
  parity 锁测试随之删除（其注释已预告「T08 挂载后应删除镜像并撤掉本测试」）；`PREVIEW_PATH` 与
  transport 白名单随之单份化（面板不再持有副本）。
- 备注：转交发现建议的另一路径「index.ts 导出 plugins 域」——`packages/api-client/src/index.ts` 与
  `client.ts` 不在 T08 所有权清单内（T03 面板注释与 index.ts 现状均表明挂载归后续拥有 client.ts/index.ts
  的切片，计划 Produces 亦只要求 `client.plugins.*` 由 T08 提供 API 构造能力）。深路径导入已消除全部四条
  finding 的漂移面；index.ts 正式挂载（`client.plugins`）留给该后续切片，此处记录边界。

## 疑虑 / Concerns

1. **gates 未完整跑完**（test:web 被与 T08 无关的 agent-editor.test.tsx 拖住 >3.5h，环境 load ~20；
   test:shared 唯一失败为 mobile 性能断言的环境性超窗）——T08 所有权内测试全部聚焦跑通过，全量门控
   建议主流程低负载时复跑。
2. **plugins section 的页面接线未完成**：registry 注册后，settings 抽屉会出现 `integration-plugins`
   导航项（SettingsPage.tsx:629 遍历 INTEGRATION_SECTIONS 生成），点击渲染 IntegrationsRoutePage 的
   plugins tab——views 版 page.tsx 对该 tab 仅渲染 heading（copy key 未迁入 i18n 词条时显示原文 key）；
   `apps/web` 侧 `route.ts` 的 integrationTabs（他人所有权）不含 plugins，PluginsPanel 组件已就绪但
   未挂载进 IntegrationsPage。接线（route.ts/i18n 词条/page.tsx 分支）超出 T08 文件所有权，转交主流程
   （T12 成员授权入口或专项接线任务）。
3. worktree 内存在其他任务的未提交改动（internal/、package.json、scripts/ 等），提交时逐一指定路径，
   未触碰他人文件。

## 提交

- `<SHA1>` feat(plugins): 安装确认/插件管理面板与成员发现页——api-client 四方法与安装 envelope 严格
  解析器、管理面板删镜像深路径复用单一来源、成员页只读无管理操作、registry 注册 plugins section [T08]
