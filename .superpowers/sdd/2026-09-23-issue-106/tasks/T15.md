# T15 执行记录：前端差异预览 UI（Issue #114）

## 做了什么

按 Brief（`briefs/T15.md`）与切片计划 Task 15，以 TDD（RED→GREEN）完成：

1. **api-client（`packages/api-client/src/plugins.ts`）**
   - 新类型：`PluginUpgradeToolSnapshot` / `PluginUpgradeToolChange` / `PluginVersionDiff` / `PluginUpgradePreview`（对齐 T14 `dto.PluginUpgradePreviewResponse` wire JSON，camelCase 映射）。
   - `parsePluginUpgradePreview(value)`：严格 envelope 解析——success 校验、diff 五维逐字段校验（plugin_id/current_version/candidate_version/is_downgrade/endpoint_changed/current_endpoint/candidate_endpoint/added_tools/removed_tools/changed_tools）、快照行逐字段校验（name/description/input_schema_digest/read_only/requires_personal_auth/scopes，null scopes 折叠为 `[]`，Go nil slice 先例）、changed 行四个变更理由布尔（schema/scope/read_write_class/personal_auth）独立校验 + current/candidate 快照、候选指纹与工具目录摘要。列表字段强制数组（handler 层 `make([]…, 0, …)` 保证非 nil）。
   - `PluginsApi.previewUpgrade(installationId, signal?)`：bodyless `POST /api/v1/plugins/installations/:id/upgrade-preview`，空 id 本地拒绝（先例 getInstallation）。
2. **管理端面板（`apps/web/src/settings/PluginsSettingsPanel.tsx`）**
   - 安装行新增「检查升级」按钮（`canEdit` 即 admin+；viewer 不可见）→ `previewUpgrade` 只读预览；状态与停用/启用互不影响。
   - 差异面板（`data-testid="plugin-upgrade-preview"`）五维全可见：版本对（current → candidate）+ **降级徽标**（is_downgrade → warn 色「降级」，否则 info 色「升级」）；端点变化行（「端点已变化/未变化」+ 双端点对照）；新增/移除工具表（`DiffToolTable`：工具/说明/分类/授权/Scopes）；变更工具表 + **四个独立变更理由徽标**（「schema 变更」「scope 变更」「读写分类变更」「授权面变更」，仅渲染命中维度）+ 现版/候选版快照对照（scopes·只读/写入·授权面）；候选身份指纹与工具目录摘要。面板含「关闭」按钮，只读说明文案（不提供接受入口——接受升级属 T16）。
   - 错误条（`data-testid="plugin-upgrade-error"`）：`升级预览失败（插件名）：message`——ApiError 原文透传（后端 503 不可达文案）、非 ApiError 统一中文 + console.warn 留痕；前缀只由渲染层拼一次（防 T08-OCR1-F3 同类重复）。安装列表零变形（列表行/版本徽标/停用按钮/空态逻辑全部不动）。
3. **测试**
   - `plugins.test.ts` +5：五维字段映射（含降级、null scopes）、非 success/缺字段/畸形字段拒绝矩阵、`previewUpgrade` 请求形状（POST/编码 id/无 body）、空 id 本地拒绝。
   - `PluginsSettingsPanel.test.tsx` +6：检查升级按钮→面板五维+降级标注、升级方向无降级徽标、候选不可达 ApiError 错误条+列表不变形、非 ApiError 统一文案+console.warn+防重复前缀、viewer 无按钮、（间接）预览不切换已接受版本。

## 测试证据（真实运行）

- RED（实现前）：
  - `pnpm exec tsx --test packages/api-client/src/plugins.test.ts` → **fail 1**：`SyntaxError: The requested module './plugins.ts' does not provide an export named 'parsePluginUpgradePreview'`（失败原因=实现未存在）。
  - `node --import tsx --test 'src/settings/PluginsSettingsPanel.test.tsx'`（apps/web 下）→ 新增 6 例全 fail 于 `assert.ok(checkButton)` actual null（按钮未实现）。
- GREEN（实现后，单文件）：
  - `pnpm exec tsx --test packages/api-client/src/plugins.test.ts` → **tests 21 / pass 21 / fail 0**（含提交前对「裁剪版=提交版」的复核）。
  - apps/web `node --import tsx --test 'src/settings/PluginsSettingsPanel.test.tsx'` → **tests 19 / pass 19 / fail 0**。
- `pnpm gates`（全量）：**未能全过，非本任务代码原因**（详见疑虑 1）——其中 typecheck:shared **PASS**（33.8s）；test:shared FAIL 仅因并行 T12 会话在共享文件 `plugins.test.ts` 写入悬空 import（`parsePluginMyConnection` 无对应导出）；typecheck:web 独立复跑仅有 2 个 TS 错误且**全部位于 `src/integrations/PluginsPanel.test.tsx`（T12 会话工作中的文件）**，本任务四个文件零错误；test:web 因机器上 3 个并发 agent-editor 测试进程（其中一个已占 CPU 200+ 分钟，属其他会话）拖慢至本记录时点仍未完成。

## 提交

- `f7ac0100d` feat(plugins): 管理端候选版本差异预览面板——api-client previewUpgrade/parsePluginUpgradePreview 五维严格解析（版本对/降级标注/端点变化/新增/移除/变更工具与 schema·scope·读写·授权四理由徽标），安装行检查升级按钮只读预览，候选不可达错误条不破坏安装列表 [T15]（4 files，+698/−2，仅本任务所有权 4 文件）
- `025f78fc6` docs(sdd): T15 执行记录 [T15]
- `3abf1b6dc` fix(plugins): T15-OCR1 修复轮 [T15]（4 files，+114/−12，详见下节）

## OCR 增量轮（docs/plans/issue-106-ocr-T15.md，6 findings）

分流与处置：

- **T15-OCR1-F3（medium，已修复）**：其他安装行预览进行中「检查升级」按钮无禁用反馈、点击被 `upgradeBusyId !== null` 静默吞掉。修复：非当前行补 `disabled={upgradeBusyId !== null && upgradeBusyId !== item.installationId}`（packages/ui Button 已支持 disabled，`Button.tsx:48` `disabled={disabled || loading}`）。回归测试「其他行升级预览进行中时检查升级按钮呈现禁用态且完成后恢复」：gate 挂起 inst-1 预览 → 断言两行按钮均 disabled、禁用行点击零请求、resolve 后恢复可点且面板渲染。
- **T15-OCR1-F1（low，已修复）**：`inputSchemaDigest` 由 `optionalText` 收紧为 `required`（digest 是 schema_changed 徽标判定依据，后端两处保证非空 64-hex；与模块「拒绝任何缺失/畸形字段」契约一致）。回归测试：空串 digest 拒绝。
- **T15-OCR1-F2（low，已修复）**：changedTools 解析块 8 处 `${UPGRADE_PREVIEW_PATH}.data.diff.changed_tools[${index}]` 前缀提取局部常量 `entry`（纯可读性，行为由既有 26 测试覆盖）。
- **T15-OCR1-F4（low，已修复，取自洽子集）**：与 F6 存在张力（F4 建议用 `installationId` 做面板存在性兜底，F6 建议删该死字段）。裁决取自洽组合：`toggleState` 成功分支同步 `setUpgradeError(null)`（横幅清理与 listError 一致，陈旧升级错误条不再悬挂）；不引入存在性兜底（本版无卸载入口，installationId 不会从列表消失，兜底无消费场景）。回归测试：「停用成功后升级预览错误条同步清除」。
- **T15-OCR1-F5（low，已修复）**：删 `UpgradePreviewState.installationId` 死字段（面板仅消费 pluginName/result；T16 真正需要定位来源安装时再引入）。
- **T15-OCR1-F6（low，不修，纯风格重构）**：DiffToolTable/变更工具表/预览卡工具表三处表壳同构（overflow-x-auto + h4 标题 + table 类名）。OCR-REJECT: T15-OCR1-F6 非缺陷而是可选重构——三表列定义各异（5 列预览表 ×2、4 列变更表），提取共享表壳收益有限且触碰 T03 既有预览卡表格的稳定结构，纯风格调整不构成缺陷，留待后续统一表格组件化时处理。

OCR 轮测试证据（真实运行）：

- RED（修复前）：`pnpm exec tsx --test packages/api-client/src/plugins.test.ts` → tests 26 / pass 25 / **fail 1**（空 digest 测试：optionalText 静默接受空串）；apps/web `node --import tsx --test 'src/settings/PluginsSettingsPanel.test.tsx'` → tests 21 / pass 19 / **fail 2**（禁用态断言 actual false / 错误条清除断言失败）。
- GREEN（修复后）：api-client 单文件 **26/26 全绿**（含 T12 的 getMyConnection 测试）；面板单文件 **21/21 全绿**；`pnpm --filter @weknora/web exec tsc -p tsconfig.json --noEmit` → **0 错误**（T12 完成后全 apps/web 类型面亦干净）。
- 全量 `pnpm gates`（T12 完成后启动，日志 /tmp/t15-gates-ocr.log）：**test:shared PASS（42.2s）**、**typecheck:shared PASS（8.2s）**；test:web 因环境级并发竞争两轮均未完成（本轮 25+ 分钟、上轮 45+ 分钟卡在同一非本任务文件区域 agent-editor/GeneralPreferencesPanel——机器上 4 个并发测试进程互相竞争 CPU，其中个别已占 50+ 分钟 CPU，属其他会话/环境问题）；其余 gate 独立补齐验证：**typecheck:web 0 错误**、**check:integrity PASS（0 P0）**、**build:web EXIT=0（48.06s）**。面板测试以单文件 21/21 作为 test:web 所有权面的替代证据（如实报告：test:web 整体未在本任务内完成，非本任务代码失败——无任何指向本任务文件的失败输出）。

## OCR2 增量轮（docs/plans/issue-106-ocr-T15.md 更新为 10 findings）

任务书转发的 4 条（T15-OCR2-F2 high / F3 / F4 / F9 medium）**全部位于 `apps/web/src/integrations/PluginsPanel.tsx`**——`git log` 证实该文件仅有 T12（e81eb3b25）与 T08（2e2413f0a）两次提交，属 T12 切片代码；任务书 evidence 亦逐条自标「属 T12 代码，标注归属」。该文件不在本任务 Brief 所有权（`packages/api-client/src/plugins.ts(+test)`、`apps/web/src/settings/PluginsSettingsPanel.tsx(+test)`）内，**未做任何修改**，全部转交（详见提交 TaskResult concerns 的 OCR-HANDOFF 声明）。

报告其余 low 条目处置：

- **plugins.ts:488 CONNECTION_PATH（low，我所有权内，已修复，提交 7d978659e）**：诊断前缀由 `/api/v1/plugins/installations/connections/me` 改为 `` `${INSTALLATIONS_PATH}/:id/connections/me` ``，对齐 UPGRADE_PREVIEW_PATH 的 `:id` 占位约定（真实请求含 installationId 段）。TDD：新增断言 `/installations\/:id\/connections\/me/`（RED：旧前缀不匹配 fail 1 → GREEN：27/27）；T12 既有 `/connections\/me/` 断言不受影响。
- **plugins.ts:481-485 doc 注释与实际用法不符（low，我所有权内，已修复，同提交）**：接口 doc 原称「the panel drives authorize/revoke through those existing endpoints」——实际面板按 serviceId 走既有 mcp-oauth 客户端方法，不消费下发路径。doc 修正为「authorizeUrlPath/revokePath 仅为 envelope 完整性与诊断保留」，两字段注释补「(diagnostics only)」。
- **plugintest/server.go:529-540 错误判定顺序漂移（low）**：`internal/modules/plugins/plugintest/` 非 T15 所有权（T04/T05/T10/T11 域）——转交，不修改。
- **PluginsPanel.tsx:196-198 pending 单飞禁用 / :227-232 嵌套三元 / :132-135 OAuth 回调路径三处硬编码（low ×3）**：同属 T12/T08 文件——转交，不修改（:196 的修复口径可参照本任务 3abf1b6dc 在 PluginsSettingsPanel 已落地的跨行互斥 disabled 先例）。

OCR2 轮测试证据（真实运行）：RED `pnpm exec tsx --test packages/api-client/src/plugins.test.ts` → 27/26/**fail 1**（前缀断言）；GREEN 同命令 → **27/27 全绿**；面板单文件 `node --import tsx --test 'src/settings/PluginsSettingsPanel.test.tsx'` → **21/21 全绿**（确认无回归）。

## OCR 轮全量 gates 最终结果（后台运行 3.4h 后完成，日志 /tmp/t15-gates-ocr.log）

`pnpm gates` 整体 **EXIT=1**，六 gate 明细：test:shared **PASS**(42.2s) / typecheck:shared **PASS**(8.2s) / **test:web FAIL**(12228.8s) / typecheck:web **PASS**(26.1s) / check:integrity **PASS**(1.5s) / build:web **PASS**(52.7s)。此前记录的「test:web 未完成」更新为「已完成但 FAIL」。

test:web 仅有的两个失败**均非本任务文件**，且本任务面板测试在全量运行中全部通过（日志 ✔ 行：五维差异面板/不可达错误条/禁用态/错误条清除等 13 条插件面板测试）：

1. `src/agents/agent-editor.test.tsx` 文件级失败（"Promise resolution is still pending but the event loop has already resolved"，单文件跑 12227s≈3.4h）——本会话两轮 gates 观察到该文件在多会话并发测试进程竞争下始终无法完成（多个 50-200+ 分钟 CPU 的同文件进程），agents 域文件，非本任务所有权，转交。
2. `src/settings/settings-error-ux.test.tsx:164`（"models: localized load-failed toast"，断言 `null !== '加载模型列表失败'`）——单独复跑（HEAD 状态、无未提交 web/packages 改动）稳定复现（15 tests / 14 pass / 1 fail，同断言），非瞬时抖动。归属分析：失败链路（settings-error-ux.test.tsx / SettingsPage.tsx models toast 段 :244-250 / model-settings.ts 词条 / settings-toast.tsx）在 **T15 全部提交（f7ac0100d/3abf1b6dc/7d978659e）中零改动**（T15 只碰 plugins.ts+test 与 PluginsSettingsPanel+test）；该测试文件自 09-22（d20ee55c9）未变；SettingsPage 最后一次改动是 T03（8448cdef1，09-24）且其 diff 不含 toast 路径（仅加 plugins 面板挂载/导航/文案）。引入时点在本 issue 之外或 T03 前后的某次改动，定位需临时 worktree+完整 install 的历史二分（机器当前负载下成本过高，未执行）。该文件非本任务所有权，**转交**。

## 疑虑 / 转交

1. **并行 T12 共享文件冲突（concern，非本任务缺陷；OCR 轮时已解除）**：T12 会话与本任务同时声明 `packages/api-client/src/plugins.ts`/`plugins.test.ts` 所有权（总索引「前端共享面分工」未覆盖 api-client 这对文件）。T12 于本会话进行中向 `plugins.test.ts` 写入其 RED 测试（import + 4 个 `parsePluginMyConnection`/`getMyConnection` 测试，92 行），实现未落盘 → 共享套件 test:shared 必红直至 T12 完成。本任务提交采用 hunk 级隔离：暂存前临时裁掉 T12 的 92 行（裁剪版 21/21 全绿后 add+commit），提交后已将 T12 的行**逐字恢复**回工作区（`git diff` 复核仅剩该 92 行未提交）。**后续：T12 已以 e81eb3b25 完成提交（实现落盘），test:shared 阻碍消除，OCR 轮已启动正式全量 gates（结果补记于 OCR 轮节）。**
2. RED 阶段曾按要求后台启动全量 `pnpm gates`，其在运行中被本任务实现覆盖（测试文件即时加载）失去 RED 代表性，已停止并以单文件 RED 证据为准（如实记录）。
3. 全量 `pnpm gates` 的 test:web/check:integrity/build:web 在本记录时点未完成（CPU 被并发会话竞争）；本任务以单文件 21/21 + 19/19、typecheck:shared PASS、typecheck:web 本任务文件零错误作为所有权面验证证据。

## Ruling（模型路由，任务书要求原样记录）

> Ruling（模型路由）：任务书要求按 gpt-5.6-sol/terra/luna 分级派发，但本 host 仅配置 GLM-5.3 系列模型，动态工作流亦不支持按子代理选模型。实际全部子代理运行在会话模型 GLM-5.3（最高推理档）上。此为记录的替代方案，不声称使用了不可用模型。

另：本任务未派发任何子代理/Reviewer（任务书要求），全部实现与自审在本会话完成。
