# T16 执行记录：接受升级服务与对话验证

- 任务：plan 08 Task 16（Issue #115）
- 提交：`92a5a83c4 feat(plugins): 管理员接受插件升级（…）[T16]`（单提交，8 文件，+1026/-2）
- 开始前恢复语义核查：`git log --oneline -F --grep="[T16]"` 为空、`tasks/T16.md` 不存在 → 首次实施（注：`--grep="[T16]"` 不加 `-F` 时是字符集正则，会误匹配 [T12]/[T15] 等，已用 `-F` 复核）。

## 做了什么

1. **Step 1-2（RED）**：新建 `internal/modules/plugins/upgrade_accept_test.go`（`package plugins_test`，import plugintest 外部测试包约定）——三个用例：
   - `TestAcceptUpgradeSwitchesSnapshotAndEndpoint`：安装行切换（版本/端点/digest 重算/快照/漂移 detected→none）、物化服务仅 URL 切换且 Name/ID 不变、策略行增量（新只读 `lookup` Enabled=true、新写 `create_issue` Enabled=false、既有 `search` 预置停用后保持）、closer 回收、运行时守卫目录切 v2。
   - `TestAcceptUpgradeIdempotentAndFingerprintGuard`：同指纹二次接受零写入（`acceptedWrites` 计数、安装行深比较、物化不 Update、策略行数不变）；远端前进 v3 后持旧指纹 → `ErrUpgradeCandidateChanged`（含 "candidate changed since preview"）且安装行/物化服务零变化。
   - `TestAcceptUpgradeFailureKeepsOldVersion`：清单 host 下线（零写入）、物化 Update 注入故障（正向+补偿 2 次写、全回 v1）、策略写注入故障（服务 URL 与安装行双双回 v1），守卫目录均保持 v1。
   - RED 证据：`AcceptUpgrade undefined`/`ErrUpgradeCandidateChanged undefined`（build failed，符合计划 Step 2 预期）。
2. **Step 3-4（GREEN）**：
   - `internal/types/interfaces/plugin.go`：`PluginService.AcceptUpgrade(ctx, tenantID, actorID, installationID, candidateFingerprint)`（返回 `*types.PluginInstallationResult`——见 Ruling 2）。
   - `internal/application/service/plugin_install_service.go`：`AcceptUpgrade` 实现——安装行查找 → 能力 seam 断言 → `FetchAndVerify(installation.ManifestURL)`（错误分类与 PreviewUpgrade 一致：抓取 503 链、OAuth 标记、核验 4xx）→ 身份守卫（镜像 T14-OCR1-F1）→ 指纹守卫 → 幂等四条件早退（版本+digest+端点相等 ∧ 服务 URL 同步 ∧ 无漂移；同步条款保证半补偿态可重试收敛）→ 补偿式写序：安装行（含漂移重置）→ 物化 URL 切换（UpdatedAt 刷新 + closer 回收）→ 策略行增量（`ListByService` 建既有集合，无行才写 `Enabled=ReadOnly`）→ 失败用 `context.WithoutCancel` 新 ctx 回写内存旧值（服务行先、安装行后）。
   - `internal/application/repository/plugin.go`：新增 `UpdateInstallationAccepted`（参数绑定 gorm Updates，含 drift_state/drift_detail 重置；零行更新返回 ErrRecordNotFound）——**非 Brief 所有权文件，见 Ruling 1**。
   - `internal/handler/dto/plugin.go`：`PluginUpgradeAcceptRequest{candidate_fingerprint binding:"required"}`。
   - `internal/handler/plugin.go`：`AcceptUpgrade` handler（godoc）+ `mapPluginInstallationError` 增 `ErrUpgradeCandidateChanged`→409、`ErrUpgradeWriterNotWired`→5xx。
   - `internal/router/routes_plugins.go`：`POST /installations/:id/upgrade-accept`（Admin，紧邻 upgrade-preview）。
   - 新哨兵：`ErrUpgradeCandidateChanged`、`ErrUpgradeWriterNotWired`；新能力 seam：未导出 `installationUpgradeWriter`（服务文件内声明，gorm 仓储结构化实现）。
3. **Step 5-7（集成）**：新建 `internal/modules/plugins/plugin_upgrade_integration_test.go`（`//go:build integration`，`package plugins_test`）：
   - `TestUpgradeVisibleFromMemberConversation`：真 PG 全栈（真仓储/服务/manager + plugintest 受控远端，无 OAuth 噪声）；升级前成员会话 `discover→describe→call` v1_tool 成功；`SetTools` 热切 v2 + Version 前进 → PreviewUpgrade→AcceptUpgrade；同一成员 NEW 会话（重装 RegisterMCPTools + 生产守卫 `PluginSnapshotLookup`）：list_tools 只见 v2_tool、v2_tool 调用成功（真外呼）、v1_tool describe "tool is unavailable"。
   - `TestTwoSpacesDifferentVersions`：两空间各装同插件 v1（各自远端），仅空间 1 升 v2；安装行版本断言 2.0.0/1.0.0；空间 1 成员只见可调 v2_tool，空间 2 成员仍只见可调 v1_tool、v2_tool 不可用。
   - 无 DB RED 面已验证（blocked-env Fatal，绝不 Skip）；**偏差如实记录**：因 fake 层已先驱动实现至 GREEN，集成测试带 DB 首跑即 PASS（计划 Step 6 预期的「有 DB FAIL」发生在实现未完成的时序下，本会话时序压缩了 Step 1-4 在前）。

## 测试证据（全部真实运行于 worktree `.worktrees/issue106`）

| 命令 | 结果 |
| --- | --- |
| `go test ./internal/modules/plugins/ -run TestAcceptUpgrade -v -count=1` | **PASS**（3 顶层 + 5 子测试，ok 2.739s） |
| `PLUGIN_TEST_DATABASE_URL='postgres://…@127.0.0.1:63160/plugins_t16?sslmode=disable' go test ./internal/modules/plugins/ -tags integration -run 'TestUpgradeVisible\|TestTwoSpacesDifferentVersions' -v -count=1` | **PASS**（2 测试，ok 3.321s；一次性 docker PG 17-alpine 容器，测毕已 stop） |
| 同上命令无 DB 环境变量 | FAIL 于 `blocked-env: PLUGIN_TEST_DATABASE_URL required`（Step 6 预期面） |
| `go build ./...`（worktree 内） | **exit 0**（仅 ld 重复库告警，基线固有） |
| `go vet`（plugins/service/repository/handler/router/interfaces 六包） | **exit 0** |
| `go test ./internal/modules/plugins/ -timeout 300s` | **ok** |
| `go test ./internal/application/repository/ -run 'Plugin'` / `…/service/ -run 'Plugin'` | **ok / ok** |
| `go test ./internal/handler/ ./internal/router/ ./internal/types/interfaces/` | **ok / ok / ok** |

## 既有问题（非本任务造成，基线取证）

- `go test ./internal/application/service/` 与 `./internal/application/repository/` **全包运行会在 craft 域测试随机挂起直至 go test 超时**（挂点每次不同：TestLifecycleSweepRedrivesStrandedDeletingObject / TestCraftSessionPermissionChains / TestCraftSessionCreateRegistersTrpcSessionWithIdempotency / TestCraftScheduledRunsKeysetPagination 等，伴随 golang-migrate goroutine 泄漏堆栈）。用 `git worktree add --detach /tmp/t16-baseline-run HEAD`（fead82a50）复验：**基线全包同样超时失败** → HEAD 既有，与本任务 diff 无关（本任务仅触碰插件域文件；Plugin 作用域运行全过）。临时基线 worktree 已清理。

## Rulings（按任务书要求记录；请主流程并入 Ledger）

- **Ruling（模型路由）**：任务书要求按 gpt-5.6-sol/terra/luna 分级派发，但本 host 仅配置 GLM-5.3 系列模型，动态工作流亦不支持按子代理选模型。实际全部子代理运行在会话模型 GLM-5.3（最高推理档）上。此为记录的替代方案，不声称使用了不可用模型。
- **Ruling 1（越权最小增量）**：计划 Step 3 要求「先更新安装行（新快照/端点/版本）…失败时把安装行回写」，但 Brief 文件所有权清单未含 `internal/application/repository/plugin.go`，而仓储层此前**没有**任何能写 accepted_version/tools_snapshot 的方法——不动该文件则验收条件物理上不可能满足。裁定：对该文件做**单一纯新增**（`UpdateInstallationAccepted`，参数绑定、零行返回 ErrRecordNotFound），并选择服务侧未导出能力 seam（`installationUpgradeWriter` 类型断言）而非扩展 `interfaces.PluginRepository`——后者会连带迫使非所有权测试文件（install_service_test.go/upgrade_diff_test.go 的 T06 fake）改动。代价（若误）：T17 若需把该方法并入 PluginRepository 接口，需一次机械重构；git 历史可回退。
- **Ruling 2（返回类型）**：计划 Produces 写 `*dto.PluginInstallationResponse`，但 `interfaces/plugin.go` 既有 F2 裁决明确接口层不得依赖 handler 层（T14 同例）。服务契约取 `*types.PluginInstallationResult`，handler 映射到 DTO。代价（若误）：无——与 T06/T14 产物同构。
- **Ruling 3（409 语义）**：指纹不符映射 409 Conflict（远端状态相对预览已前移，修复路径是重新预览），区别于 confirm 域 ErrPreviewContentChanged 的 400（预览行自身 TTL 语义）。代价（若误）：前端若按 400 分支处理需对齐（T16 未含前端，api-client 的 acceptUpgrade 由后续前端任务补）。

## 疑虑 / Concerns

1. **越权文件**：`internal/application/repository/plugin.go` +1 方法（Ruling 1，最小增量，已在提交信息注明）。
2. **并发会话**：同 worktree 有其他会话正在进行 T01 OCR 与前端 gates 工作（工作区现存其未提交改动：manifest.go/manifest_test.go/package.json/scripts 等；期间还出现一次其主 checkout workbench 域的暂时性构建红，与本任务无关）。本任务提交只含上述 8 个文件。
3. **集成测试时序偏差**：见「做了什么」第 3 点——集成层未经历「实现缺失导致的 RED」，其失败面以 blocked-env 验证。
4. 既有 craft 域全包挂起（见上节）建议主流程在全量门控时知晓并按 environment.md「不新增失败」规则解释。

## Ruling 记录要求复核

本文件已把任务书要求的「Ruling（模型路由）」原样写入上文 Rulings 节。
