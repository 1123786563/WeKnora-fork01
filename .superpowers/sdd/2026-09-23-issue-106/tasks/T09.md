# T09 执行记录：RegisterMCPTools 快照守卫注入（B3 运行时核验基准）

- 日期：2026-09-24（R10 范围缺口修复轮）
- 分支/worktree：`codex/issue-106-self-hosted-plugins` @ `.worktrees/issue106`
- 提交：见 git log「范围缺口 R10 修复：Agent 目录按已接受快照过滤并拒绝漂移」[T09]
- 状态：完成（守卫注入 + 生产接线 + 手工兼容断言；测试先 RED 后 GREEN）

## 做了什么

按计划 Task 9（docs/plans/2026-09-23-issue-106-04-agent-call.md）：

1. **tools 层**（`mcp_tool.go`）：`PluginRuntimeSnapshot`/`PluginSnapshotProvider`
   /`ErrPluginDrift`/`FilterToolsBySnapshot`（导出，T17 漂移复用）；digest 口径
   与预览核验同源（`plugins.ToolSchemaDigest`，canonical JSON SHA-256）；
   `RegisterMCPTools` 第 9 参 `pluginGuard`——nil 或服务非插件物化时行为与
   现状一致（手工 MCP 兼容）；loader 闭包在 loadMCPDirectory 返回后过滤：
   守卫错误 fail-closed、快照外工具剔除、快照内 digest 不符 → ErrPluginDrift。
2. **仓储**：`PluginRepository.GetByServiceID`（参数绑定
   `WHERE tenant_id = ? AND service_id = ?`；空 service_id 短路）。
3. **生产 provider**：`service.PluginSnapshotLookup(repo)`——未命中 (nil,nil)
   =非插件服务；仓储故障透传为 error（守卫侧 fail-closed）。
4. **agent 接线**：agentService 增 pluginRepo 字段（NewAgentService 追加 1 参，
   容器 dig 自动解析——container 测试过）；registerMCPTools 注入
   `s.pluginSnapshotGuard()`（repo 为 nil 时返回 nil，测试组装保持现状）。
5. **既有调用点**：8 处测试 RegisterMCPTools 调用补 nil 参数。

## 测试证据（全部真实运行）

- RED：`go vet ./internal/modules/agentruntime/agent/tools/` →
  `undefined: PluginRuntimeSnapshot`（编译期 RED）。
- 新测试（`tools/mcp_plugin_guard_test.go` + repo 层 1 个）：
  - TestFilterToolsBySnapshot：快照外剔除/digest 不符 ErrPluginDrift（含
    工具名回显）/空目录非错误——漂移语义的原文断言在纯函数层。
  - TestRegisterMCPToolsFiltersPluginToolsToAcceptedSnapshot：受控服务 2 工具、
    快照 1 个 → list_tools 恰见快照内工具（schema 用 NewToolWithRawSchema
    钉死字节，digest 按构造一致）。
  - TestRegisterMCPToolsRejectsSchemaDrift / TestRegisterMCPToolsGuardErrorFailsClosed：
    控制孪生（同 server 双服务行，control 用匹配快照先证明连通性）+ 故障行
    fail-closed（catalog 既有设计将 loader 错误折叠为确定性 error 态消息，
    对外断言该消息；空洞断言已用控制孪生排除）。
  - TestRegisterMCPToolsNilGuardKeepsLegacyBehavior：nil guard 与 guard 返回
    (nil,nil)（手工服务）全工具可见。
  - TestGetByServiceIDScopesByTenant：同租户命中/跨租户 nil/手工无行 nil/
    空 service_id 短路。
- 门控（-count=1）：`go test ./internal/modules/agentruntime/...`（agent 6.3s、
  tools 37s、approval/native/skills 等 18 包 ok）；`./internal/modules/airesource/mcp/` ok；
  `./internal/application/service/` ok（471s）；`./internal/application/repository/`
  单独重跑 ok（437s，15m timeout 下；首轮并行 ~20 包争用触 10m 默认超时假红，
  单独复跑通过）；`./internal/modules/agentruntime/agent/opencode/` 单独重跑
  ok（23s，确证假红与本轮无关）；`./internal/container/`、`./internal/types/...`、
  `./internal/handler/`、`./internal/modules/plugins/` ok。
- `go build ./...` exit 0；本轮改动文件 gofmt 无输出。

## 实现偏差与发现

- 首版测试用 `sdkmcp.NewTool(name, WithRawInputSchema(...))` → mcp-go 运行时
  报 "both InputSchema and RawInputSchema set"（NewTool 预填 InputSchema 与
  raw 选项冲突）；改 `NewToolWithRawSchema` 钉死 schema 字节。
- 首版 drift/guard-error 断言漂移原文，实际 catalog（mcp_catalog.go:293-306）
  把 loader 错误折叠为 "MCP server %q is error; retry discovery..."——既有
  错误面设计（确定性、不泄漏底层文本）。改为：漂移原文断言留在纯函数测试，
  集成面用控制孪生 + fail-closed 消息断言（连接失败假阳性被孪生排除）。

---

# T09 增量执行记录：转交发现修复轮（OCR T01-OCR1-F4/F5/F6、T01-OCR2-F9、T04-OCR1-F10、T06-OCR1-F3 + 收录 R12-C F20）

- 日期：2026-09-25
- 分支/worktree：同上
- 提交：`938bb6096` fix(plugins): 快照守卫增量修复——描述漂移同判 ErrPluginDrift/插件目录绕缓存直核服务器实况/过滤前置落库不再持久化未过滤全量/守卫失败点服务端审计日志 [T09]
  （既有提交：`26b0a3b43` 主实现 + `c566f0473` fix-plans 回填，恢复语义核实后不重复实施）
- 状态：完成（TDD RED→GREEN；任务书三条门控命令全过）

## 做了什么（全部在 T09 所有权文件内：mcp_tool.go + mcp_plugin_guard_test.go）

1. **F4/F10（描述漂移）**：`FilterToolsBySnapshot` 在 schema digest 之后新增
   `def.Description != tool.Description` → `ErrPluginDrift: tool %q description changed`；
   描述进入 Agent 可见工具元数据面，安装后改写即提示注入通道。
2. **F9（缓存非实况，ErrPluginDrift 不可达）**：loader 闭包重构——guard 查询
   提前到任何目录加载之前；插件物化服务（snap 非 nil）改走新函数
   `loadPluginDirectory`：**绕过 metadata.Get 缓存**（持久化目录是缓存不是
   运行时核验基准，每次 live 列举后核验），漂移在常规路径真实可达。
   手工服务继续走 `loadMCPDirectory`（签名与行为零改动，手工兼容逐字节保持）。
3. **F6 前半（过滤时机晚于持久化）**：过滤移入 `loadPluginDirectory` 且在
   `metadata.Put` **之前**——落库目录永远是过滤后版本，不再持久化未过滤
   live 全量目录。
4. **F5/F3（守卫失败零服务端痕迹）**：三类失败点（provider 错误 / 漂移 /
   孤儿插件服务）在检测点 `logger.GetLogger(loadCtx).Errorf` 落审计日志，
   带 service ID、tenant、installation ID、工具名/底层错误——catalog 对外
   折叠为通用 error 态消息后，此日志是管理员可见的审计痕迹。
5. **R12-C F20（孤儿插件服务）**：收录上一会话已写但未提交的修复——guard
   查无安装行且 `PluginInstallationID != nil` 时 fail-closed（(nil,nil) 只对
   manual NULL 行成立），本轮为其补审计日志。

## TDD 证据（全部真实运行）

- **RED**（实现前）：`go test ./internal/modules/agentruntime/agent/tools/ -run 'TestFilterToolsBySnapshot|TestRegisterMCPToolsRejectsDescriptionDrift|TestRegisterMCPToolsPluginServiceLiveVerifiesAndPersistsFiltered|TestRegisterMCPToolsLogsDriftAndGuardFailures|TestRegisterMCPToolsRejectsSchemaDrift|TestRegisterMCPToolsFiltersPluginToolsToAcceptedSnapshot' -v`
  → FAIL，且失败原因与转交发现一致：
  - `TestRegisterMCPToolsRejectsDescriptionDrift`：描述漂移当前**放行**（发现成功）；
  - `TestRegisterMCPToolsPluginServiceLiveVerifiesAndPersistsFiltered`：
    `Should be zero, but was 1`——插件服务吃了 metadata 缓存（F9 复现）；
  - `TestRegisterMCPToolsLogsDriftAndGuardFailures`：漂移行放行（同 F4）。
- **GREEN**：同命令 → 全 PASS（新 3 测试 + 既有 6 守卫测试）。

## 测试证据（全部真实运行，-count=1 门控）

- 任务书命令 1：`go test ./internal/modules/agentruntime/agent/tools/ -run 'TestRegisterMCPTools|TestFilterToolsBySnapshot|TestMCPProxy' -v`
  → 10 测试全 PASS（含 TestMCPProxyHTTPApprovalArgumentsAndImages），exit 0。
- 任务书命令 2：`go test ./internal/modules/airesource/mcp/ -count=1` → ok，exit 0。
- 任务书命令 3：`go build ./...` → exit 0（仅既有 ld duplicate '-lc++' 告警，
  与 environment.md 基线一致）。注：一次命令链内瞬时失败（stderr 被丢弃），
  单独重跑 exit 0，疑为并行会话文件瞬时状态，与本轮改动无关。
- 全量回归：`go test ./internal/modules/agentruntime/... -count=1` → 20 包全
  ok、0 FAIL、exit 0（日志 /tmp/t09-agentruntime.log）。
- `gofmt -l` 改动文件无输出。

## 新增测试清单（mcp_plugin_guard_test.go）

- `TestRegisterMCPToolsRejectsDescriptionDrift`：控制孪生——schema 一致、
  描述不符 → fail-closed（F4/F10）。
- `TestRegisterMCPToolsPluginServiceLiveVerifiesAndPersistsFiltered`：污染缓存
  stub（Get 返回含 extra_tool 的目录）→ 插件服务 Get 0 次、外呼>0、只见快照
  内工具、Put 恰收 [snapshot_tool]；manual 服务保持缓存语义（原样返回、零
  新增外呼）（F9+F6）。
- `TestRegisterMCPToolsLogsDriftAndGuardFailures`：logger.SetOutput 捕获（先例
  yuque/client_test.go:127）——漂移/provider 故障/孤儿三场景日志断言
  （service ID、tenant 7、installation ID、工具名、事件定性词）（F5/F3）。
- `TestFilterToolsBySnapshot` 扩展描述漂移断言；`TestRegisterMCPToolsRejectsSchemaDrift`
  的 driftedSnap 补 Description 使其专注 schema 维度。

## Ruling（模型路由，按任务书要求原样记录）

任务书要求按 gpt-5.6-sol/terra/luna 分级派发，但本 host 仅配置 GLM-5.3 系列模型，动态工作流亦不支持按子代理选模型。实际全部子代理运行在会话模型 GLM-5.3（最高推理档）上。此为记录的替代方案，不声称使用了不可用模型。

## 疑虑 / concerns（未修项，均超 T09 所有权）

1. **F6 后半未修**：元数据服务路径（GET/POST /mcp-services/:id/metadata，
   internal/handler/mcp_metadata.go）对插件物化服务无快照守卫——任何成员可
   刷新并读取全量目录。该 handler 不在 T09 Brief 所有权内，需转交（建议
   handler 层对 PluginInstallationID 非空行走同一 guard 过滤）。注意：本轮
   修复后 Agent 侧 Put 只写过滤版，但该 handler 有独立的刷新写路径。
2. **server instructions 未核验**：快照（types.PluginToolSnapshot）无
   instructions 字段可比对，插件服务 instructions 仍来自 live 端点（catalog
   侧 mcpExternalDataNotice 已作提示注入缓解）。补齐需 T06 所有的快照结构
   扩展，未在本轮处理。
3. **catalog 内存缓存层**：目录 ready 后常规调用不再走 loader（refresh=true
   或服务行 UpdatedAt 变化才重列）——既有 catalog 语义，改动会破坏手工兼容
   约束。loader 层已尽：loader 被调时插件服务必走 live 核验。
4. 工作区存在其他任务未提交改动（internal/modules/plugins/manifest.go、
   scripts/ 等），非本任务所有权，未触碰、未纳入本次提交。

---

# T09 增量执行记录：任务级 OCR 分流轮（T09-OCR1-F1/F3/F8/F9）

- 日期：2026-09-25
- 报告：docs/plans/issue-106-ocr-T09.md（10 项发现，本轮任务书下达 4 项
  medium；其余 low 已记台账不阻塞）
- 结论：**零代码改动，零项属 T09 所有权**——4 项逐一核验代码证据属实后
  按归属分流（3 转交 + 1 已修）。
- 提交：仅本记录（docs），无代码提交。

## 分流明细（每项均核验过代码现状）

- **T09-OCR1-F1 → OCR-HANDOFF**：`internal/application/service/mcp_service.go`
  不在 T09 Brief 所有权清单（T09 只持有 agent_service.go / plugin_service.go，
  不含 mcp_service.go）。已核验属实：UpdateMCPCredentials 531-540 有
  `PluginInstallationID != nil → ErrPluginManagedService` 守卫（539 行），
  ClearMCPCredential（572 起）在 IsBuiltin 检查（585-587）后无插件行守卫直接
  进入清凭据逻辑；可达路径为 routes_infra.go:185
  `mcpServices.DELETE("/:id/credentials/:field", g.Admin(), ...)` →
  mcp_credentials.go:146（报告写 routes_agent.go:184，行号有偏差但可达性
  成立）。归属 **T06 域**：该文件的插件行哨兵体系由 T06 落地
  （提交 8e639c0f7「Update/Delete/Credentials 对 PluginInstallationID 非空行
  哨兵拒绝」），Clear-DELETE 路径缺口与其同源（发现自标 T06-OCR1-F12 同源）。
- **T09-OCR1-F3 → OCR-HANDOFF**：`apps/web/src/settings/PluginsSettingsPanel.tsx`
  前端文件不在 T09 所有权。已核验 87-89 行面板镜像解析器属实，注释自身
  写明「T08 挂载 client.plugins 后收敛为单一来源」；发现建议也落在 T08
  优先落地。归属 **T03/T08 前端域**。
- **T09-OCR1-F8 → OCR-DONE**：已由 T07 会话在提交 `0f52225db` [T07] 修复
  （提交信息明载「顺带修 reclassifyPreviewMiss 两处 low（行消失误报已消费
  →已过期、重读故障 400→5xx 哨兵）」）。当前代码现状
  plugin_install_service.go:291-292 `if preview == nil || preview.ConsumedAt ==
  nil { return ErrPreviewExpired }` 与函数注释承诺一致（row vanished reads
  as expired / never misreports as consumed）；次要观察（GetPreview DB 故障
  误分类 4xx）也已修为 5xx 哨兵（286 行 ErrInstallationPersistFailed +
  Errorf 日志）。该文件亦非 T09 所有权（属 T07 预览/安装闭环域）。
- **T09-OCR1-F9 → OCR-HANDOFF**：`examples/plugins/jira-todo-mcp/main.go`
  不在 T09 所有权。已核验 259-261 行 `return nil, fmt.Errorf(...)` 兜底拒绝
  形态属实。归属 **T04/T05 域**：Jira 示例服务是 T04/T05 的交付物，发现
  自标「与 T05-OCR1-F1 同源，T05 未豁免」。

## 佐证命令（真实运行）

- `grep -n "ErrPluginManagedService|ClearMCPCredential|IsBuiltin" internal/application/service/mcp_service.go`
  → 539 行守卫仅 Update 路径；Clear（585）仅 IsBuiltin。
- `grep -rn "credentials/:field" internal/router/` → routes_infra.go:185。
- `sed -n '285,298p' internal/application/service/plugin_install_service.go`
  → F8 修复后形态。
- `sed -n '250,265p' examples/plugins/jira-todo-mcp/main.go` → F9 原形态。
- `git log --oneline -S 'preview == nil || preview.ConsumedAt == nil' --
  internal/application/service/plugin_install_service.go` → 0f52225db [T07]。

---

# T09 增量执行记录：任务级 OCR 二轮分流（T09-OCR2-F1）

- 日期：2026-09-25
- 结论：**零代码改动——OCR-DONE**（已修于 T07 域，非 T09 所有权）。

## 分流明细

- **T09-OCR2-F1 → OCR-DONE**：发现自标「与 T07-OCR2-F3 同源同一问题」，
  T07 会话已在提交 `0d7941e6c` [T07] 修复（提交信息明载「T07-OCR2-F3 连接
  视图空 service_id 中断窗口自愈——GetMyConnectionStatus 复用
  plugin_installation_id 反查孤儿物化服务……」）。当前代码现状
  （plugin_install_service.go，已核验）：
  - 697-712 行：`if inst.ServiceID == ""` 触发自愈——复用
    `serviceIDByInstallation`（与 UninstallInstallation 的 T07-OCR1-F5
    同款锚点）反查孤儿物化服务，成功则 `inst.ServiceID = resolved`
    （覆盖发现的诉求 (a)：720 行 `GetTokenForPrincipal` 以自愈后的
    service_id 查令牌，不再误报 unauthorized）；
  - 735-738 行：`inst.ServiceID != ""` 时给出 AuthorizeURLPath/RevokePath
    （覆盖诉求 (b)：不再死端）；
  - 反查故障 fail-closed（ErrConnectionQueryFailed 5xx 哨兵）——比发现
    建议的「保守按空处理」更严格的安全方向，注释已明示此选择。
  归属：plugin_install_service.go 不在 T09 Brief 所有权清单，属 T07
  预览/安装/连接闭环域。

## 佐证命令（真实运行）

- `sed -n '680,745p' internal/application/service/plugin_install_service.go`
  → 自愈块与端点路径赋值现状。
- `grep -n serviceIDByInstallation internal/application/service/plugin_install_service.go`
  → 503/529/534/705 四处（卸载自愈 + 本读路径自愈复用同一锚点）。
- `git log --oneline -S 'connection status healing empty service_id anchor'
  -- internal/application/service/plugin_install_service.go` → 0d7941e6c [T07]。
