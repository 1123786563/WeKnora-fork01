# T09 执行记录：RegisterMCPTools 快照守卫注入（B3 运行时核验基准）

- 日期：2026-09-24（R10 范围缺口修复轮）
- 分支/worktree：`codex/issue-106-self-hosted-plugins` @ `.worktrees/issue106`
- 提交：见 git log「范围缺口 R10 修复：Agent 目录按已接受快照过滤并拒绝漂移」[T09]
- 状态：完成（守卫注入 + 生产接线 + 手工兼容断言；测试先 RED 后 GREEN）

## 做了什么

按计划 Task 9（docs/plans/2026-09-23-issue-106-04-agent-call.md）：

1. **tools 层**（`mcp_tool.go`）：`PluginRuntimeSnapshot`/`PluginSnapshotProvider`
   /`ErrPluginDrift`/`FilterToolsBySnapshot`（导出，T17 漂移复用）；digest 口径
   与预览核验同源（`plugins.ToolSchemaDigest`，canonical JSON SHA-256）；
   `RegisterMCPTools` 第 9 参 `pluginGuard`——nil 或服务非插件物化时行为与
   现状一致（手工 MCP 兼容）；loader 闭包在 loadMCPDirectory 返回后过滤：
   守卫错误 fail-closed、快照外工具剔除、快照内 digest 不符 → ErrPluginDrift。
2. **仓储**：`PluginRepository.GetByServiceID`（参数绑定
   `WHERE tenant_id = ? AND service_id = ?`；空 service_id 短路）。
3. **生产 provider**：`service.PluginSnapshotLookup(repo)`——未命中 (nil,nil)
   =非插件服务；仓储故障透传为 error（守卫侧 fail-closed）。
4. **agent 接线**：agentService 增 pluginRepo 字段（NewAgentService 追加 1 参，
   容器 dig 自动解析——container 测试过）；registerMCPTools 注入
   `s.pluginSnapshotGuard()`（repo 为 nil 时返回 nil，测试组装保持现状）。
5. **既有调用点**：8 处测试 RegisterMCPTools 调用补 nil 参数。

## 测试证据（全部真实运行）

- RED：`go vet ./internal/modules/agentruntime/agent/tools/` →
  `undefined: PluginRuntimeSnapshot`（编译期 RED）。
- 新测试（`tools/mcp_plugin_guard_test.go` + repo 层 1 个）：
  - TestFilterToolsBySnapshot：快照外剔除/digest 不符 ErrPluginDrift（含
    工具名回显）/空目录非错误——漂移语义的原文断言在纯函数层。
  - TestRegisterMCPToolsFiltersPluginToolsToAcceptedSnapshot：受控服务 2 工具、
    快照 1 个 → list_tools 恰见快照内工具（schema 用 NewToolWithRawSchema
    钉死字节，digest 按构造一致）。
  - TestRegisterMCPToolsRejectsSchemaDrift / TestRegisterMCPToolsGuardErrorFailsClosed：
    控制孪生（同 server 双服务行，control 用匹配快照先证明连通性）+ 故障行
    fail-closed（catalog 既有设计将 loader 错误折叠为确定性 error 态消息，
    对外断言该消息；空洞断言已用控制孪生排除）。
  - TestRegisterMCPToolsNilGuardKeepsLegacyBehavior：nil guard 与 guard 返回
    (nil,nil)（手工服务）全工具可见。
  - TestGetByServiceIDScopesByTenant：同租户命中/跨租户 nil/手工无行 nil/
    空 service_id 短路。
- 门控（-count=1）：`go test ./internal/modules/agentruntime/...`（agent 6.3s、
  tools 37s、approval/native/skills 等 18 包 ok）；`./internal/modules/airesource/mcp/` ok；
  `./internal/application/service/` ok（471s）；`./internal/application/repository/`
  单独重跑 ok（437s，15m timeout 下；首轮并行 ~20 包争用触 10m 默认超时假红，
  单独复跑通过）；`./internal/modules/agentruntime/agent/opencode/` 单独重跑
  ok（23s，确证假红与本轮无关）；`./internal/container/`、`./internal/types/...`、
  `./internal/handler/`、`./internal/modules/plugins/` ok。
- `go build ./...` exit 0；本轮改动文件 gofmt 无输出。

## 实现偏差与发现

- 首版测试用 `sdkmcp.NewTool(name, WithRawInputSchema(...))` → mcp-go 运行时
  报 "both InputSchema and RawInputSchema set"（NewTool 预填 InputSchema 与
  raw 选项冲突）；改 `NewToolWithRawSchema` 钉死 schema 字节。
- 首版 drift/guard-error 断言漂移原文，实际 catalog（mcp_catalog.go:293-306）
  把 loader 错误折叠为 "MCP server %q is error; retry discovery..."——既有
  错误面设计（确定性、不泄漏底层文本）。改为：漂移原文断言留在纯函数测试，
  集成面用控制孪生 + fail-closed 消息断言（连接失败假阳性被孪生排除）。
