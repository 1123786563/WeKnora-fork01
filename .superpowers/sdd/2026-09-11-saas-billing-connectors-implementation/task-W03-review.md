---
status: final
verdict: PASS
reviewer: independent-reviewer (W03, billing authorization / refund request / platform review pages; did NOT write this code)
commit-under-review: 184172c (implementation, 10 files +513/−4) + c09f866 (ledger), worktree saas-billing-connectors
files-reviewed: apps/web/src/commercial/refund-state.ts (7), refund-state.test.ts (19), RefundPage.tsx (107), AdminCommercialPage.tsx (279), packages/contracts/src/commercial.ts (+28), packages/contracts/src/index.ts, packages/api-client/src/commercial.ts (+17), packages/api-client/src/index.ts, packages/contracts/test/commercial.test.ts (+52), apps/web/src/App.tsx (+2), ledger c09f866, task-W03-brief.md, internal/commercial/refund.go, precedent task-W02-review.md
method: source-level evidence via git show 184172c (full diff) + file reads + grep + ONE independently re-run confirming command. Full gates NOT re-run here per instructions (coordinator already re-verified web 12/12, shared 57/57, typecheck:web + typecheck:shared clean, build:web pass, diff-check clean).
confirming-command: pnpm --filter @weknora/web test -> exit 0, tests 12, pass 12, fail 0, skipped 0, todo 0 (136.5ms); composition 3 refund-state (Step-1 verbatim first) + 5 order-state + 3 documents + 1 legacy-session — exactly the implementer's GREEN claim, existing 9 unregressed.
---

# Task W03 Independent Review — 账单授权、退款申请和平台审核页面

## AA1 — Brief compliance: file set, Step-3 byte-equivalence, Step-1 verbatim, RefundView fields, client signatures — PASS

- **File set exact incl. disclosed additions.** `git show 184172c --stat`: 4 creates per brief (refund-state.ts +7, refund-state.test.ts +19, RefundPage.tsx +107, AdminCommercialPage.tsx +279), 3 brief-named modifies (contracts/src/commercial.ts +28, api-client/src/commercial.ts +17, App.tsx +2) — plus the 3 disclosed additions (contracts/src/index.ts 2+/2−, api-client/src/index.ts 1+/1−, contracts/test/commercial.test.ts +52). 10 files, +513/−4. Commit message `feat: separate refund requests from platform review` is brief Step-6 verbatim. The two index additions are not scope creep: api-client/commercial.ts imports `parseRefundView`/`RefundView` **from '@weknora/contracts'** (the package entry), so without re-exporting from the two index.ts files the code cannot compile — the deviation is necessary and additive-only. The contracts test addition is the brief's own "contracts 显式parse / 反例" requirement landing where every prior contracts counterexample lives (W01 precedent).
- **refundMessage byte-equivalent to Step-3.** refund-state.ts:1-7 — signature `refundMessage(state:string):string`, four branches with single-space indent, Chinese literals identical to brief lines 41-47 ('退款与权益调整已完成'/'退款已完成，权益调整处理中'/'退款结果核对中'/'退款申请未通过'/'退款处理中'), same precedence completed → revocation_pending → refund_unknown → rejected → default. No extra logic.
- **Step-1 test verbatim.** refund-state.test.ts:1-7 — imports (`node:test`, `node:assert/strict`, `./refund-state.ts`), test name `remote success with pending revocation is not retried payment`, both assertions byte-identical to brief Step 1. Steps 4's counterexamples are two additional tests (lines 8-19) — terminal messages distinct; every in-flight/unrecognised state ('requested'/'reviewing'/'pending'/'failed_confirmed'/'not_created_confirmed'/'anything-else') falls back to '退款处理中'. All 3 real assertions, no skips.
- **RefundView exact fields.** contracts/src/commercial.ts:57 `export interface RefundView { id:string; state:string; amount_fen:string; locked_credits:string; }` — precisely the brief Produces line. `state:string` (not a closed union) is contractually forced: the brief's own signature is `refundMessage(state:string)` and the wire vocabulary is validated by the parser's Set instead.
- **Client signatures exact.** api-client/src/commercial.ts:69-84 — `getRefund(id: string, signal?: AbortSignal): Promise<RefundView>` and `reviewRefund(id: string, decision: 'approve' | 'reject', expectedVersion: number, signal?: AbortSignal): Promise<RefundView>`, both `parseRefundView(unwrap(await request(...)))` — envelope-unwrap-then-parse, no `any`, matches brief line 12 exactly.

## AA2 — State vocabulary alignment with internal/commercial/refund.go — PASS

- **Exact bidirectional match.** refund.go's lifecycle constants are requested/reviewing/pending/revocation_pending/completed/failed_confirmed/not_created_confirmed (7 states). `REFUND_VIEW_STATES` (contracts/src/commercial.ts:61-69) contains exactly those 7 strings — no extra wire state invented, no real state dropped (verified one-by-one against refund.go lines 13-21).
- **The display-only distinction is REAL and correctly enforced on the wire.** 'refund_unknown' and 'rejected' are absent from REFUND_VIEW_STATES, and the contracts test explicitly asserts both are **rejected** by parseRefundView (test/commercial.test.ts:150-151 `{ ...refund, state: 'refund_unknown' }`, `:151 state: 'rejected'` → throws /invalid refund/), with a comment stating "Display-level fallbacks are not wire states". Meanwhile refundMessage still covers them (refund-state.ts:3-4) — which the brief itself mandates (Step-1 asserts refundMessage('refund_unknown')). Cross-checked against refund.go: 'unknown' exists only in the **channel-outcome** vocabulary (RefundChannelUnknown, line 26) and a channel unknown maps the refund back to *reviewing* (TransitionRefundOnChannel default), never to a refund state; 'rejected' is likewise no constant in refund.go. So the implementer's framing — server projection states on the wire, superset display vocabulary in refundMessage — is accurate to the Go domain and sensible.
- **Round-trip coverage per state.** contracts test:116-131 loops all 7 C05 states through parseRefundView asserting every field, plus '0'/'0' boundary — the wire vocabulary is tested, not just documented.

## AA3 — Unknown-refund discipline: no new refund key on unknown/non-terminal — PASS

- **The pages never create refunds at all.** grep across apps/web/src: the ONLY occurrence of a refund-creation symbol is none — `requestRefund` (the W01 POST /api/v1/commercial/refunds method with idempotency_key) is called nowhere in apps/web; the sole '/refunds' string is RefundPage.tsx:64's descriptive header subtitle naming the GET path. RefundPage's only client call is `getRefund(refundId)` (line 43); AdminCommercialPage calls only getRefund (104, 142) and reviewRefund (148).
- **Every retry reuses the original id.** All three action buttons — header 重新核对 (RefundPage.tsx:65), error-path 重试 (74, labelled 复用原退款单号，不新建退款), revocation_pending 重试权益调整 (99) — merely bump `reloadToken`, which re-runs the single effect (deps line 50) with the SAME host-supplied `refundId`. No idempotency-key generation exists in either page.
- **Unknown/non-terminal renders text, not actions.** `notYetTerminal` (53-56) gates a `wk-muted` explanation only (81-85): "结果未到终态前，本页不提供对同一订单再次申请退款的新键；核对未知（unknown）时也仅重查原单" — and indeed no such key exists anywhere in the page. Terminal set = completed/failed_confirmed/not_created_confirmed, matching refund.go's "terminal states never move again" transitions.

## AA4 — Admin authority: operator capability, no self-approval, honest UI-hiding — PASS

- **Visibility tied to the capability prop.** AdminCommercialPage.tsx:116 `if (!capability.operator)` returns a bare page whose only content is an error Status (128) — none of the five admin sections render. The prop is typed `CommercialOperatorCapability { operator: boolean }` (13) with the doc comment "Backend projection of REAL platform-operator capability" — i.e. the host must feed a server-derived value, the page does not self-assert.
- **UI-hiding explicitly NOT treated as authorization.** Props comment (52-56): "Rendering this page only for operators is a UI affordance, NOT authorization: server-side capability enforcement stays authoritative for every admin action." The non-operator Status (128) repeats it: "隐藏入口不是授权：服务端能力校验才是权威." Comments and behaviour agree — this matches the brief's "平台页面只对真实运营能力显示" with the right honesty about enforcement living server-side (where C05/C01 wiring is still pending, acknowledged blocked-env).
- **No self-approval path.** viewerIsRequester (240-246): the approve/reject buttons (250, 252) are simply not rendered — replaced by an error Status explaining the request was filed by the current viewer (space Owner) and review must be done by another operator; it again names server enforcement as authoritative. There is no code path that reaches submitReview while viewerIsRequester is true.
- Minor note (non-blocking): the load-refund effect (99-114) fires getRefund even when capability.operator=false — hooks cannot be conditional, it is a read the server authorizes anyway, and nothing is rendered from it; see Improvements #2.

## AA5 — Review flow: refresh-before-decision, expectedVersion, conflict UX, body shape — PASS

- **Refresh before every decision.** submitReview (135-170) calls `getRefund` FIRST (142); if the refreshed projection is terminal (isRefundTerminal, 28-30 = refund.go's three terminal states) it stops at done WITHOUT submitting (144-147) — no double-processing of an already-settled refund. Only then reviewRefund (148).
- **expectedVersion from the reconciled target.** reviewRefund carries `reviewTarget.expectedVersion` (148), the version the host last reconciled — deviation (3), disclosed and verified genuine: RefundView has exactly the four brief-mandated fields and no version member, so the client literally cannot read a version off the refreshed projection. The refresh still does real work: it updates the displayed 可退本金/锁定额度 (setRefund at 143) and enforces terminality. The 重新核对 button (248) refetches for re-reconciliation; the conflict-clearing effect (94-97) resets state only when the host injects a NEW reviewTarget (refundId or expectedVersion identity change).
- **Conflict → explicit re-reconcile UX.** isVersionConflict (23-26) matches HTTP 409 or code VERSION_CONFLICT/VERSION_MISMATCH; on conflict: conflictedVersion pinned (157), both decision buttons disabled (`reviewDisabled = review.status === 'loading' || conflictedVersion !== null`, 172), conflict Status (258-261) shows the stale version, states 请重新核对后再审批 and 已自动重新拉取 — and reloadToken++ (158) does trigger the auto-refetch (effect dep at 114). No stale-version resubmission is possible: buttons stay disabled until the host re-injects.
- **Bodies correct.** reviewRefund request body `{ decision, expected_version: expectedVersion }` (api-client/commercial.ts:80) — snake_case wire field, exact `{decision, expected_version}` shape; decision typed 'approve'|'reject' end-to-end. Paths GET /api/v1/commercial/refunds/:id and POST /api/v1/commercial/refunds/:id/review (with encodeURIComponent) follow the existing orders/summary/quotes convention.

## AA6 — Contracts/client quality: field-by-field parse, no any, additive-only — PASS

- **Field-by-field, fail-closed.** parseRefundView (contracts/src/commercial.ts:71-78): object/null/Array guard → nonEmptyString(id) → state must be a string IN REFUND_VIEW_STATES → digitString(amount_fen) → digitString(locked_credits). digitString (23-26) is `/ /^\d+$/` — negatives ('-1'), decimals ('99.5', '1.5'), and numbers (typeof check) all throw, which the malformed test asserts for BOTH amount fields and locked_credits including `locked_credits: -1` (test:141-148). The claimed "negative locked_credits rejected" is real, with the C05 rationale commented (held credits are never negative; a release is a lower value, mirroring CommercialSummary.refund_locked).
- **No any.** The only cast is `value as Record<string,unknown>` after the typeof-object guard — the identical narrow pattern as the pre-existing parseOrderView/parseCommercialSummary/parseQuoteView. Pages catch `error: unknown` and narrow via instanceof.
- **Additive only; W01 exports intact.** Both index.ts diffs only append `RefundView` to the existing type-export line and `parseRefundView` to the existing value-export line (verified in diff: every prior symbol preserved verbatim); contracts/src/commercial.ts and api-client/src/commercial.ts changes are pure additions after existing code; contracts test adds one import name + 2 tests, original 7 untouched (file now exactly 9 `test(` blocks — matching the ledger's "contracts 9/9").

## AA7 — Page conventions: scope guards, @weknora/ui, wk-* classes, App.tsx additive — PASS

- **RefundPage** (36-50): `active` flag + `currentScope = scopeController.current()` inside the effect + `currentScope.signal` on the request + `active && scopeController.isCurrent(currentScope.scope)` before setState (44) + abort/isCurrent re-check in catch (45-46) + cleanup `active = false` (49). Button/Card/Status from @weknora/ui; wk-page/wk-header/wk-eyebrow/wk-muted/wk-debug/wk-list classes; aria-live polite section (79). A faithful W02/KnowledgeBasesPage-pattern page.
- **AdminCommercialPage**: same skeleton; all hooks (useMemo, useState×6, useEffect×2) sit unconditionally above the `if (!capability.operator)` early return (116) — hooks rules hold; both effects carry active/isCurrent/signal guards (101-113); promise chains end in `.catch` (better than the inherited void-then-without-catch shape W02 flagged).
- **App.tsx**: the commit's entire App.tsx delta is two appended re-export lines after W02's (`export { RefundPage } ...`, `export { AdminCommercialPage } ...`) — zero existing lines modified, navigation untouched (same additive pattern W02's Z6 passed).
- Cosmetic (same class as W02 Improvement #1): both pages' effect deps key on refundId/reloadToken rather than scope identity — a same-tenant invalidate() does not refetch, though stale-scope results are still dropped by isCurrent (drop-correct, recovery-only). See Improvements #1.

## AA8 — Honesty: ledger vs reality, blocked-env, deviations — PASS

- **GREEN independently reproduced.** My single confirming run: `pnpm --filter @weknora/web test` → exit 0, tests 12 / pass 12 / fail 0 / skipped 0 / todo 0, duration 136.5ms, with exactly the claimed composition — Step-1's test name first among the 3 refund-state tests, the 9 pre-existing (5 order-state + 3 documents + 1 legacy-session) unregressed. The ledger's "web 12/12" is precise, not rounded.
- **"contracts 9/9" verified.** The contracts commercial test file contains exactly 9 `test(` blocks (7 pre-existing + 2 refund) — the ledger count is accurate and covered by the coordinator-re-verified shared 57/57.
- **RED claims credible by construction.** Ledger records three distinct RED failure modes (web ERR_MODULE_NOT_FOUND on ./refund-state.ts with existing 9 passing; typecheck:web TS2307; contracts test failing to load because parseRefundView was not exported). Given the shipped test imports `./refund-state.ts` and `parseRefundView` verbatim, all three failures follow mechanically from the pre-implementation file set; nothing in the final state contradicts them. I did not revert-and-replay RED (out of the one-command budget); coordinator gates cover GREEN.
- **blocked-env explicit, no fake data.** Admin list sections (授权审计 224, paid 待履约 269, 结算差异 274) render BLOCKED_ENV_NOTE (17: "服务端接口未接入（blocked-env）…未用假数据填充") whenever the host injects no rows; publish without onPublishDraft shows its own blocked-env Status with the button disabled (216, 196); no reviewTarget → explicit 待审列表接口未接入 Status. The ledger separately lists the unwired server-side admin endpoints and the absence of browser/e2e as blocked-env not-pass — nothing is claimed beyond typecheck/build/unit.
- **Deviations disclosed truthfully.** (1) both index.ts additions — verified additive and compile-necessary (AA1); (2) admin endpoints unwired → props-injected / explicit blocked-env placeholders, never fake passing (AA4/this); (3) expectedVersion host-injected via reviewTarget because RefundView carries no version field — verified against the interface (AA5); plus the standing git add -f .gitignore note already proven in W02's review. The ledger row's claims match observable reality on every point I could check.

## AA9 — Code quality — PASS

- **Hooks rules:** every hook unconditional at top level in fixed order; the capability early-return (116) sits below all hook calls in both components; deps arrays on all effects/memos (50, 97, 114; RefundPage 35, 50).
- **No dead code:** AdminList, formatCny, isVersionConflict, isRefundTerminal, every state field (refund/review/reloadToken/conflictedVersion/previewedVersion/publishNote) is read somewhere in the render or handlers.
- **Error states rendered, not thrown:** RefundLoadState error branch with retry (72-77); ReviewState error (255) and conflict (258-261) Statuses; loading Statuses at 254/71; getRefund failures in the review panel degrade to '—' placeholders rather than crashing.
- **Types:** no `any`, closed local unions for load/review states, unknown-narrowed catches; TS strict-clean by inspection, and coordinator re-ran typecheck:web + typecheck:shared + build:web clean.
- Nits: formatCny now duplicated in three commercial pages; terminal-set logic exists twice (isRefundTerminal 28-30 vs RefundPage's notYetTerminal 53-56) — see Improvements.

## Verdict: PASS

No blocking finding. The three guarantees this task exists for are implemented and evidenced: (1) the wire state vocabulary is exactly refund.go's seven lifecycle states with the display-only fallbacks (refund_unknown/rejected) provably rejected by the parser while remaining available to refundMessage; (2) no UI path can create a second refund — the pages never call requestRefund at all, and every retry/revocation re-drive re-queries the original refund id; (3) platform review is gated on a server-projected operator capability with no self-approval path, refresh-before-decision, and a disabled-until-reconciled conflict UX whose {decision, expected_version} body is correct. Ledger claims (12/12, 9/9, gates, blocked-env, deviations) all match observable reality; my independent run reproduced the web suite exactly.

## Improvements (non-blocking)

1. **RefundPage.tsx:50 / AdminCommercialPage.tsx:114** — effect deps key on refundId/reloadToken only (not scope identity, the W02 Improvement-#1 class): a same-tenant `invalidate()` won't refetch; results from stale scopes are already dropped, so this is recovery polish, not safety.
2. **AdminCommercialPage.tsx:99-114** — the load effect fires getRefund even when `capability.operator=false` (hooks must precede the early return). Harmless read, server-authoritative, but a ref-guard or skipping when `!capability.operator && !reviewTarget` would avoid a pointless request.
3. **RefundPage.tsx:14-16 / AdminCommercialPage.tsx:19-21** — formatCny is now triplicated across commercial pages (W02's CheckoutPage has a third copy); hoist to a shared util on next touch.
4. **AdminCommercialPage.tsx:28-30 vs RefundPage.tsx:53-56** — the terminal-state set is written twice; extract `isRefundTerminal` into refund-state.ts when either changes.
5. **packages/contracts/src/commercial.ts:57** — when C05 wires the review endpoints, consider adding `expected_version` to RefundView (or a review projection) so conflict recovery no longer depends wholly on the host re-injecting `reviewTarget.expectedVersion`; today's host-injection is honest and disclosed, but it makes the 重新核对 loop host-coupled.
