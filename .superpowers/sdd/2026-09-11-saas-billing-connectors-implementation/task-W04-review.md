---
status: final
verdict: PASS
reviewer: independent-review-subagent (W04, 应用目录、连接与同步页面及API; did NOT write this code)
commit-under-review: a85c0d3 (implementation, 16 files +1136/−1) + 74996e6 (ledger), worktree saas-billing-connectors
files-reviewed: internal/handler/app_connector.go (475, full), internal/router/routes_app_connectors.go (37), internal/router/router.go (+2), internal/container/container.go (+2), packages/contracts/src/appconnector.ts (146), packages/contracts/test/appconnector.test.ts (53), packages/api-client/src/appconnector.ts (114), packages/api-client/src/appconnector.test.ts (71), packages/api-client/src/client.ts (+3), packages/api-client/src/index.ts (+2), packages/contracts/src/index.ts (+2), apps/web/src/appconnector/connection-state.ts (43), connection-state.test.ts (13), AppsPage.tsx (171), apps/web/src/App.tsx (+1), package.json (typecheck:shared list), ledger 74996e6, task-W04-brief.md, consumed models internal/appconnector/access.go + sync.go + internal/application/repository/appconnector/install.go, pattern internal/router/routes_commercial.go, precedent task-W03-review.md
method: source-level evidence via git show a85c0d3 (full diff, Go and TS sides both printed untruncated) + file reads + grep (CanInstallInstallation, IsValidPauseReason vocabulary, ApplyInstallation CAS/scopeExpanded, handler-package name collisions) + ONE independently re-run confirming command. Full gates NOT re-run here per instructions (coordinator already re-verified web 15/15, shared 65/65, typecheck:shared + typecheck:web clean, go build+vet on handler/router/container clean, build:web pass, diff-check clean, and app_connections.go pre-existing from A02 commit 18b5b45).
confirming-command: pnpm --filter @weknora/web test -> exit 0, tests 15, pass 15, fail 0, skipped 0, todo 0 (130ms); composition 3 connection-state (Step-1 verbatim first: 'installation does not imply shared credentials', then space-kind and revoked counterexamples) + 12 pre-existing (order/payment/documents/legacy) unregressed — exactly the implementer's GREEN claim.
---

# Task W04 Independent Review — 应用目录、连接与同步页面及API

## AB1 — Brief compliance: file set, verbatim artifacts, endpoint surface — PASS

- **File set exact incl. the 2 disclosed extras.** `git show a85c0d3 --stat`: all 14 brief-named files present — creates packages/contracts/src/appconnector.ts, packages/api-client/src/appconnector.ts, apps/web/src/appconnector/{connection-state.ts, connection-state.test.ts, AppsPage.tsx}, internal/handler/app_connector.go, internal/router/routes_app_connectors.go; modifies router.go (+2: RouterParams field line 92 + RegisterAppConnectorRoutes call line 305), container.go (+2: `must(container.Provide(handler.NewAppConnectorHandler))` line ~456), App.tsx (+1), both package index.ts files, client.ts, package.json — plus the 2 disclosed extra test files packages/contracts/test/appconnector.test.ts and packages/api-client/src/appconnector.test.ts. 16 files, +1136/−1. The extras are mandated by brief Step 4 (反例/counterexample tests) and land exactly where prior tasks put them (contracts test/ per W01; api-client src/*.test.ts matching the test:shared glob).
- **ConnectionView + connectionLabel verbatim.** connection-state.ts defines local `ConnectionViewWire { id:string;kind:'personal'|'space';state:string;owner_id:string|null }` — the brief Step-3 interface field-for-field — and `connectionLabel` is logic-verbatim (revoked → '连接已撤销'; else kind==='personal'?'个人连接':'空间连接'), single-space style included. The contracts `ConnectionView` reuses the same shape with `state: ConnectionState | string` (a documented union over string — set-wise identical).
- **Step-1 test verbatim.** connection-state.test.ts:1-7 is byte-identical to brief Step 1 (imports from 'node:test'/'node:assert/strict'/'./connection-state.ts', test name 'installation does not imply shared credentials', the personal→'个人连接' assertion); two counterexample tests follow (space kind; revoked regardless of kind incl. owner_id:null). All 3 real assertions, no skips — confirmed live in my run.
- **All 8 endpoints, brief's exact paths/methods.** routes_app_connectors.go:21-35 under `r.Group("/apps", RequireInstallCapabilityForWrites())` on v1: GET+POST `/installations`, POST `/installations/:id/upgrade`, POST `/installations/:id/disable`, GET+POST `/connections`, POST `/connections/:id/revoke` — exactly the brief Produces line — plus GET `/datasources/:id/sync-status` carrying the mandated binding/pause projection (8 routes total). Registration order/pattern mirrors routes_commercial.go precedent (nil-guard, group, no rbacGuards param).
- Note (non-blocking): the brief's prose says the sync API "继续现有datasource handler，响应增加binding与暂停原因的显式字段"; the implementation instead adds a NEW additive endpoint rather than modifying the existing datasource handler. Additive-only is the lower-risk reading (no cross-task conflict, existing datasource responses untouched) and the ledger describes the endpoint accurately — but it is not listed in the ledger's 偏差披露 list. See Improvements #4.

## AB2 — Credential non-disclosure — PASS

- **Go: structurally impossible.** All four view structs (appInstallationView, appConnectionView, appSyncStatusView, appSyncBindingView; app_connector.go:41-72) have only id/kind/state/owner_id/scope/binding fields — no access_token, refresh_token, credential, or even credential_ref anywhere in the file (comment at :50-52 states the design). Views are constructed field-by-field (`appConnectionViewFor` :123-129 copies exactly ID/Kind/State/OwnerID); no handler ever serializes a repo row or gin.H of a row. The only raw-map use is the revoke UPDATE (write, not a response). GetSyncStatus selects explicit columns ("id","status") via .Select; scopesForVersion unmarshals only a `{scopes}` sub-struct. appOK wraps `{"success":true,"data":…}` around the typed view.
- **TS: parsers rebuild fresh objects.** parseConnectionView/parseInstallationView/parseSyncStatusView (contracts/src/appconnector.ts:105-146) each construct a NEW literal from validated fields — any extra server field is dropped by construction. createConnection (api-client/src/appconnector.ts:82-101) returns exactly `{ connection: parseConnectionView(row.connection), authorize_url }` — nothing else crosses.
- **The leak-drop tests are real and assert on serialized output.** contracts test 'installation projection never carries credential fields even if present in the payload' (test/appconnector.test.ts:31-35) feeds `{…, access_token:'x', credential:'y'}` and asserts `JSON.stringify(v)` contains neither string. api-client test 'connection responses NEVER expose credentials even when the payload leaks them' (appconnector.test.ts:38-63) feeds a fake server leaking access_token/refresh_token/credential_ref on GET /connections and credential on POST /connections, then asserts `JSON.stringify` of the PARSED results contains none of the three strings — a genuine end-to-end (fake-transport → unwrap → parse) leak probe, not a comment. Go side is projection-struct-only (above). Minor: no equivalent credential-drop test for SyncStatusView (same parser pattern, so low risk) — Improvements #7.

## AB3 — Tenant scoping — PASS

- **Tenant only from authenticated context.** `appTenantScope` (app_connector.go:116-126) reads `types.TenantIDFromContext` / `TenantRoleFromContext` / `UserIDFromContext`; MISSING_TENANT_SCOPE 403 otherwise. No handler reads a tenant from a param/body — verified across all 8 handlers; routes file has no tenant path segment.
- **Every id lookup filtered by tenant; cross-tenant = 404.** ListInstallations/ListConnections: `Where("tenant_id = ?", tenantID)` (:170, :283). installationByID: `Where("id = ? AND tenant_id = ?")` → INSTALLATION_NOT_FOUND 404 (:199-206) — comment explicitly notes 404-not-403 to avoid existence leaks, and the code does it. RevokeConnection: conditional UPDATE `Where("tenant_id = ? AND id = ? AND auth_version = ?")`; on 0 rows a tenant-scoped count disambiguates 404 (CONNECTION_NOT_FOUND) from 409 (VERSION_CONFLICT) — a cross-tenant id yields count 0 → 404 (:305-318). GetSyncStatus: data_sources `Where("id = ? AND tenant_id = ?")` → 404; binding, connection and installation lookups all tenant-filtered (:326-360). CreateConnection never resolves ids (fails closed 501 first) — nothing to leak.

## AB4 — Optimistic concurrency — PASS

- **expected_version required on every write.** installationWriteInput/connectionCreateInput/revoke input all carry `ExpectedVersion *int64`; every write handler 400s on nil (CreateInstallation :236-240, Upgrade :259-263, Disable :273-276, CreateConnection :296-301, Revoke :295-299 region). CreateInstallation additionally enforces expected_version==0 for a fresh install (409 INSTALLATION_CONFLICT otherwise) and 409 INSTALLATION_EXISTS on an existing app — no silent overwrite of a prior install.
- **Mismatches are 409 conflicts, never silent.** applyInstallation (:208-232) routes the store's `ErrInstallationConflict` → 409 INSTALLATION_CONFLICT ("stale expected_version or invalid transition") and `ErrReauthorizationRequired` → 409 REAUTHORIZATION_REQUIRED; the store (install.go:86-135) genuinely CAS-checks expected version, rejects invalid transitions, and readBack-verifies. RevokeConnection is a conditional UPDATE on auth_version — RowsAffected 0 → 404/409 per AB3. The api-client puts expected_version in every write body and the test asserts the exact bodies (`{version, expected_version}`, `{expected_version}`) — client and server agree.

## AB5 — Authorization layering — PASS

- **Server-side install gate.** `RequireInstallCapabilityForWrites` (app_connector.go:98-114) is middleware on the whole /apps group: GET/HEAD/OPTIONS pass; every other method requires `appconnector.CanInstallInstallation(role)` (access.go:23) else 403 MEMBER_MUST_REQUEST_INSTALLATION ("submit an installation request instead"). UI hiding is explicitly documented as non-authoritative. Reads stay member-visible (catalog browsing), writes are owner/admin — matching the brief's 普通成员申请入口 requirement at the authoritative layer.
- **CreateConnection fails closed.** :283-302 validates input then returns 501 OAUTH_NOT_CONFIGURED — no connection minted, no authorize_url fabricated, no credential echoed. Honest blocked-env behavior (matches ledger). api-client's createConnection still models the real A02 shape ({connection, authorize_url}) with a leak-probe test — correct forward contract.
- **Upgrade scope change forces re-authorization.** The store's scopeExpanded check (install.go:137, used at :118) makes an active target with expanded scopes return ErrReauthorizationRequired, surfaced as 409 REAUTHORIZATION_REQUIRED (handler comment :252-256); UI side, connections in pending_reauthorization render '连接等待重新授权，恢复前不会执行同步' and requiresReauthorization()/syncStatusText encode the never-pretend-clean rule.
- **Plain-member UI shows the request path.** AppsPage: `canInstall = role==='owner'||'admin'`; when false, the installations section renders '普通成员不能直接安装或升级应用。如需新应用或新版本，请通过申请入口向空间管理员提交申请。' — a request entry, not a fake install success; and the page contains no install/upgrade/revoke action buttons at all, so the UI cannot fake a write.
- Note (non-blocking): the page has no wired upgrade flow UI — the 409 → prompt contract exists at API level only. Acceptable for this task's scope (host injects datasources/role; e2e is blocked-env).

## AB6 — Sync projection — PASS

- **Explicit fields.** appSyncStatusView (app_connector.go:58-72): datasource_id, state, pause_reason (*string), binding (*appSyncBindingView{installation_id, connection_id, auth_version}), requires_reauthorization — exactly the contract's SyncStatusView; TS parseSyncStatusView validates each field incl. boolean requires_reauthorization and nullable binding.
- **pause_reason in-vocabulary only.** The handler sets pause_reason exclusively to `appconnector.PauseReasonPermission` and only when RequiresReauthorization (:381-384); "permission" ∈ validPauseReasons (sync.go:28-34). No free-form reason ever crosses.
- **Legacy unbound datasource → binding:null, no ownership rewrite.** gorm.ErrRecordNotFound on the binding row returns `appSyncStatusView{…, PauseReason:nil, Binding:nil, RequiresReauthorization:false}` (:338-344) — parsed as binding:null TS-side; the handler is read-only on data_sources/bindings, so no credential-ownership rewrite is possible. Bound sources resolve through A07 `ResolveSyncBinding` with a live BindingState (or nil → RequiresReauthorization bookkeeping), reusing StoredSyncBinding via the tiny rowStore adapter.
- Blemish (non-blocking): the TS `pauseReasonLabels` map (connection-state.ts:14-22) claims to mirror IsValidPauseReason but its keys are plan/permission/conflict/rate_limit while sync.go's actual closed vocabulary is budget/plan/permission/quota/provider_limit — two keys don't exist server-side and three server reasons get no label. Behavior stays safe (unknown reasons render verbatim via the ?? fallback; the Go endpoint only ever emits 'permission'), but the comment and the ledger parenthetical overstate the alignment. Improvements #1.

## AB7 — TS quality — PASS

- **Field-by-field parsers, no any.** asRecord/nonEmptyString/stringArray guards; every parse builds a fresh literal; the only casts are post-guard `as Record<string, unknown>` (the W01-established pattern). No `any` in any new file.
- **Additive-only wiring.** contracts/src/index.ts appends one type-export + one value-export line after commercial's; api-client/src/index.ts appends createAppConnectorApi + a type re-export line; client.ts adds an import line and `apps` to the returned object — every prior symbol preserved (visible in the diffs). package.json adds exactly `packages/api-client/src/appconnector.ts` to the typecheck:shared file list — the brief's root-typecheck requirement, done.
- **Envelope unwrap-then-parse + error propagation.** api-client unwrap() rejects non-object envelopes, throws ApiError with the server's code/message (fallbacks APP_CONNECTOR_ERROR / INVALID_RESPONSE) on success:false, and rejects a missing data field; every method is `parseX(unwrap(await request(...)))`. The error test asserts getSyncStatus rejects with the server's code/message rather than returning data. encodeURIComponent on all path ids; AbortSignal threaded through every call.

## AB8 — Page conventions — PASS

- **Sections in brief order.** AppsPage renders exactly the brief's sequence: 安装版本与费用/权限说明 (app@version, scopes or 无额外权限, 按量计费) → 个人/空间连接 (connectionLabel + pending_reauthorization warning) → 资源范围 → 目标知识库 (follows datasource config, not editable here) → 同步进度与暂停原因 (state + pauseReasonLabel + requires_reauthorization + binding connection/auth_version). Header/eyebrow/wk-* classes, Button/Card/Status from @weknora/ui — the W02 page pattern.
- **Scope-guarded effects, late responses dropped.** Both effects use the `active` flag + `scopeController.isCurrent(scope.scope)` before every setState, pass `scope.signal` into the client calls, re-check `scope.signal.aborted` in catch paths before recording errors, and clean up `active=false`; loadAppsOverview rethrows on abort so a stale-space rejection is never rendered as an error. Sync loads are guarded per-datasource with the same isCurrent gate — a space switch cannot be overwritten by a late response.
- **App.tsx additive.** The entire delta is one appended re-export line (`export { AppsPage } from './appconnector/AppsPage.tsx';`) after the commercial lines — zero existing lines touched, navigation untouched.
- Nit: 'Loading…' sentinel-string matching to distinguish loading from error is fragile but works; the per-datasource sync loop fires N requests without batching — both cosmetic.

## AB9 — Honesty: ledger vs reality — PASS

- **GREEN reproduced independently.** My run: web 15/15, exit 0, 0 skipped — composition exactly the ledger's claim (3 connection-state with Step-1 first + 12 pre-existing unregressed). contracts test file has exactly 5 `test(` blocks (ledger "contracts 5/5": round-trips, rejects, scopes, credential-drop, sync bound/unbound/paused) and api-client exactly 3 (ledger "api-client 3/3": envelope+expected_version bodies, credential non-leak on leak injection, error propagation) — counts are precise, not rounded. Remaining gates (shared 65/65, typechecks, go build+vet, build:web, diff-check) coordinator-re-verified per my mandate; nothing in the diff contradicts them.
- **RED claim credible by construction.** "tests 13/pass 12/fail 1 ERR_MODULE_NOT_FOUND on ./connection-state.ts" follows mechanically from the shipped test importing the then-absent module while the 12 prior tests existed — same shape as W02/W03's accepted REDs. Not replayed (one-command budget).
- **blocked-env explicit, nothing overclaimed.** Ledger states: CreateConnection fail-closed 501 OAUTH_NOT_CONFIGURED without fabricated authorize_url (matches code), no browser/e2e (page verified by typecheck/build/pure tests only), no Go DB integration tests (compile+vet only) — all three kept not-pass, no skip-as-pass anywhere (my run: skipped 0).
- **Deviations disclosed and verified genuine.** (1) the 2 extra test files — real, in-commit, brief-mandated counterexamples; (2) app-prefixed Go view types due to A02's app_connections.go — verified: that pre-existing file defines unexported `connectionView` (app_connections.go:158) in the same handler package, so the collision claim is factual and the app-prefix resolves it; (3) git add -f for apps/web — the W02-proven .gitignore `web/` mis-hit. Two accuracy blemishes (non-blocking): the pauseReasonLabel "对齐 sync.go" characterization is wrong on 2 of 4 keys (AB6), and the new sync-status endpoint is a silent (accurate but unflagged) deviation from "继续现有datasource handler" (AB1).

## AB10 — Go quality — PASS

- **Collision handling correct.** All new view/helper names are app-prefixed (appInstallationView, appConnectionView, appSyncStatusView, appSyncBindingView, appTenantScope, appOK, appFail, newAppID); `type rowStore` exists exactly once in internal/handler (app_connector.go:470 — grep-verified), so nothing shadows A02's AppConnectionHandler/connectionView in package handler. Coordinator confirms app_connections.go pre-exists from A02 (18b5b45) and build+vet on the three packages is clean — consistent with the diff (all imports used, incl. context in rowStore and strconv for the auth_version string).
- **Error handling consistent.** Uniform appFail(status, CODE, message) with distinct codes per failure mode (INSTALLATION_NOT_FOUND/CONFLICT, REAUTHORIZATION_REQUIRED, VERSION_CONFLICT, OAUTH_NOT_CONFIGURED, MISSING_TENANT_SCOPE, MEMBER_MUST_REQUEST_INSTALLATION…); store sentinel errors routed via errors.Is; readback failures surfaced rather than swallowed.
- **No map-dump responses.** Every 200/201 body is a typed view struct through appOK; the sole map use is the revoke UPDATE payload (write path). scopesForVersion fails closed to []string{} rather than inventing scopes.
- **Router wiring matches precedent.** routes_app_connectors.go mirrors routes_commercial.go: nil-guard, group with handler-layer middleware, no rbacGuards param — and the file comment correctly explains the consequence (routes not declared in the API-key authorizer → default-deny for X-API-Key principals), the same rationale commercial documents.
- Nits (non-blocking): missing trailing newline in routes_app_connectors.go and AppsPage.tsx; installationByID's 404 comment duplicates the (correct) behavior — fine; DisableInstallation reuses installationWriteInput whose app_key field is meaningless there (ignored) — harmless.

## Verdict: PASS

No blocking finding. The three guarantees this task exists for are implemented and evidenced end-to-end: (1) credential non-disclosure is structural — no Go view has a credential field, every TS parser rebuilds objects field-by-field, and the leak-drop tests assert on JSON.stringify of parsed output against a fake server that deliberately leaks tokens; (2) tenant isolation is complete — tenant only from authenticated context, every id lookup tenant-filtered, cross-tenant ids indistinguishable from missing ones (404), revoke disambiguating 404/409 via a tenant-scoped count; (3) write safety layers correctly — expected_version required everywhere with 409 CAS conflicts, CanInstallInstallation gates every write server-side with the member request path in the UI, scope-expanding upgrades forced into 409 REAUTHORIZATION_REQUIRED by the store, and connection creation fails closed 501 with nothing fabricated. Ledger counts, gates, deviations, and blocked-env honesty all match observable reality; my independent run reproduced the web suite exactly (15/15).

## Improvements (non-blocking)

1. **connection-state.ts:14-22** — pauseReasonLabels claims to mirror IsValidPauseReason but contains conflict/rate_limit (not server vocabulary) while budget/quota/provider_limit have no labels. Behavior is safe (verbatim ?? fallback; endpoint only emits 'permission'), but align the map or soften the comment; the ledger parenthetical repeats the misstatement.
2. **contracts/src/appconnector.ts InstallationView** drops the `state` field the Go projection sends (appInstallationView has State) — so a reauthorization_required installation cannot be displayed, and requiresReauthorization() is dead for parsed views. Add `state` to the contract/parser (also lets AppsPage render the upgrade-reauth state directly).
3. **requiresReauthorization / syncStatusText** (connection-state.ts:31-43) are exported but unused — AppsPage inlines its own status text. Use them or drop them.
4. The sync projection landed as a NEW endpoint (GET /apps/datasources/:id/sync-status) rather than extending the existing datasource handler per the brief's prose. Additive and lower-risk, but it should have appeared in the ledger's 偏差披露 list, not only as an implementation description.
5. **CAS version source:** neither InstallationView nor ConnectionView carries an expected_version/auth_version, so upgrade/disable/revoke callers must host-inject versions (the W03 reviewTarget pattern). Consider exposing the version (the store returns it; the handler currently discards it at readback) or documenting the host contract.
6. Cosmetic: trailing newline missing in routes_app_connectors.go and AppsPage.tsx; 'Loading…' sentinel-string state discrimination; per-datasource sync fetch loop unbatched.
7. No credential-drop test for parseSyncStatusView (connection/installation are covered); the parser shares the same rebuild pattern, so risk is low.
