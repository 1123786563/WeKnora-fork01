---
status: final
verdict: PASS
reviewer: independent-review-subagent (O02, 旧空间与数据源迁移、影子计量和注销保留; did NOT write this code)
commit-under-review: 4e99dc9a57a96c67c034eb74d3e5df5ab39dd92a (implementation, 6 files +845/−4) + 75c2d76a836c302c55b61de04ab2362a38aa4f08 (ledger), worktree saas-billing-connectors
files-reviewed: full diff 4e99dc9 (internal/commercial/migration.go 164ln + migration_test.go 108ln, cmd/saas-migrate/main.go 292ln, internal/application/service/tenant.go +90/−4, tenant_deletion_test.go 128ln, docs/operations/saas-commercial-migration.md 67ln); ledger diff 75c2d76; brief task-O02-brief.md; precedent task-O01-review.md. Consumed definitions verified against real code: repository/commercial/account.go (Account :14-18 tenant_id PK + customer_id uniqueIndex; Bind :39-70 INSERT ON CONFLICT DO NOTHING + read-back; ErrAccountMappingConflict :10, :66-68), appconnector/sync.go (requires_reauthorization vocabulary :19, BindingState :98-106, ResolveSyncBinding :120-148, bindingAuthorizesSpaceSync :152-156), repository/tenant.go DeleteTenant :125-132 (touches ONLY tenants + tenant_members). cmd/ listing checked for path conflicts (desktop/download/saas-migrate/server only).
method: source-level evidence via git show + post-change reads/greps with exact line numbers; every AE claim traced to code I read. Gates NOT re-run except ONE confirming command (coordinator already re-verified 9/9 migration + 4/4 tenant-delete, commercial full suite, build/vet 3 packages, gofmt, git diff --check). CLI runtime evidence (double-apply single row) NOT independently re-run — verified at the code-semantics level against account.go's conflict semantics and disclosed as such below.
confirming-command: go test ./internal/commercial -run TestMigration -count=1 -v && go test ./internal/application/service -run TestTenantDelete -count=1 -v -> exit 0; internal/commercial ok (9/9 PASS: DoesNotInventPaidCreditOrConnectionOwnership, ProvenOwnerIsReadyToBindButStillBasicWithoutCredits, ReusesExistingCustomerAndNeverDuplicates, SkipsTenantWithoutCustomerEvidence, LegacyBalanceNeverBecomesMoneyGrant, ShadowCutoverPinsBillabilityToTrustedCallStart, ApplyRequiresExplicitFlag, DeletionRefusesPendingCommercialWork, DeletionNeverAutoDeletesWithoutRetentionPolicy — all 9 names match the -run filter, no O01-style attribution gap); service ok (4/4 TestTenantDelete*); the nil-guard WARN ("No commercial deletion guard wired; using legacy deletion path") observed live in the -v output of TestTenantDeleteWithoutGuardKeepsLegacyPath.
---

# Task O02 Independent Review — 旧空间与数据源迁移、影子计量和注销保留

## AE1 — Brief compliance: file set, Step-3 exact, Step-1 verbatim, CLI flags — PASS

- **File set exact incl. the disclosed 6th file.** `git show 4e99dc9 --stat`: all 5 brief files present — creates internal/commercial/{migration.go 164ln, migration_test.go 108ln}, cmd/saas-migrate/main.go 292ln, docs/operations/saas-commercial-migration.md 67ln; modifies internal/application/service/tenant.go (+90/−4) — plus the disclosed 6th own file internal/application/service/tenant_deletion_test.go 128ln, disclosed in the ledger ("差异披露：新增 internal/application/service/tenant_deletion_test.go（brief 提交清单之外；服务测试文件模式存在）"). Service-layer test coverage of the deletion gate is untestable without it; same pattern O01 set with recovery_test.go.
- **MigrationDecision + InitialCommercialState exact per brief Step-3.** migration.go:24-27 is the brief's struct after gofmt (Plan/IssueCredits/ConnectionState, nothing added); migration.go:33-42 is the brief's function semantics verbatim: state := requires_reauthorization; if provenConnectionOwner → ready_to_bind; return {Plan:"basic", IssueCredits:false, ConnectionState:state} (:41). Both states pinned as named constants (:16-18) so no string drift.
- **Step-1 test verbatim.** migration_test.go:12-19: same name TestMigrationDoesNotInventPaidCreditOrConnectionOwnership, `got := InitialCommercialState(false)`, the same three-condition refusal (`got.Plan != "basic" || got.IssueCredits || got.ConnectionState != "requires_reauthorization"`), `t.Fatal(got)`. Only gofmt spacing differs from the compressed brief snippet. No assertion removed.
- **CLI: --dry-run default, explicit tenant list only, --apply explicit.** main.go:58 `-dry-run` default true; main.go:59 `-apply` default false; parseTenantList (main.go:217-254) refuses to run without -tenants/-tenants-file (:233-235 "an explicit tenant list is required"), refuses an empty list (:249-251), rejects id=0 (:240-241), dedups (:243-245). There is no code path anywhere in the binary that enumerates the tenants table — the fleet is never implicitly swept.

## AE2 — No invented state: unproven → requires_reauthorization, always basic, no credits, no balance conversion — PASS

- **Unproven owner never gets a shared/positive state.** InitialCommercialState(false) → requires_reauthorization (migration.go:36-38, constant :16). In the CLI the ONLY source of proven ownership is the explicit `-proven-owners` flag (main.go:57, default empty; parseIDSet :279-292); every unlisted tenant stays requires_reauthorization. The CLI never invents ownership.
- **Plan always basic, IssueCredits always false — even for proven owners.** migration.go:41 hardcodes both; pinned by TestMigrationProvenOwnerIsReadyToBindButStillBasicWithoutCredits (migration_test.go:20-23): proven owner still basic + no credits.
- **Legacy balances NEVER become money grants — traced the whole path.** TenantMigrationInput.LegacyBalanceCredits (migration.go:56-58, doc "reported for the record only ... NEVER converted into a money grant") is read by NO code: PlanTenantMigration hardcodes LegacyBalanceConverted:false (migration.go:96) and DataSourcesPreserved:true (:97) regardless of its value. grep of the full diff for grant/credit/issue: every hit is a field name, a comment, a report label, or a test name — the ONLY database write in the entire commit is AccountStore.Bind on commercial_accounts (main.go:173). No Grant table access, no budget/reservation/credit call exists. Pinned by TestMigrationLegacyBalanceNeverBecomesMoneyGrant (migration_test.go:59-75, LegacyBalanceCredits 500000 → converted=false, preserved=true, IssueCredits=false).

## AE3 — Idempotency: repeat apply reuses the F02 Customer via Bind, identical mapping is a no-op success — PASS

- **CLI path.** migrateOne reads the existing mapping read-only (main.go:151-155 store.Get → input.ExistingCustomerID), PlanTenantMigration takes the reuse branch (migration.go:91-95: ReuseExistingCustomer=true, CustomerID=existing, BindRequired=true), and apply calls AccountStore.Bind with the SAME (tenant, customer) pair (main.go:173).
- **Bind's conflict semantics make the identical re-bind succeed.** account.go:39-70: INSERT ... ON CONFLICT DO NOTHING (:46-49) → read back by tenant_id (:53-54) → `got.CustomerID != a.CustomerID` → ErrAccountMappingConflict, else nil (:66-69). A second apply with the identical mapping: insert no-ops, read-back matches → **nil, not a conflict error**. A second apply with a DIFFERENT customer → ErrAccountMappingConflict — silent rebinding is impossible. This is exactly the "double -apply → one commercial_accounts row, reuse=true" the implementer reported; that runtime run was not re-executed within this review's budget, but the code semantics guarantee it and the customer_id uniqueIndex (account.go:16) serializes competing tenants.

## AE4 — Shadow cutover: pinned to trusted call start + config version; replay can never flip the outcome — PASS

- **Pure function boundary conditions.** ShadowBillable (migration.go:119-125): version gate first — eventConfigVersion != cutoverConfigVersion → false; then `!callStartedAt.Before(enabledAt)`. Reporting/replay time is NOT an input, so no replay can change a decision. Pinned at migration_test.go:62-76: (a) call started 1s before cutover → shadow FOREVER even when "reported later" (:64-66); (b) exactly-at-cutover → billable, inclusive boundary explicit (:67-69); (c) post-cutover → billable (:70-72); (d) post-cutover call recorded under an older config version → shadow forever (:73-75) — the old-config-replay-never-billable rule.
- **Shadow records but never externally consumes.** O02's scope is the cutover decision function; usage recording is U01's store and no consumption path is added here (no writes outside Bind anywhere in the commit — see AE2). The rule is documented for operators at docs §5 (:45-51, "影子模式本身只记录 usage，不投外部消费").

## AE5 — Deletion retention: pending refuses with list, settled ordered gate, no cascade, no auto-delete, nil-guard legacy WARN — PASS

- **Pending → refuse with explicit list.** CheckDeletionReadiness (migration.go:156-164): Allow=false when any pending; tenant.go:279-282 returns an error carrying the count and strings.Join of every blocked record. Test: refusal error contains "settlement:41" AND "payment:7", and the tenant SURVIVES (tenant_deletion_test.go:67-83, GetTenantByID → NotNil).
- **Settled → ordered disable→revoke→check→policy.** guardCommercialDeletion tenant.go:265-277 runs exactly DisableNewScheduling → RevokeConnections → PendingCommercialWork → RetentionPolicyVersion; order pinned by the stub's recorded calls in BOTH the refusal and the allow test (tenant_deletion_test.go:77-79, 93-95).
- **Guard failure aborts BEFORE any deletion.** The guard runs before repo.DeleteTenant (tenant.go:238 vs :245); TestTenantDeleteAbortsWhenGuardStepFails (:101-111): revoke fails → only the first two calls ran, tenant still exists.
- **NO cascade delete of commercial records.** The TenantDeletionGuard interface has no delete method (tenant.go:31-43, doc "A guard NEVER cascade-deletes"); repo.DeleteTenant (repository/tenant.go:125-132) deletes ONLY TenantMember + Tenant rows — commercial tables are structurally untouched by tenant deletion.
- **No auto-delete without a configured policy.** AutoDelete = Allow && RetentionPolicyVersion != "" (migration.go:163); pinned by TestMigrationDeletionNeverAutoDeletesWithoutRetentionPolicy (migration_test.go:99-108). AutoDelete is only ever LOGGED (tenant.go:283) — no code path in the diff acts on it. The retention policy version is recorded on the decision and in the log line (tenant.go:283).
- **Nil guard keeps the legacy path with WARN.** tenant.go:261-264 Warnf + return nil; TestTenantDeleteWithoutGuardKeepsLegacyPath (:115-128) constructs NewTenantService(repo, nil) with the legacy TWO-ARG call (proving existing callers compile unchanged against the variadic signature, tenant.go:63) and deletion proceeds. I additionally observed the WARN line live in my confirming -v run.

## AE6 — CLI safety: dry-run zero writes, per-tenant apply, isolated failures, no list → refuse — PASS

- **Dry-run performs ZERO writes.** applyAllowed := p.apply && ApplyAllowed(p.apply) (main.go:107 — the domain rule the CLI defers to). Without -apply, migrateOne returns at main.go:170-172 BEFORE the only write call in the binary (Bind, :173); the store handle is used solely for the read Get (:151-155); without -dsn in dry-run NO database handle is opened at all (:110-118). There is no other Exec/Save/Update/Migrate call in the file.
- **Apply requires -dsn (fail-closed).** main.go:116-118: --apply without a database → explicit error, non-zero exit. openDB (:206-215) performs no AutoMigrate — if F02's commercial_accounts table does not exist, each Bind fails per-tenant and the process exits non-zero rather than creating schema it does not own.
- **Failures isolated per tenant + non-zero exit.** The loop (main.go:123-129) continues past a failing tenant, counts failures, and run() returns an error (:132-134) → main os.Exit(1) (:74-77). Each tenant's apply is one idempotent INSERT+read-back (single-statement atomicity via Bind) — no cross-tenant batch transaction, matching the brief's 逐tenant幂等事务应用.
- **No -tenants → refuse; explicit list only.** parseTenantList error at main.go:233-235 (also :249-251 for empty) — run aborts before any planning.
- **Non-blocking caveat:** the `-dry-run` flag's VALUE is never read (run gates on p.apply only). The gate is fail-closed regardless — `-dry-run=false` without `-apply` still writes nothing — but see Improvement #2.

## AE7 — Manual quality: dry-run reading, apply procedure, idempotency, shadow rules, retention — PASS

docs/operations/saas-commercial-migration.md covers: §1 domain rules matching migration.go 1:1; §2 dry-run report reading (:18-25) including what to do with status=skip (verify Customer externally via -customer-map, or defer) and that dry-run opens no write path; §3 the 5-step apply procedure (:27-35) — dry-run same params first, apply+dsn mandatory, per-tenant idempotent Bind, FAIL isolation + non-zero exit, post-apply verification (reuse=true, issue_credits=false); §4 idempotency guarantees (:37-41) stating exactly account.go's semantics (identical → no-op; different → ErrAccountMappingConflict, never silent rebind); §5 shadow cutover rules (:43-51) matching ShadowBillable's boundary conditions including the replay rule; §6 the deletion gate (:53-61) with the 4-step order, refusal listing, no-auto-delete-without-policy, and nil-guard legacy WARN; §7 safety boundaries + honest blocked-env (production fleet/PostgreSQL/external billing NOT exercised — SQLite/in-memory evidence only).

## AE8 — Honesty: ledger matches reality, no premature PASS — PASS

- **No premature PASS.** Ledger row status is **review**, final column "待审查后派发 O03" — the O01 lesson (pre-writing 已 PASS) is applied.
- **RED claim consistent.** "undefined: InitialCommercialState/TenantMigrationInput/PlanTenantMigration/ShadowBillable/CheckDeletionReadiness 构建失败" — all five symbols are new in this commit's migration.go; the Step-1 test references InitialCommercialState, so the build failure is the expected RED shape.
- **Counts exact — no attribution error.** I counted exactly 9 Test* functions in migration_test.go and ALL 9 match `-run TestMigration`; exactly 4 TestTenantDelete* functions, all matching the service filter. My confirming run: 9/9 + 4/4, exit 0.
- **Full-suite honesty.** The ledger separately records that the FULL `go test ./internal/application/service -count=1` still fails the pre-existing unrelated TestSkillPythonVerifier (A02-era) and explicitly does not mark it pass.
- **blocked-env explicit.** "真实 DB 迁移未对生产执行（仅 SQLite/内存证据）、真实旧租户 fleet 不存在、CLI 未对生产运行、guard 生产接线待容器装配" — each verified true by my reading (no container wiring in the commit; guard production implementation is an interface + test stub only).
- **Deviation disclosures match the commit.** The 6th file tenant_deletion_test.go and the unwired container guard are disclosed exactly as they occur.
- **One attribution nuance (non-blocking):** the CLI SQLite runtime evidence (double -apply → one row/reuse=true, non-zero exit without -tenants, dry-run zero writes) is implementer-reported; neither the coordinator nor this review re-ran the binary. Every claim is nonetheless fully determined by code I verified (Bind's ON CONFLICT + read-back, the :107 gate, :233-235 refusal, os.Exit(1)), so I accept it as consistent; noted for the record (Improvement #4).

## AE9 — Code quality: repo patterns, variadic compatibility, real tests, no weakening — PASS

- **Variadic option keeps every existing caller compiling.** NewTenantService(repo, storageRepo, opts ...TenantServiceOption) (tenant.go:63-71) — the legacy two-arg call is exercised verbatim in TestTenantDeleteWithoutGuardKeepsLegacyPath (:122); nil options are skipped (:66-68).
- **Error handling follows the repo idiom.** fmt.Errorf("%w: …") wrapping around each guard step (tenant.go:266-281) so callers can errors.Is; sentinel conflict error reused from F02 (account.go:10) rather than redefined; ctx-first signatures throughout.
- **Tests are real.** tenant_deletion_test.go:52-62 opens real in-memory SQLite, AutoMigrates the REAL Tenant/TenantMember schemas, uses the real repository.NewTenantRepository and the real service constructor — assertions include negatives (tenant survives refusal/abort, calls order exact-match, tenant gone after allow). No mocks of the unit under test (the stub is the deliberately-unwired guard SEAM, exactly as in production), no t.Skip anywhere in either test file.
- **No test weakening.** Step-1 kept verbatim (AE1); counterexamples end in t.Fatal with the offending value; domain tests assert negatives (ApplyAllowed(false), AutoDelete false, ShadowBillable false cases) as well as positives.
- **CLI follows cmd/ precedent** (flag set + run() + os.Exit(1) pattern like cmd/server); imports only stdlib + already-vendored internal packages and gorm drivers (main.go:17-33) — no new dependency, per the coordinator note.

## AE10 — Cross-task safety: only F02's table via F02's API, no parallel-task writes, no file conflicts — PASS

- **Single write surface.** The only table written anywhere in the commit is commercial_accounts, through F02's own public AccountStore.Bind (main.go:173) — the table F02 owns, via its published idempotency semantics. All other stores are used read-only (Get, :152) or not at all. U01 usage, F04 subscription, A07 binding tables are untouched.
- **tenant.go change is additive and owned.** The brief lists internal/application/service/tenant.go as Modify; the diff adds the guard interface, option, struct field, and the pre-delete check — no existing behavior changed when no option is passed (AE5 nil-guard test).
- **No migration/SQL file conflicts.** No .sql or migration files added; cmd/ contains only desktop/download/saas-migrate/server — saas-migrate is a new directory no parallel task touches. All new symbols (TenantMigrationInput, PlanTenantMigration, ShadowBillable, CheckDeletionReadiness, MigrationDecision, InitialCommercialState, TenantDeletionGuard, WithDeletionGuard) are defined only in this commit's files.

## Verdict — PASS

No blocking finding. The brief's mandated artifacts are all present, tested, and honestly disclosed: Step-1 verbatim; MigrationDecision/InitialCommercialState exact; no invented state on any path (basic/no-credits/unproven→requires_reauthorization structurally enforced); Customer reuse with Bind's identical-mapping no-op; legacy balance reported but never convertible (no grant write exists in the commit); shadow cutover pinned to trusted call start + config version with replay-proof boundary conditions tested; deletion refused with an explicit pending list, ordered gate, no cascade path, no auto-delete without policy; nil guard preserving the legacy path with a WARN; CLI defaulting to zero-write dry-run over an explicit tenant list only. The ledger claims match what the code and my confirming run show, and no unrun check is marked pass.

### Improvements (non-blocking)

1. **Vocabulary comment overstates A07 reuse** (migration.go:12-14): "ready_to_bind" is not an A07 string — appconnector expresses positive authorization as RequiresReauthorization=false + ConnectionActive (sync.go:101-106, :146, :152-156); only requires_reauthorization is shared vocabulary (the brief pins just that one). Reword the comment so no reader expects A07 to emit ready_to_bind.
2. **Dead `-dry-run` flag value** (main.go:58 vs :107): run() gates on p.apply only, so p.dryRun is never read. Fail-closed as-is; either require -dry-run=false AND -apply for writes, or drop the field to avoid operator confusion.
3. **Silently swallowed Get error** (main.go:151-155): a store.Get failure other than not-found plans the tenant as "no mapping" — in apply mode this can only yield a skip or a Bind conflict (fail-closed), but surfacing the read error in the report would improve diagnostics.
4. **Pin the CLI double-apply in CI** (implementer-reported runtime evidence): a small Go test driving migrateOne twice against in-memory SQLite would make the "exactly one commercial_accounts row, reuse=true" guarantee a checked invariant instead of a manual run; account.go's ON CONFLICT + read-back already guarantee it today.
