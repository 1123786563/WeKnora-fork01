---
status: final
verdict: PASS
reviewer: independent-review-subagent (O01, 运营恢复队列、审计与收费开关; did NOT write this code)
commit-under-review: bf5ab4d35ad357452ebe0f8a68e87769e5fc97cc (implementation, 7 files +1025) + 314aeaa (ledger), worktree saas-billing-connectors
files-reviewed: full diff bf5ab4d untruncated (internal/commercial/rollout.go 242ln + rollout_test.go 156ln, internal/application/service/commercial/recovery.go 298ln + recovery_test.go 220ln, internal/config/config.go +57, internal/container/container.go +6, docs/operations/saas-commercial-recovery.md 46ln); ledger diff 314aeaa; brief task-O01-brief.md; precedent task-W05-review.md. Consumed models verified against real definitions: repository/commercial/outbox.go (OutboxEvent :31-40 — event_key primaryKey/UNIQUE, TenantID, Kind, PayloadJSON, State, LeaseToken, LeaseUntil, AttemptCount; OutboxStatePending/Sent/Dead :18-22; EnqueueTx conflict = ErrOutboxEventDuplicate), commercial/refund.go (:17-18 RefundStatePending/"revocation_pending"), repository/commercial/refund.go (RefundRow :34-47 — NO CreatedAt field), service/commercial/budget.go (NewBudgetService pattern :48-58) and refund.go (sentinel-error var block + ctx-first signatures). grep for Replay/CheckNewConsumption/AllowDuringRollback/NewRecoveryService/RedactForLog consumers outside task files: only container.go:475 Provide.
method: source-level evidence via git show + post-change greps with exact line numbers + signature cross-checks of every consumed C01/C05/U04 symbol against its real definition. Gates NOT re-run except ONE confirming command (coordinator already re-verified full two-package suites ok, go build+vet 4 packages, gofmt, git diff --check).
confirming-command: go test ./internal/commercial ./internal/application/service/commercial -run "Test(Rollback|Recovery)" -count=1 -v -> exit 0, both packages ok; 10 tests ran and passed = 4 domain (TestRollbackKeepsPaidRecoveryAndSettlement, TestRollbackRejectsEveryOtherOperation, TestRecoveryCategoryRoundTrip, TestRecoveryAlertThresholdsValidation) + 6 service (all Test*). NOTE: the -run filter matches only 4 of the 6 domain test names — TestCommercialSwitchesDefaultOpenAndGateNewConsumption (rollout_test.go:70) and TestRedactForLogMasksCredentialsAndTruncatesPayloads (rollout_test.go:125) do not match "Test(Rollback|Recovery)" and execute only in the full-suite run (coordinator re-verified full suites ok; source of both verified here). This exactly explains the coordinator's "10 targeted" figure.
---

# Task O01 Independent Review — 运营恢复队列、审计与收费开关

## AD1 — Brief compliance: file set, verbatim artifacts, exact signatures — PASS

- **File set exact incl. the disclosed extra.** `git show bf5ab4d --stat`: all 6 brief-named files present — creates internal/commercial/{rollout.go, rollout_test.go}, internal/application/service/commercial/recovery.go, docs/operations/saas-commercial-recovery.md; modifies internal/config/config.go (+57, purely additive hunks) and internal/container/container.go (+6, one Provide) — plus the disclosed 8th own file internal/application/service/commercial/recovery_test.go (ledger diff in 314aeaa discloses it: "brief Step6 七文件清单之外加性暂存第 8 个自有新文件"; service-layer tests are mandated by the brief's 行为与边界 column "测试开关关闭后新消费拒绝但回调/历史结算仍处理", which is untestable without a service test). The ledger itself is committed separately in 314aeaa, exactly as the prior tasks did.
- **AllowDuringRollback equivalent to brief Step 3.** rollout.go:26-33 is the brief's function after gofmt normalization (the brief snippet is single-space compressed, which gofmt would reject): identical case list `"payment_callback", "refund_query", "fulfillment_replay", "settlement"` → true, default → false, nothing else added. The four strings additionally pinned as named constants (rollout.go:16-21) used everywhere else, so no drift is possible.
- **Step-1 test verbatim.** rollout_test.go:8-19 is the brief's Step-1 test gofmt-normalized: same name `TestRollbackKeepsPaidRecoveryAndSettlement`, same four-element loop, same `t.Fatal(s)` / `t.Fatal("new money accepted while paused")` messages. No assertion removed.
- **Replay signature exact.** recovery.go:213 `func (s *RecoveryService) Replay(ctx context.Context, operationID, actor string) error` — matches the brief Produces line `RecoveryService.Replay(ctx context.Context,operationID,actor string) error` field-for-field (ctx first, two strings, error return). `AllowDuringRollback(operation string) bool` matches likewise.

## AD2 — Rollback semantics: closed default for everything but the four — PASS

- **ONLY the four operations allowed.** rollout.go:26-33: the switch admits exactly payment_callback/refund_query/fulfillment_replay/settlement; default false. TestRollbackRejectsEveryOtherOperation (rollout_test.go:22-30) pins the brief-named counterexamples — new_order, new_dispatch, connector_action, refund_create, and `""` — all false.
- **Switches-off rejects new consumption while callbacks/settlement still process, tied to the REAL config fields.** TestRecoverySwitchesClosedRejectNewConsumptionWhileReplayContinues (recovery_test.go:153-182) constructs `&config.Config{CommercialNewOrders: &off, CommercialNewDispatch: &off, ConnectorNewActions: &off}` — the actual fields added in config.go:49-51 — passes it through the real `NewRecoveryService` (which reads them via the Are*Enabled accessors, recovery.go:114-137), asserts CheckNewConsumption refuses new_order/new_dispatch/connector_action with ErrNewConsumptionPaused, then seeds a dead payment_callback event and asserts `svc.Replay` SUCCEEDS under the closed switches. This is the brief's 行为与边界 sentence rendered as an executable test. The per-lane gating itself (AllowsNewConsumption, rollout.go:125-136) maps each of the three ops to its own switch only; recovery ops never consult a switch.
- **ClassifyRecovery fail-closed too.** rollout.go:86-107: unknown kind → unknown_action (never dropped, never a replayable category), pinned at rollout_test.go:51-52 (connector_action and "" → unknown_action).

## AD3 — Replay discipline: original key only, state verified, audited, no balance path — PASS

- **Only the ORIGINAL operation id + idempotency key; never a new one.** replayChecked (recovery.go:221-273) loads the event by `event_key = operationID` (recovery.go:229; missing → ErrRecoveryOperationUnknown), then re-arms THE SAME row via a conditional UPDATE (recovery.go:258-266: state→pending, lease cleared, attempt_count+1) — there is no Create on the outbox table anywhere in the service, and `res.RowsAffected != 1` refuses a no-op (recovery.go:268-270). The storage layer's event_key UNIQUE (outbox.go:31-32 + OnConflict DoNothing→ErrOutboxEventDuplicate) is the second wall. TestRecoveryReplayUsesOriginalIdempotencyKey (recovery_test.go:52-78) pins: exactly 1 outbox row after replay, still `evt_orig`, state pending, attempt_count 1.
- **Object version + current state checked, fail-closed.** No resolver wired → ErrRecoveryStateUnverified (recovery.go:243-245); resolver error → same (recovery.go:247-248); `snap.Version <= 0` → refused (recovery.go:249-251). Current-state verification is carried by the resolver abstraction (RecoveryObjectState{BusinessID, Version, State, Revoked}, recovery.go:56-61): heterogeneous objects (orders/refunds/connections) have no shared state vocabulary, so the per-kind resolver decides acceptability and the service enforces the cross-object invariants centrally — version positive and not revoked. Reasonable design; see Improvement #4.
- **Revoked connections never re-enabled.** recovery.go:252-254 refuses BEFORE any write; TestRecoveryReplayRefusesRevokedConnection (recovery_test.go:81-104) pins the event stays dead and the refusal is audited. Structurally, Replay's only writes are the outbox re-arm and the audit insert — there is no code path that touches a connection row at all, so "re-enable" is impossible by construction.
- **Actor + result recorded on EVERY attempt.** Replay wraps the check and always calls recordAudit (recovery.go:213-219); failures write result="error" + truncated detail, successes "ok" (recovery.go:275-297). Tests pin actor op_alice / result ok (recovery_test.go:70-75) and TWO audit rows for two refused attempts (recovery_test.go:122-126).
- **NO direct balance mutation path.** Read the whole 298-line file: the only persistence calls are the outbox UPDATE (recovery.go:258-266), the audit INSERT (recovery.go:293), and SELECTs. No budget/reservation/credit/order/refund table is ever written; RecoveryAuditRow is the only new table. Matches the doc comment "the service never touches balances directly" and manual rule 4.

## AD4 — Unknown-action discipline: query/reconcile only, never re-dispatch — PASS

- **Code.** recovery.go:236-238: any event kind outside AllowDuringRollback's four → ErrRecoveryQueryOnly BEFORE the sent-check, the resolver, and the re-arm write — an unknown write cannot reach the UPDATE. Since the allowlist IS the four recovery ops, any future/unknown kind (connector writes, new_order, refund_create, novel kinds) is query-only by default — fail-closed, not fail-open.
- **Test.** TestRecoveryUnknownWriteIsQueryOnly (recovery_test.go:133-151) drives connector_action, new_order AND a novel "totally_new_kind", asserting ErrRecoveryQueryOnly and attempt_count unchanged (0) for each — the no-re-dispatch invariant is pinned per kind.
- **Manual.** docs/operations/saas-commercial-recovery.md unknown_action row: 允许动作 = "**仅核对/查询，绝不重派**"; abnormal path = keep queued and escalate. Manual matches code.

## AD5 — Queue classification: six categories with age/tenant/business-ID projection — PASS

- All six brief categories exist as a CLOSED set with round-trip parse: rollout.go:35-77 (paid_unfulfilled, refund_unknown, revocation_pending, usage_unconfirmed, balance_discrepancy, unknown_action; RecoveryCategories :62-69 in display order; ParseRecoveryCategory :73-81 rejects strangers — pinned rollout_test.go:40-43). ClassifyRecovery (:86-107) maps kind+state including refund_query+revocation_pending→revocation_pending and settlement dead/discrepancy→balance_discrepancy (pinned :44-53).
- **Queue projection is real and tested end-to-end against SQLite.** ListQueue (recovery.go:155-210) unions undelivered outbox events (state <> sent) with refunds in pending/revocation_pending (the real C05 states, refund.go:17-18), projecting Category/OperationID/Kind/TenantID/BusinessID/State/Age/Attempts. TestRecoveryQueueListingCategorizesWithAgeAndBusinessID (recovery_test.go:184-220) seeds three events + one revocation-pending refund and asserts all four land in the right categories with TenantID=7, non-empty BusinessID, and evt_paid age ≈1h (≥30min). Minor projection shortcuts noted as Improvements #2/#3 — none affect classification correctness.

## AD6 — Metrics/alerts/logs: per-category counts + age percentiles, thresholds, redaction — PASS

- **Metrics model.** RecoveryCategoryMetrics (rollout.go:151-160): Count + AgeP50/P90/P99 + MaxAge — pure structure, no side effects, per-category by design.
- **Thresholds explicit and validated.** RecoveryAlertThresholds{MaxCount, MaxAgeP90, MaxAge} with Validate (rollout.go:164-174) rejecting negatives AND the all-zero config (which could never alert); Breached (:176-189) fires on any of the three configured axes. TestRecoveryAlertThresholdsValidation (rollout_test.go:94-123) pins: negative count/age rejected, all-zero rejected, valid accepted, healthy metrics not breached, and count/P90/max-age breaches each detected — the "3-axis breach" claim is real.
- **Redaction actually nests and masks.** credentialKeyFragments (rollout.go:192-199: token, secret, password, passwd, credential, authorization, api_key, apikey, private, signature, cookie, session) matched case-insensitively as substrings; RedactForLog (rollout.go:225-241) recurses through map[string]any so NESTED credential keys are replaced with [REDACTED] at any depth, long strings truncated at 512 with an explicit `…[truncated]` marker (TruncateLogValue :215-222), scalars pass through. Verified the test cases mentally: "Authorization"→lowercased contains "authorization" ✓; nested api_key hits via recursion ✓ (rollout_test.go:139-142); the 4096-byte payload comes back <4096 ✓ (:144-146); order_id/description untouched ✓ — no false-positive mangling of plain fields. The audit path itself already uses TruncateLogValue on error detail (recovery.go:289). Caveats → Improvements #5 (no []any recursion; RedactForLog not yet wired into a production logger — disclosed in the manual's blocked-env note).

## AD7 — Config safety: three switches default TRUE, additive only, container additive — PASS

- **Nil-safe safe-on defaults.** config.go:49-51 `*bool` pointers; accessors :56-69 return true when receiver nil, pointer nil, OR value true — unset can never close a lane. Domain-level DefaultCommercialSwitches (rollout.go:117-119) mirrors this; NewRecoveryService keeps defaults for a nil cfg (recovery.go:121-130). TestCommercialSwitchesDefaultOpenAndGateNewConsumption (rollout_test.go:70-92) pins default-open, per-lane closed rejection, and non-new ops unaffected.
- **Additive only.** The config.go diff adds: 3 fields + 3 accessor methods + one call `applyCommercialRolloutDefaults(&cfg)` (config.go:615) + that function and applyRolloutSwitchEnv. No existing field, default, or function body was touched (diff hunks show pure additions with unchanged context). Env override naming WEKNORA_COMMERCIAL_NEW_ORDERS/WEKNORA_COMMERCIAL_NEW_DISPATCH/WEKNORA_CONNECTOR_NEW_ACTIONS follows the WEKNORA_* precedent; unparseable values are ignored with a log line and — critically — can only OPEN or CLOSE explicitly, never silently close a lane.
- **Container additive.** container.go:470-475 adds exactly one `must(container.Provide(commercialsvc.NewRecoveryService))` with an honest comment: registered only, capability check unset until OPS wiring, so Replay fails closed out of the box. dig.Provide is lazy — nothing else's construction changes.

## AD8 — Manual quality: per-state query paths, outcomes, allowed actions — PASS

docs/operations/saas-commercial-recovery.md covers all six categories in a table with columns 查询路径 / 正常结果 / 异常结果 / 允许动作, and the content matches the code: paid_unfulfilled replays fulfillment_replay; refund_unknown queries the ORIGINAL refund key and forbids re-creating; revocation_pending retries revocation only and explicitly forbids re-paying (不得再次打款); usage_unconfirmed keeps reservations (不得清零); balance_discrepancy is read-only reconciliation with 禁止手改余额; unknown_action is verification-only, 绝不重派. Six numbered core invariants at the top mirror the implemented behavior 1:1 (allowlist, original idempotency key + storage UNIQUE backstop, unknown=query-only, no balance writes, revoked refusal, fail-closed capability + mandatory audit). The replay-rules and alert/redaction sections state the real thresholds model and disclose that real alerting/metrics endpoints were NOT exercised (blocked-env).

## AD9 — Honesty: ledger vs reality, blocked-env explicit — PASS

- **Claims verified:** RED description (undefined AllowDuringRollback failing the build) is consistent with rollout.go being a new file; the 6 domain test names and 6 service test names in the ledger all exist at the cited behavior level and all pass; switch semantics, revoked refusal, capability fail-closed, query-only, original-key replay, audit actor+result, and queue projection claims each map to a test I read and (for the 10 filter-matching ones) re-ran. The deviations are disclosed in the ledger exactly as they occur in the commit (8th file recovery_test.go; capability injected-not-wired fail-closed; config purely additive).
- **One imprecision (non-blocking):** the ledger's GREEN line says the targeted command showed "internal/commercial 6 用例", but `-run "Test(Rollback|Recovery)"` matches only 4 of those 6 names — TestCommercialSwitches… (rollout_test.go:70) and TestRedactForLog… (rollout_test.go:125) run only in the full-suite invocation, which the SAME ledger cell separately records as passing ("go test ./internal/commercial ./internal/application/service/commercial -count=1 全量不回归", corroborated by the coordinator). All 12 tests do pass; only the attribution of 6 to the filtered command is off. My confirming run: 10 passed, exit 0 — matching the coordinator's "10 targeted" figure.
- **blocked-env explicit:** "真实告警/指标端点未运行（仅配置与代码级证据，手册已注明），运营能力检查注入待 OPS 接线" — matches reality (grep: no production consumer of Replay/CheckNewConsumption/RedactForLog outside the task files; only the container Provide). No check that was not run is marked pass.

## AD10 — Code quality: patterns, real tests, no weakening — PASS

- **Service follows the established budget/refund pattern.** Sentinel errors in a documented var block (recovery.go:19-45, mirroring refund.go's), struct + constructor with nil-guard/AutoMigrate/error return (recovery.go:114-137 vs budget.go:48-58), ctx-first methods, `fmt.Errorf("%w: …")` wrapping so callers can errors.Is — and the tests DO use errors.Is (recovery_test.go:91,111,117,140-146), not string matching. Errors are distinct and explicit (9 sentinels); no silent successes.
- **SQLite tests are real.** recovery_test.go:19-33 opens a real in-memory shared-cache SQLite with MaxOpenConns(1), AutoMigrates the REAL OutboxEvent/RefundRow/RecoveryAuditRow schemas, and every assertion queries persisted rows (counts, states, attempt counters, audit rows) — no mocks of the unit under test, no skip, no t.Skip anywhere in either test file.
- **No test weakening.** Counterexamples end in t.Fatalf with the offending value; the allowlist test keeps the brief's exact failure messages; assertions check negatives (attempt_count unchanged, rows=1, no new key) as well as positives.

## Verdict — PASS

No blocking finding. The brief's mandated artifacts (Step-1 test verbatim, AllowDuringRollback equivalent, exact Replay signature, three safe-on config switches, six-category queue, audited original-key replay, fail-closed capability and state verification, no balance writes, unknown=query-only, per-state manual) are all present, tested, and honestly disclosed. The single factual imprecision in the ledger (6-vs-4 domain tests under the filtered command) does not affect any pass/fail claim since both the filtered and full-suite runs are recorded and green.

### Improvements (non-blocking)

1. **Ledger attribution:** say "10 tests match the -run filter (4 domain + 6 service); the other 2 domain tests pass in the full-suite run" — the current "同命令… 6 用例" wording over-attributes to the filtered command (task-O01 ledger, 314aeaa).
2. **Refund queue Age is always 0:** RefundRow has no timestamp (repository/commercial/refund.go:34-47), so ListQueue's refund items carry Age=0 (recovery.go:194-207). Consider deriving from an outbox event or asking the owning task to stamp refunds.
3. **Outbox BusinessID = EventKey** (recovery.go:176): a projection shortcut; parsing the business id out of PayloadJSON would give operators the real order/refund id in that column.
4. **snap.State is not consumed beyond Revoked** (recovery.go:249-256): consider audit-logging the resolved state so operators see WHY a replay was allowed/refused per object.
5. **RedactForLog does not recurse into []any** (rollout.go:225-241) and is not yet wired into any production logger (audit path uses only TruncateLogValue) — fine under the disclosed blocked-env, but note for the OPS wiring task.
6. **AllowsNewConsumption returns true for out-of-lane ops** like refund_create (rollout.go:125-136) — by design those are governed by AllowDuringRollback during a pause (which rejects them), but a one-line comment at the refund-create call site would prevent future confusion when O02/O03 wire the gate.
7. **Settlement dead→balance_discrepancy via substring "discrepan"** (rollout.go:94-96) is a loose heuristic; an explicit state vocabulary would be sturdier if settlement states grow.
