# T08 (#80) real-stack run — operator timeline

Stack: `deploy/lago/` pinned images (images.lock.json → release v1.53.0),
ticket-isolated Compose project `weknora-lago-80`, ports 48903 (API) /
48904 (front), loopback only. Secrets generated by `./deploy/lago/lago.sh
init` + first-run seed (throwaway operator account + org API key); the API
key was stashed at /tmp/lago80-api-key (mode 600) for the test run only and
deleted after. No secret is committed, logged, or echoed.

Timeline (2026-09-20/21, local):

1. `./deploy/lago/lago.sh init`
   → wrote deploy/lago/.env (mode 600); project/ports edited to
     COMPOSE_PROJECT_NAME=weknora-lago-80, LAGO_API_PORT=48903,
     LAGO_FRONT_PORT=48904 (+ matching *_URL values).
2. First-run seed appended to .env (LAGO_CREATE_ORG=true,
   LAGO_ORG_NAME=weknora-t08-lab, throwaway operator email/password,
   LAGO_ORG_API_KEY=<random, stashed to /tmp/lago80-api-key mode 600>).
3. `./deploy/lago/lago.sh up`
   → `docker compose up -d --wait`; all healthchecked services Healthy
     (api, api-worker, db, pdf, redis; migrate one-shot Exited 0 after
     seeding). gotenberg runs amd64-emulated on arm64 (slow start is
     normal and did not gate --wait). One intermediate up attempt failed
     while the seed lacked LAGO_ORG_USER_EMAIL/PASSWORD (migrate exit 1);
     appending them and re-running `up` seeded cleanly.
4. `./deploy/lago/lago.sh status --json`
   → overall "ready", release "v1.53.0", every service healthy/running;
     auth probe: GET /api/v1/organizations with the operator key → 200.
5. `LAGO_INTEGRATION_BASE_URL=http://127.0.0.1:48903 \
     LAGO_INTEGRATION_API_KEY=<stashed operator key> \
     go test -tags lago_integration ./internal/infrastructure/commercialplatform/ \
       -run TestLagoBasePlanIntegration -v -count=1`
   → --- PASS: TestLagoBasePlanIntegration (123s). Phases proven against
     the real pinned release:

     1. ZERO-PRICE PLAN VERDICT: `POST /api/v1/plans` with
        `amount_cents: 0` answered **200 (ACCEPTED)** on v1.53.0 — the
        1-fen fallback is NOT needed; (base, v1) publishes at price 0.
     2. Full chain for tenant A (run-unique lab tenant 8085536369)
        through the REAL adapter + BenefitsService: seed (base, v1
        published at 0) → customer → subscription created WITHOUT
        activation_rules and reached `active` → monthly grant (wallet
        created, settle-wait completed) → benefits snapshot → projection
        quotas applied (members hard_limit 5). Credits balance = exactly
        one month (1_000_000 micro).
     3. Idempotent replay: a second full EnsureBenefits left EXACTLY ONE
        subscription under weknora-tenant-<A>-sub (identity index with
        explicit status[]), EXACTLY ONE active wallet for the period, and
        the balance UNCHANGED — the E3 no-grant-idempotency anti-pattern
        disproven on the real runtime. The registry UNIQUE
        (tenant_id, period) fired on the replay insert exactly as
        designed (idempotent read-back path exercised on a real DB).
     4. Cross-tenant isolation: tenant B's chain (8085536370) completed
        independently; A's balance unchanged; B's identities B-derived;
        wallet lists did not cross.
     5. Wallet-slot / lazy-termination EVIDENCE: a synthetic short-TTL
        wallet (minutes-scale expiration_at) STAYED ACTIVE past its
        expiry within the bounded observation window — lazy termination
        confirmed on the real runtime; the registry expiry overlay (and
        the ≤2-transient-slots budget: monthly current + expired-awaiting-
        termination) is required, exactly as the T03 verdict prescribed.
     6. Quota CAS end-to-end (service level, sqlite-backed in the lab
        env — the lab wires no WeKnora PostgreSQL; honest sqlite-only
        note): admits N, refuses N+1, allows −1 (超限状态 cleanup).

6. `./deploy/lago/lago.sh down`
   → containers/network removed; volumes PRESERVED per the T01
     convention (stack rebuildable without re-seeding).
7. /tmp/lago80-api-key deleted.

Artifact index: docs/migrations/lago/t08-base-plan/README.md.
