# T07 (#79) real-stack run — operator timeline

Stack: `deploy/lago/` pinned images (images.lock.json → release v1.53.0),
ticket-isolated Compose project `weknora-lago-79`, ports 48901 (API) /
48902 (front), loopback only. Secrets generated by `./deploy/lago/lago.sh
init` + first-run seed (throwaway operator account + org API key); the API
key was stashed at /tmp/lago79-api-key (mode 600) for the test run only and
deleted after. No secret is committed, logged, or echoed.

Timeline (2026-09-20, local):

1. `./deploy/lago/lago.sh init`
   → wrote deploy/lago/.env (mode 600); project/ports edited to
     COMPOSE_PROJECT_NAME=weknora-lago-79, LAGO_API_PORT=48901,
     LAGO_FRONT_PORT=48902 (+ matching *_URL values), then the first-run
     seed values appended (LAGO_CREATE_ORG=true, LAGO_ORG_API_KEY=<random>).
2. `./deploy/lago/lago.sh up`
   → `docker compose up -d --wait`; all healthchecked services Healthy
     (api, api-worker, db, pdf, redis; migrate one-shot Exited 0 after
     seeding). gotenberg runs amd64-emulated on arm64 (slow start is
     normal and did not gate --wait).
3. `./deploy/lago/lago.sh status --json`
   → overall "ready", release "v1.53.0", every service healthy/running.
4. `LAGO_INTEGRATION_BASE_URL=http://127.0.0.1:48901 \
     LAGO_INTEGRATION_API_KEY=<stashed operator key> \
     go test -tags lago_integration ./internal/infrastructure/commercialplatform/ \
       -run TestLagoPlanPublishIntegration -v -count=1`
   → --- PASS: TestLagoPlanPublishIntegration (0.65s)
   Phases proven against the real pinned release:
     1. publish v1 through the frozen seam (POST /api/v1/plans with
        pay_in_advance, integer amount_cents, CNY, feature ensure +
        map-form entitlement attach) → receipt; read-back confirmed
        amount_cents/amount_currency/interval/pay_in_advance.
        READ-BACK CHANNEL VERDICT: the single-object
        GET /api/v1/plans/{code} ANSWERED the plan on v1.53.0 (logged
        "read-back via single-object GET"); the list-endpoint fallback
        stayed unused.
     2. same command key replayed byte-equal → success receipt; the list
        endpoint showed EXACTLY ONE plan with the code (no double plan).
     3. subscription pinned to v1 (customer t07-<ns>-cust, subscription
        t07-<ns>-sub with its own external_id) survived the v2 publish:
        plan_code / external_id / status / lago_id all byte-stable.
     4. same key with different amount_fen → ErrPlatformInvalidResponse
        (the read-back compare caught the content conflict).
   Cleanup duty (t02 convention): subscription, customer, both plans
   deleted best-effort after the phases.
5. `LAGO_INTEGRATION_BASE_URL=... go test -tags lago_integration ... \
     -run TestLagoAdapterIntegration -v -count=1`
   → --- PASS (readiness snapshot ready at the locked release).
6. `./deploy/lago/lago.sh down`
   → stack stopped; named data volumes preserved per the T01 convention.
7. Stashed API key file deleted.

No step was skipped, simulated, or fabricated.
