[project]
name = "semantica-experiments"
version = "0.0.0"
description = "Isolated Semantica V01 installed-distribution verification"
requires-python = "==3.12.*"
dependencies = [
    "semantica==0.6.8",
]

[dependency-groups]
dev = [
    "pytest==8.4.2",
]

[tool.uv]
package = false
