# V03 native smoke evidence — 2026-09-20

The transactional native smoke ran from source commit `38b88295dce5c82903e2d84382d19676ad3dc039` with the fixed loopback ports 15432, 16379, 18687, 18474, and 18081. It used the digest-pinned PostgreSQL, Redis, and Neo4j images recorded by the run manifest, and a remote-loopback OpenAI-compatible model entry for the already-present `qwen2.5:0.5b` host Ollama model. No model download was requested.

The process completed isolated startup, same-login registration/login, graph KB configuration persistence, two synthetic Chinese manual documents, terminal document polling with `pending_subtasks_count=0`, and a source-linked Neo4j probe. The probe found `graph_node_count: 2` for the uploaded runtime knowledge IDs. The same session then completed `POST /api/v1/knowledge-chat/{session}` with actual mode `native`.

The run exited `1` intentionally because the actual SSE stream contained no `response_type: references` graph reference mapped to an evidence ID. The result is a negative result, not success evidence: `evidence_ids: []`, `references: []`, `status: negative`, and `failure_stage: graph_reference`. The raw SSE was sanitized into the ignored runtime artifact and had SHA-256 `8edf8e0dcbff22c9c20eb2ab32d01c58c749588388a782a3a149c4f5afb564cc`.

`up` exited 0 and `teardown` exited 0. A subsequent idempotent teardown also exited 0 and no `semantica-v03-native-v03-native` containers remained. This is bounded native-path evidence only; it does not establish Semantica, A03, benchmark performance, or production acceptance.
