// Pass A compatibility alias for internal/modules/datasource/connector/feishu/core — zero logic. Deleted by Pass B task B-datasource.
package core

import newcore "github.com/Tencent/WeKnora/internal/modules/datasource/connector/feishu/core"

// RegionFeishu forwards the moved region constant (Chinese mainland cloud).
var RegionFeishu = newcore.RegionFeishu

// RegionLark forwards the moved region constant (international cloud).
var RegionLark = newcore.RegionLark

// RegionFeishuDrive forwards the moved region constant (mainland, Drive mode).
var RegionFeishuDrive = newcore.RegionFeishuDrive

// RegionLarkDrive forwards the moved region constant (international, Drive mode).
var RegionLarkDrive = newcore.RegionLarkDrive
